"function callOptionalReturn(IERC721 token, bytes memory data) private {        require(address(token).isContract(), \"SafeERC721: call to non-contract\");        (bool success, bytes memory returndata) = address(token).call(data);        require(success, \"SafeERC721: low-level call failed\");        if (returndata.length > 0) {             require(abi.<mask0>, \"SafeERC721: ERC20 operation did not succeed\");        }    }"