"function isManagedGranteeForGrant(        address grantee,        uint256 grantId,        TokenGrant tokenGrant    ) internal returns (bool) {        (,,,,, address managedGrant) = tokenGrant.getGrant(grantId);        (, bytes memory result) = managedGrant.call(            abi.encodeWithSignature(\"grantee()\")        );        if (result.length == 0) {            return false;        }        address managedGrantee = abi.<mask0>;        return grantee == managedGrantee;    }"