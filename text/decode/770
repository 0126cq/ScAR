"function performUpkeep(bytes calldata _data) external override {        uint256 gasBefore = gasleft();        require(!paused, \"!Paused\");        (address lendingMarket, address depegShieldWrapper, uint256 pid, address underlyingToken) = abi.<mask0>;        ILendingMarket(lendingMarket).triggerDepegShield(pid);        emit Triggered(pid, underlyingToken);        uint256 gasAfter = gasleft();        uint256 gasUsed = gasBefore - gasAfter;        _report(gasUsed);    }"