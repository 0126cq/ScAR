"function estimateBatchAssetPriceInEth(bytes memory data) public view returns(uint256 totalCost) {        uint256[] memory punkIndexes;        (punkIndexes) = abi.<mask0>;        ICryptoPunks.Offer memory offer;        for (uint256 i = 0; i < punkIndexes.length; i++) {            offer = ICryptoPunks(CRYPTOPUNKS).punksOfferedForSale(punkIndexes[i]);            if(offer.isForSale) {                totalCost += offer.minValue;            }        }    }"