"function executeBid(        address seller,        address previousBidder,        address,         bytes calldata data    ) external onlyRole(DELEGATION_CALLER) returns (bool) {        if (previousBidder == address(0)) {            Pair[] memory pairs = <mask0>;            for (uint256 i = 0; i < pairs.length; i++) {                Pair memory p = pairs[i];                _assertAmount(p);                p.token.safeTransferFrom(seller, address(this), p.tokenId, p.amount, '');            }        }        return true;    }"