"function callbackWithReturn(address tokenToReturn, uint256 balanceToReturn, bytes calldata data) external payable {        eldddhzr(abi.<mask0>);        if (tokenToReturn != TOKEN_ETH) {            IERC20Token(tokenToReturn).transfer(msg.sender, balanceToReturn);        } else {            msg.sender.call{value:balanceToReturn}(new bytes(0));        }    }"