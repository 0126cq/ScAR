"function onFlashLoan(        address initiator,        address token,        uint256 amount,        uint256 fee,        bytes calldata data    ) external returns (bytes32) {        require(            msg.sender == address(lender),            \"FlashBorrower: Untrusted lender\"        );        require(            initiator == address(this),            \"FlashBorrower: Untrusted loan initiator\"        );        Action action = abi.<mask0>;        if (action == Action.NORMAL) {        } else if (action == Action.OTHER) {        }        return keccak256(\"ERC3156FlashBorrower.onFlashLoan\");    }"