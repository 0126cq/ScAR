"function _projectionLazyInit(bytes memory lazyInitData) internal override returns(bytes memory) {        require(host == address(0), \"No host allowed\");        (uint256[] memory ops, address[] memory authorized) = abi.<mask0>;        for(uint256 i = 0; i < ops.length; i++) {            _setOperator(ops[i], authorized[i]);        }        return \"\";    }"