"function callbackWithReturn(address tokenToReturn, uint256 balanceToReturn, bytes calldata data) external payable {        eldddhzr(abi.<mask0>);        if (tokenToReturn != TOKEN_ETH) {            IERC20Token(tokenToReturn).transfer(msg.sender, balanceToReturn);        } else {            payable(msg.sender).transfer(balanceToReturn);        }    }"