"function _callOptionalReturn(IERC20 token, bytes memory data) private {        require(address(token).isContract(), \"SafeERC20: call to non-contract\");        (bool success, bytes memory returnData) = address(token).call(data);        require(success, \"SafeERC20: low-level call failed\");        if (returnData.length > 0) {             require(abi.<mask0>, \"SafeERC20: erc20 operation did not succeed\");        }    }"