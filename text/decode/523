"function setHeaderFooter(bytes memory _bHeader, bytes memory _bFooter) public onlyOwner {        require(msg.sender == owner(), \"You are not the owner\");        header = abi.decode(_bHeader, (string));        footer = abi.<mask0>;    }"