"function executeAuctionRefund(        address seller,        address,         bytes calldata data    ) external onlyRole(DELEGATION_CALLER) returns (bool) {        Pair[] memory pairs = <mask0>;        for (uint256 i = 0; i < pairs.length; i++) {            Pair memory p = pairs[i];            _assertAmount(p);            p.token.safeTransferFrom(address(this), seller, p.tokenId, p.amount, '');        }        return true;    }"