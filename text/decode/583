"function executeSell(        address seller,        address buyer,        bytes calldata data    ) external onlyRole(DELEGATION_CALLER) returns (bool) {        Pair[] memory pairs = <mask0>;        for (uint256 i = 0; i < pairs.length; i++) {            Pair memory p = pairs[i];            _assertAmount(p);            p.token.safeTransferFrom(seller, buyer, p.tokenId, p.amount, '');        }        return true;    }"