"function getRecipient(address _operator, address _from, bytes memory _data)    internal view    returns(address)    {        if (_operator == bridgeData.getAddress(\"bridgeHead\") && _data.length > 0) {            return abi.<mask0>;        }        if (_from.isContract()) {            revert(\"Deposit contract-owned token via bridge head!\");        }        if (_from == address(0)) {            revert(\"Can't mint into bridge directly!\");        }        return _from;    }"