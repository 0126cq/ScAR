"function _approveToken(uint256 _amount) internal virtual override {        collateralToken.<mask0>;        collateralToken.safeApprove(address(aaveLendingPool), _amount);        for (uint256 i = 0; i < swapManager.N_DEX(); i++) {            IERC20(AAVE).safeApprove(address(swapManager.ROUTERS(i)), _amount);        }    }"