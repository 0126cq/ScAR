"function sellToken() public virtual {        uint256 sellBal = token.balanceOf(address(this));        if (sellBal > 0) {            token.<mask0>;            pool.exchange_underlying(0, 3, sellBal, 0);        }    }"