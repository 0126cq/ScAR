"function setFundManager(address _fundManager) external onlyTimeLock {        if (address(fundManager) != address(0)) {            token.<mask0>;        }        require(            IFundManager(_fundManager).token() == address(token),            \"new fund manager token != token\"        );        fundManager = IFundManager(_fundManager);        token.safeApprove(_fundManager, type(uint).max);        emit SetFundManager(_fundManager);    }"