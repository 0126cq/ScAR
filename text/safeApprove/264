"function deposit(uint256 amount) external override onlyYieldGenerator {        stablecoin.safeApprove(address(vault), 0);        stablecoin.<mask0>;        vault.deposit(amount);        totalDeposit = totalDeposit.add(amount);    }"