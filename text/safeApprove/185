"function _swapTo(address _stableAsset, uint256 _amount) internal override returns (uint256) {    uint256 coinIndex = _getCoinIndex(_stableAsset);    IERC20(_stableAsset).safeApprove(address(IRONBANK), 0);    IERC20(_stableAsset).<mask0>;    uint256[3] memory amountsAdded;    amountsAdded[coinIndex] = _amount;    IRONBANK.add_liquidity(amountsAdded, 0, true);    return IERC20(COLLATERAL).balanceOf(address(this));  }"