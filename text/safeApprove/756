"function _supplyToPool(        address _poolTokenAddress,        ERC20 _underlyingToken,        uint256 _amount    ) internal {        if (_poolTokenAddress == cEth) {            IWETH(wEth).withdraw(_amount);             ICEther(_poolTokenAddress).mint{value: _amount}();        } else {            _underlyingToken.<mask0>;            if (ICToken(_poolTokenAddress).mint(_amount) != 0) revert MintOnCompoundFailed();        }    }"