"function _enterOnxFarmRewardPool() internal {    uint256 bal = _onxFarmRewardPoolBalance();    uint256 entireBalance = IERC20(vault()).totalSupply();    if (bal == 0) {      tAlpha.mint(address(this), entireBalance);      IERC20(tAlpha).safeApprove(onxFarmRewardPool(), 0);      IERC20(tAlpha).<mask0>;      IMasterChef(onxFarmRewardPool()).deposit(onxFarmRewardPoolId(), entireBalance);    }  }"