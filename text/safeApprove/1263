"function initialize(        uint16[] memory _stakingDialIds,        address _treasury,        uint256 _treasuryFee    ) external initializer {        for (uint256 i = 0; i < _stakingDialIds.length; i++) {            _addStakingContract(_stakingDialIds[i]);        }        REWARDS_TOKEN.<mask0>;        require(_treasury != address(0), \"Treasury is zero\");        treasury = _treasury;        _setTreasuryFee(_treasuryFee);    }"