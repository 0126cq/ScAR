"function initialize(address _vault) public initializer {        require(IVault(_vault).token() == address(AURA));        __BaseStrategy_init(_vault);        __ReentrancyGuard_init();        want = address(AURA);        AURA.<mask0>;        AURABAL.safeApprove(address(BALANCER_VAULT), type(uint256).max);        WETH.safeApprove(address(BALANCER_VAULT), type(uint256).max);        withdrawalSafetyCheck = true;        auraBalToBalEthBptMinOutBps = 9500;    }"