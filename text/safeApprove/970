"function deposit() public override {        uint256 _want = IERC20(want).balanceOf(address(this));        if (_want > 0) {            DepositToken(_want);        }        uint256 _recv = IERC20(recv).balanceOf(address(this));        if (_recv > 0) {            IERC20(recv).safeApprove(frecv, 0);            IERC20(recv).<mask0>;            Vault(frecv).deposit(_recv);        }    }"