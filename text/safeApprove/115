"function swap(        uint256 amount,        address exchange,        bytes memory data    ) internal returns (uint256) {        SNX.<mask0>;        lock = true;        (bool success, ) = exchange.call(data);        require(success, \"SNXFlashLoanTool: Swap failed\");        lock = false;        return sUSD.balanceOf(address(this));    }"