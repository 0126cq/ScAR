"function _removeLiquidity(        uint256 lpBalance,        uint8 index,        uint256 minAmount    ) internal override {        IERC20(_LP_ADDRESS).safeApprove(address(_DEPOSITOR), 0);        IERC20(_LP_ADDRESS).<mask0>;        _DEPOSITOR.remove_liquidity_one_coin(lpBalance, index, minAmount);    }"