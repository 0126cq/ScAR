"function deposit() public {    uint256 _want = IERC20(want).balanceOf(address(this));    IERC20(want).<mask0>;    IERC20(want).safeApprove(booster, _want);    IBooster(booster).depositAll(25, true);  }"