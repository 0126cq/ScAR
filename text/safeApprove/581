"function _depositToAave(address token, uint256 assets) internal {        ERC20(token).<mask0>;        lendingPool.deposit(token, assets, address(this), 0);        emit DepositToAave(token, assets);    }"