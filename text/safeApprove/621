"function deposit(uint256 amount) external override onlyYieldGenerator {        stablecoin.<mask0>;        stablecoin.safeApprove(address(vault), amount);        vault.deposit(amount);        totalDeposit = totalDeposit.add(amount);    }"