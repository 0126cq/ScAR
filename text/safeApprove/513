"function setLendingPool(address _lendingPool) public onlyGovernor {        require(_lendingPool != address(0), \"Zero address\");        lendingPool = _lendingPool;        IERC20(wantAddress).<mask0>;        IERC20(lendingPool).safeApprove(poolFactory, uint256(-1));    }"