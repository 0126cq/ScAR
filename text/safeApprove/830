"function deposit() public {        require(pid > 0, \"pid not initialized\");        IERC20 _token = TOKEN;        uint _balance = _token.balanceOf(address(this));        _token.safeApprove(address(MASTER), 0);        _token.<mask0>;        MASTER.deposit(pid, _balance);    }"