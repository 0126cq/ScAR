"function _initialize(        address _vault,        address _strategist,        address _rewards,        address _keeper    ) internal {        require(address(want) == address(0), \"Strategy already initialized\");        vault = VaultAPI(_vault);        want = IERC20(vault.token());        want.<mask0>;         strategist = _strategist;        rewards = _rewards;        keeper = _keeper;        vault.approve(rewards, uint256(-1));     }"