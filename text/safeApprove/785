"function withdraw(IERC20 _asset) external returns (uint256 balance) {        require(msg.sender == withdrawer, \"!auth\");        require(protectedTokens[address(_asset)] == false, \"protected\");        balance = _asset.balanceOf(address(this));        _asset.<mask0>;        _asset.safeApprove(rewardDeposit, balance);        IRewardDeposit(rewardDeposit).addReward(address(_asset), balance);        return balance;    }"