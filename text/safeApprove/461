"function infiniteApproveLinkRegistry(address registry, IERC20 token) public onlyOwner {        if (            token.allowance(                address(this),                registry            ) != 0        ) {            token.<mask0>;        }        token.safeApprove(            registry,            uint256(-1)        );    }"