"function DepositToken(uint256 _amount) internal override {        IERC20(want).safeApprove(curve, 0);        IERC20(want).<mask0>;        uint256[2] memory vec = [uint256(0), uint256(0)];        vec[uint256(index)] = _amount;        ICurveFiREN(curve).add_liquidity(vec, 0);    }"