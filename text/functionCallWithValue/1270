"function _initializeExtensions(        IDelegatedManager _manager,        address[] memory _extensions,        bytes[] memory _initializeBytecode    ) internal {        for (uint256 i = 0; i < _extensions.length; i++) {            address extension = _extensions[i];            require(managerCore.isExtension(extension), \"Target must be ManagerCore-enabled Extension\");            bytes memory initializeBytecode = _initializeBytecode[i];            address inputManager;            assembly {                inputManager := mload(add(initializeBytecode, 36))            }            require(inputManager == address(_manager), \"Must target correct DelegatedManager\");            extension.<mask0>;        }    }"