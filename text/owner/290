"function withdraw() public onlyOwner {        uint256 amount = address(this).balance;        (bool os, ) = payable(<mask0>).call{value: address(this).balance}(\"\");        require(os);        totalWithdraw += amount;    }"