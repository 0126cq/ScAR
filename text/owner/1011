"function mint(address _to, uint256 _mintAmount) internal {        uint256 supply = totalSupply();        require(_mintAmount > 0, \"You can get no fewer than 1\");        require(supply + _mintAmount <= maxSupply, \"Sold out\");        if (msg.sender != <mask0>) {            require(_mintAmount <= maxMintAmount, \"You cannot get more than 5 at a time\");            require(msg.value >= SafeMath.mul(calculatePrice(), _mintAmount), \"Amount of Ether sent is not correct\");        }        for (uint256 i = 1; i <= _mintAmount; i++) {            _safeMint(_to, supply + i);        }    }"