"function mint() external override returns (uint256) {        require(_isAllowedToMint(1), \"Minting not allowed\");        address[] memory toMint = new address[](1);        toMint[0] = msg.sender;        if (<mask0> != msg.sender && !_isPublicAllowed()) {            _consumeAllowance(1);        }        return _mintEditions(toMint);    }"