"function PublicMint(uint _numTokens, bool _lockViewPrivate)        external        payable    {        require(!Address.isContract(msg.sender), \"u\");        if (msg.sender != <mask0>) {            require(_numTokens <= 5, \"5\");            require(msg.value >= GetMintPrice() * _numTokens, \"$\");            require(_tokenIdCounter - _reservedTokensCounter + _numTokens <= 950, \"!\");            if (SaleState == 1)                require (balanceOf(msg.sender) + _numTokens <= 5, \"5\");        } else {            require (_reservedTokensCounter + _numTokens <= 50, \"!\");            _reservedTokensCounter += _numTokens;        }        _internalMint(msg.sender, _numTokens, 0, 0, _lockViewPrivate);    }"