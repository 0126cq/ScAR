"function transfer(address to, uint256 amount) public override returns (bool) {        if(locked) {            require(msg.sender == <mask0> || whitelist[msg.sender]);        }        return super.transfer(to, amount);    }"