"function withdraw() public onlyOwner{        uint amount = address(this).balance;        (bool success, ) = <mask0>.call{value: amount}(\"\");        require(success, \"Failed to send Ether\");    }"