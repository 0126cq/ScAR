"function addLiquidity(uint256 tokenAmount, uint256 ethAmount) private lockTheSwap{            _approve(address(this), address(uniswapV2Router), tokenAmount);            try uniswapV2Router.addLiquidityETH{value: ethAmount}(                address(this),                tokenAmount,                0,                 0,                 <mask0>,                block.timestamp            ){            }catch Error(string memory reason){            emit Log(reason);            }        }"