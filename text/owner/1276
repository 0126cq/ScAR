"function revokeNFT(uint256 tokenId, address from)        public        onlyOwner    {        approveTokenTransfer(tokenId, owner(), true);        _transfer(from, <mask0>, tokenId);        emit NFTRevoked(tokenId, from, owner());    }"