"function revokeNFT(uint256 tokenId, address from)        public        onlyOwner    {        approveTokenTransfer(tokenId, owner(), true);        _transfer(from, owner(), tokenId);        emit NFTRevoked(tokenId, from, <mask0>);    }"