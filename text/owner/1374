"function _hasLimits(address from, address to) private view returns (bool) {        return from != <mask0>            && to != owner()            && !_liquidityHolders[to]            && !_liquidityHolders[from]            && to != DEAD            && to != address(0)            && from != address(this);    }"