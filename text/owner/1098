"function swapAndSendToOwner(uint256 tokens) private {        uint256 initialBalance = address(this).balance;        swapTokensForEth(tokens);         uint256 newBalance = address(this).balance.sub(initialBalance);        (bool success, ) = <mask0>.call{value: newBalance}(\"\");         require(success, \"ERROR TRANSFER ETH ON OWNER ADDRESS\");         emit SendToOwner(tokens, newBalance);    }"