"function mintReservedTokens(uint8 amount) public onlyOwner {        require(            reservedTokensMinted + amount <= NUMBER_RESERVED_TOKENS,            \"This amount is more than max allowed\"        );        for (uint256 i = 0; i < amount; i++) {            _safeMint(<mask0>, supply);            supply++;            reservedTokensMinted++;        }    }"