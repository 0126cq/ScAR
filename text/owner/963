"function transferOwnership(address newOwner) override public onlyOwner {        address prevOwner = <mask0>;        super.transferOwnership(newOwner);        for (uint256 i = 0; i < _numOptions; i++) {            if (!_exists(i)) {                emit Transfer(prevOwner, newOwner, i);            }        }    }"