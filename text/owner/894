"function setHeaderFooter(bytes memory _bHeader, bytes memory _bFooter) public onlyOwner {        require(msg.sender == <mask0>, \"You are not the owner\");        header = abi.decode(_bHeader, (string));        footer = abi.decode(_bFooter, (string));    }"