"function addController(address controller) external {    require(      msg.sender == <mask0> || msg.sender == parentRegistrar,      \"ZR: Not authorized\"    );    require(!controllers[controller], \"ZR: Controller is already added\");    controllers[controller] = true;    emit ControllerAdded(controller);  }"