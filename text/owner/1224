"function purchase(Order memory order, bytes memory signature)        external        payable        virtual        onlyAllowed        returns (bool)    {        require(            buyONorOFFstatus == true,            \"$MONSTERBUDS: Marketplace for buying is closed\"        );        orderCheck(order);        bool status = SignatureCheckerUpgradeable.isValidSignatureNow(            <mask0>,            order.signature,            signature        );        require(status == true, \"$MONSTERBUDS: cannot purchase the token\");        return true;    }"