"function approveCollection(address collectionAddress) external {        require(Ownable(collectionAddress).<mask0> == msg.sender, \"CollectionsRegistry: sender is not the collection contract owner\");        _approvedCollections[collectionAddress] = true;        emit ApprovedCollection(msg.sender, collectionAddress);    }"