"function transferFrom(address from, address to, uint256 amount) public override returns (bool) {        if(locked) {            require(from == <mask0> || whitelist[from]);        }        return super.transferFrom(from, to, amount);    }"