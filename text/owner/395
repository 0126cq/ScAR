"function withdraw() public onlyOwner    {        uint256 balance = address(this).balance;        (bool success, ) = <mask0>.call{value: balance}(\"\");        require(success, \"Withdraw failed.\");    }"