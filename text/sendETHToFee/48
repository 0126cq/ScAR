"function swapAndLiquidify(uint256 contractTokenBalance) private {            swapTokensForEth(contractTokenBalance);            uint256 ETHBalance = address(this).balance;            if(ETHBalance > 0){                uint256 EthForTeams = ETHBalance.mul(66).div(10**2);                <mask0>;                uint256 EthForLottery = (ETHBalance).sub(EthForTeams);                sendETHToLottery(EthForLottery);            }        }"