"function _takeTeam(uint256 tTeam) private {        uint256 currentRate =  <mask0>;        uint256 rTeam = tTeam.mul(currentRate);        _rOwned[_teamWallet] = _rOwned[_teamWallet].add(rTeam);        if(_isExcluded[_teamWallet])            _tOwned[_teamWallet] = _tOwned[_teamWallet].add(tTeam);    }"