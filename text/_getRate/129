"function tokenFromReflection(uint256 rAmount) private view returns (uint256) {        require(rAmount <= _rTotal,\"Amount must be less than total reflections\");        uint256 currentRate = <mask0>;        return rAmount.div(currentRate);    }"