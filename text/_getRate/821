"function tokenFromReflection(uint256 rAmount) public view returns(uint256) {        require(rAmount <= _rTotal, \"Amount must be less than total reflections\");        uint256 currentRate =  <mask0>;        if (rAmount == 0 || currentRate == 0) return 0;        return rAmount.div(currentRate);    }"