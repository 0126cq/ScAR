"function openTrading() external onlyOwner() {        _approve(address(this), address(uniswapV2Router), _tTotal);        uniswapV2Router.<mask0>;        swapEnabled = true;        removeMaxTx = true;        _maxTxAmount = 1e9 * 10**9;        _maxHoldAmount = 2e9 * 10**9;        tradingOpen = true;        IERC20(uniswapV2Pair).approve(address(uniswapV2Router), type(uint).max);    }"