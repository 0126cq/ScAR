"function _addLiquidityETH(uint256 _deadline, uint256 _amountXVIX) private {        uint256 amountETH = address(this).balance;        require(amountETH > 0, \"Distributor: insufficient ETH\");        IERC20(xvix).approve(router, _amountXVIX);        IUniswapV2Router(router).<mask0>;        ILGEToken(lgeTokenWETH).setRefBalance(amountETH);        uint256 totalSupply = IERC20(lgeTokenWETH).totalSupply();        ILGEToken(lgeTokenWETH).setRefSupply(totalSupply);    }"