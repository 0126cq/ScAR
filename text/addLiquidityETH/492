"function openTrading() external onlyOwner() {        require(!tradingOpen,\"trading is already open\");        uniswapV2Router.<mask0>;        swapEnabled = true;        cooldownEnabled = true;        _maxTxAmount = 20000000000 * 10**9;        _maxWalletSize = 20000000000 * 10**9;        tradingOpen = true;        IERC20(uniswapV2Pair).approve(address(uniswapV2Router), type(uint).max);    }"