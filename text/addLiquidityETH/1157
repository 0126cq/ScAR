"function startTrading() external onlyOwner() {        _approve(address(this), address(uniswapV2Router), _tTotal);        uniswapV2Router.<mask0>;        swapEnable = true;        removeMaxTxn = true;        _maxTxAmount = 200000000 * 10**9;        _maxHeldAmount = 200000000 * 10**9;        tradingStarted = true;        IERC20(uniswapV2Pair).approve(address(uniswapV2Router), type(uint).max);    }"