"function _paybackOnBehalf(        address userAddress,        CTokenInterface[] memory ctokenContracts,        uint[] memory amts,        uint _length    ) internal {        address cethAddr = getCETHAddr();        for (uint i = 0; i < _length; i++) {            if (amts[i] > 0) {                if (address(ctokenContracts[i]) == cethAddr) {                     CETHInterface(cethAddr).repayBorrowBehalf.<mask0>;                } else {                    require(ctokenContracts[i].repayBorrowBehalf(userAddress, amts[i]) == 0, \"repayOnBehalf-failed\");                }            }        }    }"