"function migrate(address payable _recipient, uint _amount, PaymentDeposit _otherDeposit) public onlyOwner {        require(address(this).balance >= _amount, \"Not enough balance to migrate.\");        require(address(_otherDeposit) != address(this), \"Cannot migrate to same contract.\");        _otherDeposit.depositFor.<mask0>;         emit Withdraw(address(this), _recipient, _amount);    }"