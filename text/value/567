"function Collect(uint _am)    public    payable    {        var acc = Acc[msg.sender];        if( acc.balance>=MinSum && acc.balance>=_am && now>acc.unlockTime)        {            if(msg.sender.call.<mask0>())            {                acc.balance-=_am;                LogFile.AddMessage(msg.sender,_am,\"Collect\");            }        }    }"