"function withdraw(uint amount) public {    if (tokens[0][msg.sender] < amount) revert();    tokens[0][msg.sender] = safeSub(tokens[0][msg.sender], amount);    if (!msg.sender.call.<mask0>()) revert();    emit Withdraw(0, msg.sender, amount, tokens[0][msg.sender]);  }"