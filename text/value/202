"function transferToUser(address _reserve, address payable _user, uint256 _amount)        external        onlyLendingPool    {        if (_reserve != EthAddressLib.ethAddress()) {            ERC20(_reserve).safeTransfer(_user, _amount);        } else {            (bool result, ) = _user.call.<mask0>.gas(50000)(\"\");            require(result, \"Transfer of ETH failed\");        }    }"