"function toCompound(address _token, uint _amount) external onlyAuthorized {        address cToken = globalConfig.tokenInfoRegistry().getCToken(_token);        if (Utils._isETH(address(globalConfig), _token)) {            ICETH(cToken).mint.<mask0>;        } else {            require(ICToken(cToken).mint(_amount) == 0, \"mint failed\");        }    }"