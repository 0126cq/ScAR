"function withdrawFunds(DepositUtils.Deposit storage _d) internal {        uint256 available = _d.withdrawableAmounts[msg.sender];        require(_d.inEndState(), \"Contract not yet terminated\");        require(available > 0, \"Nothing to withdraw\");        require(address(this).balance >= available, \"Insufficient contract balance\");        _d.withdrawableAmounts[msg.sender] = 0;        (bool ok,) = msg.sender.call.<mask0>(\"\");        require(            ok,            \"Failed to send withdrawable amount to sender\"        );    }"