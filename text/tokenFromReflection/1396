"function excludeAccount(address account) external onlyOwner() {            require(account != 0xE78ecb8A825ea14bCC63A4C6CffBFb5Ce1dc002B, 'We can not exclude Uniswap router.');            require(!_isExcluded[account], \"Account is already excluded\");            if(_rOwned[account] > 0) {                _tOwned[account] = <mask0>;            }            _isExcluded[account] = true;            _excluded.push(account);        }"