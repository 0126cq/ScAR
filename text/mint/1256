"function _feeRewardsNxm(uint256 reward)      internal    returns (uint256 userReward)    {        uint256 adminReward = arNxmValue( reward.mul(adminPercent).div(DENOMINATOR) );        uint256 referReward = arNxmValue( reward.mul(referPercent).div(DENOMINATOR) );        if (adminReward > 0) {            arNxm.mint(beneficiary, adminReward);        }        if (referReward > 0) {            arNxm.<mask0>;            rewardManager.notifyRewardAmount(referReward);        }        userReward = reward.sub(adminReward).sub(referReward);    }"