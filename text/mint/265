"function _mintToWPUNKS(    uint256[] calldata punkIndices,    uint256[] calldata amounts) internal {    require(punkIndices.length == amounts.length, \"PunkGateway: Invalid amount\");    for(uint256 i = 0; i < punkIndices.length; ++i){      uint256 punkIndex = punkIndices[i];      address owner = PUNKS.punkIndexToAddress(punkIndex);      require(amounts[i] > 0, \"PunkGateway: Invalid amount\");      require(owner == _msgSender(), \"PunkGateway: not owner of punkIndex\");      PUNKS.buyPunk(punkIndex);      PUNKS.transferPunk(proxy, punkIndex);      WPUNKS.<mask0>;    }  }"