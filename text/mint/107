"function publicMint(uint256 amount_) public payable returns(bool) {\t\trequire(amount_ > 0, \"You should mint at least one token.\");\t\trequire(amount_ < 5, \"You can mint a max of 5 tokens per transaction.\");\t\trequire(msg.value >= mintPrice * amount_, \"Please check amount sent for mint.\");\t\trequire(mintEnabled, \"Public mint is closed.\");\t\t_token.<mask0>;\t\tpayout(msg.value);\t\treturn true;\t}"