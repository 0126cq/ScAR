"function claimHODLRewards(uint256[] memory tokenIds) public nonReentrant {        for (uint256 i = 0; i < tokenIds.length; i++) {            require(                UU.ownerOf(tokenIds[i]) == msg.sender,                \"UniQuest: Not Owner of token\"            );        }        uint256 rewards = calculateHODLRewards(tokenIds);        for (uint256 i = 0; i < tokenIds.length; i++) {            HODLLastClaim[tokenIds[i]] = block.timestamp;        }        UCD.<mask0>;    }"