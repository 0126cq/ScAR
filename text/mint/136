"function publicMint(uint256 quantity) public payable {        bytes32 _contractState = keccak256(abi.encodePacked(contractState));        require(            _contractState == publicMintState,            \"Public minting is not active\"        );        require(            msg.value >= mintPrice * quantity,            \"Insufficient value for public mint\"        );        <mask0>;    }"