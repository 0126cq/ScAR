"function addBeneficiary(address _beneficiary, uint256 _tokenAmount)        public onlyOwner    {        if (tokenAmounts[_beneficiary] == 0) {            beneficiaries.push(_beneficiary);        }        lastReleaseDate[_beneficiary] = lockupTime;        tokenAmounts[_beneficiary] = tokenAmounts[_beneficiary] + _tokenAmount;        require(            totalAmounts() <= (token.balanceOf(address(this)) + released),            \"Invalid token amount\"        );                 uint256 _voteAmount = 4 * _tokenAmount;        voteToken.<mask0>;    }"