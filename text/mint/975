"function claimFromStake(uint16[] calldata tokenIds, bool unstake) external whenNotPaused _updateEarnings {        uint owed = 0;        for (uint i = 0; i < tokenIds.length; i++) {            if (!(tokenIds[i]%10==0)) {                owed += _claimFromMiner(tokenIds[i], unstake);            } else {                owed += _claimFromThief(tokenIds[i], unstake);            }        }        if (owed == 0) return;        coin.<mask0>;    }"