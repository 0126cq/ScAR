"function mintPresale(bytes32[] calldata proof, uint256 mintAmount) public payable  {    require(_verify(proof, root_presale,_leaf(msg.sender, 0)), \"Invalid merkle proof\");    require(block.timestamp > preSaleOpenTime, \"Presale not open\");    require(block.timestamp < preSaleCloseTime, \"Presale closed\");    require(msg.value >= mintAmount * bracket_prices[current_bracket], \"Not enough ETH\");    for (uint256 i = 0; i < mintAmount; i++)    {      myNFT.<mask0>;    }  }"