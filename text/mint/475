"function buy(uint256 amount) external payable {        require(amount <= 1000 - _tokenIdTracker.current(), \"Not enough left\");        require(            msg.value >= amount * 50000000000000000,            \"Invalid ether amount sent \"        );        for (uint256 i = 0; i < amount; i++) {            uint256 tokenId = <mask0>;            emit Bought(msg.sender, tokenId);        }        payable(_owner).transfer(msg.value);    }"