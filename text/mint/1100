"function claim(uint256 _wid, uint256 amount) external payable {        UserEnftInfo storage userEnft = userEnftInfo[msg.sender][_wid];        require(amount > 0, \"amount must not zero\");        require(userEnft.amount >= amount, \"amount is bad\");        require(msg.value == claimFee(_wid, amount), \"need payout claim fee\");        _removeUserEnft(msg.sender, _wid, amount);        ETradingNFT.<mask0>;    }"