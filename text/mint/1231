"function pledgeHEX(uint256 amount) nonReentrant external {        require(hex_token.currentDay()<=MINTING_PHASE_END, \"Minting Phase is Done\");        require(hex_contract.allowance(msg.sender, MAXI_ADDRESS)>=amount, \"Please approve contract address as allowed spender in the hex contract.\");        address from = msg.sender;        hex_contract.transferFrom(from, MAXI_ADDRESS, amount);        <mask0>;    }"