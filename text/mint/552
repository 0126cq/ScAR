"function mintVested(address _to, uint256 _amount, uint256 _startTime, uint256 _duration) public onlyOwner canMint    returns(bool minted)    {        require(vesting == address(0), \"TokenVesting already activated\");        vesting = new TokenVesting(_to, _startTime, 0, _duration, true);        minted = <mask0>;        require(pricingPlan.payFee(VESTING_SERVICE_NAME, _amount, msg.sender), \"vesting fee failed\");    }"