"function finalize() public inState(State.Success) onlyOwner stopInEmergency {    uint sold = tokensSold.add(  initial_tokens);    uint toShare = sold.mul(25).div(75).mul(10**uint(token.decimals()));    token.setMintAgent(address(this), true);    token.<mask0>;    token.setMintAgent(address(this), false);    token.releaseTokenTransfer();    token.burn(token.balanceOf(address(this)));    super.finalize();  }"