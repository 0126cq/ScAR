"function claimFromStake(uint16[] calldata tokenIds, bool unstake) external whenNotPaused _updateEarnings {        uint owed = 0;        for (uint i = 0; i < tokenIds.length; i++) {            if (!goldHunter.isPirate(tokenIds[i])) {                owed += _claimFromMiner(tokenIds[i], unstake);            } else {                owed += _claimFromPirate(tokenIds[i], unstake);            }        }        if (owed == 0) return;        gold.<mask0>;    }"