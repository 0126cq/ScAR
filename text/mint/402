"function mint(bytes32[] calldata merkleProof_, uint256 quantity_) public payable{        require(msg.value >= price,\"Insuffient Funds Provided\");        require(claimed[msg.sender] + quantity_ <= mintQuantity, \"Already claimed.\");        claimed[msg.sender] = claimed[msg.sender] + quantity_;        for(uint256 i = 0; i < quantity_; i++){            erc721.<mask0>;        }    }"