"function mint(address to, uint256 count) external payable {        if (whiteListEnabled) {            require(                whiteList[msg.sender],                \"mint enable only for whitelist at moment\"            );        }        uint256 needPriice = pricePpm * 1e15 * count;        require(msg.value >= needPriice, \"not enough ether value\");        for (uint256 i = 0; i < count; ++i) nft.<mask0>;    }"