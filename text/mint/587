"function claimMany(uint16[] calldata tokenIds, bool unstake) external whenNotPaused  {        uint owed = 0;        for (uint i = 0; i < tokenIds.length; i++) {            require (initiateGame[tokenIds[i]]==true,'Game Not Initiated');            require(fighters[tokenIds[i]].tokenId == tokenIds[i] || palace[tokenIds[i]].tokenId == tokenIds[i], \"Yeti is not staked\");            owed += _claimYeti(tokenIds[i], unstake);        }        require(owed > 0, \"Claiming before 1 day\");        frxst.<mask0>;    }"