"function mintTeamAlloc(bytes32[] calldata proof, uint256 authorizedAmount, uint256 mintAmount)  public  {    require(_verify(proof, root_team_alloc,_leaf(msg.sender, authorizedAmount)), \"Invalid merkle proof\");    require(current_bracket < 2);    require(mintedFromTeam[msg.sender] + mintAmount <= authorizedAmount, \"Already minted max amount\");    mintedFromTeam[msg.sender] += mintAmount;    for (uint256 i = 0; i < mintAmount; i++)    {      myNFT.<mask0>;    }    _checkPriceBrackets();  }"