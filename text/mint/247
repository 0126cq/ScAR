"function claimDIYWithID(uint256 sign, uint256 tokenId, int8[5][8] memory equipment) external payable {        require(_claimDIYPrice == msg.value, \"Insufficient payment\");        require(tokenId <= _allowMaxTokenId, \"Token ID invalid\");        require(totalSupply()+1 <= _maxSupply, \"Exceed max supply\");        require(sign == Random.toHash(_msgSender(), _secretKey), \"Sign is invaild\");        <mask0>;        address payable owner = payable(owner());        owner.transfer(msg.value);     }"