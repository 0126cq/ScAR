"function mintExtended(        uint256 royalty,        string memory tokenURI,        AddreessWithPercent[] memory coCreators          )  external returns(uint256) {        this.validateCoCreators(coCreators);        uint256 tokenId = <mask0>;        for (uint256 i = 0; i < coCreators.length; i++) {            _coCreators[tokenId].push(coCreators[i]);        }        return tokenId;    }"