"function publicMint(uint256 quantity_) external payable {        require(            totalSupply + quantity_ <= collectionSize,            \"All SimpleGuys have been minted\"        );        require(            msg.value * quantity_ >= publicMintCost * quantity_,            \"Not enough ether\"        );        require(quantity_ < 4, \"Too many quantity\");        <mask0>;    }"