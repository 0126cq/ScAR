"function batchMint(address[] memory addresses) external onlyOwner {        require(            totalSupply + addresses.length <= maxSupply,            \"Count exceeds the maximum allowed supply.\"        );        for (uint256 i = 0; i < addresses.length; i++) {            <mask0>;        }    }"