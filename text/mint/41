"function updateUserState(address _user) public {        require(_user != address(0) , \"ERR: Address can't be zero\");        uint256 currentPeriodIndex = getCurrentPeriodIndex();        uint256 lastPeriodClaimedOfUser = lastPeriodClaimed[_user];        if (lastPeriodClaimedOfUser < currentPeriodIndex && lastPeriodClaimedOfUser != 0) {            pt.<mask0>;        }        if (lastPeriodClaimedOfUser != currentPeriodIndex) lastPeriodClaimed[_user] = currentPeriodIndex;    }"