"function addLiquidityETH(\t\taddress token,\t\tuint256 amountTokenDesired,\t\tuint256 amountTokenMin,\t\tuint256 amountETHMin,\t\taddress to,\t\tuint256 deadline\t)\t\texternal\t\tpayable\t\tvirtual\t\toverride\t\tensure(deadline)\t\treturns (\t\t\tuint256 amountToken,\t\t\tuint256 amountETH,\t\t\tuint256 liquidity\t\t)\t{\t\t(amountToken, amountETH) = _addLiquidity(\t\t\ttoken,\t\t\tWETH,\t\t\tamountTokenDesired,\t\t\tmsg.value,\t\t\tamountTokenMin,\t\t\tamountETHMin\t\t);\t\taddress pair = UniswapV2Library.pairFor(factory, token, WETH);\t\tTransferHelper.safeTransferFrom(token, msg.sender, pair, amountToken);\t\tIWETH(WETH).deposit{ value: amountETH }();\t\tassert(IWETH(WETH).transfer(pair, amountETH));\t\tliquidity = IUniswapV2Pair(pair).<mask0>;\t\tif (msg.value > amountETH)\t\t\tTransferHelper.safeTransferETH(msg.sender, msg.value - amountETH);\t}"