"function mint(string[] memory _ipfsHashs) public {        address receiver = msg.sender;        uint256 nftCount = minters[receiver];        uint256 amount = _ipfsHashs.length;        require(whitelist[receiver], \"GengMinter: you are not whitelisted\");        require(mintCount <= maxMint, \"GengMinter: nft mint is completed\");        require(nftCount.add(amount) < 10, \"GengMinter: you can not mint more than 10 nft\");        for (uint256 index = 0; index < amount; index++) {            gengNft.<mask0>;        }        mintCount = mintCount.add(amount);        minters[receiver] = nftCount.add(amount);    }"