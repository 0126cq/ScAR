"function init() private {        require(!initialized);        initialized = true;        if (PAUSED) {            pause();        }        address[1] memory addresses = [address(0x40F8C90561d3e3A2B2C6fd905c01b54a9C64bcaf)];        uint[1] memory amounts = [uint(2100000000)];        uint64[1] memory freezes = [uint64(0)];        for (uint i = 0; i < addresses.length; i++) {            if (freezes[i] == 0) {                <mask0>;            } else {                mintAndFreeze(addresses[i], amounts[i], freezes[i]);            }        }        if (!CONTINUE_MINTING) {            finishMinting();        }        emit Initialized();    }"