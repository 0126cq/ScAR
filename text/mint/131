"function mintAll(address[] calldata users, InvestorTier[] calldata tiers) external onlyOwner {        _requireNotPaused();        require(users.length == tiers.length);        for (uint i; i < users.length; ++i) {            <mask0>;        }    }"