"function _deposit() internal override {        uint256 _amount = balanceOfWant();        if (_amount > 0) {            dERC20(dToken).<mask0>;        }        uint256 _dToken = IERC20(dToken).balanceOf(address(this));        if (_dToken > 0) {            dRewards(pool).stake(_dToken);        }    }"