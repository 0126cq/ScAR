"function mintPresale(bytes32 leaf, bytes32[] memory proof, uint256 quantity) public payable {        address to = _msgSender();        require(presaleIsActive, \"PresaleNotActive\");        require(totalSupply().add(quantity) <= presaleSupply, \"PurchaseExeedsPresaleSupply\");        require(presalePrice.mul(quantity) <= msg.value, \"IncorrectPresaleValue\");        addPresalePurchaseFor(to, quantity);        checkWhitelisted(to, leaf, proof);        <mask0>;    }"