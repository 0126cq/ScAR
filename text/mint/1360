"function claimToken(address token) public {        require(stakers[msg.sender].balance[token]>0,\"you have no balance and cant claim\");        uint256 earnings = earned(token);        if(block.timestamp < stakers[msg.sender].lockedUpTill[address(token)]){            uint256 fee = earnings.div(100).mul(penaltyFee);            earnings = earnings.sub(fee);            IERC20Mintable(dappToken).mint(owner, fee);        }        stakers[msg.sender].timestamp[token]=block.timestamp;        IERC20Mintable(dappToken).<mask0>;    }"