"function harvest (address wallet, uint128 epochId) external returns (uint){        require(isApprovedPool[msg.sender], \"must be approved sender\");        require (_getEpochId() > epochId, \"This epoch is in the future\");        require(epochId <= _numberOfEpochs, \"Maximum number of epochs is 12\");        require (lastEpochIdHarvested[wallet].add(1) == epochId, \"Harvest in order\");        uint userReward = _harvest(wallet, epochId);        if (userReward > 0) {             _bacon.<mask0>;        }        emit Harvest(wallet, epochId, userReward);        return userReward;    }"