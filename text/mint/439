"function mintRewards(address _recipient, uint256 _amount) external {    require(isRewardManager[msg.sender], \"Not approved\");    require(_amount <= excessReserves(), \"Insufficient reserves\");    ERC20PresetMinterPauser(HADES).<mask0>;    emit RewardsMinted(msg.sender, _recipient, _amount);  }"