"function claimFromStake(uint16[] calldata tokenIds, bool unstake) external whenNotPaused nonReentrant {        uint owed = 0;        for (uint i = 0; i < tokenIds.length; i++) {            if (!metaMoose.isHunter(tokenIds[i])) {                owed += _claimFromHarvester(tokenIds[i], unstake);            } else {                owed += _claimFromHunter(tokenIds[i], unstake);            }        }        if (owed == 0) return;        batt.<mask0>;        totalBattClaimed += owed;    }"