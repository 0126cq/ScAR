"function _purchase(uint256 amountIn, address to)        internal        returns (uint256 amountOut)    {        amountOut = getAmountOut(amountIn);        _incrementTotalPurchased(amountOut);        rusd().<mask0>;        emit Purchase(to, amountIn, amountOut);        return amountOut;    }"