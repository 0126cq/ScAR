"function initialize(        address _roles,        address _arrayToken,        uint256 initialAmountLPToken    )    public initializer    {        require(msg.sender == owner, \"!owner\");        arrayToken = IArray(_arrayToken);        require(arraySmartPool.transferFrom(DAO_MULTISIG_ADDR, address(this), initialAmountLPToken), \"Transfer failed\");        ArrayRolesStorage.initialize(_roles);        arrayToken.<mask0>;        virtualBalance = initialAmountLPToken;        virtualSupply = STARTING_ARRAY_MINTED;    }"