"function mint(        address to,        uint256 amount,        uint256 otherChainNonce    ) external onlyAdmin {        require(            processedNonces[otherChainNonce] == false,            'transfer already processed'        );        processedNonces[otherChainNonce] = true;        token.<mask0>;        emit CrossTransfer(            _msgSender(),            to,            amount,            block.timestamp,            otherChainNonce,            Step.Mint        );    }"