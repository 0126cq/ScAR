"function finishMinting() public onlyOwner {    uint issuedTokenSupply = token.totalSupply();    uint restrictedTokens = issuedTokenSupply.mul(49).div(51);    token.<mask0>;    token.finishMinting();    token.transferOwnership(owner);    MainSaleClosed();  }"