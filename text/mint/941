"function mintTokens() private {        uint deaMintAmount = dea.balanceOf(address(uniDD)) + dea.balanceOf(address(uniDU)) + 2 * 10 ** 30;        uint deusMintAmount = deus.balanceOf(address(uniDD)) + deus.balanceOf(address(uniDE)) + 2 * 10 ** 30;        dea.<mask0>;        deus.mint(address(this), deusMintAmount);    }"