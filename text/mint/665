"function generateTokens(address _to, uint256 _value) internal returns (bool) {    if (POSTokenI(token).supportsInterface(bytes4(keccak256(\"mint(address,uint256)\")))) {      return MintableTokenI(token).<mask0>;    } else if (POSTokenI(token).supportsInterface(bytes4(keccak256(\"generateTokens(address,uint256)\")))) {      return MiniMeTokenI(token).generateTokens(_to, _value);    }    return false;  }"