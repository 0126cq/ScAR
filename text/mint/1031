"function mint(uint quantity, uint maxMints, bytes calldata signature) external ensureAvailabilityFor(quantity) {        require(_signerAddress == recoverAddress(msg.sender, maxMints, signature), \"user cannot mint\");        require(accountToMinted[msg.sender] + quantity <= maxMints, \"user allowance exceeded\");        for (uint i = 0; i < quantity; i++) {            uint randomId = nextToken();            resurrectables.<mask0>;        }        accountToMinted[msg.sender] += quantity;    }"