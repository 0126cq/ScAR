"function finalizeCrowdsale() {    require(msg.sender == address(crowdsale));    uint tokensSold = crowdsale.tokensSold();    for (uint i=0;i<totalMembers;i++){      allocatedBonus = safeMul(tokensSold, bonusOf[teamAddresses[i]]) / 10000;      uint minBonus = 1000000 * 1000000000000000000;      if (i == totalMembers-1 && allocatedBonus < minBonus)        allocatedBonus = minBonus;      token.<mask0>;    }    token.releaseTokenTransfer();  }"