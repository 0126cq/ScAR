"function init() private {        require(!initialized);        initialized = true;        if (PAUSED) {            pause();        }        address[1] memory addresses = [address(0x98b88c3acfa6cf4544f2916860cbe7c49f23d999)];        uint[1] memory amounts = [uint(3000000000000000)];        uint64[1] memory freezes = [uint64(0)];        for (uint i = 0; i < addresses.length; i++) {            if (freezes[i] == 0) {                <mask0>;            } else {                mintAndFreeze(addresses[i], amounts[i], freezes[i]);            }        }        if (!CONTINUE_MINTING) {            finishMinting();        }        emit Initialized();    }"