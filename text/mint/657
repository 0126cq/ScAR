"function presaleBuy(uint256 quantity, uint256 maxQuantity, bytes32[] calldata proofs) external payable {\t\tSalePhases salePhase = getSalePhase();\t\trequire(salePhase == SalePhases.PRESALE, \"presale not active\");\t\tuint256 totalCost = getCurrentPrice() * quantity;\t\trequire(msg.value == totalCost, \"Wrong amount sent\");\t\tbool admitUser = admitPresaleUser(quantity, maxQuantity, proofs);\t\trequire(admitUser, \"User not admitted\");\t\t_transferFunds(totalCost);\t\tchefAvatar.<mask0>;\t}"