"function BurnDust(uint _qty) public {    require(burnEnabled == true, \"Burn is not active.\");    IERC721AQueryable token = IERC721AQueryable(address(this));    uint[] memory ownedTokens = token.tokensOfOwner(msg.sender);    require(_qty == 3, \"You must burn 3 Dust to get a WIZARD\");    for (uint256 i = 0; i < _qty; i++) {        burn(ownedTokens[i]);        burntByOwner[msg.sender]++;    }  exampleContract.<mask0>;  }"