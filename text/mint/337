"function _mint(uint256 _quantity, address _to) internal returns (uint256[] memory) {        require(availableSupply >= _quantity, \"Not enough supply\");        uint256[] memory tokenIds = new uint256[](_quantity);        for (uint256 i = 0; i < _quantity; i++) {            uint256 tokenId = getNextTokenId();            availableSupply = availableSupply - 1;            tokenIds[i] = tokenId;        }        if (_quantity == 1) {            nftCollection.<mask0>;        } else {            nftCollection.mintBatch(_to, tokenIds);        }        return tokenIds;    }"