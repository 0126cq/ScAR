"function claimRewardsMultiV2(uint256[] memory tokenIds) public {        for (uint256 i; i < tokenIds.length; i++) {            require(                SS.ownerOf(tokenIds[i]) == msg.sender,                \"ShogunStaking: Claimant is not the owner!\"            );        }        uint256 rewards = calculateRewardsMultiV2(tokenIds);        for (uint256 j; j < tokenIds.length; j++) {            lastClaim[tokenIds[j]] = block.timestamp;        }        SHO.<mask0>;    }"