"function configurationCrowdsale(address team_multisig, uint start, uint end, address token_retriever, uint[] init_tranches, uint multisig_supply, uint crowdsale_supply, uint8 token_decimals, uint max_tokens_to_sell) public onlyOwner {      initial_tokens = multisig_supply;        token = new CrowdsaleToken(multisig_supply, token_decimals, team_multisig, token_retriever);      token.setMintAgent(address(this), true);      token.setReleaseAgent(address(this));      token.setTransferAgent(address(this), true);      token.<mask0>;      sellable_tokens = max_tokens_to_sell;      configurationGenericCrowdsale(team_multisig, start, end);      configurationTokenTranchePricing(init_tranches);  }"