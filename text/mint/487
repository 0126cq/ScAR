"function init() public onlyOwner {        require(!initialized);        initialized = true;        if (PAUSED) {            MainToken(token).pause();        }        address[1] memory addresses = [address(0xc46e5282ca98b982b9cd5d7b029a77573b2f8307)];        uint[1] memory amounts = [uint(20000000000000000000000000)];        uint64[1] memory freezes = [uint64(0)];        for (uint i = 0; i < addresses.length; i ++) {            if (freezes[i] == 0) {                token.<mask0>;            }            else {                FreezableMintableToken(token).mintAndFreeze(addresses[i], amounts[i], freezes[i]);            }        }        transferOwnership(TARGET_USER);        Initialized();    }"