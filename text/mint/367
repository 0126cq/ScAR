"function mintBatch(        address[] calldata receivers_,        uint256[] calldata years_,        bool withPoap_    ) public onlyRole(MINTER_ROLE) {        require(            receivers_.length == years_.length,            \"LuckyVR: receivers and years lengths must match\"        );        if (withPoap_) require(poapActive, \"LuckyVR: POAP is not active\");        for (uint256 i = 0; i < receivers_.length; i++) {            <mask0>;        }    }"