"function paperMint(PaperMintData.MintData calldata _mintData)        external        payable        onlyPaper(_mintData)        nonReentrant    {        require(mintIsActive, \"Mint is not active\");        require(_mintData.quantity <= MAX_TOKENS_PURCHASE, \"You went over max tokens per transaction\");        require(numTokensMinted + _mintData.quantity <= MAX_TOKENS, \"Not enough tokens left to mint that many\");        <mask0>;    }"