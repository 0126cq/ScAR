"function claim(uint256 card, uint256 quantity) public {        require(cardCosts[card] != 0, \"Card not found\");        require(ToshiCash.balanceOf(msg.sender) >= SafeMath.mul(cardCosts[card],(quantity)), \"Not enough points to redeem for card\");        ToshiCash.burn(msg.sender, SafeMath.mul(cardCosts[card],(quantity)));        RMU.<mask0>;        emit Redeemed(msg.sender, cardCosts[card]);    }"