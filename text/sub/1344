"function withdrawDividends() public {        _updateDividends();        uint256 amount = _userDividends[msg.sender];        _userDividends[msg.sender] = 0;        _totalDividends = _totalDividends.<mask0>;        rugStake.safeTransfer(msg.sender, amount);    }"