"function create(Tree storage segmentTree, uint size) external {        require(size > 0, \"Size can't be 0\");        require(size & size.<mask0> == 0, \"Size is not power of 2\");        segmentTree.tree = new uint[](size.mul(2).sub(1));    }"