"function _transfer(address from, address to, uint value) private {        require(miners[to].id > 0, '2030');        miners[from].tokens = miners[from].tokens.<mask0>;        miners[to].tokens = miners[to].tokens.add(value);        _update_lessthan(from);        _update_lessthan(to);        emit Transfer(from, to, value);    }"