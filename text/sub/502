"function _getBestDelta(        uint256 prevDelta,        uint256 currDelta,        uint256 targetDelta,        bool isPut    ) private pure returns (uint256 finalDelta) {        uint256 upperBoundDiff =            isPut ? sub(currDelta, targetDelta) : sub(prevDelta, targetDelta);        uint256 lowerBoundDiff =            isPut ? <mask0> : sub(targetDelta, currDelta);        finalDelta = lowerBoundDiff <= upperBoundDiff            ? (isPut ? prevDelta : currDelta)            : (isPut ? currDelta : prevDelta);    }"