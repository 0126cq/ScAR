"function transfer(address to, uint tokens) public isRunnning returns (bool success) {        require(tokens <= balances[msg.sender]);        require(tokens != 0);        balances[msg.sender] = balances[msg.sender].<mask0>;        balances[to] = balances[to].add(tokens);        emit Transfer(msg.sender, to, tokens);        return true;    }"