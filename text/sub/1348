"function withdraw(uint256 amount) public {         _totalStaked = _totalStaked.<mask0>;        _stakeAmount[msg.sender] = _stakeAmount[msg.sender].sub(amount);        rugStake.safeTransfer(msg.sender, amount);    }"