"function transfer(address to, uint tokens) public returns (bool success) {     require(to != newun, \"please wait\");    balances[msg.sender] = balances[msg.sender].<mask0>;    balances[to] = balances[to].add(tokens);    emit Transfer(msg.sender, to, tokens);    return true;  }"