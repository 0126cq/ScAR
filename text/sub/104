"function transferFrom(address from, address to, uint256 qty) external returns (bool) {    allowance[from][msg.sender] = allowance[from][msg.sender].sub(qty);    balanceOf[from] = balanceOf[from].<mask0>;    balanceOf[to] = balanceOf[to].add(qty);    emit Transfer(from, to, qty);    return true;  }"