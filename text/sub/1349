"function withdraw(uint256 amount) public {         _totalStaked = _totalStaked.sub(amount);        _stakeAmount[msg.sender] = _stakeAmount[msg.sender].<mask0>;        rugStake.safeTransfer(msg.sender, amount);    }"