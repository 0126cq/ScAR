"function status(uint96 tic, uint256 top) internal view returns (bool done, uint256 price) {        price = calc.price(top, <mask0>);        done  = (sub(block.timestamp, tic) > tail || rdiv(price, top) < cusp);    }"