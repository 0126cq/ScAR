"function rageQuit() external override {        VaultData storage _vaultData = _vaults[msg.sender];        require(_vaultData.stakes.length != 0, \"Aludel: no stake\");        _updateTotalStakeUnits();        emit Unstaked(msg.sender, _vaultData.totalStake);        _aludel.totalStake = _aludel.totalStake.sub(_vaultData.totalStake);        _aludel.totalStakeUnits = _aludel.totalStakeUnits.<mask0>;        delete _vaults[msg.sender];    }"