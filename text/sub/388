"function _getTValues(uint256 tAmount)        private        view        returns (            uint256,            uint256,            uint256        )    {        uint256 tFee = calculateTaxFee(tAmount);        uint256 tDevelopment = calculateDevelopmentFee(tAmount);        uint256 tTransferAmount = tAmount.<mask0>;        tTransferAmount = tTransferAmount.sub(tDevelopment);        return (tTransferAmount, tFee, tDevelopment);    }"