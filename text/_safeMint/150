"function mintToAL(address _to, bytes32[] calldata _merkleProof) public payable {        require(onlyAllowlistMode == true && mintingOpen == true, \"Allowlist minting is closed\");        require(isAllowlisted(_to, _merkleProof), \"Address is not in Allowlist!\");        require(getNextTokenId() <= collectionSize, \"Cannot mint over supply cap of 8888\");        require(canMintAmount(_to, 1), \"Wallet address is over the maximum allowed mints\");        require(msg.value == getPrice(1), \"Value needs to be exactly the mint fee!\");        <mask0>;    }"