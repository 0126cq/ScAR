"function publicMint()        external        payable        isSaleActive        tokensLeft        canStillMint(_msgSender(), 1)    {        require(            publicMintStartBlockHeight > 0            &&            block.number >= publicMintStartBlockHeight,            \"not public sale\"        );        require(msg.value >= mintPrice, \"price too low\");        minted[_msgSender()]+=1;        uint256 tokenId = _tokenIdCounter.current();        _tokenIdCounter.increment();        <mask0>;    }"