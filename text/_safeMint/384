"function _mint(uint256 token_id) private onlyOwner {        require(!_exists(token_id), \"Token has already been minted\");        address recipient = holders[token_id];        require(recipient != address(0), \"Cannot mint to zero address\");        <mask0>;    }"