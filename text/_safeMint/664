"function mint(address to, InvestorTier tier) public onlyOwner returns (uint256 tokenId){        _requireNotPaused();        tokenId = totalSupply() + 1;        <mask0>;        tierOf[tokenId] = tier;    }"