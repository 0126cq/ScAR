"function mint(uint256 quantity) public {        require(msg.sender == minter, \"You are not owner!\");        require(totalSupply() + quantity <= 10000, \"Can't mint anymore\");        <mask0>;    }"