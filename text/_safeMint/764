"function communitySaleMint(uint256 _maxAmount, bytes32[] calldata _proof) external payable callerIsUser {        require(isCommunitySaleActive, \"COMMUNITY SALE NOT ACTIVE\");        require(msg.value == communitySalePrice, \"NEED TO SEND CORRECT ETH AMOUNT\");        require(totalSupply() + maxCommunityMint <= MAX_SUPPLY, \"MAX SUPPLY REACHED\" );        require(!communitySaleMinted[msg.sender], \"EXCEEDS MAX CLAIM\");         bytes32 sender = keccak256(abi.encodePacked(msg.sender, _maxAmount));        bool isValidProof = MerkleProof.verify(_proof, communitySaleMerkleRoot, sender);        require(isValidProof, \"INVALID PROOF\");        communitySaleMinted[msg.sender] = true;        <mask0>;    }"