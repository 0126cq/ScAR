"function mint(uint count,uint signature) external payable {\t\trequire(!isPaused, \"Sales are off\");        require(_totalMinted() + count <= maxSupply,\"Exceeds max supply\");        require(_walletMintedCount[msg.sender] + count <= MAX_PER_WALLET,\"Exceeds max per wallet\");        require(signature<=block.timestamp && signature >= block.timestamp-300,\"Bad Signature!\");\t\t_walletMintedCount[msg.sender] += count;\t\t<mask0>;\t}"