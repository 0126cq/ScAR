"function teamMint(address _address) public onlyOwner {    require(enableMinter, \"Minter not active\");    require(teamMintSize != 0, \"Team minting not enabled\");    require(_tokenIdCounter.current() < mintSize, \"Can not mint that many\");    require(_teamMintCounter.current() < teamMintSize, \"Can not team mint anymore\");    <mask0>;    _setTokenURI(_tokenIdCounter.current(), mintID());    _tokenIdCounter.increment();    _teamMintCounter.increment();  }"