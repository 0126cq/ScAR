"function mint(uint256 tierIndex, address owner)        external        override        onlyMinter    {        uint256 tokenId = _tokenCounter.current();        _tokenCounter.increment();        <mask0>;        _tokenTier[tokenId] = tierIndex;        _setOwner(owner, tokenId);    }"