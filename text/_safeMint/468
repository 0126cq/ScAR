"function mint(uint256 _amount) external payable {    uint256 supply = totalSupply();    require(isActive, 'Not Active');    require(_amount <= MAX_TOKENS_PURCHASE, 'Amount Denied');    require(supply + _amount <= maxTokens, 'Supply Denied');    require(msg.value >= price * _amount, 'Ether Amount Denied');    _amount = _amount + _amount / TOKENS_FOR_ONE_FREE;    for(uint256 i=1; i <= _amount; i++){      <mask0>;    }  }"