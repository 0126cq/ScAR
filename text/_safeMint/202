"function _mintBatch(           address account,         uint256[] memory _ids,         string[] memory uri    )         internal virtual    {        require(account != address(0));        require(_ids.length == uri.length);        address operator = _msgSender();        for (uint i = 0; i < _ids.length; i++) {               <mask0>;               _setTokenURI(_ids[i], uri[i]);                }        emit TransferredBatch(operator, address(0), account, _ids);    }"