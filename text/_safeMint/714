"function bridgeIn(        uint256 _tokenId,        address _to,        string memory _extra    ) public {        require(msg.sender == bridge, \"wrong bridge address\");        <mask0>;        emit BridgeIn(_tokenId, _to, _extra);    }"