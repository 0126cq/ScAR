"function bmwFreeMint() public {        require(bmwActive, \"Must be active state for minting\");        require(bmwRemainingOfOwner(msg.sender) != 0, \"No available token\");        uint256 balance = BMW.balanceOf(msg.sender);        require(balance != 0, \"Balance of BMW must greater 0\");        for (uint i = 0; i < balance; i++) {            uint256 id = BMW.tokenOfOwnerByIndex(msg.sender, i);            if (!bmwTokenUsed[id]) {                <mask0>;                bmwTokenUsed[id] = true;                currentTokenId += 1;                break;            }        }    }"