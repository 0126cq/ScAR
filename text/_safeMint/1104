"function MintFBH (uint256 mintCount) internal {        require(mintCount > 0, \"No. of tokens <= zero\");        require(_tokenIds.current() + mintCount <= _maxSupply, \"Only 9999 can be minted\");        for (uint256 i = 0; i < mintCount; i++) {          _tokenIds.increment();          uint256 newItemId = _tokenIds.current();          <mask0>;      }    }"