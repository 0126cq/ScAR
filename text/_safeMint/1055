"function reserveDerp(uint256 numberOfTokens) public onlyOwner {        require((adminMinted + numberOfTokens) <= adminReserved, \"Purchase would exceed reserved supply\");        uint256 supply = totalSupply();        uint256 i;        for (i = 0; i < numberOfTokens; i++) {            if (totalSupply() < MAX_DERP) {                uint256 mintIndex = supply + i;                adminMinted++;                <mask0>;            }        }    }"