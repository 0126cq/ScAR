"function presaleMintAnimals(        uint8 v,        bytes32 r,        bytes32 s    ) external botDefender verifySig(PRESALE_HASH, r, s, v) {        Config memory config = _config;        if (!config.isPresaleActive) revert ErrorPresaleNotStarted();        ensureSupply(config, 1);        if (incrementPresaleMinted(1) > PRESALE_MAX_MINT) revert ErrorExceedPresaleMintLimit();        <mask0>;    }"