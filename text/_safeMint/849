"function freeMint(uint256 quantity)        public        callerIsUser        nonReentrant        mintCompliance(quantity)    {        if (_freeMintCount + quantity > MAX_FREE_MINT_RESERVE)            revert FreeMintReserveExceeded();        <mask0>;        unchecked {            _freeMintCount += quantity;        }    }"