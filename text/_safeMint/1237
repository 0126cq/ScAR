"function mint(uint256 amount) external payable mintGuard(amount) {        require(isPublicLive, \"Sale not live\");        require(publicAddressMintCount[msg.sender] + amount <= MAX_PALS_PURCHASE, \"Address already minted allocation\");        publicAddressMintCount[msg.sender] += amount;        <mask0>;    }"