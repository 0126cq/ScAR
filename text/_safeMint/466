"function claim(bytes32[] calldata _merkleProof) external {    uint256 supply = totalSupply();    require(isClaimActive, 'Not Active');    require(supply < maxTokens, 'Supply Denied');    require(!claimParticipants[_msgSender()], 'Mint Already Claimed');    bytes32 leaf = keccak256(abi.encodePacked(_msgSender()));    require(MerkleProof.verify(_merkleProof, claimMerkleRoot, leaf), 'Proof Invalid');    <mask0>;    claimParticipants[_msgSender()] = true;  }"