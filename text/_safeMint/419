"function safeMint() public payable whenNotPaused publicMintChecks {        address to = msg.sender;        _tokenIdCounter.increment();        uint256 tokenId = _tokenIdCounter.current();        <mask0>;    }"