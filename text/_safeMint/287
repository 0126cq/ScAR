"function publicMint(address _wallet, uint256 numToMint) payable external nonReentrant {        require(publicSaleOpened, \"Sale not open\");        require(msg.sender == tx.origin, \"Contract mint not allowed\");        require(numToMint > 0, \"Should mint at least one\");        require((numToMint + totalSupply()) <= maxSupply, \"Not enough token left\");        require(numToMint <= publicMaxMintsPerTx, \"Too many tokens claimed\");        require(msg.value >= publicPrice * numToMint, \"Not enough ETH sent\");        <mask0>;    }"