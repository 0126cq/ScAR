"function gift(address to, uint256 amount) external onlyOwner {        require(amount > 0, \"Need to mint at least one token\");        uint256 supply = totalSupply();        for (uint256 i = 0; i < amount; i++) {            tokensCount += 1;            <mask0>;        }    }"