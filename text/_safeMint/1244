"function reserve(address _to, uint256 _reserveAmount) public onlyOwner {        uint256 supply = totalSupply();        require(            _reserveAmount > 0 && _reserveAmount <= teamReserveAvailableMints,            \"Not enough reserve left for team\"        );        for (uint256 i = 0; i < _reserveAmount; i++) {            <mask0>;        }        teamReserveAvailableMints -= _reserveAmount;    }"