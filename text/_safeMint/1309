"function mint(address _to, uint256 _quantity) public payable {        uint256 supply = totalSupply();        if (msg.sender != owner()) {            require(mainSale, \"The Baby Ghosts main sale is not open\");            require(_quantity > 0 && _quantity <= maxMint, \"You can only mint 1 to 20 Baby Ghosts\");            require(msg.value >= price * _quantity, \"Ether sent is not correct\");        }        require(supply + _quantity <= MAX_TOKENS, \"Exceeds maximum supply\");        for (uint256 i = 1; i <= _quantity; i++) {          <mask0>;        }    }"