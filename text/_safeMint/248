"function mint(        address _to,        uint256 _quantity,        bytes memory _data    ) public {        require(            _msgSender() == controller || _msgSender() == owner(),            \"AviumNFT: only owner or controller are allowed\"        );        require(            _currentIndex + _quantity <= totalMint + 1,            \"AviumNFT: the total mint has been exeeded\"        );        <mask0>;    }"