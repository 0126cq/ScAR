"function mint(uint256 _quantity)        external        payable        mintCompliance(_quantity)    {        uint256 requiredValue = _quantity * price;        uint256 userMinted = _numberMinted(msg.sender);        if (userMinted == 0) {            requiredValue = _quantity <= maxFree                ? 0                : requiredValue - (price * maxFree);        }        require(msg.value >= requiredValue, \"Sent Ether is too low\");        if (_totalMinted() + _quantity <= collectionSize) {            <mask0>;        }    }"