"function publicSaleMint(uint256 quantity) external payable {        if (saleStartTime == 0 || block.timestamp < saleStartTime)            revert SaleNotStarted();        if (quantity == 0 || quantity > maxTokensPerTx)            revert QuantityOffLimits();        if (totalSupply() + quantity > maxSupply) revert MaxSupplyReached();        if (msg.value != price * quantity) revert InsufficientFunds();        <mask0>;    }"