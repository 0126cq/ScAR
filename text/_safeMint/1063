"function mint(uint256 _count) external payable saleIsOpen {        uint256 totalSupply = totalSupply();        require(totalSupply <= maxSupply, \"Sold out.\");        require(            _count <= maxPerTransaction,            \"Surpasses max items per transaction.\"        );        require(msg.value == price * _count, \"Invalid Payment Value.\");        for (uint256 i = 0; i < _count; i++) {            <mask0>;        }    }"