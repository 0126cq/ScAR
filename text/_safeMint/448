"function mintBot(uint256 num) public payable {        uint256 supply = totalSupply();        require( !_paused,                              \"Sale paused\" );        require( num <= maxPerTx,                              \"Exceeds maximum amount of Bots per tx\" );        require( supply + num <= MAX_SUPPLY - _giftReserved - _esReserved,      \"Exceeds maximum Bots supply\" );        require( msg.value >= _price * num,             \"Ether sent is not correct\" );        for(uint256 i; i < num; i++){            <mask0>;        }    }"