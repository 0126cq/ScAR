"function Claim(uint256 quantity)    external    payable    callerIsUser  {    require(quantity <= freemintAmount, \"Too many minted at once\");    require(totalSupply() + quantity <= collectionSizeFree, \"reached max supply\");    <mask0>;  }"