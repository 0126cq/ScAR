"function _mintCommon(uint256 numTokensToMint) internal {        require(mintingAllowed, \"Minting unallowed.\");        require(block.timestamp <= mintEndTime, \"Minting closed.\");        for (uint256 i = 0; i < numTokensToMint; ++i) {            _tokenIds.increment();            uint256 newItemId = _tokenIds.current();            require(newItemId <= maxSupply, \"Can't mint that many NFTs.\");            super.<mask0>;        }    }"