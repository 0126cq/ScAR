"function mint(uint256 _mintAmount) public payable{\t\trequire(status, \"Sale inactive\" );        require(msg.sender == tx.origin, \"No contracts!\");\t\trequire(minted + _mintAmount <= maxSupply, \"Would excced supply\" );        if(minted + _mintAmount > freeSupply){\t\t    require(msg.value >= cost * _mintAmount, \"Not enough ETH\");            require(_mintAmount <= maxMint, \"Too many\" );        }else if(minted + _mintAmount < freeSupply){            require(_mintAmount <= maxFreeMint, \"Too many\" );        }   \t\tfor (uint256 i = 0; i < _mintAmount; i++) {            minted++;\t\t\t<mask0>;\t\t}\t}"