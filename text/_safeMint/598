"function teamMint(address _to, uint256 _amount) external onlyOwner {        require(totalSupply()+_amount<MAX_SUPPLY, \"SoldOut\");        require(_numberMinted(msg.sender) <= TEAM_MINT_SUPPLY, \"Max\");        <mask0>;    }"