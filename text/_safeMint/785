"function OGpresaleMint(uint256 quantity, bytes32[] calldata OGPresaleMerkleProof) external payable callerIsUser nonReentrant {        require(OGpreSale, \"pre sale is not live\");        require(totalSupply() + quantity <= MAX_TOKENS, \"minting this many would exceed supply\");        bytes32 leaf = keccak256(abi.encodePacked(msg.sender));        require(MerkleProof.verify(OGPresaleMerkleProof, OGmerkleroot, leaf), \"Invalid proof.\");        _checkOGPreMintRequirements(quantity);        <mask0>;    }"