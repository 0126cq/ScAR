"function giveaway(uint256 _count, address _to) external onlyOwner {        uint256 totalSupply = totalSupply();        require(totalSupply + _count <= maxSupply, \"Sold out!\");        for (uint256 i = 0; i < _count; i++) {            <mask0>;        }    }"