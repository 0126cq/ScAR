"function award (address _to) external  {    require(!paused, \"the contract is paused\");    require(            msg.sender == address(SideQuestMoose),            \"Caller not authorized\"        );        _tokenId.increment();        uint256 tokenId = _tokenId.current();        <mask0>;        emit MiniMooseMinted(tokenId);    }"