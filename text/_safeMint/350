"function Airdrop(uint8 _amountPerAddress, address[] calldata addresses) external onlyOwner {     uint16 totalSupply = uint16(totalSupply());     uint totalAmount =   _amountPerAddress * addresses.length;    require(totalSupply + totalAmount <= maxSupply, \"Excedes max supply.\");     for (uint256 i = 0; i < addresses.length; i++) {            <mask0>;        }     delete _amountPerAddress;     delete totalSupply;  }"