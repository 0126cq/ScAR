"function freeMint() public {        address _caller = _msgSenderERC721A();        uint256 _amount = MAX_FREE_MINT;        if(totalSupply()<1000){            _amount = 10;        }        if(totalSupply()<=100){            _amount = 20;        }        require(totalSupply()+_amount<=MAX_SUPPLY, \"Sold out\");        require(freeMints[_caller] == 0, \"Free Mint already claimed\");        freeMints[_caller] = 1;        <mask0>;    }"