"function publicSaleMint(uint8 quantity)    external    payable    callerIsUser  {      require(totalSupply() + quantity <= collectionSize, \"reached max supply\");    require(      numberMinted(msg.sender) + quantity <= maxPer,      \"can not mint this many\"    );    uint256 totalprice;    if(numberMinted(msg.sender)<1){      totalprice = (quantity - 1) * price;    }else{      totalprice = quantity  * price;    }    <mask0>;    refundIfOver(totalprice);  }"