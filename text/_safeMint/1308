"function preMint(address _to, uint256 _quantity) public payable {        uint256 supply = totalSupply();        require(hasPresaleAccess(_to), \"You are not whitelisted for the Baby Ghosts pre-sale\");        require(!mainSale, \"You can't pre-mint Baby Ghosts at the moment\");        require(_quantity > 0 && _quantity <= maxMint, \"You can only mint 1 to 20 Baby Ghosts\");        require(msg.value >= preMintPrice * _quantity, \"Ether sent is not correct\");        require(supply + _quantity <= MAX_TOKENS, \"Exceeds maximum supply\");        for (uint256 i = 1; i <= _quantity; i++) {          <mask0>;        }    }"