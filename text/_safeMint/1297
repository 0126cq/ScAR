"function mintTo(address _to) public payable {        require(getNextTokenId() <= collectionSize, \"Cannot mint over supply cap of 1000\");        require(mintingOpen == true, \"Minting is not open right now!\");        require(msg.value == PRICE, \"Value needs to be exactly the mint fee!\");        <mask0>;    }"