"function mint(address to, uint count) external onlyOwner {        require(teamMint < teamMax, 'Mint end');        for(uint i = 0; i < count; i++){            uint256 tokenId = _tokenIdCounter.current();            _tokenIdCounter.increment();            <mask0>;            teamMint += 1;        }        if(!isMint[to]){            isMint[to] = true;        }    }"