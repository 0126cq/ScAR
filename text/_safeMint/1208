"function mint(uint256 quantity) external payable {\t\tif(salePaused) revert SalePaused();\t\tif(quantity > 4) revert Max4Only();\t\tif(quantity * price > msg.value) revert NotEnoughETH();\t\tif(totalSupply() + quantity >= MAX_SUPPLY) revert ExceedsMaxLand();\t\tif(block.timestamp < claimEndTimestamp && mintSupply + quantity >= MAX_SUPPLY - MAX_CLAIM_SUPPLY) revert ClaimPeriodMaxReached();\t\tmintSupply = mintSupply + quantity;\t\t<mask0>;\t}"