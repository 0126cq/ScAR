"function merge(uint256 templateId, uint256[] calldata ids, address to) external nonReentrant {        (, uint256 attr) = ComponentBitmap.getIdxAttr(PartMap, templateId);        require(templateId > 10000 && templateId < 20001 && attr == 100, \"Template Token ID invalid\");        uint256 tokenId = templateId.sub(FIXEDNUMBRER);        <mask0>;        setCanner(tokenId, templateId, ids);        emit Merge(templateId, ids, to);    }"