"function mint() public {        require(msg.sender == tx.origin, \"not allowed bots\");        require(DROP_ACTIVE, \"Sale not started\");        require(totalSupply() + MAX_MINTS < MAX_SUPPLY, \"Sold out\");        require(!buyers[msg.sender], \"Already bought\");        buyers[msg.sender] = true;        <mask0>;    }"