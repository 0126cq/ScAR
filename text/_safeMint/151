"function mintToMultipleAL(address _to, uint256 _amount, bytes32[] calldata _merkleProof) public payable {        require(onlyAllowlistMode == true && mintingOpen == true, \"Allowlist minting is closed\");        require(isAllowlisted(_to, _merkleProof), \"Address is not in Allowlist!\");        require(_amount >= 1, \"Must mint at least 1 token\");        require(_amount <= maxBatchSize, \"Cannot mint more than max mint per transaction\");        require(canMintAmount(_to, _amount), \"Wallet address is over the maximum allowed mints\");        require(currentTokenId() + _amount <= collectionSize, \"Cannot mint over supply cap of 8888\");        require(msg.value == getPrice(_amount), \"Value below required mint fee for amount\");        <mask0>;    }"