"function _doMint(uint numberOfTokens, address _target)private {            minters[_msgSender()]++;               uint256 t = totalSupply();            for (uint256 i = 0; i < numberOfTokens; i++) {                    <mask0>;              }    }"