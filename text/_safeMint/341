"function endSale() external onlyOwner {        require(!hasSaleEnded, \"Sale already ended\");        while (totalSupply() < MAX_SUPPLY) {            <mask0>;        }        isPresale = false;        isPublicSale = false;        hasSaleEnded = true;        emit SaleEnded();    }"