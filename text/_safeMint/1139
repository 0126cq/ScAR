"function managerMint(address to)        external        isSaleActive        tokensLeft        canStillMint(to, 1)        onlyManager    {        uint256 tokenId = _tokenIdCounter.current();        _tokenIdCounter.increment();        minted[to]+=1;        <mask0>;    }"