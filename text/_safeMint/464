"function _mintNFT(uint _amountToMint) internal nonReentrant {        require (!paused, \"Contract paused\");         require (_amountToMint > 0, \"Amount should be greater than 0\");        require (_amountToMint + totalSupply() <= maxSupply - remainingReserves, \"Request superior max Supply\");        require (msg.value == price*_amountToMint, \"Incorrect Payment Amount\");        require (_amountToMint <= maxBatchSize, \"Quantity to mint too high\");        <mask0>;    }"