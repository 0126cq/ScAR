"function getCheeb(address to, uint256 amount)        public        payable        whenNotPaused        callerIsUser    {        require(amount <= saleConfig.maxMint, \"You can mint a maximum of 20 Cheebiez at a time!\");        require(msg.value == PRICE * amount, \"Ether amount sent is not correct!\");        require(isPublicSaleOn() == true, \"Public sale hasn't started or is paused!\");        require(totalSupply() + amount <= saleConfig.maxSupply, \"Too many Cheebiez!\");        <mask0>;    }"