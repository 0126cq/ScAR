"function allowlistMint(uint256 _quantity) external onlyOwner {        require(            _totalMinted() + _quantity <= collectionSize,            \"Collection is full\"        );        <mask0>;    }"