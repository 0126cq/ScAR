"function mintTo(address _to) public payable {        require(getNextTokenId() <= collectionSize, \"Cannot mint over supply cap of 50\");        require(mintingOpen == true, \"Minting is not open right now!\");        require(canMintAmount(_to, 1), \"Wallet address is over the maximum allowed mints\");        require(msg.value == getPrice(1), \"Value needs to be exactly the mint fee!\");        <mask0>;    }"