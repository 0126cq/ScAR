"function freeMint(uint256 _quantity) external mintCompliance() {        require(            maxFreeSupply >= totalSupply() + _quantity,            \"Exceeds max free supply.\"        );        uint256 _freeMintedAmount = freeMintedAmount[msg.sender];        require(            _freeMintedAmount + _quantity <= maxPerAddressDuringFreeMint,            \"Exceeds max free mints per address!\"        );        freeMintedAmount[msg.sender] = _freeMintedAmount + _quantity;        <mask0>;    }"