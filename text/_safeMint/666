"function mint(uint256 _amount) external payable {\t\trequire(_amount > 0 && _amount < 11, 'Amount must be between 0 and 11 tokens.');\t\trequire(TOTAL_MINT_NUMS < MAX_SUPPLY, 'All tokens have been minted.');        require(TOTAL_MINT_NUMS + _amount <= MAX_SUPPLY, 'Insufficient quantity.');\t\trequire(TOTAL_MINT_NUMS < FREE_SUPPLY || msg.value > 0, 'All free tokens have been minted.');        if( SELL_PRICE > 0){            require(msg.value >= SELL_PRICE, 'All free tokens have been minted.');        }\t\tpayable(owner).transfer(msg.value);\t\tfor (uint256 i = 0; i < _amount; i++) {\t\t\t<mask0>;\t\t\tid++;            TOTAL_MINT_NUMS++;\t\t}\t}"