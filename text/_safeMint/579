"function ownerMint(uint _quantity) public onlyOwner(){        require(            totalSupply() + _quantity <= collectionSize,            \"Minting over collection size\"        );        require(_quantity > 0, \"Quantity must be greater than 0\");        <mask0>;    }"