"function mintAllowList(uint256 amount, uint allowance, bytes32[] calldata proof) public payable mintCompliance(amount) {        require(state == ContractMintState.ALLOWLIST, \"Allowlist mint is disabled\");         require(msg.value >= allowlistCost * amount, \"Insufficient funds\");        require(numberMinted(msg.sender) + amount <= allowance, \"Can't mint that many\");        require(_verify(_leaf(msg.sender, allowance), proof), \"Invalid proof\");        <mask0>;    }"