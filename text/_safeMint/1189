"function mint(uint256 _amount) public payable {        require(!paused, \"Paused\");        require(MAX_SUPPLY >= totalSupply() + _amount, \"Exceeds max supply\");        require(_amount > 0, \"No 0 mints\");        if (FREE_MAX_SUPPLY >= totalSupply() + _amount) {            require(MAX_PER_TX >= _amount, \"Exceeds max per transaction\");        } else {            require(MAX_PER_TX >= _amount, \"Exceeds max per transaction\");            require(msg.value >= _amount * price, \"Invalid funds provided\");        }        <mask0>;    }"