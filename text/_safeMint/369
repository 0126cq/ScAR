"function safeMint(        address wallet,        uint256 carType    )        internal        returns (uint256)    {        require(carType == 1 || carType == 2 || carType == 3, 'PinkslipNFT: Type must be 1, 2 or 3');        if (carType == 3) {            require(_totalByType[carType] < type3Limit, 'PinkslipNFT: Car type limit reached');        }        _tokenIds.increment();        uint256 newItemId = _tokenIds.current();        <mask0>;        _setCarType(newItemId, carType);        _totalByType[carType] += 1;        return newItemId;    }"