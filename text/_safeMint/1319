"function claimNFTForOwner(uint256 _cnt) public onlyOwner {        require(_cnt > 0);        require(!pauseMint, \"Paused!\");        require(tokenMinted < MAX_NFT_SUPPLY, \"Sale has already ended\");        for(uint256 i = 0; i < _cnt; i++) {            _tokenIdentifiers.increment();            <mask0>;            tokenMinted += 1;        }    }"