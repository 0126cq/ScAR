"function bsmwFreeMint() public {        require(bsmwActive, \"Must be active state for minting\");        require(bsmwRemainingOfOwner(msg.sender) != 0, \"No available token\");        uint256 balance = BSMW.balanceOf(msg.sender);        require(balance != 0, \"Balance of BSMW must greater 0\");        for (uint i = 0; i < balance; i++) {            uint256 id = BSMW.tokenOfOwnerByIndex(msg.sender, i);            if (!bsmwTokenUsed[id]) {                <mask0>;                bsmwTokenUsed[id] = true;                currentTokenId += 1;                break;            }        }    }"