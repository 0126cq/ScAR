"function mint(uint256 _count) public payable saleHasStarted saleIsOpen {        uint256 totalSupply = totalSupply();        require(totalSupply + _count <= MAX_RUGS, \"Beyond max limit\");        require(totalSupply < MAX_RUGS, \"Sale end\");        require(_count < 21, \"Exceeds 20\");        require(msg.value >= 20000000000000000 * _count, \"Value below price\");        for (uint256 i; i < _count; i++) {            <mask0>;        }    }"