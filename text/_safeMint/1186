"function mintTo(address _to) public payable {        require(getNextTokenId() <= collectionSize, \"Cannot mint over supply cap of 3000\");        require(mintingOpen == true, \"Minting is not open right now!\");        require(publicDropTimePassed() == true, \"Public drop time has not passed!\");        require(msg.value == getPrice(1), \"Value needs to be exactly the mint fee!\");        <mask0>;    }"