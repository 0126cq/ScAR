"function reservedMint(uint8 amount, address to) public onlyOwner {        require(            mintedReservedTokenCount.add(amount) <= teamReservedTokens,            \"Can not mint more than reserved\"        );        require(            mintedPublicTokens.add(amount) <= maxPublicMintableTokens,            \"No tokens left to mint\"        );        for (uint8 i = 0; i < amount; i++) {            <mask0>;        }        mintedReservedTokenCount += amount;        mintedPublicTokens += amount;    }"