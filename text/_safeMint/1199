"function treasuryMint(uint quantity)    public    onlyOwner  {    require(      quantity > 0,      \"Invalid mint amount\"    );    require(      totalSupply() + quantity <= maxSupply,      \"Maximum supply exceeded\"    );    <mask0>;  }"