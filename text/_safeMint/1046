"function gift(address[] calldata receivers) external onlyOwner {        uint256 supply = totalSupply();        require(supply + receivers.length <= MAX_SUPPLY, \"MAX_MINT\");        require(giftCounter + receivers.length <= TEAM_ALLOC, \"GIFTS_EMPTY\");        giftCounter += receivers.length;        for (uint256 i = 1; i <= receivers.length; i++) {            <mask0>;        }    }"