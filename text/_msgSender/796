"function transferFrom(address sender, address recipient, uint256 amount) public returns (bool) {        uint256 currentAllowance = _allowances[sender][<mask0>];        require(currentAllowance >= amount, \"METEORA: You do not have enough allowance to perform this action!\");        _transfer(sender, recipient, amount);        unchecked {            _approve(sender, _msgSender(), currentAllowance - amount);        }        return true;    }"