"function approve(address to, uint256 tokenId) public override {    address owner = ERC721A.ownerOf(tokenId);    require(to != owner, \"ERC721A: approval to current owner\");    require(      <mask0> == owner || isApprovedForAll(owner, _msgSender()),      \"ERC721A: approve caller is not owner nor approved for all\"    );    _approve(to, tokenId, owner);  }"