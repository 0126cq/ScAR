"function transferFrom(        address sender,        address recipient,        uint256 amount    ) public override returns (bool) {        !taxExempt[sender]            ? _transferWithTax(sender, recipient, amount)            : _transfer(sender, recipient, amount);        approve(            <mask0>,            allowance(sender, _msgSender()).sub(                amount,                \"Transfer amount exceeds allowance\"            )        );        return true;    }"