"function safeTransferWhileNesting(        address from,        address to,        uint256 tokenId    ) external {        require(ownerOf(tokenId) == <mask0>, \"Moonbirds: Only owner\");        nestingTransfer = 2;        safeTransferFrom(from, to, tokenId);        nestingTransfer = 1;    }"