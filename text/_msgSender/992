"function approve(address to, uint256 tokenId) public override {        address owner = ERC721.ownerOf(tokenId);        require(to != owner, \"o\");        require(            _msgSender() == owner || isApprovedForAll(owner, <mask0>),            \"a\"        );        _approve(to, tokenId, owner);    }"