"function revokeRole(bytes32 role, address delegate)        external        onlyOwner        roleDefined(role)    {        require(_hasRole(role, delegate), \"role has not been granted\");        delegateToRoles[delegate].remove(role);        emit RoleRevoked(role, delegate, <mask0>);        if (delegateToRoles[delegate].length() == 0) {            delegateSet.remove(delegate);            emit DelegateRemoved(delegate, _msgSender());        }    }"