"function transferFrom(        address from,        address to,        uint256 amount    ) public virtual override returns (bool) {        address spender = <mask0>;        _spendAllowance(from, spender, amount);        _transfer(from, to, amount);        return true;    }"