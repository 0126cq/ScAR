"function tokenURI(uint _nftId) public view override(ERC721) returns (string memory) {        require(<mask0>, \"This NFT does not exist.\");        if(revealed == false) {            return               bytes(notRevealedURI).length > 0               ? string(abi.encodePacked(notRevealedURI, _nftId.toString(), baseExtension))              : \"\";          }        string memory currentBaseURI = _baseURI();        return             bytes(currentBaseURI).length > 0             ? string(abi.encodePacked(currentBaseURI, _nftId.toString(), baseExtension))            : \"\";    }"