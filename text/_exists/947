"function tokenURI(uint256 tokenId)        public        view        override        returns (string memory)    {        require(<mask0>, \"TellerNFT: URI query for nonexistent token\");        string memory baseURI = _baseURI();        return            bytes(baseURI).length > 0                ? string(abi.encodePacked(baseURI, _tokenURIHash(tokenId)))                : \"\";    }"