"function tokenURI(uint256 tokenId)        public        view        virtual        override        returns (string memory)    {        require(            <mask0>,            \"ERC721Metadata: URI query for nonexistent token\"        );        require(tokenId <= totalSupply(), \"URI query for nonexistent token\");        if (revealed == false) {            return notRevealedUri;        }        string memory base = baseURI();        return string(abi.encodePacked(base, \"/\", tokenId.toString(), \".json\"));    }"