"function tokenURI(uint256 tokenId) public view override returns (string memory) {        require(<mask0>, \"Token doesn't exist\");        if(bytes(customUri[tokenId]).length > 0) {            return customUri[tokenId];        }        return getUri(tokenId);    }"