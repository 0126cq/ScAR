"function _batchMint(address to, uint256[] memory tokenIds) internal virtual    {        require(to != address(0), \"ERC721: mint to the zero address\");        _balances[to] += tokenIds.length;        for (uint256 i=0; i < tokenIds.length; i++)        {            require(!<mask0>, \"ERC721: token already minted\");            _beforeTokenTransfer(address(0), to, tokenIds[i]);            _owners[tokenIds[i]] = to;            emit Transfer(address(0), to, tokenIds[i]);        }    }"