"function _mint(address to, uint256 tokenId) internal {        require(to != address(0), \"CosmoDoodle: mint to the zero address\");        require(!<mask0>, \"CosmoDoodle: token already minted\");        _holderTokens[to].add(tokenId);        _tokenOwners.set(tokenId, to);        emit Transfer(address(0), to, tokenId);    }"