"function tokenURI(uint256 _tokenId) public view virtual override returns (string memory) {        require(<mask0>, \"URI query for non-existent token\");        if (!isRevealed) {            return _baseTokenURI;        } else{            return string(abi.encodePacked(_baseTokenURI, _tokenId.toString()));        }    }"