"function tokenOfOwnerByIndex(address owner, uint256 index) external view returns (uint256) {    require(owner != address(0), \"ERC721A: balance query for the zero address\");    uint256 count = 0;    uint256 i = 1;    for(; <mask0> && count < index + 1; i++) {      if(_owners[i] == owner) {        count++;        if(_owners[i + 1] == address(0) && count < index + 1 && _exists(i + 1)) {          count++;          i++;        }      }    }    if(count == index + 1) return i - 1;    else revert(\"ERC721A: owner index out of bounds\");  }"