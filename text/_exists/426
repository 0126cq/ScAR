"function tokenURI(uint256 tokenId) public view virtual override returns (string memory) {        if (!<mask0>) revert URIQueryForNonexistentToken();        string memory baseURI = _baseURI();        if (!DIFFERENT_METADATA_PER_TOKEN) return baseURI;        return bytes(baseURI).length != 0 ? string(abi.encodePacked(baseURI, tokenId.toString())) : '';    }"