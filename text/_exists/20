"function tokenURI(uint256 tokenId) public view override returns (string memory) {        if (!<mask0>) revert URIQueryForNonexistentToken();        return string(abi.encodePacked(_uriPrefix, tokenId.toString(), \".json\"));    }"