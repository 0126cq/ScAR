"function tokenURI(uint256 tokenId) override public view returns (string memory) {        require(<mask0>, \"URI query for nonexistent token\");        uint256 metaHead = FinishHead[tokenId];        if (metaHead == 0) {            (uint256 index, uint256 attr) = ComponentBitmap.getIdxAttr(PartMap, tokenId);            return string(abi.encodePacked(baseURI_prefix, baseURI, template[index], sprit, attr.toString()));        } else {            return getFullTokenURI(tokenId, metaHead);        }    }"