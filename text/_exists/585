"function _transfer(        address from,        address to,        uint256 tokenId    ) internal override{        require(<mask0>, \"Token Does Not Exist\");        require(_staked[tokenId] != true, \"Token is staked, cannot transfer\");        super._transfer(from, to, tokenId);    }"