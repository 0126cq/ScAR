"function tokenURI(uint256 _tokenId)        public        view        override        returns (string memory)    {        if (!<mask0>) revert URIQueryForNonexistentToken();        if (!isRevealed) {            return unrevealedUri;        }        return string(abi.encodePacked(baseUri, _tokenId.toString(), \".json\"));    }"