"function tokenURI(uint256 id)        public        view        override(ERC721AUpgradeable, IERC721AUpgradeable)        returns (string memory)    {        if (!<mask0>) revert URIQueryForNonexistentToken();        string memory tokenURIOverride = _tokenURIOverride;        return            bytes(tokenURIOverride).length == 0                ? string.concat(baseURI, _toString(id))                : tokenURIOverride;    }"