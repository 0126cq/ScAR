"function getApproved(uint256 tokenId)        public        view        override        returns (address)    {        require(            <mask0>,            \"ERC721A: approved query for nonexistent token\"        );        return _tokenApprovals[tokenId];    }"