"function getReward() public updateReward(msg.sender) checkStart {        uint256 reward = earned(msg.sender);        if (reward > 0) {            rewards[msg.sender] = 0;            xbse.<mask0>;            totalAccumulatedReward = totalAccumulatedReward.add(reward);            emit RewardPaid(msg.sender, reward);        }    }"