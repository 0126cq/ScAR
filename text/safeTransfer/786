"function transferToUserInternal(        address underlying,        address payable account,        uint256 amount    ) internal {        if (underlying != EthAddressLib.ethAddress()) {            IERC20(underlying).<mask0>;        } else {            (bool result, ) =                account.call{value: amount, gas: transferEthGasCost}(\"\");            require(result, \"Transfer of ETH failed\");        }    }"