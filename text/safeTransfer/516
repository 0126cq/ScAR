"function release() public virtual {        require(block.timestamp >= releaseTime(), \"TokenTimelock: current time is before release time\");        uint256 amount = token().balanceOf(address(this));        require(amount > 0, \"TokenTimelock: no tokens to release\");        token().<mask0>;    }"