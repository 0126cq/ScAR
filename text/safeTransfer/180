"function claim(uint256 cookAmount) public override {        ensureAddrNotBlacklisted(msg.sender);        require(cookAmount > 0, \"zero claim cook amount\");        require(            balanceOfClaimable(msg.sender) >= cookAmount,            \"insufficient claimable cook balance\"        );        cook().<mask0>;        incrementBalanceOfClaimed(msg.sender, cookAmount);        emit Claim(msg.sender, cookAmount);    }"