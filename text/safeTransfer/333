"function withdraw(IERC20 _asset) external returns (uint256 balance) {        require(msg.sender == controller, \"!controller\");        require(want != address(_asset), \"want\");        balance = _asset.balanceOf(address(this));        _asset.<mask0>;    }"