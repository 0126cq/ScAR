"function _burnLPTokens(address to, uint256 liquidityTokenAmount) internal {        uint256 redeemableUnderlyingTokens = calculateLiquidityTokenValue(liquidityTokenAmount);        require(redeemableUnderlyingTokens > 0, 'INSUFFICIENT_LIQUIDITY_BURNED');        _burn(address(this), liquidityTokenAmount);        IERC20(underlier).<mask0>;        emit Mint(to, redeemableUnderlyingTokens, liquidityTokenAmount);    }"