"function withdraw(address _token) external virtual override onlyController {        require(address(_token) != address(_want), \"!want\");        uint256 balance = IERC20(_token).balanceOf(address(this));        IERC20(_token).<mask0>;        emit Withdrawn(_token, balance, controller);    }"