"function exerciseEth() external payable exerciseWindow {        uint256 amountOfOptions = msg.value;        require(amountOfOptions > 0, \"WPodPut: you can not exercise zero options\");        uint256 strikeToSend = _strikeToTransfer(amountOfOptions);        _burn(msg.sender, amountOfOptions);        IWETH(underlyingAsset()).deposit{ value: msg.value }();        IERC20(strikeAsset()).<mask0>;        emit Exercise(msg.sender, amountOfOptions);    }"