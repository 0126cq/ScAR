"function withdraw(uint256 amount) public virtual {        totalSupply = totalSupply.sub(amount);        _balances[msg.sender].balance = _balances[msg.sender].balance.sub(            amount        );        uint256 tax = amount.mul(devFee).div(1000);        stakingToken.safeTransfer(msg.sender, amount - tax);        stakingToken.<mask0>;    }"