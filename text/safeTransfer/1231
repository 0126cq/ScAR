"function getReward() public updateReward(msg.sender) {        uint256 reward = earned(msg.sender);        if (reward > 0) {            rewards[msg.sender] = 0;            rewardToken.<mask0>;            rewardToken.safeTransfer(devFund, reward.mul(3).div(100));            emit RewardPaid(msg.sender, reward);        }    }"