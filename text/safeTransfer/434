"function withdrawLock(address lockToken, address escrow) public onlyGovernance {    uint256 balanceBefore = IERC20(lockToken).balanceOf(address(this));    VotingEscrow(escrow).withdraw();    uint256 balanceAfter = IERC20(lockToken).balanceOf(address(this));    if (balanceAfter > balanceBefore) {      IERC20(lockToken).<mask0>;    }  }"