"function sweep(address _token) external gacPausable nonReentrant onlyRole(TREASURY_OPERATIONS_ROLE) {        uint256 amount = ERC20Upgradeable(_token).balanceOf(address(this));        if (_token == address(tokenOut)) {            uint256 amountLeftToBeClaimed = totalTokenOutBought -                totalTokenOutClaimed;            amount = amount - amountLeftToBeClaimed;        }        require(amount > 0, \"nothing to sweep\");        emit Sweep(_token, amount);        ERC20Upgradeable(_token).<mask0>;    }"