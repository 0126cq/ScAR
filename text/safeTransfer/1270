"function deposit() public {        uint256 _want = IERC20(want).balanceOf(address(this));        if (_want > 0) {            IERC20(want).<mask0>;            VoterProxy(proxy).deposit(gauge, want);        }    }"