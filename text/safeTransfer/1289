"function withdraw() external {        LockRecord[] memory t = locksTable[msg.sender];        uint256 _bal;        for (uint256 i = 0; i < t.length; i++) {            if (t[i].isWithdrawn == 0 && (block.timestamp >= t[i].unlockDate || block.timestamp >= stakingLastUnlock)) {                _bal = _bal.add(t[i].amountLocked);                locksTable[msg.sender][i].isWithdrawn = 1;                emit StakesClaimed(msg.sender, t[i].id, t[i].amountLocked);            }        }        require(_bal > 0, \"No stakes to withdraw\");        IERC20(lockToken).<mask0>;    }"