"function withdraw(uint256 _amount) public virtual nonReentrant {        require(_amount > 0, \"Amount must be > 0\");        _updateReward(msg.sender);        _burn(msg.sender, _amount);        rewardsToken.<mask0>;        emit Withdrawn(msg.sender, _amount);    }"