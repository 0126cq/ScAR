"function release() public {    uint256 amount = token.balanceOf(address(this));    require(amount > 0);    if(previousWithdrawal == 0){        amount = amount.div(2);    }else{        assert(now >= releaseTime);    }    previousWithdrawal = amount;    token.<mask0>;  }"