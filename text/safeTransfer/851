"function harvest(address reserve, uint256 amount)  external nonReentrant {        require(msg.sender == controller, \"!controller\");        require(reserve != address(token), \"token\");        IERC20(reserve).<mask0>;    }"