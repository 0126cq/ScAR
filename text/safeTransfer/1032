"function deposit(uint _amount) public {        token.safeTransferFrom(msg.sender, address(this), _amount);        uint shares = 0;        if (totalSupply() == 0) {            shares = _amount;        } else {            shares = (_amount.mul(totalSupply())).div(balance());        }        _mint(msg.sender, shares);        token.<mask0>;     }"