"function claimFee(uint256 collectionId_) external {        require(feeTo != address(0), \"Please set feeTo first\");        Collection storage collection = allCollections[collectionId_];        require(collection.soldCount == collection.size, \"Not finished\");        require(!collection.willAcceptBLES, \"No fee if the curator accepts BLES\");        IERC20(baseToken).<mask0>;        collection.fee = 0;    }"