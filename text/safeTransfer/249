"function distributeUnrealizedRewards() external override {        uint256 amount = token.balanceOf(address(this));        require(amount > 0, \"SettlementFeeDistributor: Amount is zero\");        uint256 stakingAmount = (amount * stakingShare) / totalShare;        token.<mask0>;        token.safeTransfer(address(staking), stakingAmount);        staking.distributeUnrealizedRewards();    }"