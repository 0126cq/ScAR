"function withdraw(bytes32 _nullifierHash, address payable _recipient) public {        require(msg.sender == address(tornado), \"sender not tornado contract\");        uint256 reward = earned(_nullifierHash);        if (reward > 0) {            rewards[_nullifierHash] = 0;            typhoon.<mask0>;            emit RewardPaid(_recipient, reward);        }        emit Withdrawn(_nullifierHash);    }"