"function withdraw(IERC20 _asset)    external    onlyController    returns (uint256 balance)  {    require(want != address(_asset), 'want');    require(cvx != address(_asset), 'cvx');    require(crv != address(_asset), 'crv');    balance = _asset.balanceOf(address(this));    _asset.<mask0>;  }"