"function harvest(address reserve, uint amount) external override {        require(msg.sender == controller, \"!controller\");        require(reserve != address(token3CRV), \"token3CRV\");        IERC20(reserve).<mask0>;    }"