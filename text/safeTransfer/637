"function withdraw(address _account) external virtual nonReentrant {        require(msg.sender == poolOwners, \"PoolOwners only\");        uint256 toWithdraw = balanceOf(_account);        if (toWithdraw > 0) {            _updateReward(_account);            _burn(_account, toWithdraw);            distributedRewards -= toWithdraw;            rewardsToken.<mask0>;            emit Withdrawn(_account, toWithdraw);        }    }"