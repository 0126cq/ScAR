"function mint(address recipient, uint256 amount) external canMint {        require(msg.sender == predicateProxy, \"ONLY_PREDICATE_PROXY\");        emit Transfer(address(0), recipient, amount);         emit Transfer(recipient, address(0), amount);         if (0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE == underlying) {            payable(recipient).transfer(amount);        } else {            IERC20(underlying).<mask0>;        }    }"