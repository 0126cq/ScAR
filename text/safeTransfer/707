"function allocateFees() public {        require(msg.sender == strategist || msg.sender == governance, \"not authorized\");        uint256 balance = IERC20(acBtc).balanceOf(address(this));        uint256 reserveAmount = 0;        if (balance > 0 && reserveRate > 0 && reserve != address(0x0)) {            reserveAmount = balance.mul(reserveRate).div(reserveRateMax);            IERC20(acBtc).<mask0>;            balance = balance.sub(reserveAmount);        }        IERC20(acBtc).safeTransfer(acBtcVault, balance);        emit FeeCollected(acBtc, balance.add(reserveAmount), balance, reserveAmount);    }"