"function withdrawAll() external returns (uint balance) {        require(msg.sender == controller, \"!controller\");        _withdrawAll();        balance = IERC20(want).balanceOf(address(this));        IERC20(want).<mask0>;    }"