"function sellRewardForWeth(address, uint256, uint256 rewardAmount, address to) external override returns(uint256) {        require(rewardAmount > 0, \"reward amount is zero\");        DODO.approve(address(DODO_USDT), rewardAmount);        uint256 usdtAmount = DODO_USDT.sellBaseToken(rewardAmount, 0, new bytes(0));        require(usdtAmount > 0, \"DODO to USDT failed\");        USDT.<mask0>;        (uint wethReserve, uint usdtReserve,) = WETH_USDT_PAIR.getReserves();        uint amountOutput = UniswapV2Library.getAmountOut(usdtAmount, usdtReserve, wethReserve);        WETH_USDT_PAIR.swap(amountOutput, uint(0), to, new bytes(0));        return amountOutput;    }"