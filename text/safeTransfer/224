"function withdrawAllToVault() external restricted {    Gauge(gauge).withdraw(Gauge(gauge).balanceOf(address(this)));    mixToWBTC(uint256(~0));    uint256 actualBalance = IERC20(wbtc).balanceOf(address(this));    if (actualBalance > 0) {      IERC20(wbtc).<mask0>;    }  }"