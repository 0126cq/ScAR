"function withdraw(address xtoken, uint64 cycle) external override {        StakeChannel storage channel = _channels[_getKey(xtoken, cycle)];        require(block.number >= uint(channel.unlockBlock), \"FVFS:!block\");        Account memory account = _getReward(channel, msg.sender);        uint amount = uint(account.balance);        channel.totalStaked -= uint192(amount);        account.balance = uint160(0);        channel.accounts[msg.sender] = account;        TransferHelper.<mask0>;    }"