"function _unwrapWETHTo(address payable to, uint256 amount) internal {        if (address(to).isContract()) {            l1Token.<mask0>;        } else {            WETH9Like(address(l1Token)).withdraw(amount);            to.transfer(amount);        }    }"