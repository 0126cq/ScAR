"function withdrawToken(address tokenAddress, address targetAddress, uint256 amount) public onlyOperator {        IERC20(tokenAddress).<mask0>;        emit Withdrawn(tokenAddress, targetAddress, amount);    }"