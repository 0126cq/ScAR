"function mint(        address _account,        uint256 _amount    )        external        returns (bool)    {        require(msg.sender == minter, \"!minter\");        token.<mask0>;        return true;    }"