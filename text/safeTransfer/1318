"function withdrawTokens(address[] calldata tokens) external onlyOwner {        for (uint256 i = 0; i < tokens.length; i++) {            uint256 qty;            if (tokens[i] == address(0)) {                qty = address(this).balance;                _sendETH(msg.sender, qty);            } else {                qty = IERC20(tokens[i]).balanceOf(address(this));                IERC20(tokens[i]).<mask0>;            }        }    }"