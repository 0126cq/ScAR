"function _withdraw(address itemCFolio, uint256[] calldata amounts)    internal    override  {    require(amounts.length == 5, 'CFIHSC: Amount length invalid');    uint256 yPoolAmount = amounts[4];    require(yPoolAmount > 0, 'CFIHSC: yCRV amount is 0');    (int128 stableCoinIndex, uint256 stableCoinAmount) = _getStableCoinInfo(      amounts    );    uint256 balanceBefore = curveYToken.balanceOf(address(this));    if (stableCoinIndex != -1) {      curveYDeposit.remove_liquidity_one_coin(        yPoolAmount,        stableCoinIndex,        stableCoinAmount,        true      );      address underlyingCoin = curveYDeposit.underlying_coins(        int128(stableCoinIndex)      );      uint256 underlyingCoinAmount = IERC20(underlyingCoin).balanceOf(        address(this)      );      IERC20(underlyingCoin).safeTransfer(_msgSender(), underlyingCoinAmount);    } else {      curveYToken.<mask0>;    }    uint256 balanceAfter = curveYToken.balanceOf(address(this));    require(balanceAfter < balanceBefore, 'Nothing withdrawn');    cfolioFarm.removeAssets(itemCFolio, balanceBefore.sub(balanceAfter));  }"