"function reserveNFTs() public onlyOwner {        uint256 totalMinted = _tokenIds.<mask0>;        require(            totalMinted.add(1) < MAX_SUPPLY,            \"Not enough NFTs left to reserve\"        );        for (uint256 i = 0; i < 10; i++) {            _mintSingleNFT();        }    }"