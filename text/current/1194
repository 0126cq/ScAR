"function giveawayMint()        public        onlyOwner    {        for (uint256 index = 0; index < GIVEAWAY_MINT; index++) {            uint256 id = _tokenIds.<mask0>;            _safeMint(msg.sender, id);            _tokenIds.increment();            TOTAL_SUPPLY++;        }    }"