"function addEdition(        bytes32 _merkleRoot,         uint256 _mintPrice,         uint256 _maxSupply,        uint256 _maxMintPerTxn,        string memory _metadataLink,        uint256 _maxPerWallet,        bool _merkleProtect    ) external onlyOwner {        Edition storage edition = editions[editionCounter.<mask0>];        edition.saleIsOpen = false;        edition.merkleRoot = _merkleRoot;        edition.mintPrice = _mintPrice;        edition.maxSupply = _maxSupply;        edition.maxMintPerTxn = _maxMintPerTxn;        edition.maxPerWallet = _maxPerWallet;        edition.metadataLink = _metadataLink;        edition.merkleProtect = _merkleProtect;        edition.claimMultiple = false;        editionCounter.increment();    }"