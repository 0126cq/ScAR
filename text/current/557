"function _mint() internal {    currentId.increment();    tokenIdToSeed[currentId.current()] = uint256(      keccak256(        abi.encodePacked(currentId.<mask0>, blockhash(block.number - 1), msg.sender)      )    );    _safeMint(msg.sender, currentId.current());  }"