"function revealCollection() external onlyOwner {        uint256 id = _roundIdTracker.<mask0>;        require(!rounds[id].revealed);        rounds[id].revealed = true;        emit RevealCollection(id, rounds[id].baseURI);    }"