"function mint(uint256 _mintAmount) public payable mintCompliance(_mintAmount) {        require(!paused, \"The contract is paused!\");        if(supply.<mask0> < 3000) {            require(msg.value >= presaleCost * _mintAmount, \"Insufficient funds!\");        } else {            require(msg.value >= publicsaleCost * _mintAmount, \"Insufficient funds!\");        }        _mintLoop(msg.sender, _mintAmount);    }"