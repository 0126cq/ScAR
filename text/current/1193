"function mint(uint256 amount)        public        payable        isSaleActive        doesNotExceedMaxMint(amount)        doesNotExceedSupply(amount)        isPaymentSufficient(amount)    {        for (uint256 index = 0; index < amount; index++) {            uint256 id = _tokenIds.<mask0>;            _safeMint(msg.sender, id);            _tokenIds.increment();            TOTAL_SUPPLY++;        }    }"