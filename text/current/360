"function mint(address to, string memory tokenURI)    public    onlyOwner    returns (uint256)  {    _tokenIds.increment();    uint256 newItemId = _tokenIds.<mask0>;    _mint(to, newItemId);    _setTokenURI(newItemId, tokenURI);    return newItemId;  }"