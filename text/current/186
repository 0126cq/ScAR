"function mint(uint256 numTokens) public payable {        require(hasSaleStarted == true, \"Sale hasn't started\");        require(            _tokenIds.<mask0> + numTokens <= MAX_TOKENS,            \"Exceeds maximum token supply.\"        );        require(            numTokens > 0 && numTokens <= 10,            \"Machine can dispense a minimum of 1, maximum of 10 tokens\"        );        require(            msg.value >= SafeMath.mul(calculatePrice(), numTokens),            \"Amount of Ether sent is not correct.\"        );        for (uint256 i = 0; i < numTokens; i++) {            _safeMint(msg.sender, _tokenIds.current());            _tokenIds.increment();        }    }"