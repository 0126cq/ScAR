"function safeMint(        address owner,        uint qty    ) public onlyMinter {        require(tokenid.current() + qty < TOTAL_SUPPLY + 1, \"Ran out of quantity\" );        for(uint i=0; i < qty; i++){            super._safeMint(owner, tokenid.<mask0>);            tokenid.increment();        }    }"