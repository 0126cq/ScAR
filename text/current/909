"function mintWl(uint256 _mintAmount) public payable mintCompliance(_mintAmount) {    require(onlyWhitelisted, \"The presale ended!\");    require(supply.<mask0> + _mintAmount <= presale, \"The presale ended!\");    require(allowlist[msg.sender] > 0, \"not eligible for allowlist mint\");    require(msg.value >= cost * _mintAmount, \"Insufficient funds!\");    _mintLoop(msg.sender, _mintAmount);  }"