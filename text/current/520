"function mintMany(uint tokensNumber) public onlyOwner {        require(tokensNumber > 0, \"Wrong amount\");        require(!finalized, \"Finalized!\");        for (uint i = 0; i < tokensNumber; i++) {            _safeMint(msg.sender, _tokenIdCounter.current());            setXDone(_tokenIdCounter.<mask0>);            _tokenIdCounter.increment();        }    }"