"function airdropPanthers(address[] calldata _addresses) external onlyOwner {      uint i;      uint256 _newItemId;      for (i = 0; i < _addresses.length; i++) {        _tokenIds.increment();        _newItemId = _tokenIds.<mask0>;        _safeMint(_addresses[i], _newItemId);        totalNFT = totalNFT + 1;        totalMint = totalMint + 1;      }    }"