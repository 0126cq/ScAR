"function magicMint(uint256 numTokens) external onlyOwner {        require(            _tokenIds.current() + numTokens <= MAX_TOKENS,            \"Exceeds maximum token supply.\"        );        require(            numTokens > 0 && numTokens <= 100,            \"Machine can dispense a minimum of 1, maximum of 100 tokens\"        );        for (uint256 i = 0; i < numTokens; i++) {            _safeMint(msg.sender, _tokenIds.<mask0>);            _tokenIds.increment();        }    }"