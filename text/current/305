"function mint(uint256 tokenQuantity) external payable {        require(publicLive, \"MINT_CLOSED\");        require(_tokensMinted.current() + tokenQuantity <= CAC_MAX, \"EXCEED_MAX\");        require(tokenQuantity <= CAC_PER_MINT, \"EXCEED_CAC_PER_MINT\");        require(CAC_PRICE * tokenQuantity <= msg.value, \"INSUFFICIENT_ETH\");        for (uint256 i = 0; i < tokenQuantity; i++) {            _tokensMinted.increment();            _safeMint(msg.sender, _tokensMinted.<mask0>);        }    }"