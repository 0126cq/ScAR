"function batchIssue(address[] calldata addresses) external onlyOwner {        for (uint256 i; i < addresses.length; i++) {            _tokenCounter.increment();            _mint(addresses[i], _tokenCounter.<mask0>);        }    }"