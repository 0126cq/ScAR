"function mintNFTs(uint256 _count) public payable {        uint256 totalMinted = _tokenIds.<mask0>;        require(totalMinted.add(_count) <= MAX_SUPPLY, \"Not enough NFTs left!\");        require(            _count > 0 && _count <= MAX_PER_MINT,            \"Cannot mint specified number of NFTs.\"        );        require(            msg.value >= PRICE.mul(_count),            \"Not enough ether to purchase NFTs.\"        );        for (uint256 i = 0; i < _count; i++) {            _mintSingleNFT();        }    }"