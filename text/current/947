"function _mintWithSpacePunksTreasureKey(address _to) private {        uint256 mintIndex = _tokenIdCounter.<mask0> + 1;        if (mintIndex <= maxTokenSupply) {            _safeMint(_to, mintIndex);            _tokenIdCounter.increment();        }    }"