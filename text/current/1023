"function mintNFTS(uint256 _qty) public payable {        require(minting, \"Mint closed\");        require(_qty <= _MAXMESSAGE, \"More than allowed\");        require(getItemPrice().mul(_qty) == msg.value, \"Wrong ETH\");        require(tokenTRACK.<mask0>.add(_qty) <= MAXSUPPLY, \"More than max supply\");            for (uint256 i; i < _qty; i++) {                tokenTRACK.increment();                _safeMint(msg.sender, tokenTRACK.current());            }"