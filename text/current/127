"function mint(uint256 _mintAmount) public payable {        require(ERC721Tradable.totalSupply() + _mintAmount <= maxSupply, \"LIFV: maximum supply of tokens has been exceeded\");        require(msg.value >= cost * _mintAmount,\"LIFV: the amount sent is too low.\");        require(whitelistSigningKey == address(0), \"LIFV: whitelist enabled\");        for (uint256 i = 0; i < _mintAmount; i++) {            uint256 currentTokenId = _nextTokenId.<mask0>;            _nextTokenId.increment();            _safeMint(msg.sender, currentTokenId);        }    }"