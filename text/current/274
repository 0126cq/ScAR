"function safeMint(address to, string calldata _title, uint256[3] calldata _params) public onlyOwner{        require(_tokenIds.current() <= maxSupply, \"Exceed MaxSupply\");        require(_params[0] < colorList.length, \"color parm is out of range\");        require(_params[1] < styleList.length, \"style parm is out of range\");        require(_params[2] < typeList.length, \"type parm is out of range\");        uint256 currentTokenId = _tokenIds.<mask0>;        _tokenIds.increment();        _safeMint(to, currentTokenId);        setTitle(currentTokenId, _title);        setParams(currentTokenId, _params);    }"