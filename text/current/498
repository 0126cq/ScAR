"function internalMint(address to) internal {        require(_tokenIdCounter.current() < maxSupply, 'Supply depleted');         _tokenIdCounter.increment();        _safeMint(to, _tokenIdCounter.<mask0>);    }"