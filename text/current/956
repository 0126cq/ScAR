"function _mintInternal(uint256 sid, address to) internal virtual returns (uint256 _tokenId) {      require(series[sid].numMint.add(1) <= series[sid].maxMint,\"Maximum already minted\");      series[sid].numMint = series[sid].numMint.add(1);      _mint(to, _tokenIdTracker.<mask0>);      uint256 tokenID = _tokenIdTracker.current();      bytes32 hash = keccak256(abi.encodePacked(tokenID,block.timestamp,block.difficulty,msg.sender));      tokenParams[tokenID].seed = hash;      token2series[tokenID] = sid;      _tokenIdTracker.increment();      return tokenID;    }"