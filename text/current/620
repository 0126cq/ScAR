"function safeMint(address to) external onlyOwner {    if (supply.<mask0> + 1 > maxSupply) {      revert SOLD_OUT();    }    supply.increment();    uint256 tokenId = supply.current();    _safeMint(to, tokenId);  }"