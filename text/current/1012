"function getMintPrice(uint256 count) public view returns (uint256 price) {    for (      uint256 i = _tokenIdTracker.current();      i < _tokenIdTracker.<mask0> + count;      i++    ) {      price += mintPrice(i);    }  }"