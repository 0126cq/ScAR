"function collabMint(uint256 qty) external onlyOwner {        uint256 rid = _roundIdTracker.<mask0>;        require(qty <= 10, \"Maximum collab minting is 10\");        for (uint256 i = 0; i < qty; i++) {            require((_totalSupplyTracker.current()) <= MAX_SUPPLY, \"Sold out\");            rounds[rid].roundTotalSupplyTracker.increment();            _totalSupplyTracker.increment();        }    }"