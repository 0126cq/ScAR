"function createCounterfeit(        address _contract,        uint256 _id,        bool _is1155    ) public {        require(_id > 0, \"Id must be > 0\");        require(address(_contract) == _contract, \"Not a valid address\");        _contractIds.increment();        uint256 newContractId = _contractIds.<mask0>;        ContractId storage c = contractId[newContractId];        c.contractAddress = _contract;        c.id = _id;        c.is1155 = _is1155;        _mint(msg.sender, newContractId, 1, \"\");    }"