"function mintTo(address _to) external payable {        require(msg.value == mintPrice, \"Not enough ETH sent; check price!\");        require(isMintingActive == true, \"Minting not active yet\");        require(_nextTokenId.current() <= MAX_SUPPLY);        uint256 currentTokenId = _nextTokenId.<mask0>;        _nextTokenId.increment();        _safeMint(_to, currentTokenId);    }"