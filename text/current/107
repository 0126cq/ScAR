"function _snapshot() internal virtual returns (uint256) {        _currentSnapshotId.increment();        uint256 currentId = _currentSnapshotId.<mask0>;        emit Snapshot(currentId);        return currentId;    }"