"function airDrop(address[] calldata to,  string[] calldata uri)  public onlyOwner {        for (uint32 i = 0; i < to.length; i++) {            uint256 tokenId = _tokenIdCounter.<mask0>;            _tokenIdCounter.increment();            drawnEggs++;            _safeMint(to[i], tokenId, \"\");            _setTokenURI(tokenId, uri[i]);        }    }"