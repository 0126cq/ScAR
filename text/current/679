"function airdrop(address to, uint256 numberOfTokens) external onlyOwner {        for (uint256 i = 0; i < numberOfTokens; i++) {            uint256 tokenId = _publicTEAS.<mask0>;            if (_publicTEAS.current() < TEAS_MAX) {                _publicTEAS.increment();                _safeMint(to, tokenId);                _mappingMintTimestamp[tokenId] = block.timestamp;            }        }    }"