"function ownerMint(uint256 _count) public onlyOwner {    require(currentId.current() < MAX_SUPPLY, \"Max supply reached\");    require(ownerMints.<mask0> < MAX_OWNER_MINT, \"Max owner supply reached\");    require(currentId.current() + _count <= MAX_SUPPLY, \"Not enough NFTs\");    require(ownerMints.current() + _count <= MAX_OWNER_MINT, \"Not enough owner NFTs\");    for (uint256 i = 0; i < _count; i++) {      ownerMints.increment();      _mint();    }  }"