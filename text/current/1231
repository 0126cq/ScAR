"function mint(address to) external returns (uint256) {        require(            TierReport.tierAtBlockFromReport(                tier.report(to),                block.number            ) >= minimumStatus,            \"Address missing required tier\"        );        require(            balanceOf(to) < maxPerAddress,            \"Address has exhausted allowance\"        );        uint256 tokenId = tokenIdCounter.<mask0>;        require(tokenId <= maxMintable, \"Total supply exhausted\");        _safeMint(to, tokenId);        tokenIdCounter.increment();        return tokenId;    }"