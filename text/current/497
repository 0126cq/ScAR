"function internalMint(address to) internal {        require(_tokenIdCounter.<mask0> < maxSupply, 'Supply depleted');         _tokenIdCounter.increment();        _safeMint(to, _tokenIdCounter.current());    }"