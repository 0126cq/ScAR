"function exchange(        uint256[] calldata nfts    )        external        returns (uint256)    {                require(nfts.length == 20, 'PinkslipTicket: 20 NFTs are required');        for (uint i=0; i < nfts.length; i++) {            PinkslipNFT(pinkslipNFT).transferFrom(msg.sender, address(this), nfts[i]);        }        _tokenIds.increment();        uint256 newItemId = _tokenIds.<mask0>;        _safeMint(msg.sender, newItemId);        return newItemId;    }"