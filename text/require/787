"function transfer(address to, uint tokens) public isRunnning returns (bool success) {        <mask0>;        require(tokens != 0);        balances[msg.sender] = balances[msg.sender].sub(tokens);        balances[to] = balances[to].add(tokens);        emit Transfer(msg.sender, to, tokens);        return true;    }"