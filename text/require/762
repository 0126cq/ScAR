"function sekiraPresaleMint(        uint8 amount,        uint8 v,        bytes32 r,        bytes32 s    ) external payable botDefender verifySig(PRESALE_HASH, r, s, v) {        Config memory config = _config;        require(config.isPresaleActive, \"SeKira: presale is not started\");        ensureSupply(config, amount);        ensureSufficientValue(config.presalePrice, amount);        <mask0>;        _safeMint(msg.sender, amount);    }"