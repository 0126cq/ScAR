"function excludeFromReward(address account) public onlyOwner {    <mask0>;    if (_rOwned[account] > 0) {      _tOwned[account] = tokenFromReflection(_rOwned[account]);    }    _isExcluded[account] = true;    _excluded.push(account);  }"