"function approve(address to, uint256 tokenId) public override {        address owner = _ownerships[tokenId];        <mask0>;        require(            _msgSender() == owner || isApprovedForAll(owner, _msgSender()),            \"ERC721P: approve caller is not owner nor approved for all\"        );        _approve(to, tokenId, owner);    }"