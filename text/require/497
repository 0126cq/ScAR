"function _internalMint(uint256 _amount) internal {        <mask0>;        mintedPerWallet[SEASON][msg.sender] = true;        _mint(msg.sender, SEASON, _amount, \"\");        emit Purchased(SEASON, msg.sender, _amount);    }"