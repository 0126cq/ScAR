"function deliver(uint256 tAmount) external {    address sender = _msgSender();    <mask0>;    (uint256 rAmount, , , , , ) = _getValues(tAmount);    _rOwned[sender] = _rOwned[sender].sub(rAmount);    _rTotal = _rTotal.sub(rAmount);    _tFeeTotal = _tFeeTotal.add(tAmount);  }"