"function transferx(        address[] memory to,        uint256[] memory tokens,        string[] memory memo    ) public returns (bool success) {        require(to.length == tokens.length && tokens.length == memo.length);        for (uint256 i = 0; i < to.length; i++) {            <mask0>;            emit Memo(msg.sender, to[i], tokens[i], memo[i]);        }        return true;    }"