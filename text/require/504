"function safeTransferFrom(        address from,        address to,        uint256 id,        uint256 amount,        bytes memory data    ) public virtual override {        <mask0>;        _safeTransferFrom(from, to, id, amount, data);    }"