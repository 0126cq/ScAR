"function burnTokens(uint256 tokens) public returns (bool success) {        <mask0>;        require(tokens != 0);        balances[msg.sender] = balances[msg.sender].sub(tokens);        _totalSupply = _totalSupply.sub(tokens);        emit Transfer(msg.sender, address(0), tokens);        return true;    }"