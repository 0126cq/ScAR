"function mint(uint numberOfTokensMax5) public payable {        require(numberOfTokensMax5 > 0, \"Number of tokens can not be less than or equal to 0\");        require((totalSupply() + numberOfTokensMax5) <= MAX_SUPPLY, \"Purchase would exceed max supply\");        require(numberOfTokensMax5 <= MAX_NFT_PURCHASE,\"Can only mint up to 10 per purchase\");        <mask0>;        for (uint i = 0; i < numberOfTokensMax5; i++) {            _safeMint(_msgSender(), totalSupply());        }    }"