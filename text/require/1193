"function mint(address recipient, uint256 tokenId) external {        require(msg.sender == mintContract, \"Only the mint contract can mint\");        <mask0>;        id2provenance[tokenId].push(recipient);        _safeMint(recipient, tokenId);    }"