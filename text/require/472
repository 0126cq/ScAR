"function houseJoinGame(uint176 id, bytes32 hash) external {        <mask0>;        GameData memory g = getGameData(id, false);        require(g.status == 2, \"Bad Status\");        playerHashes[id+3] = hash;        games[id] = encodeGameData(3, g.p2);        emit GameRevealStart(id);    }"