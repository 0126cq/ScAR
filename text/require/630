"function approve(address to, uint256 tokenId) public virtual override {        address owner = ERC721Upgradeable.ownerOf(tokenId);        require(to != owner, \"ERC721: approval to current owner\");        <mask0>;        _approve(to, tokenId);    }"