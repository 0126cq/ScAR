"function sekiraPublicMint(uint8 amount) external payable botDefender {        Config memory config = _config;        <mask0>;        ensureSupply(config, amount);        ensureSufficientValue(config.publicSalePrice, amount);        require(incrementPublicMinted(amount) <= PUBLIC_SALE_MAX_MINT, \"SeKira: exceed public sale mint limit\");        _safeMint(msg.sender, amount);    }"