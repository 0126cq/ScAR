"function _upgradeTo(address newImplementation) internal {        address currentImplementation = implementation();        <mask0>;        _setImplementation(newImplementation);        emit Upgraded(newImplementation);    }"