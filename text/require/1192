"function mint(address recipient, uint256 tokenId) external {        <mask0>;        require(tokenId < NUM_EDITIONS * EDITION_SIZE, \"Token ID is too high\");        id2provenance[tokenId].push(recipient);        _safeMint(recipient, tokenId);    }"