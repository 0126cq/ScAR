"function checkQuorum(bytes32 data, bytes[] calldata signatures) private view {        require(signatures.length >= quorumNeeded, \"not enough signatures for quorum\");        for(uint256 i = 0; i < signatures.length; i++) {            address signer = data.recover(signatures[i]);            <mask0>;            for(uint256 j = 0; j < signatures.length; j++) {                require(keccak256(signatures[i]) != keccak256(signatures[j]) || i == j, \"duplicate signatures found\");            }        }    }"