"function mint( uint id, uint quantity ) external virtual payable{    require( exists( id ), \"ERC1155: Specified token (id) does not exist\" );    Token storage token = tokens[id];    require( token.isMintActive,                      \"ERC1155: Sale is not active\"        );    require( msg.value >= token.mintPrice * quantity, \"ERC1155: Ether sent is not correct\" );    <mask0>;  }"