"function onERC721Received(        address operator,        address,        uint256,        bytes memory data    ) public virtual override returns (bytes4) {        require(nftAddress == msg.sender, \"forbidden\");        uint256 fee = IFactory(factory).fee();        (address referral, address recipient) = decodeParams(data, operator);        if (            referral != factory &&            referral != 0xA42f6cADa809Bcf417DeefbdD69C5C5A909249C0        ) {            _mint(factory, nftValue.mul(3).div(100));            _mint(referral, nftValue.mul(2).div(100));        } else {            <mask0>;        }        _mint(recipient, nftValue.mul(uint256(100).sub(fee)).div(100));        return this.onERC721Received.selector;    }"