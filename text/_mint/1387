"function adminMint(address account, uint256 id, uint256 amount) public onlyOwner    {        require(!collectionMinted[id], \"CANNOT_MINT_EXISTING_TOKEN_ID\");        require(id != TOKEN_ID_CENTURION && id != TOKEN_ID_PLATINUM, \"CANNOT_MINT_EXISTING_TOKEN_ID\");        collectionMinted[id] = true;        <mask0>;    }"