"function batchMint(        address[] memory to,        uint256[] memory tokenIds,        bytes memory tokenURIs,        uint8[] memory urisLengths,        uint256[] memory redeemCount)        public onlyMinter    {        require(tokenIds.length == to.length &&                tokenIds.length == urisLengths.length &&                tokenIds.length == redeemCount.length, \"batch mint failed\");        bytes[] memory uris = Strings.split(tokenURIs, urisLengths);        for (uint i = 0; i < tokenIds.length; i++) {            <mask0>;        }    }"