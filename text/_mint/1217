"function wrapTokenBatch(uint256[] calldata tokenId) public {        for (uint256 i = 0; i < tokenId.length ; i++){            require(tokenId[i] <= 1573, \"Wrapping not existing token \");            mainToken.transferFrom(msg.sender, address(this), tokenId[i] + OFFSETNG);            if(!_exists(tokenId[i] + OFFSETAN2)) {                <mask0>;                _mint(msg.sender, tokenId[i] + OFFSET_SPIRITS + OFFSETAN2);            }            else {                _safeTransfer(address(this), msg.sender, tokenId[i] + OFFSETAN2, \"\");            }        }    }"