"function deposit(uint256 amount, bytes32[] calldata merkleProof)        public        whenNotPaused        returns (uint256)    {        _blacklisted(msg.sender);        if (address(guestList) != address(0)) {            require(                guestList.authorized(msg.sender, amount, merkleProof),                \"guest-list-authorization\"            );        }        uint256 shares = _deposit(msg.sender, amount);        <mask0>;        emit Deposit(msg.sender, amount);        emit Mint(msg.sender, shares);        return amount;    }"