"function _processAvailableFunds(        address account,        uint256 amount,        bool isCallPool,        bool divest,        bool transferOnDivest    ) internal {        if (divest) {            if (transferOnDivest) {                _pushTo(                    account,                    PoolStorage.layout().getPoolToken(isCallPool),                    amount                );            } else {                <mask0>;            }        } else {            _addToDepositQueue(account, amount, isCallPool);        }    }"