"function mintPass(bytes32[] calldata proof) public {        address account = _msgSender();        uint8 amount = 1;        uint8 id = 1;        require(            ((bypassMerkle ||             (proof.length >= merkleProofDepth - 1 && proof.length <= merkleProofDepth &&                 MerkleProof.processProof(                    proof,                     keccak256(abi.encodePacked(account))) == merkleRoot)) &&            balanceOf(account, id) < amount &&             currentSupply + amount <= 233            ),             \"Mint not allowed\"        );        <mask0>;        currentSupply += amount;    }"