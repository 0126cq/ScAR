"function mint(        address to,        string memory _tokenURI,        uint256 royaltyRate    ) public payable returns (uint256 tokenId) {        require(msg.value >= mintFeeAmount, \"msg value too low\");        require(royaltyRate <= 50, \"royaltyRate should <= 50\");        require(royaltyRate >= 0, \"royaltyRate should >= 0\");        TransferHelper.safeTransferETH(mintFeeAddr, mintFeeAmount);        tokenId = nextTokenId;        <mask0>;        nextTokenId++;        _setTokenURI(tokenId, _tokenURI);        _tokenRoyaltyMeta[tokenId] = RoyaltyMeta({            royalty: to,            royaltyRate: royaltyRate        });        emit Royalty(tokenId, to, royaltyRate);        if (msg.value > mintFeeAmount)            TransferHelper.safeTransferETH(                msg.sender,                msg.value - mintFeeAmount            );    }"