"function _mint(address account, uint256 id, uint256 amount, bytes memory data) internal override {    require( exists( id ), \"ERC1155: Specified token (id) does not exist\" );    Token storage token = tokens[id];    require( token.balance + amount <= token.supply, \"ERC1155: Not enough supply\" );    token.balance += amount;    super.<mask0>;  }"