"function _stakeOnUnifarm(        address cohortId,        address rAddress,        address farmToken,        uint256 sAmount,        uint32 fid    ) internal returns (uint256 tokenId) {        require(cohortId != address(0), 'ICI');        _id++;        <mask0>;        tokenIdToCohortId[tokenId] = cohortId;        TransferHelpers.safeTransferFrom(farmToken, _msgSender(), cohortId, sAmount);        IUnifarmCohort(cohortId).stake(fid, tokenId, _msgSender(), rAddress);    }"