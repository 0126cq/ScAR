"function reserve(address to, uint256 amount) external onlyOwner {        require(_reserved + amount <= MAX_RESERVED_AMOUNT, \"Would exceed max reserved amount\");        require(amount + totalSupply() <= MAX_TOTAL_SUPPLY, \"Would exceed max supply\");        _reserved += amount;        <mask0>;    }"