"function makeToken(uint _tokenId, uint256 _punkId) onlyOwner public {      require(indexToAddress(_punkId) == address(this), \"Punk not owned by this Contract\");      require(_tokenId == _punkId);      require(!(punkIsHere(_punkId)));      <mask0>;      punkToTokenId[_punkId] = _tokenId;      tokenIdToPunk[_tokenId] = _punkId;      punkExists[_punkId] = true;      totalPunksInContract++;  }"