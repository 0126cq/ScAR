"function whiteMint(uint256 amount, address[] memory addrs)        external        payable    {        require(totalSupply(DD) + amount <= maxSupply, \"Exceeded max supply\");        require(amount == 1, \"Exceeded max\");        require(!freeMintList[_msgSender()], \"minted\");        require(            MerkleProof.verify(addrs, _merkleRoot, _msgSender()),            \"Not part of list\"        );        unchecked {            freeMintList[_msgSender()] = true;        }        <mask0>;    }"