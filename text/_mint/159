"function mint(uint256 _times) payable public {        require(started, \"Not spooky enough yet.\");        require(_times >0 && _times <= maxBatch, \"Too many souls!\");        require(totalSouls + _times <= totalCount, \"sold out\");        require(msg.value == _times * price, \"value error, please check price.\");        payable(owner()).transfer(msg.value);        emit MintSoul(_msgSender(), totalSouls+1, _times);        for(uint256 i=0; i< _times; i++){            <mask0>;        }    }"