"function _safeMint(        address to,        uint256 quantity,        bytes memory _data    ) internal virtual {        <mask0>;        unchecked {            if (to.code.length != 0) {                uint256 end = ERC721AStorage.layout()._currentIndex;                uint256 index = end - quantity;                do {                    if (!_checkContractOnERC721Received(address(0), to, index++, _data)) {                        revert TransferToNonERC721ReceiverImplementer();                    }                } while (index < end);                if (ERC721AStorage.layout()._currentIndex != end) revert();            }        }    }"