"function mint(uint256 _times) payable public {        require(started, \"waifu's aren't ready for you yet.\");        require(_times >0 && _times <= maxBatch, \"STOP CHEATING ON YOUR WAIFUS!!\");        require(totalWaifus + _times <= totalCount, \"Everyone has a waifu!\");        require(msg.value == _times * price, \"value error, please check price.\");        payable(owner()).transfer(msg.value);        emit MintWaifu(_msgSender(), totalWaifus+1, _times);        for(uint256 i=0; i< _times; i++){            <mask0>;        }    }"