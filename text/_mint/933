"function ownerMint(uint256 _amount) external onlyOwner {        require(            ownerMinted + _amount <= OWNER_CAP,            \"OwnerMint: exceeding quota\"        );        require(            totalSupply() + _amount <= MAX_SUPPLY,            \"OwnerMint: exceeding max supply\"        );        ownerMinted += _amount;        <mask0>;        emit Mint(msg.sender, totalSupply() - _amount + 1, _amount, 0);    }"