"function getReward(uint256 _punk) external {\t\trequire(msg.sender == address(PunkContract.ownerOf(_punk)), \"You are not the owner of this punk\");\t\trequire(!isPauseEnabled, \"Staking is on pause\");\t\tuint256 ramount;\t\tuint256 time = block.timestamp;\t\t\t\tif(lastUpdate[_punk] == 0) {\t\t   ramount= FirstRedeemRate;\t\t}else{\t\tramount = uint(time - lastUpdate[_punk]) / rewardtimeframe * BASE_RATE; } \t\t\trequire(ramount > 1, \"Your punk didn't work hard enough\");\t\t\tlastUpdate[_punk] = time;\t\t\t<mask0>;\t\t\temit RewardPaid(msg.sender, ramount);\t}"