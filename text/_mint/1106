"function mint(uint16 _count) public payable {    uint256 ts = totalSupply();    require(saleIsActive, \"saleIsActive\");    require(_count <= MINT_LIMIT, \"MINT_LIMIT\");    require(ts + _count <= MAX_SUPPLY, \"MAX_SUPPLY\");    require(msg.value >= mintPrice * _count, \"mintPrice\");    for (uint16 i = 0; i < _count; i++) {      <mask0>;      emit MintPublic(msg.sender, ts);      ts++;    }  }"