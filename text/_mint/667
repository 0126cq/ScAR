"function onERC1155Received(        address operator,        address,        uint256,        uint256 value,        bytes memory data    ) public virtual override returns (bytes4) {        require(nftAddress == msg.sender, \"forbidden\");        if (keccak256(data) != keccak256(\"INTERNAL\")) {            uint256 fee = IFactory(factory).fee();            (address referral, address recipient) = decodeParams(                data,                operator            );            if (                referral != factory &&                referral != 0xA42f6cADa809Bcf417DeefbdD69C5C5A909249C0            ) {                _mint(factory, (nftValue.mul(value)).mul(3).div(100));                _mint(referral, (nftValue.mul(value)).mul(2).div(100));            } else {                _mint(referral, (nftValue.mul(value)).mul(fee).div(100));            }            <mask0>;        }        return this.onERC1155Received.selector;    }"