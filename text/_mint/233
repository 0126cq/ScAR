"function claim(Proof memory proof) external {        bytes32 node = keccak256(abi.encodePacked(proof.index, proof.account, proof.quantity));        require(MerkleProof.verify(proof.merkleProof, giveaway, node), \"INVALID_PROOF\");        require(!claimed[proof.account], \"CLAIMED\");        claimed[proof.account] = true;        <mask0>;    }"