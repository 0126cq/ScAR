"function publicMint(uint256 _mints) external payable noContracts {        require(saleStatus == SaleStatus.Public, \"FD: Public mint isn't active\");        require(_mints < MAX_MINT + 1, \"FD: Exceeds Max per TXN\");        require(totalSupply() + _mints <= MAX_DODOS, \"FD: Exceeds Max Allocation\");        require(msg.value == publicPrice * _mints, \"FD: Insufficient funds\");        <mask0>;    }"