"function mint(address user, uint256 machineType, uint256 level) external payable nonReentrant onlyComponent {        require(machinePoolMap[machineType] != address(0), \"mint machine type not set\");        publicTokenIdCounter += 1;        <mask0>;        userMachines[user].add(publicTokenIdCounter);        IMiningMachinePool(machinePoolMap[machineType]).addHashRate(user, level);        tokenIdToHashRateMap[publicTokenIdCounter] = level;        tokenIdToMachineTypeMap[publicTokenIdCounter] = machineType;    }"