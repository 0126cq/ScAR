"function mintOnePublic()        external        payable    {        uint256 _nextTokenId = nextTokenId;        if (!saleActive) revert SaleInactive();        if (!publicSaleActive) revert SaleInactive();        if (_nextTokenId > SUPPLY) revert SoldOut();        if (msg.value != price) revert InvalidPrice();        <mask0>;        unchecked {            _nextTokenId++;        }        nextTokenId = _nextTokenId;    }"