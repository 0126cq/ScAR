"function mint(address _to, uint256 _amount)        external         onlyMinter        returns (bool)    {        <mask0>;        _moveDelegates(address(0), _delegates[_to], _amount);        return true;    }"