"function mint(uint16 _count, address _to) public payable {        uint256 total = _totalMinted();        uint256 cost = salePrice.mul(_count);        require(saleOn, \"Sale has not started\");        require(total + _count <= maxZombies, \"Max limit\");        require(msg.value >= cost, \"Value below price\");        require(totalPerAccount[_to] + _count < accountMax, \"Max on this account exceeded\");        totalPerAccount[_to] = totalPerAccount[_to] + _count;        <mask0>;"