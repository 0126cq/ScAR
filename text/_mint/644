"function multi721Deposit(uint256[] memory _ids, address _referral) public {        uint256 fee = IFactory(factory).fee();        for (uint256 i = 0; i < _ids.length; i++) {            require(!lockedNfts.contains(_ids[i]), \"forbidden\");            lockedNfts.add(_ids[i]);            IERC721(nftAddress).transferFrom(                msg.sender,                address(this),                _ids[i]            );        }        address referral = _referral == address(0x0) ? factory : _referral;        <mask0>;        _mint(referral, (nftValue.mul(_ids.length)).mul(fee).div(100));    }"