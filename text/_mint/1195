"function airDrop(address[] memory addrs, uint256 amount)        external        onlyOwner    {        require(            totalSupply(DD) + addrs.length * amount <= maxSupply,            \"Exceeded max supply\"        );        for (uint256 i = 0; i < addrs.length; i++) {            <mask0>;        }    }"