"function claim() external {        require(!hasClaimed[msg.sender], \"Already claimed!\");        GemstoneTypes tier = tierByAddress[msg.sender];        require(tier != GemstoneTypes.None, \"Not eligible to claim!\");        uint256 total = _tokenCountByTier[tier] + 1;        require(total <= _maxTokensByTier[tier], \"Max supply reached!\");        <mask0>;        if (GemstoneTypes(tier) == GemstoneTypes.RubyHeartGemstone) {            _attemptClaim(GemstoneTypes.DiamondMindGemstone);            _attemptClaim(GemstoneTypes.AurumGemstoneOfTruth);        } else if (GemstoneTypes(tier) == GemstoneTypes.DiamondMindGemstone) {            _attemptClaim(GemstoneTypes.AurumGemstoneOfTruth);        }        hasClaimed[msg.sender] = true;    }"