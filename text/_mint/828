"function mint(address recipient_, uint256 amount_)        public        onlyOperator        returns (bool)    {        uint256 supply = totalSupply();        require(supply.add(amount_) <= MAX_SUPPLY, \"Share: Minting over max supply\");        uint256 balanceBefore = balanceOf(recipient_);        <mask0>;        uint256 balanceAfter = balanceOf(recipient_);        return balanceAfter >= balanceBefore;    }"