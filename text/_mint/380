"function _mint(address _user, uint256 _amount)\t\tinternal\t\toverride\t\treturns (uint256)\t{\t\taddress repTarget = reputationRecipients[_user];\t\trepTarget = repTarget != address(0) ? repTarget : _user;\t\tsuper.<mask0>;\t\taddress delegator = delegates[repTarget];\t\tif (delegator == address(0)) {\t\t\tdelegates[repTarget] = repTarget;\t\t\tdelegator = repTarget;\t\t}\t\tuint256 previousVotes = getVotes(delegator);\t\t_updateDelegateVotes(\t\t\tdelegator,\t\t\trepTarget,\t\t\tpreviousVotes,\t\t\tpreviousVotes + _amount\t\t);\t\treturn _amount;\t}"