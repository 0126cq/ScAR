"function PublicMint(uint256 amount) public payable {        require(!_isPresaleActive && _isMainSaleActive, \"Main sale must be active to mint.\");    require(msg.sender == tx.origin, \"mint from contract not allowed\");    require(msg.value >= public_price * amount, \"incorrect price\");    require(nextId <= maxTotalSupply, \"not enough tokens\");    require(mints[msg.sender] + amount <= MAX_PER_WALLET2, \"mint limit reached\");            mints[msg.sender] += amount;    <mask0>;  }"