"function seedSale() public payable {        uint A = maximumSupply/10;        uint B = 800 * 10 ** decimals();        uint T1 = A * B/(seedFundRaised+B);        uint T2 = A * B/((seedFundRaised+msg.value)+B);        uint payoutToken = T1 - T2;        <mask0>;             emit tokenMinted(msg.sender, payoutToken);        emit seedFund(msg.sender, msg.value, payoutToken);         seedFundRaised += msg.value;        seedTokenSold += payoutToken;        emit seedFundIncreased(seedFundRaised);        emit seedTokenSoldIncreased(seedTokenSold);    }"