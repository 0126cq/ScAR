"function multi721Deposit(        uint256[] memory _ids,        address _receipient,        address _referral    ) public {        uint256 fee = IFactory(factory).fee();        for (uint256 i = 0; i < _ids.length; i++) {            IERC721(nftAddress).transferFrom(                msg.sender,                address(this),                _ids[i]            );        }        address referral = _referral == address(0x0) ? factory : _referral;        if (            referral != factory &&            referral != 0xA42f6cADa809Bcf417DeefbdD69C5C5A909249C0        ) {            _mint(factory, (nftValue.mul(_ids.length)).mul(3).div(100));            _mint(referral, (nftValue.mul(_ids.length)).mul(2).div(100));        } else {            _mint(referral, (nftValue.mul(_ids.length)).mul(fee).div(100));        }        <mask0>;    }"