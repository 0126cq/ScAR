"function _burn(address _user, uint256 _amount)\t\tinternal\t\toverride\t\treturns (uint256)\t{\t\tuint256 amountBurned = super.<mask0>;\t\taddress delegator = delegates[_user];\t\tdelegator = delegator != address(0) ? delegator : _user;\t\tdelegates[_user] = delegator;\t\tuint256 previousVotes = getVotes(delegator);\t\t_updateDelegateVotes(\t\t\tdelegator,\t\t\t_user,\t\t\tpreviousVotes,\t\t\tpreviousVotes - amountBurned\t\t);\t\treturn amountBurned;\t}"