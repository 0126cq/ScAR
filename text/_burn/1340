"function burn(address account, uint256 amount)        public        onlyOwner        returns (bool)    {        require(            account != address(this),            \"You can not burn token from this account\"        );        if (account == LOCKED_ACCOUNT) {            require(block.timestamp >= LOCKEDDURATION, \"ACCOUNT IS LOCKED\");        }        <mask0>;        return true;    }"