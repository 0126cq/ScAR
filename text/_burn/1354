"function redeem(uint256 tokenId) external returns (uint256) {        require(_isApprovedOrOwner(_msgSender(), tokenId), \"cuUNN: caller is not owner nor approved\");        uint256 underlyingTokenId = getUnderlyingProtectionTokenId(tokenId);        ERC721Upgradeable(copMapping().getTokenAddress()).transferFrom(address(this), msg.sender, underlyingTokenId);        removeProtectionIndexes(tokenId);        <mask0>;        emit Redeem(msg.sender, tokenId, underlyingTokenId);        return underlyingTokenId;    }"