"function exerciseEth() external payable exerciseWindow {        uint256 amountOfOptions = msg.value;        require(amountOfOptions > 0, \"WPodPut: you can not exercise zero options\");        uint256 strikeToSend = _strikeToTransfer(amountOfOptions);        <mask0>;        IWETH(underlyingAsset()).deposit{ value: msg.value }();        IERC20(strikeAsset()).safeTransfer(msg.sender, strikeToSend);        emit Exercise(msg.sender, amountOfOptions);    }"