"function exercise(uint256 amountOfOptions) external virtual override exerciseWindow {        require(amountOfOptions > 0, \"PodPut: you can not exercise zero options\");        uint256 amountOfStrikeToTransfer = _strikeToTransfer(amountOfOptions);        <mask0>;        IERC20(underlyingAsset()).safeTransferFrom(msg.sender, address(this), amountOfOptions);        IERC20(strikeAsset()).safeTransfer(msg.sender, amountOfStrikeToTransfer);        emit Exercise(msg.sender, amountOfOptions);    }"