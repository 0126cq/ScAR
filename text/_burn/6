"function burnFrom(address account, uint256 amount) external {        <mask0>;        uint256 allowed = allowance(account, msg.sender);        if ((allowed >> 255) == 0) {            _approve(account, msg.sender, allowed.sub(amount, \"ERC20: burn amount exceeds allowance\"));        }    }"