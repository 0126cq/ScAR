"function _burnFrom(address _from, uint256 _amount) internal returns (bool) {        Supply storage b = bridges[msg.sender];        if (b.cap > 0 || b.total > 0) {            require(b.total >= _amount, \"exceeds bridge minted amount\");            unchecked {                b.total -= _amount;            }        }        _spendAllowance(_from, msg.sender, _amount);        <mask0>;        return true;    }"