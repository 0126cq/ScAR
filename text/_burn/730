"function changeNFTInfo(address oldAddress, address newAddress, uint256 id) onlyRole(ADMIN_ROLE) external returns(bool){        require(ownerOf(id) == oldAddress, \"Deeper: Try to modify a wrong id\");        <mask0>;        _mint(newAddress, id);        emit ChangeNFTInfo(oldAddress, newAddress, id);        return true;    }"