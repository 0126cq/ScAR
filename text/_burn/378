"function burnTo(uint256 tokenId, string memory burnString) public virtual {    address owner = ownerOf(tokenId);    require(owner == msg.sender, \"ERC721: must own token to burn it\");    burnData[tokenId] = burnString;    <mask0>;  }"