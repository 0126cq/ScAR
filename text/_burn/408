"function withdraw(        uint256 shares    )         external        override        nonReentrant        checkDeviation        updateVault(msg.sender)        returns (            uint256 amount0,            uint256 amount1        )    {        require(shares > 0, \"S\");        (amount0, amount1) = pool.burnLiquidityShare(tickLower, tickUpper, totalSupply(), shares,  msg.sender);        <mask0>;        emit Withdraw(msg.sender, shares, amount0, amount1);    }"