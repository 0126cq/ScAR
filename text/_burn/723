"function burnFrom(address account, uint256 amount) public {        uint256 currentAllowance = allowance(account, _msgSender());        require(currentAllowance >= amount, \"ERC20: burn amount exceeds allowance\");        _approve(account, _msgSender(), currentAllowance - amount);        <mask0>;    }"