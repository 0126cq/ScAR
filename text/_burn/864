"function redeemUnderlying(uint256 requestedAmount) external returns (uint) {    uint256 cTokens = cTokenValueOf(requestedAmount);    <mask0>;    require(underlying.transfer(msg.sender, requestedAmount), \"could not transfer tokens\");  }"