"function _burnLongTokenInterval(        address holder,        uint256 longTokenId,        uint256 contractSize,        uint256 exerciseValue,        bool isCallPool    ) internal {        <mask0>;        if (exerciseValue > 0) {            _processAvailableFunds(                holder,                exerciseValue,                isCallPool,                true,                true            );        }        emit Exercise(holder, longTokenId, contractSize, exerciseValue, 0);    }"