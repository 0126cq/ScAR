"function split(uint256 tokenId, address to) external nonReentrant {        require(_msgSender() == ownerOf(tokenId), \"Caller is not the owner\");        require(to != address(0), \"To the zero address\");        uint256 metahead = FinishHead[tokenId];        require(metahead > 0, \"Token ID invalid\");        require(!TokenIds.getSeal(metahead), \"Token ID is sealed\");        _transferFromBatch(metahead, to);        delete FinishHead[tokenId];        <mask0>;        emit Split(tokenId, to);    }"