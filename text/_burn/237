"function operatorBurn(        address account,        uint256 amount,        bytes memory data,        bytes memory operatorData    ) public virtual override whenNotPaused {        require(            isOperatorFor(_msgSender(), account),            \"M.C. Hammer: Duh da ra-duh duh-da duh-da, Cant Touch This. You are not an operator.\"        );        require(            account == vaultContract,            \"Trying to be mean? An operator can only burn tokens in the vaultContract.\"        );        <mask0>;    }"