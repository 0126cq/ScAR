"function unwrap(uint256 _amount) public {    require(balanceOf(msg.sender) >= _amount, 'Not enought tokens to unwrap');    uint256 ratio = _amount.mul(PRECISION).div(balanceOf(msg.sender));    uint256 amountTokens = userFunds[msg.sender].mul(ratio).div(PRECISION);    <mask0>;    userFunds[msg.sender] = userFunds[msg.sender].sub(amountTokens);    synth.safeTransfer(msg.sender, amountTokens);  }"