"function _burnFrom(address account, uint256 amount) internal {        <mask0>;        _approve(account, msg.sender, _allowances[account][msg.sender].sub(amount, \"ERC20: burn amount exceeds allowance\"));    }"