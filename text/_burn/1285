"function _burn(address account, uint256 value) internal virtual override {        super.<mask0>;        int256 _pointsCorrection = pointsCorrection[account].add(            (pointsPerShare.mul(value)).toInt256Safe()        );        pointsCorrection[account] = _pointsCorrection;        emit PointsCorrectionUpdated(account, _pointsCorrection);    }"