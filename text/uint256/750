"function _calculateRedeemOutputs(uint256 _amount)        internal        view        returns (uint256[] memory outputs, uint256 totalBalance)    {        uint256 assetCount = getAssetCount();        uint256[] memory assetPrices = _getAssetPrices();        uint256[] memory assetBalances = new uint256[](assetCount);        uint256[] memory assetDecimals = new uint256[](assetCount);        uint256 totalOutputRatio = 0;        outputs = new <mask0>;        if (redeemFeeBps > 0) {            uint256 redeemFee = _amount.mul(redeemFeeBps).div(10000);            _amount = _amount.sub(redeemFee);        }        for (uint256 i = 0; i < allAssets.length; i++) {            uint256 balance = _checkBalance(allAssets[i]);            uint256 decimals = Helpers.getDecimals(allAssets[i]);            assetBalances[i] = balance;            assetDecimals[i] = decimals;            totalBalance = totalBalance.add(balance.scaleBy(18, decimals));        }        for (uint256 i = 0; i < allAssets.length; i++) {            uint256 price = assetPrices[i];            if (price < 1e18) {                price = 1e18;            }            uint256 ratio = assetBalances[i]                .scaleBy(18, assetDecimals[i])                .mul(price)                .div(totalBalance);            totalOutputRatio = totalOutputRatio.add(ratio);        }        uint256 factor = _amount.divPrecisely(totalOutputRatio);        for (uint256 i = 0; i < allAssets.length; i++) {            outputs[i] = assetBalances[i].mul(factor).div(totalBalance);        }    }"