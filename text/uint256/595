"function _linearInterpolation(        uint256 value,        uint256 parts    )        internal        pure        returns (uint256[] memory rets)    {        rets = new <mask0>;        for (uint i = 0; i < parts; i++) {            rets[i] = value * (i + 1) / parts;        }    }"