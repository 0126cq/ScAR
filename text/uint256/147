"function getOwnedTokens(address owner)        external        view        returns (uint256[] memory owned_)    {        EnumerableSet.UintSet storage set = _ownedTokenIds[owner];        owned_ = new <mask0>;        for (uint256 i; i < owned_.length; i++) {            owned_[i] = set.at(i);        }    }"