"function mintPriceForAll(uint256[] memory tokenIds) public view returns (uint256[] memory) {        require(tokenIds.length <= MAX_MORPHYS, \"Can not calculate price for count exceeding MAX_MORPHYS\");        uint256[] memory pricing = new <mask0>;         for(uint i = 0; i < tokenIds.length; i++) {            pricing[i] = mintPrice(tokenIds[i]);        }        return pricing;    }"