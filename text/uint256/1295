"function getFlowerFamFlowersOfUserWithBees(address user) public view returns (uint256[] memory) {        uint256 counter;        uint256 balance = flowerFamNFT.balanceOf(user);        uint256[] memory userNFTs = new <mask0>;        uint256 startToken = flowerFamNFT.startTokenId();        for (uint i = startToken; i < startToken + flowerFamNFT.totalSupply(); i++) {            if (flowerToBee[i].beeId != 0 && flowerFamNFT.realOwnerOf(i) == user) {                userNFTs[counter] = i;                counter++;            }                       }        return userNFTs;    }"