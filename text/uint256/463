"function getRedeemProportionAmount(uint256 _amount) external view returns (uint256[] memory, uint256) {        uint256[] memory _balances = balances;        require(_amount > 0, \"zero amount\");        uint256 D = totalSupply;        uint256[] memory amounts = new <mask0>;        uint256 feeAmount = 0;        if (redeemFee > 0) {            feeAmount = _amount.mul(redeemFee).div(feeDenominator);            _amount = _amount.sub(feeAmount);        }        for (uint256 i = 0; i < _balances.length; i++) {            amounts[i] = _balances[i].mul(_amount).div(D).div(precisions[i]);        }        return (amounts, feeAmount);    }"