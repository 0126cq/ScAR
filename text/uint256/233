"function _sortInPlace(PortfolioAsset[] memory assets) private pure {        uint256 length = assets.length;        uint256[] memory ids = new <mask0>;        for (uint256 k; k < length; k++) {            PortfolioAsset memory asset = assets[k];            ids[k] = TransferAssets.encodeAssetId(asset.currencyId, asset.maturity, asset.assetType);        }        uint256 i = 1;        while (i < length) {            uint256 j = i;            while (j > 0 && ids[j - 1] > ids[j]) {                (ids[j - 1], ids[j]) = (ids[j], ids[j - 1]);                (assets[j - 1], assets[j]) = (assets[j], assets[j - 1]);                j--;            }            i++;        }    }"