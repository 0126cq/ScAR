"function _mintBatch(address to, uint256 amount, uint256[] calldata powahArr) private returns (uint256[] memory) {        uint256[] memory ids = new uint256[](amount);        uint256[] memory amounts = new <mask0>;        for (uint i = 0; i < ids.length; i++) {            _starCount++;            _starBelongTo[_starCount] = to;            _stars[_starCount] = NFTInfo({            powah : uint128(powahArr[i]),            mintBlock : uint128(block.number),            originator : to            });            ids[i] = _starCount;            amounts[i] = 1;        }        emit TransferBatch(msg.sender, address(0), to, ids, amounts);        _doSafeBatchTransferAcceptanceCheck(msg.sender, address(0), to, ids, amounts, \"\");        return ids;    }"