"function activeTokenIdsOf(uint8 _pid, address _owner)         public view returns (uint256[] memory, string[] memory){        uint256 tokenCount = ActiveStakes[_pid][_owner];        if (tokenCount == 0) {            return (new <mask0>, new string[](0));        } else {            uint256[] memory tokenIds = new uint256[](tokenCount);            string[] memory tokenUrls = new string[](tokenCount);            uint256 index;            for (index = 0; index < tokenCount; index++) {                tokenIds[index] = stakedTokenIdByIndex(_pid, _owner, index);                tokenUrls[index] = ERC721(poolInfo[_pid].nftContract).tokenURI(tokenIds[index]);            }            return (tokenIds, tokenUrls);        }    }"