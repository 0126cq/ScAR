"function tokensOfOwner(address owner) external view returns(uint256[] memory) {        uint256 ownerTokenCount = balanceOf(owner);        if (ownerTokenCount == 0) {            return new <mask0>;        } else {            uint256[] memory result = new uint256[](ownerTokenCount);            uint256 index;            for (index = 0; index < ownerTokenCount; index++) {                result[index] = tokenOfOwnerByIndex(owner, index);            }            return result;        }    }"