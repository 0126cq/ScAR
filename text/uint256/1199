"function walletOfOwner(address _owner)        public        view        returns (uint256[] memory)    {        uint256 ownerTokenCount = balanceOf(_owner);        uint256[] memory ownedTokenIds = new <mask0>;        Token[] memory ownerTokenList = tokens[_owner];        for (uint256 i = 0; i < ownerTokenCount; i++) {            ownedTokenIds[i] = ownerTokenList[i].tokenId;        }        return ownedTokenIds;    }"