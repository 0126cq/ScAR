"function stakingScheduleAmount(address account)        public        view        returns (uint256[] memory)    {        uint256 stakingsLength = _state.accounts[account].stakings.length;        uint256[] memory array = new <mask0>;        for (uint256 i = 0; i < stakingsLength; i++) {            array[i] = _state.accounts[account].stakings[i].amount;        }        return array;    }"