"function getIdBatch(address[] calldata tokenAddresses) external pure returns (uint256[] memory) {        uint256[] memory ids = new <mask0>;        for (uint256 i = 0; i < tokenAddresses.length; i++) {            ids[i] = _getId(tokenAddresses[i]);        }        return ids;    }"