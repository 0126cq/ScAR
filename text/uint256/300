"function claimableOfOwner(address contractAddress, address owner) public view ifContractExists(contractAddress) returns (uint256[] memory stakedTimestamps, uint256[] memory claimableTimestamps) {    EnumerableSet.UintSet storage userTokens = addressToStakedTokensSet[contractAddress][owner];    uint256[] memory stakedTs = new <mask0>;    uint256[] memory claimableTs = new uint256[](userTokens.length());    uint256 lastClaimTs = accountToLastClaim[owner].servertime;    for (uint256 i = 0; i < userTokens.length(); i++) {      uint256 tokenId = userTokens.at(i);      stakedTs[i] = contractTokenIdToStakedTimestamp[contractAddress][tokenId];      if (lastClaimTs > stakedTs[i]) {        claimableTs[i] = lastClaimTs;      } else {        claimableTs[i] = stakedTs[i];      }    }    return (stakedTs, claimableTs);  }"