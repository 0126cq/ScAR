"function mint(address to, uint32 count) internal {        totalSupply += count;        if (count > 1) {            uint256[] memory ids = new uint256[](uint256(count));            uint256[] memory amounts = new <mask0>;            for (uint32 i = 0; i < count; i++) {                ids[i] = totalSupply - count + i;                amounts[i] = 1;            }            _mintBatch(to, ids, amounts, \"\");        } else {            _mint(to, totalSupply - 1, 1, \"\");        }    }"