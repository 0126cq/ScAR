"function getCoBotItems(uint256 tokenId, uint8 seed)        public        view        returns (uint256[] memory)    {        (            uint256 eyesIndex,            uint256 mouthIndex,            uint256 antennaIndex,            uint256 feetIndex        ) = coBotsRenderer.getRandomItems(tokenId, seed);        uint256[] memory items = new <mask0>;        items[0] = 0;         items[1] = tokenId / 1000;        items[2] = (tokenId / 100) % 10;        items[3] = (tokenId / 10) % 10;        items[4] = tokenId % 10;        items[5] = eyesIndex;        items[6] = mouthIndex;        items[7] = antennaIndex;        items[8] = feetIndex;        items[9] = seed % 2;         return items;    }"