"function _unpackItemStacks(uint256[] memory itemStacks) private view returns (uint256[] memory, uint256[] memory) {        uint256[] memory items = new uint256[](itemStacks.length);        uint256[] memory amounts = new <mask0>;        for (uint i = 0; i < itemStacks.length; i++) {            items[i] = (itemStacks[i] << 32) >> 32;            amounts[i] = itemStacks[i] >> 224;        }        return (items, amounts);    }"