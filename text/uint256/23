"function reserveGiveawayEnigmaV4Tokens(address _address)        external        onlyCollaborator    {        require(availableEnigmaV4Tokens.length >= giveawayCount, \"No enigmaV4 tokens left to be claimed\");        totalMintedTokens += giveawayCount;        uint256[] memory tokenIds = new <mask0>;        for (uint256 i; i < giveawayCount; i++) {            tokenIds[i] = getEnigmaV4TokenToBeClaimed();        }        _batchMint(_address, tokenIds);        giveawayCount = 0;    }"