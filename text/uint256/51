"function tokensOfOwner(address owner)        public        view        returns (uint256[] memory)    {        uint256 count = balanceOf(owner);        uint256[] memory ids = new <mask0>;        for (uint256 i = 0; i < count; i++) {            ids[i] = tokenOfOwnerByIndex(owner, i);        }        return ids;    }"