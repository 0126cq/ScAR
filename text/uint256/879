"function isReserved(uint256 _endTime, uint256[] memory _startTimes, bytes[] memory _terms) public pure returns (bool reserved, uint256 nextIndex) {    uint256[] memory endTimes = new <mask0>;    for(uint256 i = 0; i < _startTimes.length; i++){      endTimes[i] = _startTimes[i] + getDuration(_terms[i]) -1;    }    for(uint256 i = 0; i <= _startTimes.length; i++){      nextIndex = i;      if(i == _startTimes.length){        return (false, i);      }      if(endTimes[i] > _endTime){        if(_startTimes[i] > _endTime){          break;        }else{          return (true, 0);        }      }    }    reserved = false;  }"