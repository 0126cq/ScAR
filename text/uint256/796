"function reclaimAll(address _to) public onlyOwner whenEnded {        uint256 length = presaleAlpacaIDs.length();        uint256[] memory ids = new <mask0>;        uint256[] memory amount = new uint256[](length);        for (uint256 i = 0; i < length; i++) {            ids[i] = presaleAlpacaIDs.at(i);            amount[i] = 1;        }        cryptoAlpaca.safeBatchTransferFrom(address(this), _to, ids, amount, \"\");    }"