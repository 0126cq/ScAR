"function tokensOfOwner(address _owner) external view returns (uint256[] memory) {        require(_owner != address(0), \"owner_zero_address\");        uint256 tokenCount = balanceOf(_owner);        if (tokenCount <= 0) {            return new uint256[](0);        } else {            uint256[] memory result = new <mask0>;            uint256 index;            for (index = 0; index < tokenCount; index++) {                result[index] = tokenOfOwnerByIndex(_owner, index);            }            return result;        }    }"