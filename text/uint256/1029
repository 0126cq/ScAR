"function freeMint() external payable {        address _caller = _msgSender();        require(!paused, \"Paused\");        require(MAX_SUPPLY >= totalSupply() + 1, \"Exceeds max supply\");        require(tx.origin == _caller, \"No contracts\");        require(MAX_PER_TX_FREE >= <mask0> + 1, \"Excess max per free wallet\");        _setAux(_caller, 1);        _safeMint(_caller, 1);    }"