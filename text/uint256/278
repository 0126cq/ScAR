"function balanceOfBatch(address[] memory accounts, uint256[] memory ids)        public        view        override        returns (uint256[] memory)    {        require(accounts.length == ids.length, \"ERC1155: length mismatch\");        uint256[] memory batchBalances = new <mask0>;        for (uint256 i = 0; i < accounts.length; ++i) {            require(accounts[i] != address(0), \"ERC1155: zero address\");            batchBalances[i] = _balances[ids[i]][accounts[i]];        }        return batchBalances;    }"