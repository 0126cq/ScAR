"function getERC721OffChainTokensOf(address _address)        public        view        returns (uint256[] memory)    {        uint256 ownerBalance = getERC721BalanceOffChain(_address);        uint256[] memory result = new <mask0>;        uint256 counter = 0;        for (uint256 i = 1; i <= totalSupply; i++) {            if (_owners[i].addr == _address) {                result[counter] = i;                unchecked {                    counter++;                }            }        }        return result;    }"