"function generateTokenId(        address creator,        uint256 supply,        uint40 packId,        uint16 numFTs,        uint16 packIndex    ) internal pure returns (uint256) {        require(supply > 0 && supply <= MAX_SUPPLY, \"invalid supply\");        return            uint256(creator) * CREATOR_OFFSET_MULTIPLIER +             (supply == 1 ? uint256(1) * IS_NFT_OFFSET_MULTIPLIER : 0) +             <mask0> * PACK_ID_OFFSET_MULTIPLIER +             numFTs * PACK_NUM_FT_TYPES_OFFSET_MULTIPLIER +             packIndex;     }"