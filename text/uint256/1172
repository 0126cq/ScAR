"function mintExtensionBatch(address to, string[] calldata uris)        public        virtual        override        nonReentrant        extensionRequired        returns (uint256[] memory tokenIds)    {        tokenIds = new <mask0>;        for (uint256 i = 0; i < uris.length; i++) {            tokenIds[i] = _mintExtension(to, uris[i]);        }    }"