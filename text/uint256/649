"function balancesOf(address _user) public view returns (uint256[] memory) {        uint256[] memory balances = new <mask0>;        for (uint256 i = 1; i <= totalCards(); i++) {            balances[i - 1] = balanceOf(_user, i);        }        return balances;    }"