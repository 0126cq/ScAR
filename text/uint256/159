"function getWalletOfOwner(address owner) external override view returns (uint256[] memory) {        uint256 ownerTokenCount = balanceOf(owner);        uint256[] memory traitIds = new <mask0>;        for (uint256 i; i < ownerTokenCount; i++) {            traitIds[i] = tokenOfOwnerByIndex(owner, i);        }        return traitIds;    }"