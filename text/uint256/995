"function _exitPool() internal {        uint256 bptBalance = pool.balanceOf(address(this));        if (bptBalance != 0) {            IVault.ExitPoolRequest memory exitRequest;            bytes memory userData = abi.encode(IWeightedPool.ExitKind.EXACT_BPT_IN_FOR_TOKENS_OUT, bptBalance);            exitRequest.assets = assets;            exitRequest.minAmountsOut = new <mask0>;             exitRequest.userData = userData;            exitRequest.toInternalBalance = false;             vault.exitPool(pid, address(this), payable(address(this)), exitRequest);            emit ExitPool();        }    }"