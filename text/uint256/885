"function getTokensForAddress(address fromAddress) external view returns (uint256 [] memory){        uint256 tokenCount = balanceOf(fromAddress);        uint256 [] memory result = new <mask0>;         uint256 total = totalSupply();        uint256 resultIndex = 0;        for (uint id = 0; id < total; id++) {            if (super.ownerOf(id)==fromAddress){                result[resultIndex] = id;                resultIndex++;            }        }        return result;    }"