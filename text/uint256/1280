"function mintBatch(address account, uint256 amount, uint256[] calldata powahArr) external onlyMinter override returns (uint256[] memory) {        uint256[] memory ids = new uint256[](amount);        uint256[] memory amounts = new <mask0>;        for (uint i = 0; i < ids.length; i++) {            starCount++;            ids[i] = starCount;            amounts[i] = 1;        }        _mintBatch(account, ids, amounts, \"\");        return ids;    }"