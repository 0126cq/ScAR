"function exchange(        uint256[] calldata nfts    )        external        returns (uint256)    {                require(nfts.length == 20, 'PinkslipTicket: 20 NFTs are required');        for (uint i=0; i < nfts.length; i++) {            PinkslipNFT(pinkslipNFT).<mask0>;        }        _tokenIds.increment();        uint256 newItemId = _tokenIds.current();        _safeMint(msg.sender, newItemId);        return newItemId;    }"