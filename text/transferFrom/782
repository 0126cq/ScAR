"function rename(uint256 tokenId, string memory newName) external {        require(_msgSender() == ownerOf(tokenId), \"Only rename your own NFTs\");        require(            sha256(bytes(newName)) != sha256(bytes(tokenName[tokenId])),            \"No name change necessary\"        );        require(validName(newName), \"Invalid name\");        peach.<mask0>;        tokenName[tokenId] = newName;    }"