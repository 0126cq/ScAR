"function multiBurn(uint256[] memory tokenId) public {         for (uint i = 0; i < tokenId.length; i++) {             if (_isApprovedOrOwner(_msgSender(), tokenId[i])){                <mask0>;             }         }    }"