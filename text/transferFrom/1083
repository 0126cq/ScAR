"function unstakeGen0(uint16 [] calldata data) external {    require(stakeOwners[ msg.sender].gen0.tokens.length> 0, \"You have not staked any NFTs\");     uint16 tokens =uint16(data.length);    require(tokens > 0, \"You have not selected any NFT to unstake\");     uint16 tokenID;    for(uint16 i; i<tokens; i++)    {    tokenID=data[i];    genesis0.<mask0>;    removeGen0Token(tokenID);    }   totalgen0NFTStaked -= tokens; }"