"function _transferFunds    (        uint _weiAmount,        address _tokenAddress,        uint _tokenAmount,        address payable _receiver    )    internal returns (bool)    {      if (tx.origin != _receiver) {         feeReceiver.transfer(sponsoredFeeAmount);      }        if (_weiAmount > 0) {            _receiver.transfer(_weiAmount);        }        if (_tokenAmount > 0) {            IERC20(_tokenAddress).<mask0>;        }        return true;    }"