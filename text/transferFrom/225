"function swapReward(        address _tokenFrom,        uint256 _amountIn,        address _dex,        bytes memory _data    ) private onlyWhitelisted(_dex) {        IERC20(_tokenFrom).<mask0>;        (bool success, ) = _dex.call(_data);        require(success, _tokenFrom == crv ? \"!swap crv\" : \"!swap cvx\");    }"