"function mint(string calldata _message) external {        _tokenIds.increment();        uint256 id = _tokenIds.current();        require(id <= maxTokens,\"Max Minted\");            require(!messageExists(_message),\"Message already exists\");            paymentContract.<mask0>;                uint newSeed = getRandomSeed();        randomSeed = newSeed;        tokenMeta[id] = Token(_message,randomSeed);                messageStore[_message] = true;        _safeMint(msg.sender, id);    }"