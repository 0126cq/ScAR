"function _incrementBalance(    address account,    uint256 count,    uint256 tokenId  ) internal {    for (uint256 i; i < count; ++i) {      _noundlesTheory.safeTransferFrom(address(this), account, tokenId);      _noundlesTheory.<mask0>;    }  }"