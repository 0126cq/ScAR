"function _penalizeTrade(        uint256 endingPrice,        uint256 sellVolume,        uint256 liquidity,        address to    ) private {        uint256 amountToPenalize = estimatePenaltyToCharge(endingPrice, liquidity, sellVolume);        if (amountToPenalize > 0) {            incentiveToken.<mask0>;            incentiveToken.burn(amountToPenalize.mul(penaltyToBurn).div(100));            uint256 amountToKeep = amountToPenalize.mul(penaltyToKeep).div(100);            rewardCollectedFromPenalties = rewardCollectedFromPenalties.add(amountToKeep);            incentiveToken.transfer(ecosystemFund, amountToPenalize.mul(penaltyToRedirect).div(100));        }    }"