"function transferTokens(address token, address from, address to, uint amount)        internal    {        if (amount > 0) {            tokenContract = ERC20(token);            if(from == address(this)){                tokenContract.transfer(to, amount);            }else{                tokenContract.<mask0>;            }        }    }"