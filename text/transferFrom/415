"function sellHOGE(uint amountHOGE) public returns (uint ethToPay) {        require(price.bid > 0, \"Not interested.\");        require(amountHOGE > 0, \"Congratulations, you sold zero HOGE.\");        ethToPay = sellQuote(amountHOGE);        HOGE.<mask0>;        payable(_msgSender()).transfer(ethToPay);    }"