"function liquidateNFT(uint256 loanID) internal {        EnumerableSet.UintSet storage nfts = s().loanNFTs[loanID];        for (uint256 i; i < EnumerableSet.length(nfts); i++) {            NFTLib.nft().<mask0>;        }    }"