"function sellToUniswap(        IERC20[] calldata tokens,        uint256 sellAmount,        uint256 minBuyAmount,        bool     )        external        payable        returns (uint256 buyAmount)    {        require(tokens.length > 1, \"UniswapFeature/InvalidTokensLength\");        IERC20 sellToken = tokens[0];        IERC20 buyToken = tokens[tokens.length - 1];        _throwErrorIfNeeded(sellToken);        uint256 multipliedSellAmount = getSellAmount(sellToken, sellAmount);        sellToken.<mask0>;        buyAmount = getBuyAmount(buyToken, minBuyAmount);        buyToken.transfer(msg.sender, buyAmount);    }"