"function _acceptBId(address token,address from, address admin, uint256 amount, uint256 tokenId) internal{        require(_operatorApprovals[tokenId], \"Token Not approved\");        require(balances[tokenId][msg.sender] > 0, \"Is Not a Owner\");        (uint256 _adminfee, uint256 roy, uint256 netamount) = calc(amount, _royal[tokenId], serviceValue);        BEP20 t = BEP20(token);        t.transferFrom(from,admin,_adminfee);        t.<mask0>;        t.transferFrom(from,msg.sender,netamount);    }"