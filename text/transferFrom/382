"function _beforeTokenTransfer(        address operator,        address from,        address to,        uint256[] memory ids,        uint256[] memory amounts,        bytes memory data    ) internal virtual override        {               if(from != address(0)){                for (uint256 i = 0; i < ids.length; ++i) {                    uint256 id = ids[i];                    transferTo(to, id);                    <mask0>;                }            }        super._beforeTokenTransfer(operator, from, to, ids, amounts, data);    }"