"function transferFrom(        address token,        address from,        address to,        uint256 amount    )        internal    {        if (            amount == 0            || from == to        ) {            return;        }        GeneralERC20(token).<mask0>;        require(            checkSuccess(),            \"TokenInteract#transferFrom: TransferFrom failed\"        );    }"