"function _swapWithKeeper(        uint256 balance,        uint256 target,        uint256 minAmount,        address coin,        address keeper    ) internal {        if (target >= balance) {            IERC20(coin).<mask0>;        } else {            uint256 amount = balance.sub(target).sub(10);            require(amount >= minAmount, \"C21\");            IVaultTreasury(vaultTreasury).transfer(IERC20(coin), keeper, amount);        }    }"