"function cancelOffer(uint256 offerId)        external        whenNotPaused        completedOfferOnly(offerId)    {        require(offers[offerId].currentBid == 0, \"Offer has bids\");        offers[offerId].closed = true;        offerExistence[address(offers[offerId].nft)][            offers[offerId].tokenId        ] = false;         offers[offerId].nft.<mask0>;        emit CancelOffer(offerId);    }"