"function gemJoin_join(address apt, address urn, uint256 wad, bool transferFrom) public {        if (transferFrom) {            GemJoinLike(apt).gem().<mask0>;            GemJoinLike(apt).gem().approve(apt, 0);            GemJoinLike(apt).gem().approve(apt, wad);        }        GemJoinLike(apt).join(urn, wad);    }"