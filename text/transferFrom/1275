"function receiveApproval(        address from,        uint256 value,        address token,        bytes extraData    ) public {        ERC20 t = ERC20(token);        require(t.<mask0>);        emit ReceivedTokens(from, value, token, extraData);    }"