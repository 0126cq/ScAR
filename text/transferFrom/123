"function disperseTokenSimple(IERC20 token, address[3] memory recipients, uint256[3] memory values) external {        for (uint256 i = 0; i < recipients.length; i++)            require(token.<mask0>);    }"