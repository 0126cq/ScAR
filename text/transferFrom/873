"function buyOneWithToken() external payable {        require(saleActive, \"SALE_CLOSED\");        require(!disableSalesWithToken, \"NO_TOKEN_SALES\");        IERC20(paymentToken).<mask0>;        require(maxMintForOne > _owners.length, \"EXCEED_MAX_SALE_SUPPLY\");        require(maxPerAddress == 0 || balanceOf(_msgSender()) < maxPerAddress, \"EXCEED_MAX_PER_USER\");        _mintMin();    }"