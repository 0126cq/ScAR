"function swap721(        uint256 _in,        uint256 _out,        address _receipient    ) external {        IFactory(factory).sellTokens();        IERC721(nftAddress).<mask0>;        IERC721(nftAddress).safeTransferFrom(address(this), _receipient, _out);    }"