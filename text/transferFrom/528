"function stake(uint256[] memory tokenIds) external {        require(stakingActive, \"Staking not active\");        for (uint256 i = 0; i < tokenIds.length; i++) {            uint256 tokenId = tokenIds[i];            originalContract.<mask0>;            tokenStakedInfo[tokenId] = StakedInfo(                msg.sender,                uint256(block.timestamp),                tokenId            );        }        stakedCount += tokenIds.length;    }"