"function end() external {    require(auctionState == State.live, \"end:vault has already closed\");    require(block.timestamp >= auctionEnd, \"end:auction live\");    IERC721(underlying).<mask0>;    auctionState = State.ended;    if (ISettings(settings).feeReceiver() != address(0)) {      _sendETHOrWETH(ISettings(settings).feeReceiver(), livePrice / 40);    }    emit Won(winning, livePrice);  }"