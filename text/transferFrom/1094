"function removeLiquidity(        address tokenA,        address tokenB,        uint liquidity,        uint amountAMin,        uint amountBMin,        address to,        uint deadline    ) public virtual override ensure(deadline) returns (uint amountA, uint amountB) {        address pair = MoxieSwapLibrary.pairFor(factory, tokenA, tokenB);        IMoxieSwapPair(pair).<mask0>;         (uint amount0, uint amount1) = IMoxieSwapPair(pair).burn(to);        (address token0,) = MoxieSwapLibrary.sortTokens(tokenA, tokenB);        (amountA, amountB) = tokenA == token0 ? (amount0, amount1) : (amount1, amount0);        require(amountA >= amountAMin, 'MoxieSwapRouter: INSUFFICIENT_A_AMOUNT');        require(amountB >= amountBMin, 'MoxieSwapRouter: INSUFFICIENT_B_AMOUNT');    }"