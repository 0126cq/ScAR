"function stakeShares(uint _shares) external {        uint _before = balanceOf(address(this));        IERC20(address(this)).<mask0>;        uint _after = balanceOf(address(this));        _shares = _after.sub(_before);        _stakeShares(_shares);    }"