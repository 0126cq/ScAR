"function safeTransferFrom(address _from, address _to, uint256 _tokenId, bytes _data) public canTransfer(_tokenId) {        <mask0>;        require(checkAndCallSafeTransfer(_from, _to, _tokenId, _data));    }"