"function cancelImport(GUID referenceId) external payable originEqualsSender {        require(pendingImportExists(referenceId), \"Bridge: import should be pre-paid\");        address user = importsPending[referenceId].user;        uint256 tokenId = importsPending[referenceId].tokenId;        require(            hasRole(VOICE_IMPORTER, msg.sender) || user == msg.sender,            \"Bridge: import can only be cancelled by its initiator or by Voice backend.\");        simpleAssets.<mask0>;        delete importsPending[referenceId];    }"