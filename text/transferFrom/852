"function safeTransferFrom(        address from,        address to,        uint256 id    ) public virtual {        <mask0>;        if (!_checkOnERC721Received(from, to, id, '')) revert TransferToNonERC721ReceiverImplementer();    }"