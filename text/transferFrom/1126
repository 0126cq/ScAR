"function masterDeposit(uint256 amount) external onlyOwner {        require(            amount > 0,            \"masterDeposit: amount to deposit should be positive\"        );        require(            kncToken.<mask0>,            \"masterDeposit: can not get token\"        );        kncToken.approve(address(kyberStaking), amount);        kyberStaking.deposit(amount);    }"