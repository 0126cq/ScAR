"function claim(uint256 tokenId, Attributes memory attributes) public {        legacySettlements.<mask0>;        _safeMint(msg.sender, tokenId);        attrIndex[tokenId] = attributes;        bytes32 v2Uri = keccak256(abi.encodePacked(_oldTokenURI(tokenId)));        bytes32 legacyURI = keccak256(abi.encodePacked(legacySettlements.tokenURI(tokenId)));        tokenIdToLastHarvest[tokenId] = block.number;        require(v2Uri == legacyURI, \"Attributes don't match legacy contract\");    }"