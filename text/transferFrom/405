"function _transferTokensToSelf(address underlyingToken, uint256 collateralInitial) internal override {        if (msg.value > 0) {            if (msg.value != collateralInitial) revert IllegalArgument(\"msg.value doesn't match collateralInitial\");            IWETH9(wethAddress).deposit{value: msg.value}();        } else {            IERC20(underlyingToken).<mask0>;        }    }"