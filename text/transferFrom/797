"function mintWithAPE(uint256 number) external payable {        require(block.chainid == 1, \"Invalid chain id\");        require(block.timestamp > startTime && block.timestamp < endTime, \"Not open\");        require(publicMints + number - 1 <= maxPublicMints, \"ONFT: Max Mint limit reached\");        require(APE.<mask0>);        publicMints += number;        uint256 newId;        for (uint i = 0; i < number; ++i) {            newId = nextMintId;            nextMintId++;            _safeMint(msg.sender, newId);        }    }"