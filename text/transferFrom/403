"function transferFromAndCall(        address _from,        address _to,        uint256 _value,        bytes _data      )        public payable returns (bool)      {        require(_to != address(this));        super.<mask0>;        require(_to.call.value(msg.value)(_data));        return true;      }"