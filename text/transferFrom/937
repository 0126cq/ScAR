"function transferFromBulk(        address[] memory from,        address[] memory to,        uint256[] memory tokenIds    ) public virtual {        require(from.length == to.length, \"FROM_TO_LENGTH_MISMATCH\");        require(from.length == tokenIds.length, \"FROM_TOKEN_LENGTH_MISMATCH\");        for (uint256 i = 0; i < tokenIds.length; i++) {            IERC721(address(this)).<mask0>;        }    }"