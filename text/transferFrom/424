"function receiveApproval(        address from,        uint256 value,        address token,        bytes memory extraData    ) public {        ERC20 t = ERC20(token);        require(            t.<mask0>,            \"ERC20 token transfer failed\"        );        emit ReceivedTokens(from, value, token, extraData);    }"