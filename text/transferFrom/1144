"function adminClaim(uint256 _nftId) external override onlyOwner {        address nftOwner;        bool claimed;        (nftOwner, claimed, ) = froyoCat.get(_nftId);        require(owner() != address(0), \"Error: NFT invalid\");         require(!claimed, \"Error: NFT claimed already\");        froyoCat.<mask0>;        froyoCat.update(_nftId, nftOwner, true);        emit Claim(_msgSender(), nftOwner, _nftId, block.timestamp);    }"