"function _unstakeNFTs(IERC721 nft, uint256[] memory tokenIds) public {        for (uint256 i = 0; i < tokenIds.length; i++) {            uint256 tokenId = tokenIds[i];            require(nftOwner(nft, tokenId) == msg.sender, \"NOT_NFT_OWNER\");            require(                nftStatus(nft, tokenId) == NFTStatus.Unlockable,                \"NOT_UNLOCKABLE\"            );            nft.<mask0>;            emit NFTUnlocked(nft, tokenId);        }    }"