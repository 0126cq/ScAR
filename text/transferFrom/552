"function stake (uint256 amount) external {        _registAddress(msg.sender);        _updateReward(msg.sender);        stakeToken.<mask0>;        STAKED_AMOUNT[msg.sender] = STAKED_AMOUNT[msg.sender].add(amount);        totalStaked = totalStaked.add(amount);    }"