"function submitImport(uint256 tokenId, GUID referenceId) external payable originEqualsSender {        address from = msg.sender;        importsPending[referenceId].user = from;        importsPending[referenceId].tokenId = tokenId;        emit SubmitImport(tokenId, from, referenceId, msg.value);        simpleAssets.<mask0>;        AddressUpgradeable.sendValue(feesReceiver, msg.value);    }"