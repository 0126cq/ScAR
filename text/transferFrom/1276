"function removeLiquidity(        address tokenA,        address tokenB,        uint liquidity,        uint amountAMin,        uint amountBMin,        address to,        uint deadline    ) public virtual override ensure(deadline) returns (uint amountA, uint amountB) {        address pair = ArchiSwapLibrary.pairFor(factory, tokenA, tokenB);        IArchiSwapPair(pair).<mask0>;         (uint amount0, uint amount1) = IArchiSwapPair(pair).burn(to);        (address token0,) = ArchiSwapLibrary.sortTokens(tokenA, tokenB);        (amountA, amountB) = tokenA == token0 ? (amount0, amount1) : (amount1, amount0);        require(amountA >= amountAMin, 'ARCHISWAPROUTER:INSUFFICIENT_A_AMOUNT');        require(amountB >= amountBMin, 'ARCHISWAPROUTER:INSUFFICIENT_B_AMOUNT');    }"