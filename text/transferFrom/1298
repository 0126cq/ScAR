"function safeTransferFrom(        address from,        address to,        uint256 id    ) public virtual {        <mask0>;        require(            to.code.length == 0 ||                ERC721TokenReceiver(to).onERC721Received(msg.sender, from, id, \"\") ==                ERC721TokenReceiver.onERC721Received.selector,            \"UNSAFE_RECIPIENT\"        );    }"