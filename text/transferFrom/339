"function swap721(uint256 _in, uint256 _out) external {        lockedNfts.add(_in);        lockedNfts.remove(_out);        IERC721(nftAddress).<mask0>;        IERC721(nftAddress).safeTransferFrom(address(this), msg.sender, _out);    }"