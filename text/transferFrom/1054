"function cancelReserveAuction(uint256 auctionId) external nonReentrant {        ReserveAuction memory auction = auctionIdToAuction[auctionId];        require(            auction.seller == msg.sender,            \"NFTMarketReserveAuction: Not your auction\"        );        require(            auction.endTime == 0,            \"NFTMarketReserveAuction: Auction in progress\"        );        delete auctionIdToAuction[auctionId];        for (uint256 k = 0; k < auction.tokenId.length; k++) {            IERC721Upgradeable(auction.nftContract).<mask0>;        }        emit ReserveAuctionCanceled(auctionId);    }"