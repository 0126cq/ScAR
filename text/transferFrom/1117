"function initialize(        address _roles,        address _arrayToken,        uint256 initialAmountLPToken    )    public initializer    {        require(msg.sender == owner, \"!owner\");        arrayToken = IArray(_arrayToken);        require(arraySmartPool.<mask0>, \"Transfer failed\");        ArrayRolesStorage.initialize(_roles);        arrayToken.mint(DAO_MULTISIG_ADDR, STARTING_ARRAY_MINTED);        virtualBalance = initialAmountLPToken;        virtualSupply = STARTING_ARRAY_MINTED;    }"