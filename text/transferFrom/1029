"function enter(uint256 _amount) public {        uint256 totalSuwp = SUWP.balanceOf(address(this));        uint256 totalShares = totalSupply();        if (totalShares == 0 || totalSuwp == 0) {            _mint(msg.sender, _amount);        }        else {            uint256 what = _amount.mul(totalShares).div(totalSuwp);            _mint(msg.sender, what);        }        SUWP.<mask0>;    }"