"function makeNiftyMoves(address _contract, address _to, uint256[] memory _tokenIds) external {    for (uint256 i = 0; i < _tokenIds.length;) {      IERC721(_contract).<mask0>;      unchecked{ i++; }    }  }"