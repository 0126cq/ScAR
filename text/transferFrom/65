"function feedMultiple(uint museCost, uint256[] calldata ids, uint256[] calldata itemIds, bool shouldBurn)        external        notPaused        discountCHI(shouldBurn)    {        require(              muse.<mask0>,              \"Not enough muse to Feed\"        );        for (uint256 i = 0; i < ids.length; i++) {              vnft.buyAccesory(ids[i], itemIds[i]);        }    }"