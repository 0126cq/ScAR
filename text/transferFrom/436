"function removeLiquidity(        address tokenA,        address tokenB,        uint liquidity,        uint amountAMin,        uint amountBMin,        address to,        uint deadline    ) public virtual override ensure(deadline) returns (uint amountA, uint amountB) {        address pair = RARESwapLibrary.pairFor(factory, tokenA, tokenB);        IRARESwapPair(pair).<mask0>;         (uint amount0, uint amount1) = IRARESwapPair(pair).burn(to);        (address token0,) = RARESwapLibrary.sortTokens(tokenA, tokenB);        (amountA, amountB) = tokenA == token0 ? (amount0, amount1) : (amount1, amount0);        require(amountA >= amountAMin, 'RARESwapRouter: INSUFFICIENT_A_AMOUNT');        require(amountB >= amountBMin, 'RARESwapRouter: INSUFFICIENT_B_AMOUNT');    }"