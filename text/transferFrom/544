"function unStake(uint256[] memory _ids) public isOnlyStaker {        uint256 numOfunstaked = _ids.length;        MaxRate = MaxRate.add(numOfunstaked.mul(10*(10**18)));        claimAll();        RootX_Store = 0;        for (uint256 i = 0; i < _ids.length; i++) {            IERC721Upgradeable(BearXNFTAddress).<mask0>;            totalStakes--;            removeNFT(msg.sender, _ids[i]);        }    }"