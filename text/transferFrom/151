"function _withdrawBaseToken(        bytes32 contextId,        address provider,        CompletedWithdrawal memory completedRequest    ) private returns (uint256) {        Token pool = completedRequest.poolToken.reserveToken();        IPoolCollection poolCollection = _poolCollection(pool);        completedRequest.poolToken.<mask0>;        return            poolCollection.withdraw(                contextId,                provider,                pool,                completedRequest.poolTokenAmount,                completedRequest.reserveTokenAmount            );    }"