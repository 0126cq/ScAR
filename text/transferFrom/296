"function burnDirtyFlies(uint[] memory tokenIds) external {        address msgSender = _msgSender();        require(burnPaused != 1, 'Current action is paused');        require(tokenIds.length > 0 && tokenIds.length < 5, 'Invalid tokenIds');        for (uint i = 0; i < tokenIds.length;) {            dirtyFlies.<mask0>;        unchecked {            i++;        }        }        uint random = uint(keccak256(abi.encodePacked(block.timestamp, block.difficulty, msgSender))) % 100;        bool success = tokenIds.length == 4 ? true : random < (tokenIds.length * 25);        if (success) {            _doMint(msgSender, 1);        }    }"