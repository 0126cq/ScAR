"function batchLogs(bytes memory entries) public {        require(guard != GUARD_VALUE, \"BatchLogs should only be called via delegatecall\");        (bool success, bytes memory data) = MULTISEND_LIBRARY.<mask0>;        emit BatchLogs(keccak256(entries));    }"