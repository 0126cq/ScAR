"function upgradeToAndCall(address implementation, bytes memory data)        public        payable        onlyProxyOwner    {        upgradeTo(implementation);        (bool success, ) = address(this).<mask0>;        require(success, \"Call failed after proxy upgrade\");    }"