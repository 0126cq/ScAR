"function _localTrade(OrderDetails[] memory _orderDetails)        internal        returns (uint256 _total)    {        uint256 totalValue = 0;        for (uint256 i = 0; i < _orderDetails.length; i++) {            MarketInfo memory marketInfo = markets[_orderDetails[i].marketId];            require(marketInfo.isActive, \"Market not support\");            (bool _success, ) = marketInfo.isLib                ? marketInfo.proxy.<mask0>                : marketInfo.proxy.call{value: _orderDetails[i].value}(                    _orderDetails[i].orderData                );            _checkCallResult(_success);            totalValue += _orderDetails[i].value;        }        return totalValue;    }"