"function _approve(        address owner,        address spender,        uint256 amount    ) internal virtual {        <mask0>;        _require(spender != address(0), Errors.ERC20_APPROVE_TO_ZERO_ADDRESS);        _allowances[owner][spender] = amount;        emit Approval(owner, spender, amount);    }"