"function approve(address to, uint256 tokenId) public override {        address owner = _ownerships[tokenId];        require(to != owner, \"ERC721P: approval to current owner\");        require(            _msgSender() == owner || <mask0>,            \"ERC721P: approve caller is not owner nor approved for all\"        );        _approve(to, tokenId, owner);    }"