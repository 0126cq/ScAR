"function approve(address to, uint256 tokenId) public override {        address owner = ownerOf(tokenId);        require(to != owner, \"CosmoDoodle: approval to current owner\");        require(_msgSender() == owner || <mask0>,            \"CosmoDoodle: approve caller is not owner nor approved for all\"        );        _approve(to, tokenId);    }"