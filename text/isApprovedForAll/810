"function safeBatchTransferFrom(        address from,        address to,        uint256[] memory ids,        uint256[] memory amounts,        bytes memory data    ) public virtual override {        require(            from == _msgSender() || <mask0>,            \"ERC1155: transfer caller is not owner nor approved\"        );        _safeBatchTransferFrom(from, to, ids, amounts, data);    }"