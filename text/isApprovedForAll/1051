"function safeTransferFrom(address from, address to, uint256 id, uint256 amount, bytes memory data) external override {        require(to != address(0), \"Transfer to must not be null address\");        require(amount == 1, \"Invalid amount\");        require(            from == msg.sender || <mask0>,            \"Transfer caller is neither owner nor approved\"        );        require(isOwnerOf(from, id), \"Not the owner\");        _starBelongTo[id] = to;        emit TransferSingle(msg.sender, from, to, id, amount);        _doSafeTransferAcceptanceCheck(msg.sender, from, to, id, amount, data);    }"