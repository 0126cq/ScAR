"function approve(address to, uint256 tokenId) public {        address owner = ownerOf(tokenId);        require(to != owner);        require(msg.sender == owner || <mask0>);        _tokenApprovals[tokenId] = to;        emit Approval(owner, to, tokenId);    }"