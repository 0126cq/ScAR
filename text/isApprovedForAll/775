"function approve (      address _approved,      uint256 _id    ) external override {        address owner = _ownershipOf(_id);        if (_msgSender() != owner && !<mask0>) {            revert (\"ApprovalCallerNotOwnerNorApproved\");        }        _approve(owner, _approved, _id);    }"