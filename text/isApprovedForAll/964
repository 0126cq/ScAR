"function approve(address _to, uint256 _tokenId) public {        address owner = ownerOf(_tokenId);        require(_to != owner);        require(msg.sender == owner || <mask0>);        tokenApprovals[_tokenId] = _to;        emit Approval(owner, _to, _tokenId);    }"