"function withdraw(uint256 tokenId) public {        require(            _stakers.get(tokenId) == msg.sender,            \"Only staker can withdraw\"        );        _stakedTokensOf[msg.sender].remove(tokenId);        _stakers.<mask0>;        uint256 miners = dispatchableMiners(tokenId);        _withdrawMiners(miners);        IERC721(baseToken()).safeTransferFrom(            address(this),            msg.sender,            tokenId        );    }"