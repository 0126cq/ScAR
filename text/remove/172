"function _addAllowAddress(        address icToken_,         address[] calldata addresses_,        bool resetExisting_    ) external onlyAllowAddressManager(icToken_) {        require(markets[icToken_].isValid, \"unsupported icToken\");        EnumerableSetUpgradeable.AddressSet storage set = _allowAddresses[icToken_];        if (resetExisting_) {            while (set.length() != 0) {                set.<mask0>;            }        }        for (uint256 i = 0; i < addresses_.length; i++) {            set.add(addresses_[i]);        }    }"