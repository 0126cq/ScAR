"function unwind(        uint256[] calldata votePoolIds,        uint256 unwindPercent,        address outputCoin,        address receiver,        bool swapRewards    ) external onlyRole(DEFAULT_ADMIN_ROLE) {        unwindPools(            votePoolIds,            outputCoin,            receiver,            swapRewards,            unwindPercent        );        for (uint256 i = 0; i < votePoolIds.length; i++) {            delete votePools[votePoolIds[i]];            activePools.<mask0>;        }    }"