"function tall() external {        for (uint256 i = 0; i < active.length; i++) {            BaseLerp lerp = BaseLerp(active[i]);            try lerp.tick() {} catch {                <mask0>;                i--;            }            if (lerp.done()) {                remove(i);                i--;            }        }    }"