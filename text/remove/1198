"function updateUniRouters(ISwapRouterInternal[] calldata routers, bool isSupported)        external        onlyAdmin    {        for (uint256 i = 0; i < routers.length; i++) {            if (isSupported) {                uniRouters.add(address(routers[i]));            } else {                uniRouters.<mask0>;            }        }        emit UpdatedUniRouters(routers, isSupported);    }"