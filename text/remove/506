"function retrieve() public nonReentrant {        address sender = _msgSender();        UserInfo storage user = userInfo[sender];        require(user.alpacaID != 0, \"AlpacaFarm: you do not have any alpaca\");        if (user.amount > 0) {            updatePool();            uint256 pending = user                .amount                .mul(user.alpacaEnergy)                .mul(poolInfo.accAlpaPerShare)                .div(SAFE_MULTIPLIER)                .sub(user.rewardDebt);            if (pending > 0) {                _safeAlpaTransfer(msg.sender, pending);            }            user.rewardDebt = user                .amount                .mul(EMPTY_ALPACA_ENERGY)                .mul(poolInfo.accAlpaPerShare)                .div(SAFE_MULTIPLIER);            poolInfo.accShare = poolInfo.accShare.sub(                (user.alpacaEnergy.sub(1)).mul(user.amount)            );        }        uint256 prevAlpacaID = user.alpacaID;        user.alpacaID = 0;        user.alpacaEnergy = 0;        alpacaOriginalOwner.<mask0>;        cryptoAlpaca.safeTransferFrom(            address(this),            msg.sender,            prevAlpacaID,            1,            \"\"        );    }"