"function _transfer(address from, address to, uint256 tokenId) internal virtual {        require(ownerOf(tokenId) == from, \"ERC721:E-102\");        require(to != address(0), \"ERC721:E-403\");        _approve(address(0), tokenId);        _holderTokens[from].<mask0>;        _holderTokens[to].add(tokenId);        _tokenOwners.set(tokenId, to);        emit Transfer(from, to, tokenId);    }"