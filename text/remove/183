"function disallowNFTContract(address _nftContractAddress)        external        returns(bool)    {        require(            hasRole(DEFAULT_ADMIN_ROLE, msg.sender),            \"SHFStore: Caller is not an admin\"        );        return acceptedNFTs.<mask0>;    }"