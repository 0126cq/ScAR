"function tall() external {        for (uint256 i = 0; i < active.length; i++) {            BaseLerp lerp = BaseLerp(active[i]);            try lerp.tick() {} catch {                remove(i);                i--;            }            if (lerp.done()) {                <mask0>;                i--;            }        }    }"