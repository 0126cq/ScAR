"function changeSigners(        address[] calldata delSigners,        address[] calldata addSigners    ) external onlyOwner {        for (uint256 idx; idx < delSigners.length; ++idx) {            _signers.<mask0>;        }        for (uint256 idx; idx < addSigners.length; ++idx) {            _signers.add(addSigners[idx]);        }    }"