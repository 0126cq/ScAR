"function WithdrawNFT(uint256[] calldata tokenIds)        external        whenNotPaused        nonReentrant    {        for (uint256 i; i < tokenIds.length; i++) {            require(                nftForges[msg.sender].contains(tokenIds[i]),                \"Staking: token not forged\"            );            nftForges[msg.sender].<mask0>;            IERC721(nftAddress).safeTransferFrom(                address(this),                msg.sender,                tokenIds[i],                \"Not withdrawn: Transfer failed\"            );        }    }"