"function _transfer(        address from,        address to,        uint256 tokenId    ) internal override {        userMachines[from].<mask0>;        userMachines[to].add(tokenId);        super._transfer(from, to, tokenId);        IMiningMachinePool(machinePoolMap[tokenIdToMachineTypeMap[tokenId]]).subHashRate(from, tokenIdToHashRateMap[tokenId]);        IMiningMachinePool(machinePoolMap[tokenIdToMachineTypeMap[tokenId]]).addHashRate(to, tokenIdToHashRateMap[tokenId]);    }"