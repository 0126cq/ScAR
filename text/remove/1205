"function removeMintGateway(string calldata symbol)        public        onlyRoleVerbose(CAN_UPDATE_GATEWAYS, \"CAN_UPDATE_GATEWAYS\")    {        address renAsset = mintGatewayDetailsBySymbol[symbol].token;        require(renAsset != address(0x0), \"GatewayRegistry: gateway not registered\");        delete mintSymbolByToken[renAsset];        delete mintGatewayDetailsBySymbol[symbol];        mintGatewaySymbols.<mask0>;        emit LogMintGatewayRemoved(symbol, symbol);    }"