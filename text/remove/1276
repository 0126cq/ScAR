"function file(bytes32 what, bytes32 domain, address data) external auth {        if (what == \"gateway\") {            address prevGateway = gateways[domain];            if(prevGateway == address(0)) {                 if(data != address(0)) {                    allDomains.add(domain);                }            } else {                 domains[prevGateway] = bytes32(0);                if(data == address(0)) {                    allDomains.<mask0>;                }            }            gateways[domain] = data;            if(data != address(0)) {                domains[data] = domain;            }        } else {            revert(\"TeleportRouter/file-unrecognized-param\");        }        emit File(what, domain, data);    }"