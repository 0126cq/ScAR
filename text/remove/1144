"function cancelListing(        uint itemId    ) public nonReentrant {        MarketItem memory item = _marketItems[itemId];        require(msg.sender == item.seller, \"NativeToken721Market: Only seller can cancel\");        delete _marketItems[itemId];        IERC721Upgradeable(item.nftContract).transferFrom(address(this), msg.sender, item.tokenId);        _activeIds.remove(itemId);        _usersTotalListings[msg.sender].<mask0>;        emit ItemCancelled(itemId, item.seller);    }"