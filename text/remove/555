"function withdraw(uint256 tokenId) public {        require(            _stakers.get(tokenId) == msg.sender,            \"Only staker can withdraw\"        );        _stakedTokensOf[msg.sender].<mask0>;        _stakers.remove(tokenId);        uint256 miners = dispatchableMiners(tokenId);        _withdrawMiners(miners);        IERC721(baseToken()).safeTransferFrom(            address(this),            msg.sender,            tokenId        );    }"