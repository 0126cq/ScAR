"function unwindAll(        uint256 unwindPercent,        address outputCoin,        address receiver,        bool swapRewards    ) external onlyRole(DEFAULT_ADMIN_ROLE) {        unwindPools(            activePools.values(),            outputCoin,            receiver,            swapRewards,            unwindPercent        );        uint256 length = activePools.length();        for (uint256 i = 0; i < length; i++) {            uint256 id = activePools.at(0);            delete votePools[id];            activePools.<mask0>;        }    }"