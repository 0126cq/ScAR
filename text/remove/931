"function withdraw(uint256[] calldata tokenIds) external requireContractsSet whenNotPaused {        for (uint256 i; i < tokenIds.length; i++) {            require(                _deposits[msg.sender].contains(tokenIds[i]),                \"Staking: token not deposited\"            );            _deposits[msg.sender].<mask0>;            delete _tokenRewards[tokenIds[i]];            erc721Token.safeTransferFrom(                address(this),                msg.sender,                tokenIds[i],                \"\"            );        }    }"