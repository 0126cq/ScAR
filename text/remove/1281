"function cancelRegistration(address receiver) external onlyOwner returns (bool) {        require(registeredDistributions[receiver] != 0, 'Not registered');        uint256 amount = registeredDistributions[receiver];        delete registeredDistributions[receiver];        delete tokenVested[receiver];        registrations.<mask0>;        totalTokens = totalTokens.sub(amount);        emit Cancel(receiver, amount);        return true;    }"