"function _transfer(address from, address to, uint256 tokenId) internal {        require(ownerOf(tokenId) == from, \"CosmoDoodle: transfer of token that is not own\");        require(to != address(0), \"CosmoDoodle: transfer to the zero address\");        _approve(address(0), tokenId);        _holderTokens[from].<mask0>;        _holderTokens[to].add(tokenId);        _tokenOwners.set(tokenId, to);        emit Transfer(from, to, tokenId);    }"