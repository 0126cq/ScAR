"function removeActiveLockId(uint lockId) private {        activeLockIds.<mask0>;        activeLockIdsByRecipient[locks[lockId].recipient].remove(lockId);        activeLockIdsByToken[locks[lockId].token].remove(lockId);        inactiveLockIds.add(lockId);        inactiveLockIdsByRecipient[locks[lockId].recipient].add(lockId);        inactiveLockIdsByToken[locks[lockId].token].add(lockId);    }"