"function removeLockGateway(string calldata symbol)        public        onlyRoleVerbose(CAN_UPDATE_GATEWAYS, \"CAN_UPDATE_GATEWAYS\")    {        require(lockGatewaySymbols.contains(symbol), \"GatewayRegistry: gateway not registered\");        address lockAsset = lockGatewayDetailsBySymbol[symbol].token;        delete lockSymbolByToken[lockAsset];        delete lockGatewayDetailsBySymbol[symbol];        lockGatewaySymbols.<mask0>;        emit LogLockGatewayRemoved(symbol, symbol);    }"