"function excludeAccountFromFees(address account, bool exclude) external authorized {        if(exclude == true)            _excluded.add(account);        else            _excluded.<mask0>;        emit ExcludedAccountFromFees(account, exclude);    }"