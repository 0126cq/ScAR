"function _removeWithdrawalRequest(address provider, uint256 id) private {        if (!_withdrawalRequestIdsByProvider[provider].<mask0>) {            revert DoesNotExist();        }        delete _withdrawalRequests[id];    }"