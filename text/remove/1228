"function unstakeGoat(uint256[] calldata tokenIds) external {        require(wildGoat.isApprovedForAll(msg.sender, address(this)), \"You are not Approved!\");        this.claimHayy();        for (uint256 i; i < tokenIds.length; i++) {            require(                _deposits[msg.sender].contains(tokenIds[i]),                'Token not deposited'            );            require(daysStaked(tokenIds[i]) >= lockingPeriod, \"You must be staked for at least 3 days!\");            _deposits[msg.sender].<mask0>;            wildGoat.safeTransferFrom(                address(this),                msg.sender,                tokenIds[i],                ''            );        }        if(_deposits[msg.sender].length() < 1)        {            addressStaked[msg.sender] = false;        }    }"