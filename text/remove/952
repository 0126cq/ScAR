"function withdraw(uint256[] calldata tokenIds) external whenNotPaused nonReentrant() {        setBlock(tokenIds);        for (uint256 i; i < tokenIds.length; i++) {            require( _deposits[msg.sender].contains(tokenIds[i]),\"Staking: token not deposited\");            _deposits[msg.sender].<mask0>;            IERC721(stakingDestinationAddress).safeTransferFrom(address(this), msg.sender, tokenIds[i],\"\");}    }"