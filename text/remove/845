"function unstake(uint256[] calldata tokenIds) external notPaused {        require(tokenIds.length > 0, \"Unstake: amount prohibited\");        uint256 rewards;        for (uint256 i; i < tokenIds.length; i++) {            require(                stakedTokens[msg.sender].contains(tokenIds[i]),                 \"Unstake: token not staked\"            );            stakedTokens[msg.sender].<mask0>;            delete stakes[tokenIds[i]];            NFT.transferFrom(address(this), msg.sender, tokenIds[i]);            nonce--;        }        TOKEN.transfer(msg.sender, rewards);    }"