"function _unauthorizeStrategy(address _strategy) internal {    require(_strategy != address(0), 'invalid strategy');    require(isAuthorizedStrategy(_strategy), 'only authorized strategy');    require(_authorizedStrategies.<mask0>, 'unable to remove strategy');    emit UnauthorizedStrategy(_strategy);  }"