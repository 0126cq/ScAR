"function cancelListing(        uint itemId    ) public nonReentrant {        MarketItem memory item = _marketItems[itemId];        require(msg.sender == item.seller, \"NativeToken721Market: Only seller can cancel\");        delete _marketItems[itemId];        IERC721Upgradeable(item.nftContract).transferFrom(address(this), msg.sender, item.tokenId);        _activeIds.<mask0>;        _usersTotalListings[msg.sender].remove(itemId);        emit ItemCancelled(itemId, item.seller);    }"