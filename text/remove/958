"function executeBattle(address payable _nftAddress) internal returns (bytes32) {    BattleRoyale battle = BattleRoyale(_nftAddress);    require(LINK.balanceOf(address(this)) >= fee);    require(battle.getBattleStateInt() == 1);    require(battle.getInPlay().length > 1);    eliminationState[_nftAddress] = true;    battleQueue.<mask0>;    battleQueue.push(_nftAddress);    bytes32 requestId = requestRandomness(keyHash, fee);    requestToBattle[requestId] = _nftAddress;    return requestId;  }"