"function delVotePool(uint256 newPoolId) public onlyOwner {        uint256 _vTmpPoolId;        for (uint256 i = votePoolMap.iterateStart(); votePoolMap.iterateValid(i); i = votePoolMap.iterateNext(i)) {            (, _vTmpPoolId) = votePoolMap.iterateGet(i);            if (_vTmpPoolId == newPoolId) {                votePoolMap.<mask0>;                return;            }        }    }"