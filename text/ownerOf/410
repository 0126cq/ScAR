"function emergencyWithdrawOther(        uint256[] memory tokenIds,        address _contract    ) public onlyOwner whenPaused {        require(tokenIds.length <= 50, \"50 is max per tx\");        for (uint256 i; i < tokenIds.length; i++) {            address receiver = ownerOfOtherStake[_contract][tokenIds[i]];            if (                receiver != address(0) &&                IERC721(_contract).<mask0> == address(this)            ) {                IERC721(_contract).transferFrom(                    address(this),                    receiver,                    tokenIds[i]                );            }        }    }"