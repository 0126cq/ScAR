"function tokenURI(uint256 tokenId) public view virtual override returns (string memory) {        if (<mask0> == deadAddress) {            return string(abi.encodePacked(metadataBaseURI, \"quit\"));        }        uint256 playerLevel = getPlayerLevel(tokenId);        return string(abi.encodePacked(metadataBaseURI, Strings.toString(playerLevel)));    }"