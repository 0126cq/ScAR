"function approve(address to, uint256 tokenId) public {        address owner = <mask0>;        require(to != owner);        require(msg.sender == owner || isApprovedForAll(owner, msg.sender));        _tokenApprovals[tokenId] = to;        emit Approval(owner, to, tokenId);    }"