"function split(uint256 tokenId, address to) external nonReentrant {        require(_msgSender() == <mask0>, \"Caller is not the owner\");        require(to != address(0), \"To the zero address\");        uint256 metahead = FinishHead[tokenId];        require(metahead > 0, \"Token ID invalid\");        require(!TokenIds.getSeal(metahead), \"Token ID is sealed\");        _transferFromBatch(metahead, to);        delete FinishHead[tokenId];        _burn(tokenId);        emit Split(tokenId, to);    }"