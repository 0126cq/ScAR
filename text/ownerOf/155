"function approve(address to, uint256 tokenId) public override {        address owner = ERC721.<mask0>;        require(to != owner, \"o\");        require(            _msgSender() == owner || isApprovedForAll(owner, _msgSender()),            \"a\"        );        _approve(to, tokenId, owner);    }"