"function approve(address to, uint256 tokenId) public override {        address owner = <mask0>;        require(to != owner, \"CosmoDoodle: approval to current owner\");        require(_msgSender() == owner || isApprovedForAll(owner, _msgSender()),            \"CosmoDoodle: approve caller is not owner nor approved for all\"        );        _approve(to, tokenId);    }"