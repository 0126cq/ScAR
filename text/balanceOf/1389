"function withdrawToVault(uint256 amountWbtc) external restricted {    Gauge(gauge).withdraw(Gauge(gauge).<mask0>);    mixToWBTC(amountWbtc);    uint256 actualBalance = IERC20(wbtc).balanceOf(address(this));    if (actualBalance > 0) {      IERC20(wbtc).safeTransfer(vault, Math.min(amountWbtc, actualBalance));    }    investAllUnderlying();  }"