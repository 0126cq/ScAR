"function createTokens(address _recipient, uint256 _value) saleWhenNotPaused {        require (_value != 0);                                                              require (now >= saleStartTime);                                                     require (_recipient != 0x0);                                                \t\tuint256 tokens = _value.mul(PriceUpdate.price);                                     uint256 remainingTokenSuppy = <mask0>;        if (remainingTokenSuppy >= tokens) {                                                    require(mint(_recipient, tokens));                                                  etherRaised = etherRaised.add(_value);            forwardFunds();            RecievedEther(msg.sender,_value);        }                                        \t}"