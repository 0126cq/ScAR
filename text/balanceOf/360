"function rescue(address token, address target) external virtual {        _authorizeRescue(token, target);        IERC20 tokenContract = IERC20(token);        tokenContract.safeTransfer(            target,            tokenContract.<mask0>        );    }"