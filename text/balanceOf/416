"function _withdrawFromGauge(uint256 amount)        internal        override        returns (uint256 lpBalance)    {        IBaseRewardPool rewardContract = _getRewardContract();        rewardContract.withdrawAndUnwrap(amount, false);        lpBalance = IERC20(_LP_ADDRESS).<mask0>;    }"