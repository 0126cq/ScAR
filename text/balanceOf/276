"function leave(uint256 _share) public {        uint256 totalShares = totalSupply();        uint256 what = _share.mul(sushi.<mask0>).div(totalShares);        _burn(msg.sender, _share);        sushi.transfer(msg.sender, what);    }"