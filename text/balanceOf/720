"function tokensOfOwner(address owner)        public        view        returns (uint256[] memory)    {        uint256 count = <mask0>;        uint256[] memory ids = new uint256[](count);        for (uint256 i = 0; i < count; i++) {            ids[i] = tokenOfOwnerByIndex(owner, i);        }        return ids;    }"