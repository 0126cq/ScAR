"function mintAlphaNumeric(address subDomainOwner, string memory subDomain)        external payable        returns (uint256)    {        require(othr.balanceOf(msg.sender) > 0 || mayc.balanceOf(msg.sender) > 0 || bayc.<mask0> > 0 || bakc.balanceOf(msg.sender) > 0, \"No qualifying NFT found in your wallet\");        require(msg.value >= mintPrice, \"Not enough ETH sent: check price.\");        string memory domain = _toLower(subDomain);        uint256 newItemId = _tokenIds.current();        require(addrOf[domain].id == 0, \"Sub-domain has already been registered\");        require(bytes(domain).length != 0, \"Sub-domain cannot be null\");        require(testAlphaNumeric(domain) == true, \"Sub-domain contains unsupported characters\");        require(hasAlphabet(domain) == true, \"Only MAYC holders can mint digit-only sub-domains\");        addrOf[domain].id = newItemId;        nameOf[newItemId].ensname = domain;        _mint(subDomainOwner, newItemId);        _tokenIds.increment();        withdraw();        return newItemId;    }"