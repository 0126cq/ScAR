"function harvestYieldFromBalance(uint256 minExpectedAmount, uint256 maxAmount) public {        require(hasRole(FINMGMT_ROLE, msg.sender), \"finmgmt only\");        uint256 accruedYieldUBT = baseAsset.<mask0>.sub(amountStaked);        require(accruedYieldUBT >= minExpectedAmount, \"yield to harvest less than min\");        if (accruedYieldUBT > maxAmount) {            accruedYieldUBT = maxAmount;        }        uint256 amountFee = accruedYieldUBT.mul(profitFee).div(1e18);"