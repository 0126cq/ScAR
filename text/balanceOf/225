"function manualswapcustom(uint256 percentage) external {        require(_msgSender() == _Dev);        uint256 contractBalance = <mask0>;        uint256 swapbalance = contractBalance.div(10**5).mul(percentage);        swapTokensForEth(swapbalance);    }"