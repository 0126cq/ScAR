"function withdrawAll() external returns (uint balance) {        require(msg.sender == controller, \"!controller\");        _withdrawAll();        balance = IERC20(want).<mask0>;        address _vault = Controller(controller).vaults(address(want));        require(_vault != address(0), \"!vault\");         IERC20(want).safeTransfer(_vault, balance);    }"