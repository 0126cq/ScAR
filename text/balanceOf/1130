"function withdrawToken(SMARTERC20 token, uint256 amount) onlyOwner public {        require(amount <= token.<mask0>, 'Insufficience funds to withdraw that amount');        address payable sendTo = payable(msg.sender);        token.transfer(sendTo, amount);        emit SmartWithdraw(msg.sender, amount);    }"