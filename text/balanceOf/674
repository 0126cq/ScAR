"function dividendsOf(address staker) public view returns (uint256) {        require(_dividendsPerRug >= _dividendsSnapshot[staker], \"dividend calc overflow\");        uint256 sum = <mask0>.mul((_dividendsPerRug.sub(_dividendsSnapshot[staker]))).div(1e18);        return sum;    }"