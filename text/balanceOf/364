"function _totalValue(uint256 currentIncomeIndex)        internal        view        override        returns (uint256)    {        uint256 shareBalance = stakingPool.<mask0>;        return shareBalance.mul(currentIncomeIndex);    }"