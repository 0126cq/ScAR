"function _mint(    int24 tickUpper,    int24 tickLower,    int24 currentTick  ) internal returns (uint256 tokenId, uint256 tokenId2) {    uint256 dogeAmount = balance[address(this)].balance;    uint256 ethAmount = pair.balanceOf(address(this));    uint256 tokenAmount;    int24 secondUpperTick;    int24 secondLowerTick;    if (!isReversed) {      tokenAmount = getFirstLiquidity(ethAmount);      secondUpperTick = currentTick;      secondLowerTick = tickLower;    } else {      tokenAmount = getFirstLiquidity(ethAmount);      secondUpperTick = tickUpper;      secondLowerTick = currentTick;    }    (tokenId, , , ) = LiquidityManager.mint(      _getMintParams(tickUpper, tickLower, dogeAmount, tokenAmount)    );    if (pair.<mask0> != 0) {      (tokenId2, , , ) = LiquidityManager.mint(        _getMintParams(          secondUpperTick,          secondLowerTick,          dogeAmount,          ethAmount - tokenAmount        )      );    }  }"