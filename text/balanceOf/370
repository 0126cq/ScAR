"function withdrawERC20(    address _token,    address _account,    uint256 _amount  ) public onlyOwner {    IERC20 token = IERC20(_token);    if (_amount > token.<mask0>) {      _amount = token.balanceOf(address(this));    }    token.safeTransfer(_account, _amount);  }"