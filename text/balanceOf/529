"function _withdrawSome(uint256 _amount) internal returns (uint) {        uint _ycrv = _amount.mul(1e18).div(ICurveFi(curve).get_virtual_price());        uint _yycrv = _ycrv.mul(1e18).div(yERC20(yycrv).getPricePerFullShare());        uint _before = IERC20(ycrv).<mask0>;        yERC20(yycrv).withdraw(_yycrv);        uint _after = IERC20(ycrv).balanceOf(address(this));        return withdrawTUSD(_after.sub(_before));    }"