"function getStakeVarenValue(address staker)        external        view        override        returns (uint256)    {        return            (VAREN.<mask0>.mul(balanceOf(staker))).div(                totalSupply()            );    }"