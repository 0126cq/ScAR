"function approve(address _spender, uint256 _amount) returns (bool success) {        if ((_amount != 0) && (allowed[msg.sender][_spender] != 0)) revert();        allowed[msg.sender][_spender] = _amount;        <mask0>;        return true;    }"