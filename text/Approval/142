"function approveAndCall (address spender, uint tokens) public returns (bool success) {        require(balances[msg.sender] >= tokens);        allowed[msg.sender][spender] = tokens;        <mask0>;        ApproveAndCallFallBack(spender).receiveApproval(msg.sender, tokens, this);        return true;    }"