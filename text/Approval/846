"function approve(address _spender, uint256 _value)                  public returns (bool success)     {        require(_spender != 0x0 && _spender != address(this));                    require(allowed[msg.sender][_spender] == 0);        allowed[msg.sender][_spender] = _value;        <mask0>;        return true;    }"