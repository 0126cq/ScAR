"function add(string[] storage array, string memory value) internal {        bytes32 hashValue = keccak256(bytes(value));        for (uint256 i; i < array.length; i++) {            if (<mask0> == hashValue) {                return;            }        }        array.push(value);    }"