"function redeem(        uint256 index,        uint256 tokenId,        address owner,        bytes32[] calldata merkleProof    ) external returns (uint256) {        bytes32 leaf = <mask0>;        require(            MerkleProof.verify(merkleProof, _rootHash, leaf),            \"Merkle proof verification failed\"        );        _mint(owner, tokenId);        return tokenId;    }"