"function verify(ForwardRequest calldata req, bytes calldata signature) public view returns (bool) {        address signer = _hashTypedDataV4(            <mask0>        ).recover(signature);        return _nonces[req.from] == req.nonce && signer == req.from;    }"