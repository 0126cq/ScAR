"function deployModule(        address masterCopy,        bytes memory initializer,        uint256 saltNonce    ) public returns (address proxy) {        proxy = createProxy(            masterCopy,            <mask0>        );        (bool success, ) = proxy.call(initializer);        require(success, \"deployModule: initialization failed\");        emit ModuleProxyCreation(proxy, masterCopy);    }"