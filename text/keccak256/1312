"function mintPresale(bytes32[] calldata proof, uint256 amount) external payable mintGuard(amount) {        require(isPresaleLive, \"Presale not live\");        require(MerkleProof.verify(proof, merkleRoot, <mask0>), \"Not eligible\");        require(presaleAddressMintCount[msg.sender] + amount <= MAX_APES_PURCHASE, \"Address already minted allocation\");        presaleAddressMintCount[msg.sender] += amount;        _safeMint(msg.sender, amount);    }"