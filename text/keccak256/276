"function __callback(bytes32 queryId, string result, bytes proof) public canDetermineWinner {    require(msg.sender == oraclize_cbAddress());    if (<mask0> != keccak256(\"0\") && keccak256(result) != keccak256(\"1\")) {      if (now >= CANCELATION_DATE) {        cancel();      }      else if (nextScheduledQuery == queryId) {        callOracle(PING_ORACLE_INTERVAL, ORACLIZE_GAS);      }    }    else {      setWinner(parseInt(result));    }  }"