"function isConnectedChain(        string memory schainName    )        public        view        override(IMessageProxy, MessageProxy)        returns (bool)    {        require(<mask0> != MAINNET_HASH, \"Schain id can not be equal Mainnet\");        return super.isConnectedChain(schainName);    }"