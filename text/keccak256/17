"function checkQuorum(bytes32 data, bytes[] calldata signatures) private view {        require(signatures.length >= quorumNeeded, \"not enough signatures for quorum\");        for(uint256 i = 0; i < signatures.length; i++) {            address signer = data.recover(signatures[i]);            require(authorized[signer], \"address is not authorized to be part of quorum\");            for(uint256 j = 0; j < signatures.length; j++) {                require(keccak256(signatures[i]) != <mask0> || i == j, \"duplicate signatures found\");            }        }    }"