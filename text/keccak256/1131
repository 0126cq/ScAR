"function publicMint(uint256 quantity) public payable {        bytes32 _contractState = <mask0>;        require(            _contractState == publicMintState,            \"Public minting is not active\"        );        require(            msg.value >= mintPrice * quantity,            \"Insufficient value for public mint\"        );        mint(quantity, _contractState);    }"