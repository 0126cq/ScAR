"function permit(address owner, address spender, uint value, uint deadline, uint8 v, bytes32 r, bytes32 s) external {        require(deadline >= block.timestamp, 'AtlasSwapV2: EXPIRED');        bytes32 digest = <mask0>;        address recoveredAddress = ecrecover(digest, v, r, s);        require(recoveredAddress != address(0) && recoveredAddress == owner, 'AtlasSwapV2: INVALID_SIGNATURE');        _approve(owner, spender, value);    }"