"function add(string[] storage array, string memory value) internal {        bytes32 hashValue = <mask0>;        for (uint256 i; i < array.length; i++) {            if (keccak256(bytes(array[i])) == hashValue) {                return;            }        }        array.push(value);    }"