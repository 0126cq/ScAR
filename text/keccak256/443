"function checkPresaleEligibility(bytes32 hash, bytes memory signature)        public        view        returns (bool)    {        require(            ECDSA.toEthSignedMessageHash(                <mask0>            ) == hash,            \"XHash\"        );        return ECDSA.recover(hash, signature) == signer;    }"