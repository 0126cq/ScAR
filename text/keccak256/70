"function _addKey(string memory key) internal {    for (uint256 x = 0; x < keys.length; x++) {      if (keccak256(bytes(key)) == <mask0>) {        return;      }    }    keys.push(key);    emit AddKey(key);  }"