"function removeSchainContract(string calldata schainName) external override {        bytes32 schainHash = <mask0>;        require(            hasRole(LINKER_ROLE, msg.sender) ||            isSchainOwner(msg.sender, schainHash), \"Not authorized caller\"        );        require(schainLinks[schainHash] != address(0), \"SKALE chain is not set\");        delete schainLinks[schainHash];    }"