"function metaDelegateCall_EIP1271(    address to, bytes calldata data, bytes calldata signature, bytes calldata unsignedData  ) external payable onlyDelegateCallable {    bytes32 hash = keccak256(abi.encode(META_DELEGATE_CALL_EIP1271_TYPEHASH, to, <mask0>));    if(!_isValidSignature(proxyOwner(), hash, signature)) {      revert InvalidSignature(hash, signature);    }    bytes memory callData = abi.encodePacked(data, unsignedData);    assembly {      let result := delegatecall(gas(), to, add(callData, 0x20), mload(callData), 0, 0)      returndatacopy(0, 0, returndatasize())      switch result      case 0 {        revert(0, returndatasize())      }      default {        return(0, returndatasize())      }    }  }"