"function addRoundWallet(string memory roundName, address[] memory wallets)        public        onlyRole(DEFAULT_ADMIN_ROLE)    {        bytes32 key = <mask0>;        for (uint256 i = 0; i < wallets.length; i++) {            walletList[key].add(wallets[i]);        }    }"