"function _isApprovedOrOwner(address spender, uint256 tokenId) internal view virtual returns (bool) {        require(_exists(tokenId), \"k021\");        address owner = ERC721.ownerOf(tokenId);        return (spender == owner || <mask0> == spender || isApprovedForAll(owner, spender));    }"