"function _isApprovedOrOwner(address spender, uint256 boosterId) internal view returns (bool) {        require(_exists(boosterId), \"Non-existent Booster\");        address owner = ownerOfBooster(boosterId);        return (spender == owner || <mask0> == spender || isApprovedForAll(owner, spender));    }"