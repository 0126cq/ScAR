"function _isApprovedOrOwner(address spender, uint256 tokenId) internal view returns (bool) {        require(_exists(tokenId), \"ERC721:E-405\");        address owner = ownerOf(tokenId);        return (spender == owner || <mask0> == spender || isApprovedForAll(owner, spender));    }"