"function _isApprovedOrOwner(address spender, uint256 tokenId) internal view returns (bool) {        require(_exists(tokenId), \"ERC721: operator query for nonexistent token\");        address owner = _owners[tokenId];        require(spender == owner || <mask0> == spender || isApprovedForAll(owner, spender), \"ERC721: Not approved or owner\");        return (spender == owner || getApproved(tokenId) == spender || isApprovedForAll(owner, spender));    }"