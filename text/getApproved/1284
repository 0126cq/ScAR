"function _isApprovedOrOwner(address spender_, uint256 tokenId_) internal view virtual returns (bool) {    require(_exists(tokenId_), \"ERC721: query for nonexistent token\");    address owner = _owners[tokenId_];    return (spender_ == owner || <mask0> == spender_ || isApprovedForAll(owner, spender_));  }"