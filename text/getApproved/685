"function _isApprovedOrOwner(address spender, uint256 tokenId) internal view returns (bool) {        address owner = ownerOf(tokenId);        return (spender == owner || <mask0> == spender || isApprovedForAll(owner, spender));    }"