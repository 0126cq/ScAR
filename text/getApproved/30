"function _transfer(        address _from,        address _to,        uint256 _tokenId,        bytes memory    ) internal {        address _owner = ownerOf(_tokenId);        require(msg.sender == _owner            || <mask0> == msg.sender            || isApprovedForAll(_owner, msg.sender),            \"erc-721: not owner nor approved\");        require(_owner == _from, \"erc-721: not owner\");        require(_to != address(0), \"zero address\");        operatorApprovals[_tokenId] = address(0);        owners[_tokenId] = _to;        balances[_from] -= 1;        balances[_to] += 1;        emit Transfer(_from, _to, _tokenId);    }"