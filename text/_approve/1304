"function _transfer(    address from,    address to,    uint256 tokenId  ) internal virtual {    unchecked {      require(        ownerOf(tokenId) == from,        \"ERC721A: transfer of token that is not own\"      );      require(to != address(0), \"ERC721A: transfer to the zero address\");      _beforeTokenTransfer(from, to, tokenId);      <mask0>;      if(_owners[tokenId] == address(0)) {        _owners[tokenId] = to;      } else {        _owners[tokenId] = to;        if(_owners[tokenId + 1] == address(0)) {          _owners[tokenId + 1] = from;        }      }      emit Transfer(from, to, tokenId);    }  }"