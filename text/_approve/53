"function approve(address to, uint256 tokenId) public override {        address owner = _ownerships[tokenId];        require(to != owner, \"ERC721P: approval to current owner\");        require(            _msgSender() == owner || isApprovedForAll(owner, _msgSender()),            \"ERC721P: approve caller is not owner nor approved for all\"        );        <mask0>;    }"