"function addLiquidity(uint256 tokenAmount, uint256 ethAmount) private {        <mask0>;        uniSwapRouter.addLiquidityETH{value: ethAmount}(            address(this),            tokenAmount,            0,             0,             _operator,            block.timestamp        );    }"