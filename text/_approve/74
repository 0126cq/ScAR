"function burnFrom(address account, uint256 amount) public virtual {        uint256 currentAllowance = allowance(account, _msgSender());        require(currentAllowance >= amount, \"ERC20: burn amount exceeds allowance\");        <mask0>;        _burn(account, amount);    }"