"function approve(address to, uint256 tokenId) public override {        address owner = ERC721.ownerOf(tokenId);        require(to != owner, \"o\");        require(            _msgSender() == owner || isApprovedForAll(owner, _msgSender()),            \"a\"        );        <mask0>;    }"