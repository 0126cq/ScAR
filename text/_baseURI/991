"function tokenURI(uint256 tokenId) public view virtual override(ERC721) returns (string memory) {        string memory baseURI = <mask0>;        if (_ipfs) {            string memory type_ = tokenType(tokenId);            return bytes(baseURI).length > 0 ? string(abi.encodePacked(baseURI, tokenId.toString(), \"/\", type_)) : \"\";        }        uint256 level_ = level(tokenId);        return bytes(baseURI).length > 0 ? string(abi.encodePacked(baseURI, tokenId.toString(), \"/\", level_.toString())) : \"\";    }"