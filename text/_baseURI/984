"function tokenURI(uint256 tokenId) public view virtual override returns (string memory){        require(_exists(tokenId),\"ERC721Metadata: URI query for nonexistent token\");        if(revealed == false) {            if(idToTypes[tokenId] == 0) {                return notRevealedUriC;            }            if(idToTypes[tokenId] == 1) {                return notRevealedUriB;            }            if(idToTypes[tokenId] == 2) {                return notRevealedUriA;            }            return notRevealedUriC;        }        string memory currentBaseURI = <mask0>;        return bytes(currentBaseURI).length > 0            ? string(abi.encodePacked(currentBaseURI, tokenId.toString(), \".json\"))            : \"\";    }"