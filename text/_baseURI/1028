"function tokenURI(uint256 _tokenId)        public        view        override        returns (string memory)    {        require(_exists(_tokenId), \"ERC721Metadata: URI query for nonexistent token\");        string memory base = <mask0>;        if (!revealed) {            return string(abi.encodePacked(base, _tokenId.toString()));         }        return string(abi.encodePacked(base, metadataOf(_tokenId)));    }"