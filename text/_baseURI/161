"function tokenURI(uint _nftId) public view override(ERC721) returns (string memory) {        require(_exists(_nftId), \"This NFT does not exist.\");        if(revealed == false) {            return               bytes(notRevealedURI).length > 0               ? string(abi.encodePacked(notRevealedURI, _nftId.toString(), baseExtension))              : \"\";          }        string memory currentBaseURI = <mask0>;        return             bytes(currentBaseURI).length > 0             ? string(abi.encodePacked(currentBaseURI, _nftId.toString(), baseExtension))            : \"\";    }"