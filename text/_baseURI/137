"function tokenURI(uint256 tokenId)        public        view        override        returns (string memory)    {        require(            _exists(tokenId),            \"ERC721AURIStorage: the tokenId does not exist\"        );        string memory _tokenURI = _tokenURIs[tokenId];        string memory base = <mask0>;        if (bytes(base).length == 0) {            return _tokenURI;        }        return string(string(abi.encodePacked(base, Strings.toString(tokenId))));    }"