"function getUnderlying(address cToken) internal view returns (address) {        (bool success, bytes memory data) =            cToken.<mask0>;        if (success) {            return abi.decode(data, (address));        }        return AppStorageLib.store().assetAddresses[\"WETH\"];    }"