"function call (Call[] memory requests) external view returns (bytes[] memory) {        bytes[] memory results = new bytes[](requests.length);        for (uint i = 0; i < requests.length; i++) {            Call memory request = requests[i];            (bool success, bytes memory result) = request.contractAddress.<mask0>;            require(success);            results[i] = result;        }        return results;    }"