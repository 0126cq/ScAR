"function callBalanceOf(AssetCache memory assetCache, address account) internal view FREEMEM returns (uint) {        (bool success, bytes memory data) = assetCache.underlying.<mask0>;        if (!success || data.length < 32) return 0;        return abi.decode(data, (uint256));    }"