"function getProxyAdmin(TransparentUpgradeableProxy proxy) public view virtual returns (address) {        (bool success, bytes memory returndata) = address(proxy).<mask0>;        require(success);        return abi.decode(returndata, (address));    }"