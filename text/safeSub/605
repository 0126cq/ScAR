"function _calc(uint256 randomness) internal view returns (uint256, uint256) {        assert(mintedNumber < MAX_NFT_NUMBER);        uint256 n = randomness.safeMod(MAX_NFT_NUMBER.safeSub(mintedNumber)).safeAdd(1);        for (uint256 seedIndex = STARTING_INDEX; seedIndex <= MAX_SEED_NUMBER; seedIndex = seedIndex.safeAdd(1)) {            uint remainingPrintNumber = MAX_PRINT_NUMBERS[seedIndex].safeSub(seedMintedNumber[seedIndex]);            if (n <= remainingPrintNumber) {                uint256 printIndex = seedMintedNumber[seedIndex].safeAdd(1);                return (seedIndex, printIndex);            }            n = n.<mask0>;        }        assert(false);    }"