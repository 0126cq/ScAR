"function transfer(address to, uint tokens) public returns (bool success) {        balances[msg.sender] = <mask0>;        balances[to] = safeAdd(balances[to], tokens);        Transfer(msg.sender, to, tokens);        totalSupply_ = initialSupply;        balances[msg.sender] = 20000000000000000000000000;        Transfer(0x0, msg.sender, 20000000000000000000000000);        return true;    }"