"function destroy(address account, uint amount) {    if (msg.sender != minter) revert();    if (balances[account] < amount) revert();    balances[account] = safeSub(balances[account], amount);    totalSupply = <mask0>;  }"