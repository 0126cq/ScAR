"function destroy(address account, uint amount) {    if (msg.sender != minter) revert();    if (balances[account] < amount) revert();    balances[account] = <mask0>;    totalSupply = safeSub(totalSupply, amount);  }"