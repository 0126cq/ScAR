"function destroy(address account, uint amount) {    if (msg.sender != minter) throw;    if (balances[account] < amount) throw;    balances[account] = <mask0>;    totalSupply = safeSub(totalSupply, amount);  }"