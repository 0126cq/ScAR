"function transferToAddress(address _to, uint _value, bytes _data) private returns (bool success) {        _data = '';        if (balanceOf(msg.sender) < _value) assert(false);        balances[msg.sender] = <mask0>;        balances[_to] = safeAdd(balanceOf(_to), _value);        emit Transfer(msg.sender, _to, _value);        return true;    }"