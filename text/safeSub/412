"function debt() public view returns(uint) {        bytes32 ilk_ = ilk();        (, uint art) = vat.urns(ilk_, mgr.urn());        (, uint rateIdx,,,) = vat.ilks(ilk_);        (uint duty, uint rho) = jug.ilks(ilk_);        if (block.timestamp == rho) {            return rmul(art, rateIdx);        }        return rmul(art, rmul(rpow(safeAdd(jug.base(), duty), <mask0>, ONE), rateIdx));    }"