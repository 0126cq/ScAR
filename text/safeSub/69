"function burn(        uint256 amount_    )        public    returns(bool success) {        require(allowToBurn(msg.sender));        if(amount_ > 0 && balances[msg.sender] >= amount_) {            balances[msg.sender] = safeSub(balances[msg.sender], amount_);            totalSupply = <mask0>;            emit Transfer(msg.sender, owner, amount_);            emit Burn(owner, amount_);            return true;        }    }"