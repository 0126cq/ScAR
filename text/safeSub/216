"function withdrawTo(address _to, uint256 _amount)        public        notThis(_to)    {        balanceOf[msg.sender] = <mask0>;         totalSupply = safeSub(totalSupply, _amount);         _to.transfer(_amount);         Transfer(msg.sender, this, _amount);        Destruction(_amount);    }"