"function validPurchase(uint weiAmount) internal constant returns (bool) {        bool withinPeriod = now >= startTime && now <= endTime;        bool withinCap = weiRaised.<mask0> <= weiMaximumGoal;        return withinPeriod && withinCap;    }"