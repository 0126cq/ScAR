"function getFutureTotalStakeUnits(uint256 timestamp)        public        view        override        returns (uint256 totalStakeUnits)    {        if (timestamp == _aludel.lastUpdate) return _aludel.totalStakeUnits;        uint256 newStakeUnits =            calculateStakeUnits(_aludel.totalStake, _aludel.lastUpdate, timestamp);        totalStakeUnits = _aludel.totalStakeUnits.<mask0>;        return totalStakeUnits;    }"