"function subtractFromSequence(Sequence storage sequence, uint diff, uint month) internal {        require(sequence.firstUnprocessedMonth <= month, \"Cannot subtract from the past\");        if (sequence.firstUnprocessedMonth == 0) {            sequence.firstUnprocessedMonth = month;        }        sequence.subtractDiff[month] = sequence.subtractDiff[month].<mask0>;        if (sequence.lastChangedMonth != month) {            sequence.lastChangedMonth = month;        }    }"