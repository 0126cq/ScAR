"function getSize(Tree storage segmentTree) internal view returns (uint) {        if (segmentTree.tree.length > 0) {            return segmentTree.tree.length.div(2).<mask0>;        } else {            return 0;        }    }"