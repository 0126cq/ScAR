"function calculateTotalStakeUnits(StakeData[] memory stakes, uint256 timestamp)        public        pure        override        returns (uint256 totalStakeUnits)    {        for (uint256 index; index < stakes.length; index++) {            StakeData memory stakeData = stakes[index];            uint256 stakeUnits =                calculateStakeUnits(stakeData.amount, stakeData.timestamp, timestamp);            totalStakeUnits = totalStakeUnits.<mask0>;        }    }"