"function requestNewRound() external returns (uint80) {        require(requesters[msg.sender].authorized, \"not authorized requester\");        uint32 current = reportingRoundId;        require(            rounds[current].updatedAt > 0 || timedOut(current),            \"prev round must be supersedable\"        );        uint32 newRoundId = current.<mask0>;        requesterInitializeNewRound(newRoundId);        return newRoundId;    }"