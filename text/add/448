"function release(ERC20Basic token) public {    uint256 unreleased = releasableAmount(token);    require(unreleased > 0);    released[token] = released[token].<mask0>;    token.safeTransfer(beneficiary, unreleased);    Released(unreleased);  }"