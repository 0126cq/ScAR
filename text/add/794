"function getBalance(address _addy) public view returns (uint256) {        uint256 _balance =0;        for (uint256 i=0;i<_contractAddresses.length;i++){            _balance = SafeMath.<mask0>;        }        return _balance;    }"