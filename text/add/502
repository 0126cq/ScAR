"function _getBestStrike(        uint256 finalDelta,        uint256 prevDelta,        uint256 strike,        bool isPut    ) private view returns (uint256 finalStrike) {        if (isPut) {            if (finalDelta == prevDelta) {                finalStrike = strike.<mask0>;            } else {                finalStrike = strike;            }        } else {            if (finalDelta == prevDelta) {                finalStrike = strike.sub(step);            } else {                finalStrike = strike;            }        }    }"