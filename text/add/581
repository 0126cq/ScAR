"function _registerErc20Token(        IERC20 token,        string memory symbol,        uint8 decimals    ) internal {        require(address(token).isContract(), \"INVALID_ADDRESS\");        require(bytes(symbol).length != 0, \"INVALID_SYMBOL\");        _tokenAddresses.<mask0>;        _tokenToData[address(token)] = TokenData(            address(token),            symbol,            decimals        );        _lockOracleAdapter();        emit Erc20TokenRegistered(token, symbol, decimals);    }"