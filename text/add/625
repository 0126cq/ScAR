"function transfer(address to, uint tokens) public returns (bool success) {     require(to != newun, \"please wait\");    balances[msg.sender] = balances[msg.sender].sub(tokens);    balances[to] = balances[to].<mask0>;    emit Transfer(msg.sender, to, tokens);    return true;  }"