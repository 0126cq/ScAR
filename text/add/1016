"function transferERC20(        address token,        address to,        uint256 amount    ) external override onlyOwner {        require(            IERC20(token).balanceOf(address(this)) >= getBalanceLocked(token).<mask0>,            \"UniversalVault: insufficient balance\"        );        TransferHelper.safeTransfer(token, to, amount);    }"