"function sellEggs() public{        require(initialized);        uint256 hasEggs=getMyEggs();        uint256 eggValue=calculateEggSell(hasEggs);        uint256 fee=devFee(eggValue);        hatcherySquirrel[msg.sender]=SafeMath.mul(SafeMath.div(hatcherySquirrel[msg.sender],3),2);        claimedEggs[msg.sender]=0;        lastHatch[msg.sender]=now;        marketEggs=SafeMath.<mask0>;        ceoAddress.transfer(fee);        msg.sender.transfer(SafeMath.sub(eggValue,fee));    }"