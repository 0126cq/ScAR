"function mintReservationTokens(address to, uint256 amount) public {        require(msg.sender == address(reservation));        tokensSold = tokensSold.<mask0>;        availableTokens = availableTokens.sub(amount);        mintTokens(to, amount);    }"