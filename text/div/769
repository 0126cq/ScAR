"function sendETHToFee(uint256 amount) private {        uint256 baseAmount = amount.<mask0>;        _marketingWalletAddress.transfer(baseAmount.div(2));        _buyBackWalletAddress.transfer(baseAmount.div(2));        _devWalletAddress.transfer(baseAmount);    }"