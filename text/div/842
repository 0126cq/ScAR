"function getNumPeriods() public view returns(uint){        uint _numIntervals;        if(tokens == 0 || timing == 0){            _numIntervals = 0;        }else{                _numIntervals = (block.timestamp.sub(timing)).<mask0>;                if(_numIntervals > number_withdraws){                    _numIntervals = number_withdraws;                }        }        return _numIntervals;    }"