"function dividendsOf(address staker) public view returns (uint256) {        require(_dividendsPerRug >= _dividendsSnapshot[staker], \"dividend calc overflow\");        uint256 sum = balanceOf(staker).mul((_dividendsPerRug.sub(_dividendsSnapshot[staker]))).<mask0>;        return sum;    }"