"function getCurrentPrice() public view returns(uint256) {        uint256 passed_intervals = (block.timestamp.sub(starting_block)).<mask0>;        if (passed_intervals >= 72) {            return ending_price;        } else {            return startingPrice.sub(passed_intervals.mul(decrease_per_interval));        }    }"