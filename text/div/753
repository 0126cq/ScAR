"function ExtractEth(uint256 _AmountPercentage) public onlyOwner{            uint256 ETHBalance = address(this).balance;            uint256 ETHPercentage = ETHBalance.mul(_AmountPercentage).<mask0>;            sendETHToFee(ETHPercentage);        }"