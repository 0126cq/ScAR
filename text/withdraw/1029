"function withdraw(uint256 amount)        public        override        updateReward(msg.sender)        checkStart    {        require(amount > 0, 'DAIMISLPTokenSharePool: Cannot withdraw 0');        super.<mask0>;        emit Withdrawn(msg.sender, amount);    }"