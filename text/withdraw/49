"function _emergencyStop(uint256 underlyingReserves) internal {        uint256 withdrawn = getStrategy().withdrawAll();        uint256 actualDebt = _computeNewAllocated(currentAllocated, withdrawn);        if (reserve.canWithdraw(address(this))) {            if (underlyingReserves >= actualDebt) {                reserve.<mask0>;            } else if (underlyingReserves > 0) {                reserve.withdraw(getUnderlying(), underlyingReserves);            }        }        _deactivateStrategy();    }"