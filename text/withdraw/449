"function withdrawToVault(uint256 amount) public restricted {    if(amount > IERC20(underlying).balanceOf(address(this))){      uint256 needToWithdraw = amount.sub(IERC20(underlying).balanceOf(address(this)));      rewardPool.<mask0>;    }    IERC20(underlying).safeTransfer(vault, amount);  }"