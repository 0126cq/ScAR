"function _withdraw(uint _lpAmount) internal override {        LiquidityGaugeV2(GAUGE).<mask0>;        uint lpBal = IERC20(LP).balanceOf(address(this));        uint pricePerShare = StableSwapSTETH(POOL).get_virtual_price();        uint ethAmount = lpBal.mul(pricePerShare) / 1e18;        uint min = ethAmount.mul(SLIPPAGE_MAX - slippage) / SLIPPAGE_MAX;        StableSwapSTETH(POOL).remove_liquidity_one_coin(lpBal, 0, min);    }"