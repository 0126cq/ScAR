"function removeLiquidityETH(        uint _liquidity,        address _to,        uint _minAmount,        uint _deadline    ) public virtual override ensure(_deadline) onlyEOA(msg.sender) returns (uint amount) {        amount = removeLiquidity(            WETH,            _liquidity,            address(this),            _minAmount,            _deadline        );        IWETH(WETH).<mask0>;        TransferHelper.safeTransferETH(_to, amount);        return amount;    }"