"function withdraw(address operator) public {        Deposit memory deposit = deposits[operator];        address grantee = getGrantee(deposit.grantId);        (bool success, ) = address(this).call(            abi.encodeWithSignature(\"getManagedGrantee(address)\", grantee)        );        require(!success, \"Can not be called for managed grant\");        require(            msg.sender == grantee || msg.sender == operator,            \"Only grantee or operator can withdraw\"        );        <mask0>;    }"