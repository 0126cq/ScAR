"function withdraw(uint256 _stkId,uint256 amount) public override    {        uint256 stkTime = Stakemap[_stkId].initialStakeTime;        uint256 unstakeTime = stkTime + duration;    require(block.timestamp>=unstakeTime,\"Error:User not Completed the Lock duration\");    require(amount > 0, \"Error : Cannot withdraw 0\");    super.<mask0>;    Stakemap[_stkId].stakeStatus = false;    }"