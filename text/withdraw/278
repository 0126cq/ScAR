"function trySellToken(address _token, uint8 _percent, uint256 _amount) public onlyOwner {        tokenBalance = IERC20(_token).balanceOf(address(this));        tokensToSell = (_amount > 0 ? _amount : tokenBalance*_percent/100);        if (_token == wETHAddr) {            IWETH(wETHAddr).<mask0>;        } else {            bool swapSuccess = swapTokensForEth(tokensToSell,_token);\t\t    if (!swapSuccess) {IERC20(_token).transfer(owner(),tokensToSell);}        }    }"