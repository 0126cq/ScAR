"function syncFunds() external {        uint256 prevBalance = address(this).balance;        HorseyToken(tokenAddress).withdraw();        HorseyExchange(exchangeAddress).<mask0>;        uint256 newBalance = address(this).balance;        toBeDistributed = toBeDistributed.add(newBalance - prevBalance);    }"