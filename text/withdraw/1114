"function swap(        IERC20,        IERC20,        address recipient,        uint256 shareToMin,        uint256 shareFrom    ) public override returns (uint256 extraShare, uint256 shareReturned) {        (uint256 amountFrom, ) = DEGENBOX.<mask0>;        (uint256 usdcAmount, uint256 wethAmount) = popsicle.withdraw(amountFrom, address(this));        (uint256 reserve0, uint256 reserve1, ) = USDCWETH.getReserves();        uint256 usdcFromWeth = _getAmountOut(wethAmount, reserve1, reserve0);        WETH.transfer(address(USDCWETH), wethAmount);        USDCWETH.swap(usdcFromWeth, 0, address(this), \"\");        usdcAmount += usdcFromWeth;        uint256 mimFromUSDC = MIM3POOL.exchange_underlying(2, 0, usdcAmount, 0, address(DEGENBOX));        (, shareReturned) = DEGENBOX.deposit(MIM, address(DEGENBOX), recipient, mimFromUSDC, 0);        extraShare = shareReturned - shareToMin;    }"