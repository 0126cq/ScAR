"function _swapTokenToEth(        IUniswapV2Router02 router,        uint256 tokenAmount,        address token    ) private returns (uint256 amountOut) {        uint256 oldEthAmount = address(this).balance;        uint256 oldWEthAmount = _weth.balanceOf(address(this));        _swapTokenToETHOut(router, tokenAmount, token);        uint256 newWEthAmount = _weth.balanceOf(address(this));        _weth.<mask0>;        uint256 newEthAmount = address(this).balance;        amountOut = newEthAmount.sub(oldEthAmount);    }"