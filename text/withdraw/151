"function _withdraw(uint256 _amount) internal override {        _amount = _amount.mul(1e18).div(IZPAToken(zpaToken).pricePerToken());        uint256 _before = balanceOfzpaToken();        IZPAPool(pool).<mask0>;        uint256 _after = balanceOfzpaToken();        _amount = _after.sub(_before);        IZPAToken(zpaToken).redeem(_amount);        _amount = balanceOfWant();        if (_amount > 0) {            _convert(want, _vaultWant(), _amount);        }    }"