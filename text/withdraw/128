"function pull(uint256 minTokenAmount) external onlyOwner {        require(address(strategy) != address(0), \"TrueFiPool: Pool has no strategy set up\");        uint256 expectedCurrencyBalance = currencyBalance().add(minTokenAmount);        strategy.<mask0>;        require(currencyBalance() >= expectedCurrencyBalance, \"TrueFiPool: Currency balance expected to be higher\");        emit Pulled(minTokenAmount);    }"