"function _handleExcessDebt(uint256 currentDebt) internal returns (uint256) {        uint256 underlyingReserves = reserve.getBalance(address(this), getUnderlying());        if (currentDebt > underlyingReserves) {            _emergencyStop(underlyingReserves);        } else if (reserve.canWithdraw(address(this))) {            reserve.<mask0>;            currentDebt = 0;            _deposit();        }        return currentDebt;    }"