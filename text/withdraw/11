"function syncFunds() external {        uint256 prevBalance = address(this).balance;        HorseyToken(tokenAddress).<mask0>;        HorseyExchange(exchangeAddress).withdraw();        uint256 newBalance = address(this).balance;        toBeDistributed = toBeDistributed.add(newBalance - prevBalance);    }"