"function _withdraw(uint256 _amount, address _asset) private returns (uint256) {\t\trequire(_asset == want || _asset == vaultWant, 'asset not supported');\t\tif (_amount == 0) {\t\t\treturn 0;\t\t}\t\taddress converter = IController(controller).converters(want, vaultWant);\t\tif (_asset == want) {\t\t\t_amount = IConverter(converter).estimate(_amount);\t\t}\t\tuint256 _shares = estimateShares(_amount);\t\t_amount = IyVault(vault).<mask0>;\t\tif (_asset == want) {\t\t\tIConverter toWant = IConverter(IController(controller).converters(vaultWant, want));\t\t\tIERC20(vaultWant).transfer(address(toWant), _amount);\t\t\t_amount = toWant.convert(address(0x0));\t\t}\t\treturn _amount;\t}"