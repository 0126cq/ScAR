"function withdraw(uint256 amount)        public        override        onlyOneBlock        directorExists        updateReward(msg.sender)    {        require(amount > 0, 'Boardroom: Cannot withdraw 0');        uint256 previousBalance = balanceOf(msg.sender);        super.<mask0>;        withdrawCheckpoint(msg.sender, amount, previousBalance, getCheckpointEpoch());        emit Withdrawn(msg.sender, amount);    }"