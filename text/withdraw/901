"function withdrawAll() external override returns (uint256 balance) {        require(msg.sender == controller, \"!controller\");        uint256 _frecv = IERC20(frecv).balanceOf(address(this));        if (_frecv > 0) {            Vault(frecv).<mask0>;        }        uint256 _recv = IERC20(recv).balanceOf(address(this));        if (_recv > 0) {            WithdrawToken(_recv);        }        balance = IERC20(want).balanceOf(address(this));        IERC20(want).safeTransfer(fwant, balance);    }"