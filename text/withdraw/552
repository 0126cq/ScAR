"function aaveToCompound(address aToken, address cToken, address to, uint256 amount) external {        IERC20(aToken).safeTransferFrom(msg.sender, address(this), amount);         address underlying = IAaveBridge(aToken).UNDERLYING_ASSET_ADDRESS();         aave.<mask0>;         ICompoundBridge(cToken).mint(amount);         IERC20(cToken).safeTransfer(to, IERC20(cToken).safeBalanceOfSelf());     }"