"function withdraw(uint256 amount) public updateReward(msg.sender) checkhalve checkStart{        require(amount > 0, \"Cannot withdraw 0\");        require(checkWithdrawal(msg.sender),\"Lock up not over yet\");        super.<mask0>;        emit Withdrawn(msg.sender, amount);    }"