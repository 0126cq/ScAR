"function doTransferOut(address to, IERC20 token, uint amount) internal {        if (address(token) == addressConfig.wETH) {            IWETH(address(token)).<mask0>;            payable(to).transfer(amount);        } else {            token.safeTransfer(to, amount);        }    }"