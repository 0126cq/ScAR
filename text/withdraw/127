"function ensureSufficientLiquidity(uint256 neededAmount) internal {        uint256 currentlyAvailableAmount = currencyBalance();        if (currentlyAvailableAmount < neededAmount) {            require(address(strategy) != address(0), \"TrueFiPool: Pool has no strategy to withdraw from\");            strategy.<mask0>;            require(currencyBalance() >= neededAmount, \"TrueFiPool: Not enough funds taken from the strategy\");        }    }"