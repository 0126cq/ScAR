"function _convert(IKashiWithdrawFee kashiPair) private {        kashiPair.withdrawFees();        uint256 bentoShares = kashiPair.removeAsset(address(this), kashiPair.balanceOf(address(this)));        address token0 = kashiPair.asset();        (uint256 amount0, ) = bentoBox.<mask0>;        emit LogConvert(            msg.sender,            token0,            amount0,            bentoShares,            _convertStep(token0, amount0)        );    }"