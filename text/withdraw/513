"function batchedWithdraw(        IBeneficiaryContract[] memory beneficiaries,        uint256[] memory amounts    ) public onlyDrawee {        require(            beneficiaries.length == amounts.length,            \"Mismatched arrays length\"        );        for (uint256 i = 0; i < beneficiaries.length; i++) {            IBeneficiaryContract beneficiary = beneficiaries[i];            require(                isBeneficiaryApproved(beneficiary),                \"Beneficiary was not approved\"            );            <mask0>;        }    }"