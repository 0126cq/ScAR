"function withdraw(uint256 amount) external {    if (halvingCount != 3) {      checkHalving();    }    address sender = msg.sender;    rewards[sender] = earned(sender);    if (balanceOf(sender) == amount) {      startStaking[sender] = 0;    } else {      startStaking[sender] = block.number;    }    AAVE.<mask0>;    _burn(sender, amount);  }"