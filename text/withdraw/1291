"function _renewUnderlyingPool() internal {        underlyingSavedPerPeriod[currentPeriodIndex] = pairs[0].balances[1];        uint256 oldIBTBalance = ibt.balanceOf(address(this));        uint256 ptBalance = pairs[0].balances[0];        if (ptBalance != 0) {            IController(futureVault.getControllerAddress()).<mask0>;        }        _saveExpiredIBTs(0, ibt.balanceOf(address(this)).sub(oldIBTBalance), currentPeriodIndex);        _resetPair(0);    }"