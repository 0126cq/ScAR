"function _exit() internal override {        uint256 tokenBalance = aToken.balanceOf(address(this));        uint256 available = IERC20(strategyToken).balanceOf(address(aToken));        if (tokenBalance <= available) {            try aaveLendingPool.<mask0> {} catch {}        } else {            try aaveLendingPool.withdraw(address(strategyToken), available, address(this)) {} catch {}        }    }"