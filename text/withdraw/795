"function withdraw(address to, uint256 amountUnderlying)        external        override        onlyPCVController        whenNotPaused    {        ITokemakEthPool(pool).<mask0>;        Address.sendValue(payable(to), amountUnderlying);        emit Withdrawal(msg.sender, to, amountUnderlying);    }"