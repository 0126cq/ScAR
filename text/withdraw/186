"function liquidatePosition(uint256 _amountNeeded)        internal        override        returns (uint256 _liquidatedAmount, uint256 _loss)    {        uint256 _wantBal = balanceOfWant();        if (_amountNeeded > _wantBal) {            uint256 _stakedBal = balanceOfStaked();            if (_stakedBal > 0) {                IStaking(staking).<mask0>;            }            uint256 _withdrawnBal = balanceOfWant();            _liquidatedAmount = Math.min(_amountNeeded, _withdrawnBal);            _loss = _amountNeeded.sub(_liquidatedAmount);        } else {            return (_amountNeeded, 0);        }    }"