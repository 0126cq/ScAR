"function _withdraw(uint256[] calldata _contractIdxs) internal override {        for (uint i = 0; i < _contractIdxs.length; i++) {            require(_contractIdxs[i] < contracts.length, \"contractIdx must be < contracts length\");            IOracle oracle = IOracle(contracts[_contractIdxs[i]]);            uint256 amount = oracle.withdrawable();            if (amount >= minToWithdraw) {                oracle.<mask0>;            }        }    }"