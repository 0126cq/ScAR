"function withdraw(uint256 amount)        public        updateReward(msg.sender)        checkStart    {        require(amount > 0, \"Rewards: cannot withdraw 0\");        super.<mask0>;        emit Withdrawn(msg.sender, amount);    }"