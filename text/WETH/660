"function setUniRouter(address uniRouter_) external onlyOwner {        uniRouter = uniRouter_;        uniFactory = IUniswapV2Router02(uniRouter).factory();        WETH = IUniswapV2Router02(uniRouter).<mask0>;        uniPair = IUniswapV2Factory(uniFactory).getPair(WETH, address(this));        if( uniPair == address(0))            uniPair = IUniswapV2Factory(uniFactory).createPair(address(this), WETH);    }"