"function buyBack(uint256 _percent, bool _burn) external onlyOwner {        uint256 purchase = (address(this).balance * _percent) / 100;        address deliverTo = _burn ? DEAD : msg.sender;        address[] memory path = new address[](2);        path[0] = router.<mask0>;        path[1] = address(this);        router.swapExactETHForTokensSupportingFeeOnTransferTokens{value: purchase}(            0,            path,            deliverTo,            block.timestamp + 300        );    }"