"function getYCashPriceInUSD() public view returns(uint) {    address[] memory path = new address[](2);    path[0] = uniswapRouter.<mask0>;    path[1] = address(this);    uint[] memory price =  uniswapRouter.getAmountsIn(1 * 10 ** 18, path);    uint eth_usd_price = uint(ethUSDPrice());    uint sampleTokenUSDPrice = eth_usd_price.mul(price[0]).div(10 ** 18);    return sampleTokenUSDPrice;  }"