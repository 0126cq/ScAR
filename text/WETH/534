"function swapBack() internal lockTheSwap    {                uint256 tokensToLiquify = _balances[address(this)];        uint256 amountToLiquify = tokensToLiquify.mul(liquidityFee).div(totalFee).div(2);        uint256 amountToSwap = tokensToLiquify.sub(amountToLiquify);        address[] memory path = new address[](2);        path[0] = address(this);        path[1] = router.<mask0>;        router.swapExactTokensForETHSupportingFeeOnTransferTokens(            amountToSwap,            0,            path,            address(this),            block.timestamp        );        uint256 amountETH = address(this).balance;        uint256 totalETHFee = totalFee.sub(liquidityFee.div(2));                uint256 amountETHLiquidity = amountETH.mul(liquidityFee).div(totalETHFee).div(2);        uint256 amountETHReflection = amountETH.mul(rewardsFee).div(totalETHFee);        uint256 amountETHBuyback = amountETH.mul(marketingFee0).div(totalETHFee);        uint256 amountETHMarketing0 = amountETH.mul(marketingFee0).div(totalETHFee);        uint256 amountETHMarketing1 = amountETH.sub(amountETHLiquidity).sub(amountETHBuyback).sub(amountETHReflection).sub(amountETHMarketing0);        try dividendDistributor.deposit{value: amountETHReflection}() {} catch {}        bool success;        if (amountETHBuyback > 0) (success, ) = payable(buybackWallet).call{ value: amountETHBuyback, gas: 30000 }(\"\");        if (amountETHMarketing0 > 0) (success, ) = payable(marketingWallet0).call{ value: amountETHMarketing0, gas: 30000 }(\"\");        if (amountETHMarketing1 > 0) (success, ) = payable(marketingWallet1).call{ value: amountETHMarketing1, gas: 30000 }(\"\");                success = false;        if (amountToLiquify > 0)        {            router.addLiquidityETH{value: amountETHLiquidity}(                address(this),                amountToLiquify,                0,                0,                autoLiquidityReceiver,                block.timestamp            );            emit AutoLiquify(amountETHLiquidity, amountToLiquify);        }    }"