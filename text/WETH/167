"function swap(\t\tIHasRouter _iHasRouter,\t\taddress[] memory _path,\t\tuint256 _tokenAmount,\t\tuint256 _minTokenReturn,\t\taddress _receiver\t) internal returns (uint256 swapResult) {\t\tUniswap uniswapContract = _iHasRouter.getRouter();\t\tuint256[] memory result;\t\tif (_path[0] == address(0x0)) {\t\t\t_path[0] = uniswapContract.WETH();\t\t\tresult = uniswapContract.swapExactETHForTokens{ value: _tokenAmount }(\t\t\t\t_minTokenReturn,\t\t\t\t_path,\t\t\t\t_receiver,\t\t\t\tblock.timestamp\t\t\t);\t\t} else if (_path[_path.length - 1] == address(0x0)) {\t\t\t_path[_path.length - 1] = uniswapContract.<mask0>;\t\t\tresult = uniswapContract.swapExactTokensForETH(\t\t\t\t_tokenAmount,\t\t\t\t_minTokenReturn,\t\t\t\t_path,\t\t\t\t_receiver,\t\t\t\tblock.timestamp\t\t\t);\t\t} else {\t\t\tresult = uniswapContract.swapExactTokensForTokens(\t\t\t\t_tokenAmount,\t\t\t\t_minTokenReturn,\t\t\t\t_path,\t\t\t\t_receiver,\t\t\t\tblock.timestamp\t\t\t);\t\t}\t\treturn result[result.length - 1];\t}"