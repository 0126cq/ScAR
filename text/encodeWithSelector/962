"function _safeBalance (address token, address account) internal returns (uint256) {    (bool success, bytes memory data) = token.call(abi.<mask0>);    require(success, 'BALANCE');    return abi.decode(data, (uint256));  }"