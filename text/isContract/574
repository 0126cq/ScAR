"function _setBeacon(address newBeacon) private {        require(AddressUpgradeable.<mask0>, \"ERC1967: new beacon is not a contract\");        require(            AddressUpgradeable.isContract(IBeaconUpgradeable(newBeacon).implementation()),            \"ERC1967: beacon implementation is not a contract\"        );        StorageSlotUpgradeable.getAddressSlot(_BEACON_SLOT).value = newBeacon;    }"