"function setYieldReserveAddress (address addr) public onlyOwnerOrController {        require(addr != address(0) && addr != address(this), \"Invalid address\");        require(Utils.<mask0>, \"The address must be a contract\");        emit OnYieldReserveAddressChanged(yieldReserveAddress, addr);        yieldReserveAddress = addr;    }"