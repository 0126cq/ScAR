"function mintFireNormal(address wallet, uint256 quantity,Teams team) payable public {        require(sale, \"Sale disabled\");        require(!Address.<mask0>, \"Contracts are not allowed to mint\");        require(quantity != 0, \"Requested quantity cannot be zero\");        require(quantity * FirePrice <= msg.value, \"Not enough ether sent\");        require(getTeamTierMintCount(team,FireTier)+quantity<=teamTierLimits[team][FireTier],\"Low Mint Supply for Whitelist\");        require(minted[wallet]+quantity<=maxWallet,\"Wallet Mint limit rached\");        minted[wallet] += quantity;        _safeMintLoop(wallet, team, FireTier, quantity,0);    }"