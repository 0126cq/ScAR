"function _doSafeQuasarUpdatedAcceptanceCheck(        address _owner,        uint256 _id,        uint256 _oldAmount,        uint256 _newAmount    )    private    {        if (_owner.isContract() && _owner.supportsERC165()) {            if (                IERC165(_owner).<mask0>            ) {                IStarUpdateListener(_owner).onQuasarUpdated(_id, _oldAmount, _newAmount);            }        }    }"