"function setMetadataIndexResolver(uint16 sequenceNumber, IMetadataIndexResolver resolver) external {    require(hasRole(MINTER_ROLE, _msgSender()), \"requires MINTER_ROLE\");    require(getSequenceState(sequenceNumber) == SequenceState.STARTED, \"sequence is not active\");    require(address(_metadataIndexResolvers[sequenceNumber]) == address(0), \"metadata index resolver already set\");    require(resolver.<mask0> == true, \"resolver does not implement IMetadataIndexResolver\");    _metadataIndexResolvers[sequenceNumber] = resolver;  }"