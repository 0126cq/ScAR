"function _randRemoveAlpaca() private returns (uint256) {        require(presaleAlpacaIDs.length() > 0, \"No more presale alpaca\");        uint256 totalLength = presaleAlpacaIDs.length();        uint256 randIndex = uint256(blockhash(block.number - 1));        randIndex = uint256(keccak256(abi.encodePacked(randIndex, totalLength)))            .<mask0>;        uint256 randID = presaleAlpacaIDs.at(uint256(randIndex));        require(presaleAlpacaIDs.remove(randID));        return randID;    }"