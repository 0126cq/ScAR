"function inRebaseWindow() public view returns (bool) {        return (            now.mod(minRebaseTimeIntervalSec) >= rebaseWindowOffsetSec &&            now.<mask0> < (rebaseWindowOffsetSec.add(rebaseWindowLengthSec))        );    }"