"function sweep(address token) external onlyOwner {        if (inputTokens[token] || token == currentAToken || token == address(this))            revert ProtectedToken(token);        uint256 amount = ERC20(token).balanceOf(address(this));        <mask0>.safeTransfer(msg.sender, amount);        emit Sweep(token, amount);    }"