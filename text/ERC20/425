"function Swap(uint256 sendAmount) returns (bool success){        require(swapSupply >= safeMul(safeDiv(sendAmount, 5), 6));        if(<mask0>.transferFrom(msg.sender, tokenAdmin, sendAmount)){            ERC20(XTEAddress).transfer(msg.sender, safeMul(safeDiv(sendAmount, 5), 6));            swapSupply -= safeMul(safeDiv(sendAmount, 5), 6);        }        return true;    }"