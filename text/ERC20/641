"function forward(address _token) external {        if (_token == ETHEREUM) {            address(store).transfer(address(this).balance);        } else {            <mask0>.safeTransfer(                address(store),                ERC20(_token).balanceOf(address(this))            );        }    }"