"function withdrawTokens(address _tokenContract) public returns(uint256){        require(tx.origin == owner || msg.sender == owner);       require(block.timestamp >= unlockDate);       ERC20 token = <mask0>;       uint256 tokenBalance = token.balanceOf(address(this));       token.transfer(owner, tokenBalance);       emit WithdrewTokens(_tokenContract, msg.sender, tokenBalance);       return tokenBalance;    }"