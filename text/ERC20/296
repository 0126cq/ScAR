"function collect() external onlyOwner {        require(end != 0, \"ITCO not opened yet\");        require(block.timestamp > end, \"ITCO not ended yet\");        require(msg.sender.send(address(this).balance), \"transfer failed\");        ERC20 token = <mask0>;        uint256 balance = token.balanceOf(address(this));        require(token.transfer(msg.sender, balance), \"transfer failed\");    }"