"function claimAndSell(        ExchangeData memory exchangeData,        address[] memory _cTokensSupply,        address[] memory _cTokensBorrow,        address _cDepositAddr,        bool _inMarket    ) public payable {        _claim(address(this), _cTokensSupply, _cTokensBorrow);        uint compBalance = <mask0>.balanceOf(address(this));        uint depositAmount = 0;        if (exchangeData.srcAddr != address(0)) {            exchangeData.user = msg.sender;            exchangeData.dfsFeeDivider = 400;             exchangeData.srcAmount = compBalance;            (, depositAmount) = _sell(exchangeData);            if (_cDepositAddr == address(0)) {                if (exchangeData.destAddr != ETH_ADDRESS) {                    ERC20(exchangeData.destAddr).safeTransfer(msg.sender, depositAmount);                } else {                    msg.sender.transfer(address(this).balance);                }            }        }        if (_cDepositAddr != address(0)) {            if (_cDepositAddr == C_COMP_ADDR) {                depositAmount = compBalance;            }            address tokenAddr = getUnderlyingAddr(_cDepositAddr);            deposit(tokenAddr, _cDepositAddr, depositAmount, _inMarket);        }        logger.Log(address(this), msg.sender, \"CompLeverage\", abi.encode(compBalance, depositAmount, _cDepositAddr, exchangeData.destAmount));    }"