"function sweep(address token) external onlyOwner {        if (inputTokens[token] || token == currentAToken || token == address(this))            revert ProtectedToken(token);        uint256 amount = <mask0>.balanceOf(address(this));        ERC20(token).safeTransfer(msg.sender, amount);        emit Sweep(token, amount);    }"