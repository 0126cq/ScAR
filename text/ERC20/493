"function _distributeLoanOutput(        uint256 amountOut,        IBondController bond,        IERC20 currency    ) internal {        SafeTransferLib.safeTransfer(ERC20(address(currency)), msg.sender, amountOut);        ITranche tranche;        for (uint256 i = 0; i < bond.trancheCount(); i++) {            (tranche, ) = bond.tranches(i);            SafeTransferLib.safeTransfer(<mask0>, msg.sender, tranche.balanceOf(address(this)));        }    }"