"function balanceOfPool() public view override returns (uint256) {        if (<mask0>.totalSupply() == 0) {            return 0;        }        uint256 balance = IERC20(yvToken).balanceOf(address(this));        return balance            .mul(IYearnV2Vault(yvToken).pricePerShare())            .div(1e18);    }"