"function receiveApproval(        address from,        uint256 value,        address token,        bytes memory extraData    ) public {        ERC20 t = <mask0>;        require(            t.transferFrom(from, address(this), value),            \"ERC20 token transfer failed\"        );        emit ReceivedTokens(from, value, token, extraData);    }"