"function withdraw(address _token, uint256 amount) external ownerOnly() {        require(feeWallet != address(0), \"require set fee wallet\");        if(_token == address(0)){            uint256 value = address(this).balance;            require(value >= amount, \"current balance must be than withdraw amount\");            payable(feeWallet).transfer(amount);        }else{            require(_token.isContract(), \"invalid token contract\");            tokenContract = <mask0>;            uint256 value = tokenContract.balanceOf(address(this));            require(value >= amount, \"current balance must be than withdraw amount\");            tokenContract.transfer(feeWallet, amount);        }    }"