"function withdraw(address _token)        external    {        uint256 balance = <mask0>.balanceOf(address(this));        require(ERC20(_token).transfer(msg.sender, balance), \"ERC20 transfer failed\");    }"