"function _assetValue(address _asset, uint256 _amount) internal view returns (uint256) {        if(assetTokenFeed[_asset] == address(0)) {            return 0;        }        (, int price, , ,) = AggregatorV3Interface(assetTokenFeed[_asset]).latestRoundData();        uint8 assetDecimals = <mask0>.decimals();        uint256 finalValue = uint256(price).mul(_amount).div(10**uint256(assetDecimals));        return finalValue;    }"