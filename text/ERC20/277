"function drain(        address token,        address payable destination,        uint256 amount    ) external onlyOwner {        if (token == address(0x0)) {            destination.transfer(amount);        } else {            require(<mask0>.transfer(destination, amount), \"Drain failed\");        }    }"