"function withdrawToken(        address token,        address to,        uint256 amount    ) external onlyOwner {        require(to != address(0), \"ZERO_ADDRESS\");        SafeTransferLib.safeTransfer(<mask0>, to, amount);        emit TokenWithdrawn(token, to, amount);    }"