"function withdrawERC20(        ERC20 _tokenAddress,        address payable _recipient,        uint256 _amount    ) external onlyReferrio {        ERC20 token = <mask0>;        token.transfer(_recipient, _amount);    }"