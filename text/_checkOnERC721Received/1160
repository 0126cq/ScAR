"function safeTransferFrom(        address from,        address to,        uint256 id,        bytes memory data    ) public virtual {        transferFrom(from, to, id);        if (!<mask0>) revert TransferToNonERC721ReceiverImplementer();    }"