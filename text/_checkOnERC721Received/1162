"function _safeMint(        address to,        uint256 qty,        bytes memory data    ) internal virtual {        _mint(to, qty);        if (!<mask0>)            revert TransferToNonERC721ReceiverImplementer();    }"