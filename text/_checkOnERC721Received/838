"function _safeTransfer(address from, address to, uint256 tokenId, bytes memory _data) internal virtual {        _transfer(from, to, tokenId);        isOnSale[tokenId][from] = false;        require(<mask0>, \"ERC721: transfer to non ERC721Receiver implementer\");    }"