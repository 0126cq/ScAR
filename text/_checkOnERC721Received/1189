"function _safeMint(        address to,        uint256 tokenId,        bytes memory data    ) internal virtual {        _mint(to, tokenId);        require(            <mask0>,            \"_safeMint: transfer to non ERC721Receiver implementer\"        );    }"