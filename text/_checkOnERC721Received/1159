"function safeTransferFrom(        address from,        address to,        uint256 id    ) public virtual {        transferFrom(from, to, id);        if (!<mask0>) revert TransferToNonERC721ReceiverImplementer();    }"