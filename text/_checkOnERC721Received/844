"function safeTransferFrom(    address from,    address to,    uint256 tokenId,    bytes memory _data  ) public virtual override {    _transfer(from, to, tokenId);    require(      <mask0>,      \"ERC721A: transfer to non ERC721Receiver implementer\"    );  }"