"function _safeTransfer(address from, address to, uint256 tokenId, bytes memory _data) internal {        _transfer(from, to, tokenId);        require(<mask0>, \"CosmoDoodle: transfer to non ERC721Receiver implementer\");    }"