"function safeTransferFrom(        address from,        address to,        uint256 tokenId,        bytes memory _data    ) public virtual override {        _transfer(from, to, tokenId);        if (!<mask0>) {            revert TransferToNonERC721ReceiverImplementer();        }    }"