"function _safeMintLoop(address wallet, Teams team, uint256 tier, uint256 quantity,uint256 _paymentId) internal    {        for (uint256 i = 0; i < quantity; i++) {            incrementTeamTierMintCount(team,tier);            _mint(wallet, _tokenIdCounter.current());            _setTokenURI(_tokenIdCounter.current(), string(abi.encodePacked(Strings.toString(_tokenIdCounter.current()),\"/\",Strings.toString(teamIDS[team]),\"/\", Strings.<mask0>)));            _paymentIdLogs[_paymentId][_tokenIdCounter.current()] = _tokenIdCounter.current();            _tokenIdCounter.increment();        }    }"