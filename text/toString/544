"function tokenURI(uint256 tokenId) public view virtual override returns (string memory) {    require(      _exists(tokenId),      \"ERC721Metadata: URI query for nonexistent token\"    );    uint256 hour_ = timeTool.getHour(block.timestamp);    if(hour_==6&&hour_<12)hour_=0;    else if(hour_==12&&hour_<17)hour_=1;    else if(hour_==17&&hour_<20)hour_=2;    else{hour_=3;}    if(!isRevealed[stonkers[tokenId].mintBatch]){      return string(abi.encodePacked(placeholderImage));    }else  {    return      bytes(baseURIs[stonkers[tokenId].mintBatch][hour_]).length > 0        ? string(abi.encodePacked(baseURIs[stonkers[tokenId].mintBatch][hour_], tokenId.<mask0>))        : \"\";    }  }"