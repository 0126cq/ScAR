"function tokenURI(uint256 tokenId) public view override returns (string memory) {        require(_exists(tokenId), \"Nonexistent token\");        if(_customURIContract != address(0)) {            return ITokenURICustom(_customURIContract).constructTokenURI(tokenId);        }        else {             if(tokenId > OFFSETAN2) {                 if(uriSelection[tokenId]) {                     return string(abi.encodePacked(baseURI_OE, tokenId.<mask0>));                }                return string(abi.encodePacked(baseURI, tokenId.toString()));            }            else {                if(mergeActive) {                    return IMerger(mergeContract).heroURI(tokenId);                }                else {                    return preheroURI;                }            }        }    }"