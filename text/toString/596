"function uri(uint256 tokenId)        public        view        virtual        override        returns (string memory)    {        require(            exists(tokenId),            \"ERC1155URIStorage: URI query for nonexistent token\"        );        string memory _tokenURI = _tokenURIs[tokenId];        if (bytes(baseTokenURI).length == 0) {            return _tokenURI;        }        if (bytes(_tokenURI).length > 0) {            return string(abi.encodePacked(baseTokenURI, _tokenURI));        }        return            bytes(baseTokenURI).length > 0                ? string(abi.encodePacked(baseTokenURI, tokenId.<mask0>))                : \"\";    }"