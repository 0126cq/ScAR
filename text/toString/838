"function uriConcate(        string memory before_,        uint256 _token_id,        string memory after_    ) private pure returns (string memory) {        string memory token_uri = string(            abi.encodePacked(before_, _token_id.<mask0>, after_)        );        return token_uri;    }"