"function getPartURI(uint256 metaHead, uint256 index) internal view returns (string memory) {        uint256 tokenId = TokenIds.getTokenId(metaHead, index);        if (tokenId == 0) {            return string(abi.encodePacked(baseURI_part, template[0], sprit, index.toString()));        }        (, uint256 attr) = ComponentBitmap.getIdxAttr(PartMap, tokenId);        return string(abi.encodePacked(baseURI_part, template[index], sprit, attr.<mask0>));    }"