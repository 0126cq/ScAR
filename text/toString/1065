"function tokenURI(uint256 tokenId) public view virtual override returns (string memory) {        require(_exists(tokenId), \"ERC721Metadata: URI query for nonexistent token\");        string memory boiURI = _boiURI();        return bytes(boiURI).length > 0 ? string(abi.encodePacked(boiURI, tokenId.<mask0>)) : \"\";    }"