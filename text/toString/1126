"function tokenURI(uint256 tokenId) override public view returns (string memory)  {    require(      _exists(tokenId),      \"ERC721Metadata\"    );    if(tokenURIPlan != MTS(address(0)))    {      return tokenURIPlan.tokenURI(tokenId);    }    else    {      string memory baseURI = _baseURI();      return bytes(baseURI).length > 0 ? string(abi.encodePacked(baseURI, tokenId.<mask0>)) : \"\";    }  }"