"function tokenURI(uint256 tokenId) public view override returns (string memory) {        require(_exists(tokenId), \"Edition doesn't exist\");        return string(abi.encodePacked(metadataUrl, \"?tokenId=\", StringsUpgradeable.<mask0>));    }"