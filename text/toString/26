"function getLatitude(uint256 tokenId) public view returns (string memory) {        require(tokenId < tokenIdCounter.current(), \"tokenId is not exist\");        uint256 l = tokenId / 90;        if (l < 45) {            return                string(                    abi.encodePacked(                        toString(l * 2),                        \"N\",                        \"-\",                        toString(l * 2 + 2),                        \"N\"                    )                );        } else {            return                string(                    abi.encodePacked(                        toString((l - 45) * 2),                        \"S\",                        \"-\",                        <mask0>,                        \"S\"                    )                );        }    }"