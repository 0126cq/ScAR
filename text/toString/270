"function tokenURI(uint256 tokenId)        public        view        override        returns (string memory)    {        require(_exists(tokenId), \"ERC721Metadata: URI query for nonexistent token\");        string memory baseURI = BASE_URLS[ASSET_TYPE_PER_ID[tokenId]];        return bytes(baseURI).length > 0 ? string(abi.encodePacked(baseURI, tokenId.<mask0>)) : \"\";    }"