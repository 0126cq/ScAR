"function _safeMintLoopGold(address wallet, uint256 tier, uint256 quantity,uint256 _paymentId) internal    {        for (uint256 i = 0; i < quantity; i++) {            _mint(wallet, _tokenIdCounter.current());            _setTokenURI(_tokenIdCounter.current(), string(abi.encodePacked(Strings.<mask0>,\"/13/\", Strings.toString(tier))));            _paymentIdLogs[_paymentId][_tokenIdCounter.current()] = _tokenIdCounter.current();            _tokenIdCounter.increment();        }    }"