"function pluck(uint256 tokenId, string memory keyPrefix)        internal        view        returns (string memory)    {        require(tokenId < tokenIdCounter.current(), \"tokenId is not exist\");        uint256 rand = random(            string(abi.encodePacked(keyPrefix, randomNumber[tokenId]))        );        return <mask0>;    }"