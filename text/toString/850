"function tokenURI(uint256 tokenId) public view virtual override returns (string memory) {    require(_exists(tokenId), \"MetaversusVotingMachine: URI query for nonexistent token\");    string memory baseURI = _baseURI();    string memory votec = vote_outcome[tokenId].<mask0>;    if (_winnerset == 1){      return string(abi.encodePacked(baseURI,votec,'/', tokenId.toString()));    }    else{      return string(abi.encodePacked(baseURI,tokenId.toString()));    }  }"