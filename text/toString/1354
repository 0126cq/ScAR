"function tokenURI(uint256 tokenId) public view virtual override returns (string memory) {        require(_exists(tokenId), ERROR_TOKEN_ID);        if (isRevealed) {            uint256 shiftedIndex = ((shiftValue + tokenId) % MAX_SUPPLY);            return string(abi.encodePacked(baseTokenURI, shiftedIndex.<mask0>));        } else {            return UNREVEALED_URI;        }    }"