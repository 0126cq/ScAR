"function tokenURI(uint256 tokenId)    public    view    virtual    override    returns (string memory)  {    require(      _exists(tokenId),      \"ERC721Metadata: URI query for nonexistent token\"    );    string memory currentBoiURI = _boiURI();    return bytes(currentBoiURI).length > 0        ? string(abi.encodePacked(currentBoiURI, tokenId.<mask0>, boiExtension))        : \"\";  }"