"function tokenURI(uint256 tokenId) public view override(ERC721) returns (string memory) {        require(_exists(tokenId), \"Cannot query non-existent token\");        if (revealed && evolved[tokenId]) {            return string(abi.encodePacked(_tokenEvolvedURI, tokenId.toString()));        }         else if (revealed) {            return string(abi.encodePacked(_tokenBaseURI, tokenId.<mask0>));        }else {            return  _tokenBaseURI;        }    }"