"function tokenURI (uint256 tokenId) public view virtual override returns(string memory){      require(_exists(tokenId), \"ERC721Metadata: URI query for nonexistent token\");      string memory currentBaseURI = _baseURI();      if(revealed == false){        return bytes(currentBaseURI).length > 0            ? string(                abi.encodePacked(                    currentBaseURI,                    Strings.toString(1),                    baseExtension                )            )            : \"\";      }      return bytes(currentBaseURI).length > 0            ? string(                abi.encodePacked(                    currentBaseURI,                    Strings.<mask0>,                    baseExtension                )            )            : \"\";    }"