"function tokenURI(uint256 tokenId)        public        view        override        returns (string memory)    {        require(            _exists(tokenId),            \"ERC721Metadata: URI query for nonexistent token\"        );        return            bytes(_baseTokenURI).length > 0                ? string(                    abi.encodePacked(                        _baseTokenURI,                        Strings.<mask0>,                        \".json\"                    )                )                : \"\";    }"