"function tokenURI(uint256 tokenId)        public        view        virtual        override        returns (string memory)    {        require(_exists(tokenId), \"nonexistent token\");        string memory uri = _baseURI();        if (bytes(uri).length == 0) {            return \"\";        }        return string(abi.encodePacked(uri, tokenId.<mask0>, \".json\"));    }"