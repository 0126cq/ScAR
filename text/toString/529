"function tokenURI(uint256 tokenId) public view virtual override returns (string memory) {        require(_exists(tokenId), 'ERC721Metadata: URI query for nonexistent token');        if(!_isRevealed) {            return _unrevealedURI;        }        string memory baseURI = _baseURI();        if(_isRevealed && tokenId > 0) {            tokenId = 1 + ((tokenId + _shift) % (MAX_SUPPLY - 1));        }        return bytes(baseURI).length != 0 ? string(abi.encodePacked(baseURI, tokenId.<mask0>, '.json')) : '';    }"