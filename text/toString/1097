"function tokenURI(uint256 tokenId) public view override returns (string memory) {        require(_exists(tokenId), \"troll does not exist\");        if (revealed) {            return bytes(baseURI).length != 0 ? string(abi.encodePacked(baseURI, Strings.<mask0>)) : \"\";        }        return hiddenURI;    }"