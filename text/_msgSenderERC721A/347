"function freeMint() public {        address _caller = <mask0>;        uint256 _amount = MAX_FREE_MINT;        if(totalSupply()<1000){            _amount = 10;        }        if(totalSupply()<=100){            _amount = 20;        }        require(totalSupply()+_amount<=MAX_SUPPLY, \"Sold out\");        require(freeMints[_caller] == 0, \"Free Mint already claimed\");        freeMints[_caller] = 1;        _safeMint(_caller, _amount);    }"