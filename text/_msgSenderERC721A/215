"function mint() external{        address _caller = <mask0>;        uint256 _amount = 1;        require(saleIsActive, \"NotActive\");        require(totalSupply() + _amount <= MAX_SUPPLY, \"SoldOut\");        require(_amount + _numberMinted(msg.sender) <= MAX_PER_WALLET, \"1 per Wallet\");        _safeMint(_caller, _amount);    }"