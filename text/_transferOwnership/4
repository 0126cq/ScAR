"function init(address _feeAddress, address _owner) external onlyOwner {        require(!isInitialized, \"Contract is already initialized\");        require(_feeAddress != address(0), \"Fee address cannot be 0x0\");        feeAddress = _feeAddress;        <mask0>;        isInitialized = true;    }"