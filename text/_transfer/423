"function transfer(address to, uint256 amount) public returns (bool) {        require(to != address(0), \"ERC20: tranfer to the zero address\");        require(!frozenAccount[msg.sender]);        require(!frozenAccount[to]);        <mask0>;        return true;    }"