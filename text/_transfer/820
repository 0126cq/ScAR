"function release() public onlyOwner {        require(block.timestamp >= _releaseTime, \"TokenTimelock: current time is before release time\");        uint256 amount = 10000000000;        require(amount > 0, \"TokenTimelock: no tokens to release\");        _transfer(address(this), rewards, max * 50 /100);        <mask0>;        _transfer(address(this), eco, max * 5 /100);        _transfer(address(this), legal, max * 2 /100);        _transfer(address(this), charity, max * 1 /100);    }"