"function _transfer(address from, address to, uint256 id) internal override {        require(editionTransferable[id / EDITION_SIZE], \"Edition transfer not active\");        id2provenance[id].push(to);        super.<mask0>;    }"