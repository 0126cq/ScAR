"function transferFrom(        address sender,        address recipient,        uint256 amount    ) public override returns (bool) {        !taxExempt[sender]            ? _transferWithTax(sender, recipient, amount)            : <mask0>;        approve(            _msgSender(),            allowance(sender, _msgSender()).sub(                amount,                \"Transfer amount exceeds allowance\"            )        );        return true;    }"