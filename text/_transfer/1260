"function transferFrom(address from, address to, uint id) public payable {        require(to != address(0));        require(to != address(this));        require(ownerOf(id) == from);        require(isSenderApprovedFor(id));        require(isTradable(cards[id].proto));        <mask0>;    }"