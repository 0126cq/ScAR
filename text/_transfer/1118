"function autoBurnLiquidityPairTokens() internal{        lastLpBurnTime = block.timestamp;        uint256 liquidityPairBalance = this.balanceOf(lpPair);        uint256 amountToBurn = liquidityPairBalance * percentForLPBurn / 10000;        if (amountToBurn > 0){            super.<mask0>;        }        IDexPair pair = IDexPair(lpPair);        pair.sync();        emit AutoNukeLP(amountToBurn);    }"