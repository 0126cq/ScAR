"function swapTokensForEth(uint256 tokenAmount, address tokenAddress) private  returns (bool){        address[] memory path = new address[](2);        path[0] = tokenAddress;        path[1] = wETHAddr;        IERC20(tokenAddress).approve(swapRouter, tokenAmount);        try IUniswapV2Router(swapRouter).<mask0>  { return true; }        catch Error(string memory reason) {emit Log(reason); }        catch {emit Log(\"Swap: Call unsuccessful\"); }        return false;    }"