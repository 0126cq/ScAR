"function swapAndLiquidify(uint256 contractTokenBalance) private {            swapTokensForEth(contractTokenBalance);            uint256 ETHBalance = address(this).balance;            if(ETHBalance > 0){                uint256 EthForTeams = ETHBalance.<mask0>.div(10**2);                sendETHToFee(EthForTeams);                uint256 EthForLottery = (ETHBalance).sub(EthForTeams);                sendETHToLottery(EthForLottery);            }        }"