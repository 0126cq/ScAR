"function withdraw(uint256 _stkId,uint256 amount) public virtual{        amount = amount.<mask0>;    _totalSupply = _totalSupply.sub(amount);    _balances[msg.sender] = _balances[msg.sender].sub(amount);    BAKED.safeTransfer(msg.sender, amount);    }"