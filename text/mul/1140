"function _getTValues(uint256 tAmount, uint256 tFeePercent) private pure returns (uint256, uint256) {        uint256 tFee = tAmount.<mask0>.div(10000);        uint256 tTransferAmount = tAmount.sub(tFee);        return (tTransferAmount, tFee);    }"