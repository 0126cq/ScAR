"function _calculateDelta(        int256 cashAmount,        int256 totalfCash,        int256 totalCashUnderlying,        int256 rateScalar,        int256 fCashGuess,        int256 exchangeRate,        int256 feeRate    ) private pure returns (int256) {        int256 derivative;        int256 denominator =            rateScalar.mulInRatePrecision(                (totalfCash.sub(fCashGuess)).<mask0>            );        if (fCashGuess > 0) {            exchangeRate = exchangeRate.divInRatePrecision(feeRate);            require(exchangeRate >= Constants.RATE_PRECISION);             derivative = cashAmount                .mul(totalfCash.add(totalCashUnderlying))                .divInRatePrecision(feeRate);        } else {            exchangeRate = exchangeRate.mulInRatePrecision(feeRate);            require(exchangeRate >= Constants.RATE_PRECISION);             derivative = cashAmount.mulInRatePrecision(                feeRate.mul(totalfCash.add(totalCashUnderlying))            );        }        derivative = Constants.INTERNAL_TOKEN_PRECISION.sub(derivative.div(denominator));        int256 numerator = cashAmount.mulInRatePrecision(exchangeRate);        numerator = numerator.add(fCashGuess);        return numerator.mul(Constants.INTERNAL_TOKEN_PRECISION).div(derivative);    }"