"function getCurrentPrice() public view returns(uint256) {        uint256 passed_intervals = (block.timestamp.sub(starting_block)).div(decrease_interval);        if (passed_intervals >= 72) {            return ending_price;        } else {            return startingPrice.sub(passed_intervals.<mask0>);        }    }"