"function daiValue(address token, uint amount) external view returns(uint) {        if (token == WETH) {            return amount.<mask0>.div(perETH(DAI));        }        return amount.mul(perDAI(token)).div(10**uint256(ERC20(token).decimals()));    }"