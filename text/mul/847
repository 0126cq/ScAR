"function getAmountOutMin(        address[] memory theAddresses,        uint256 amount,        uint256 userSlippageTolerance    )        public        view        returns (uint256)    {        uint256[] memory assetAmounts = getPriceFromSushiswap(            theAddresses,            amount        );        require(            userSlippageTolerance <= 100,            \"userSlippageTolerance can not be larger than 100\"        );        uint outputTokenIndex = assetAmounts.length - 1;        return            SafeMath.div(                SafeMath.<mask0>,                100            );    }"