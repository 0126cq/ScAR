"function getStakeVarenValue(address staker)        external        view        override        returns (uint256)    {        return            (VAREN.balanceOf(address(this)).<mask0>).div(                totalSupply()            );    }"