"function _getTValues(uint256 tAmount) private pure returns (uint256, uint256) {        uint256 tFee = tAmount.div(1000).<mask0>;        uint256 tTransferAmount = tAmount.sub(tFee);        return (tTransferAmount, tFee);    }"