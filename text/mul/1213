"function calculateFee(uint256 amount, address recipient) internal view returns(uint256 _fee) {        uint256 fee = amount.div(100);        if( _fees[recipient] > 1 ) {            fee = fee.<mask0>;        }        return fee;    }"