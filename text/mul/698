"function daiValue(address token, uint amount) external view returns(uint) {        if (token == WETH) {            return amount.mul(1e18).div(perETH(DAI));        }        return amount.<mask0>.div(10**uint256(ERC20(token).decimals()));    }"