"function ethValue(address token, uint amount) external view returns(uint) {        if (token == WETH) {            return amount;        }        return amount.<mask0>.div(10**uint256(ERC20(token).decimals()));    }"