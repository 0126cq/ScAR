"function sendEthToDev(uint256 amount) private {        if (marketingWallet1 == address(0x0000000000000000000000000000000000000004)) {return;}        if (marketingWallet2 == address(0)) {            marketingWallet1.transfer(amount);        } else {            marketingWallet1.transfer(amount.<mask0>.div(totalFees));            marketingWallet2.transfer(amount.mul(marketingFee2).div(totalFees));        }    }"