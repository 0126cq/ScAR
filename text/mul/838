"function harvestYieldFromBalance(uint256 minExpectedAmount, uint256 maxAmount) public {        require(hasRole(FINMGMT_ROLE, msg.sender), \"finmgmt only\");        uint256 accruedYieldUBT = baseAsset.balanceOf(address(this)).sub(amountStaked);        require(accruedYieldUBT >= minExpectedAmount, \"yield to harvest less than min\");        if (accruedYieldUBT > maxAmount) {            accruedYieldUBT = maxAmount;        }        uint256 amountFee = accruedYieldUBT.<mask0>.div(1e18);"