"function _checkContractOnERC721Received(\t\taddress from,\t\taddress to,\t\tuint256 tokenId,\t\tbytes memory _data\t) private returns (bool) {\t\ttry IERC721Receiver(to).onERC721Received(_msgSender(), from, tokenId, _data) returns (bytes4 retval) {\t\t\treturn retval == <mask0>.onERC721Received.selector;\t\t} catch (bytes memory reason) {\t\t\tif (reason.length == 0) {\t\t\t\trevert TransferToNonERC721ReceiverImplementer();\t\t\t} else {\t\t\t\tassembly {\t\t\t\t\trevert(add(32, reason), mload(reason))\t\t\t\t}\t\t\t}\t\t}\t}"