"function transferFrom(address from, address to, uint256 tokenId) public override {        require(<mask0>, \"CosmoMasks: transfer caller is not owner nor approved\");        _transfer(from, to, tokenId);    }"