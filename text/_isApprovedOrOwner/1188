"function transferFrom(address from, address to, uint256 tokenId) public virtual override {        require(<mask0>, \"ERC721: caller is not token owner nor approved\");        TOKENS[tokenId]._owner = to;        _transfer(from, to, tokenId);"