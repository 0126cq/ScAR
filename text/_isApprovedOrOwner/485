"function safeTransferFrom(        address from,        address to,        uint256 tokenId,        bytes memory _data    ) public virtual override(ERC721) {        require(<mask0>);        require(hasAuction(tokenId) == false);        _safeTransfer(from, to, tokenId, _data);    }"