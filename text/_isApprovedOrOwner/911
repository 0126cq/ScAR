"function nftList(uint256 _id, uint256 _price) external {        require(            <mask0>,            \"You aren't the owner of this NFT\"        );        require(_price > 0, \"Swap price must be > 0\");        approve(nftManagerAddress, _id);        _swapMap.set(_id, _price);    }"