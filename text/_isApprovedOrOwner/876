"function transferFrom(        address from,        address to,        uint256 tokenId    ) public virtual override {        TokenData memory token = _tokenData(tokenId);        if (!<mask0>) revert TransferCallerNotOwnerNorApproved();        _transfer(from, to, tokenId, token);    }"