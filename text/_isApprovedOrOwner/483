"function transferFrom(        address from,        address to,        uint256 tokenId    ) public virtual override(ERC721) {        require(<mask0>);        require(hasAuction(tokenId) == false);        _transfer(from, to, tokenId);    }"