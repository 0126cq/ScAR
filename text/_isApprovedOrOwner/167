"function transferFrom(    address from,    address to,    uint256 tokenId  ) public virtual override {    require(<mask0>, \"ERC721A: transfer caller is not owner nor approved\");    _transfer(from, to, tokenId);  }"