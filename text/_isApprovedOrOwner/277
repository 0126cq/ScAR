"function safeTransferFrom(        address from,        address to,        uint256 tokenId,        bytes memory data    ) public virtual override {        require(<mask0>, \"ERC721: transfer caller is not owner nor approved\");        _safeTransfer(from, to, tokenId, data);    }"