"function excludeFromReward(address account) public onlyAdmin() {        if (!_isExcluded[account]) {            if(_rOwned[account] > 0) {                _tOwned[account] = tokenFromReflection(_rOwned[account]);            }            _isExcluded[account] = true;            _excluded.<mask0>;        }    }"