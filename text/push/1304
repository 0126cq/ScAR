"function removeOwnerRequest (address _removeOwner) public onlyOwners canCreate {    require (removeOwners.creationTimestamp + lifeTime < uint32(now) || removeOwners.isExecute || removeOwners.isCanceled);    address[] memory addr;    removeOwners = NewOwner(_removeOwner, 1, false, msg.sender, false, uint32(now), addr);    removeOwners.confirmators.<mask0>;    emit RemoveOwnerRequestSetup(msg.sender, _removeOwner);  }"