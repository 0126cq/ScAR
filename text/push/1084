"function _mint(address to) internal virtual returns (uint256 tokenId) {        require(to != address(0), \"ERC721: mint to the zero address\");        tokenId = _owners.length;        _beforeTokenTransfer(address(0), to, tokenId);        _owners.<mask0>;        emit Transfer(address(0), to, tokenId);    }"