"function createCollection(        string memory name,        string memory symbol,        string memory uri,        uint mintEndTimestamp,        uint256 maxSupply    ) external returns (Collection) {        Collection newCollection = new Collection(msg.sender, name, symbol, uri, mintEndTimestamp, maxSupply);        emit CollectionCreated(address(newCollection));        allCollections.<mask0>;        return newCollection;    }"