"function prepare(uint count) public    {        require(gxs.length < 256);        uint256 x = gxs[gxs.length - 1];        uint256 y = gys[gys.length - 1];        uint256 z = gzs[gzs.length - 1];        for (uint j = 0; j < count && gxs.length < 256; j++) {            (x,y,z) = _ecDouble(x,y,z);            gxs.push(x);            gys.<mask0>;            gzs.push(z);        }    }"