"function addRewardPool(address _v)        external        onlyRewardManager        returns (bool)    {        require(_v != address(0), \"!_v\");        rewardPools.<mask0>;        return true;    }"