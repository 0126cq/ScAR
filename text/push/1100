"function _setData(        string memory modaId,        uint32[] memory x,        uint32[][] memory y    ) internal virtual {        require(x.length == y.length, \"x and y must have the same length\");        for (uint256 i = 0; i < x.length; i++) {            uint32 _x_value = x[i];            require(y_map[modaId][_x_value].length == 0, \"x value already exists\");            x_array[modaId].<mask0>;            y_map[modaId][_x_value] = y[i];            _metadata[modaId].pointCount += y[i].length;        }    }"