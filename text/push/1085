"function createBet(        string memory metadataURI,        uint256 end,        uint256 choices    ) external payable override onlyOwner {        uint256 index = _bets.length;        _snapshot();        uint256 _end = block.timestamp + end;        bet__ memory __bet;        __bet.totalReward = msg.value;        __bet.metadataURI = metadataURI;        __bet.end = _end;        __bet.choices = choices;        _bets.<mask0>;        emit betCreated(index, metadataURI, msg.value, _end);    }"