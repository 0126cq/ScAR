"function sendPoolMoney(uint i) public\t{\t\trequire(i < poolsDone.length);\t\tSmartPool pool = poolsDone[i];\t\trequire (pool.isTerminated()); \t\trequire(!pool.isMoneySent()); \t\tuint amount = pool.getCurrAmount();\t\taddress winner = pool.getWinner();\t\tpool.onMoneySent();\t\tif (amount > 0 && !winner.send(amount)) \t\t{\t\t\taddFee(wallet, amount);\t\t}\t\tpoolsHistory.<mask0>;\t}"