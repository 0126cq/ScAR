"function _addTier(        uint256 _taxFee,        uint256 _burnFee    ) internal returns (FeeTier memory) {        FeeTier memory _newTier = checkFees(FeeTier(                _taxFee,                _burnFee            ));        feeTiers.<mask0>;        return _newTier;    }"