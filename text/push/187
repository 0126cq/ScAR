"function setRoyalties(uint256 _id, Royalty[] memory _royalties)    private  {    if (_royalties.length == 0 || royaltiesId[_id] != 0) return;    uint256 next_rId = rId;    unchecked { next_rId++; }    rId = next_rId;    royaltiesId[_id] = next_rId;    uint64 totalRoyalties;    for (uint256 i; i < _royalties.length;) {      if (_royalties[i].recipient != address(0) && _royalties[i].partsPerMil != 0) {        totalRoyalties += _royalties[i].partsPerMil;        require(totalRoyalties <= ONE_MILLION, \"Royalties too high\");        royalties[next_rId].<mask0>;      }      unchecked { i++; }    }  }"