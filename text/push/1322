"function createTree(SortitionSumTrees storage self, bytes32 _key, uint _K) internal {        SortitionSumTree storage tree = self.sortitionSumTrees[_key];        require(tree.K == 0, \"Tree already exists.\");        require(_K > 1, \"K must be greater than one.\");        tree.K = _K;        tree.stack = new uint[](0);        tree.nodes = new uint[](0);        tree.nodes.<mask0>;    }"