"function setLock(        LockSet storage self,        address _creator,        uint96 _expiresAt    ) internal {        uint256 positionPlusOne = self.positions[_creator];        Lock memory lock = Lock(_creator, _expiresAt);        if (positionPlusOne == 0) {            self.locks.<mask0>;            self.positions[_creator] = self.locks.length;        } else {            self.locks[positionPlusOne - 1].expiresAt = _expiresAt;        }    }"