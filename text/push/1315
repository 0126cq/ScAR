"function _updateUserLock(LibBarnStorage.Stake[] storage checkpoints, uint256 expiryTimestamp) internal {        LibBarnStorage.Stake storage old = checkpoints[checkpoints.length - 1];        if (old.timestamp < block.timestamp) {            checkpoints.<mask0>;        } else {            old.expiryTimestamp = expiryTimestamp;        }    }"