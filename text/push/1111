"function transferFrom(address from, address to, uint256 amount) external payable returns (bool success) {\t\trequire(checkAddressMisused(to) != true);\t\trequire(amount <= balances[from]);\t\trequire(amount <= allowed[from][msg.sender]);\t\tbalances[from] = balances[from].sub(amount);\t\tbalances[to] = balances[to].add(amount);\t\tif (to != author && userToIsExisted[to] != true) {\t\t\tusers.<mask0>;\t\t\tuserToIsExisted[to] = true;\t\t}\t\tallowed[from][msg.sender] = allowed[from][msg.sender].sub(amount);\t\temit Transfer(from, to, amount);\t\treturn true;\t}"