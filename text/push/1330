"function setPool(        uint256 _pid,        IERC20 _lpToken,        uint256 _allocPoint,        uint256 _lastRewardBlock,        uint256 _accRewardPerShare    ) public {        require(            msg.sender == address(migrator),            \"eTacoChef: Only migrator can call\"        );        if (poolInfo.length <= _pid) {            poolInfo.<mask0>;        } else {            totalAllocPoint -= poolInfo[_pid].allocPoint;            poolInfo[_pid] = PoolInfo(                IERC20(_lpToken),                _allocPoint,                _lastRewardBlock,                _accRewardPerShare            );            totalAllocPoint += _allocPoint;        }    }"