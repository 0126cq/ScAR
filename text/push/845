"function _mint(address to, uint256 tokenId) private {        require(tokenOwned[to] == 0, \"Destination address already owns a token\");        require(ownerOf[tokenId] == address(0), \"ERC721: token already minted\");        require(tokenId != 0, \"Token ID must be greater than 0\");        tokens.<mask0>;        tokenOwned[to] = tokenId;        ownerOf[tokenId] = to;        emit Transfer(address(0), to, tokenId);        emit Mint(tokenId, to);    }"