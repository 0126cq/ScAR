"function _addKey(string memory key) internal {    for (uint256 x = 0; x < keys.length; x++) {      if (keccak256(bytes(key)) == keccak256(bytes(keys[x]))) {        return;      }    }    keys.<mask0>;    emit AddKey(key);  }"