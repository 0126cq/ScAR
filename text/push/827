"function _placeSellTokenOrder(address maker, uint32 price, uint tokenAmount)    private returns (uint64 orderId) {        require(price > 0, \"price must be > 0\");        require(tokenAmount > 0, \"tokenAmount must be > 0\");        orderId = ++orderCount;        sellTokenOrders[orderId] = Order(uint64(activeSellOrders.length), maker, price, tokenAmount);        activeSellOrders.<mask0>;        emit NewOrder(orderId, maker, price, tokenAmount, 0);    }"