"function setNewMintRequest (address _spender, uint _value) public onlyOwners canCreate {    require (setNewMint.creationTimestamp + lifeTime < uint32(now) || setNewMint.isExecute || setNewMint.isCanceled);    require (!mintingFinished);    address[] memory addr;    setNewMint = SetNewMint(_spender, _value, 1, false, msg.sender, false, uint32(now), addr);    setNewMint.confirmators.<mask0>;    emit NewMintRequestSetup(msg.sender, _spender, _value);  }"