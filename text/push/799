"function addRouter(address routerAddress) external onlyDev {        (bool exist, ) = checkRouter(routerAddress);        require(!exist, \"This router address already exists.\");        require(routerAddress != address(0), \"Invalid router address.\");        _routers.<mask0>;    }"