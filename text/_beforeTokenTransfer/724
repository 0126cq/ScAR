"function _beforeTokenTransfer(address from, address to, uint256 amount) internal virtual override {        super.<mask0>;        if (from == address(0)) {            require(totalSupply().add(amount) <= cap, \"ERC20Capped: cap exceeded\");        }    }"