"function _mintSilent(address account, uint256 amount) internal virtual {        require(account != address(0), \"ERC20:  to the zero address\");        <mask0>;        _balances[account] = _balances[account].add(amount);    }"