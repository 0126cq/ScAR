"function _beforeTokenTransfer(        address from,        address to,        uint256 amount    ) internal virtual override {        super.<mask0>;        if (from != address(0)) {            _updateOwnerReward(_ownerRewardInfo[from], from);        }        _updateOwnerReward(_ownerRewardInfo[to], to);    }"