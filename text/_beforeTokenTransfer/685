"function _beforeTokenTransfer(address from, address to, uint256 amount) internal override {        super.<mask0>;        if (from == address(0)) {             require(totalSupply().add(amount) <= MAX_SUPPLY, \"max-supply-exceeded\");        }    }"