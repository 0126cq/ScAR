"function _transfer(address from, address to, uint256 tokenId) internal virtual {    require(ERC721.ownerOf(tokenId) == from, \"ERC721: transfer of token that is not own\");    require(to != address(0), \"ERC721: transfer to the zero address\");    <mask0>;    _approve(address(0), tokenId);    _owners[tokenId-1] = to;    emit Transfer(from, to, tokenId);  }"