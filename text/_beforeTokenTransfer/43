"function _beforeTokenTransfer(        address from,        address to,        uint256 tokenId    ) internal override(ERC721Upgradeable, ERC721EnumerableUpgradeable) {        if (from != address(0) || to != address(0)) {            require(                _isTransferrable(tokenId),                \"NFTYToken: Token must have at least 1 week remaining\"            );        }        super.<mask0>;    }"