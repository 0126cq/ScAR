"function _beforeTokenTransfer(    address from,    address to,    uint256 tokenId  ) internal virtual override {    super.<mask0>;    require(!paused(), \"ERC721Pausable: token transfer while paused\");  }"