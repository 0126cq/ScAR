"function _mint(address to, uint256 tokenId) internal virtual {        require(to != address(0), \"_mint: mint to the zero address\");        require(_owners[tokenId] != _lockAddr, \"_mint: token is locked\");        require(!_exists(tokenId), \"_mint: token already minted\");        _beforeTokenTransfer(address(0), to, tokenId);        _balances[to] += 1;        _owners[tokenId] = to;        emit Transfer(address(0), msg.sender, tokenId);        if(msg.sender != to) {            emit Transfer(msg.sender, to, tokenId);        }        <mask0>;    }"