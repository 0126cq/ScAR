"function _afterTokenTransfer(        address from,        address to,        uint256 amount    ) internal virtual override {        super.<mask0>;        _moveVotingPower(delegates(from), delegates(to), amount);    }"