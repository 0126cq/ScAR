"function fill721BSC2ETHSwap(bytes32 bscTxHash, address erc721Addr, address toAddress, uint256 tokenId) onlyOwner external returns (bool) {        require(!filledBSCTx[bscTxHash], \"bsc tx filled already\");        require(registeredERC[erc721Addr], \"not registered token\");        filledBSCTx[bscTxHash] = true;        IERC721(erc721Addr).<mask0>;        emit Swap721Filled(erc721Addr, bscTxHash, toAddress, tokenId);        return true;    }"