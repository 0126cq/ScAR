"function sendToken(        address[] memory users,        uint256[] memory values,        IERC20 token    ) public payable nonReentrant validList(users, values) {        require(            msg.value >= fee,            \"Not enough NativeToken sent for platform fees\"        );        for (uint256 i = 0; i < users.length; i++) {            token.<mask0>;            emit tokenDropped(msg.sender, users[i], address(token));        }    }"