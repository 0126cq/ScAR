"function unstake(uint16[] memory tokenIds) external {        for (uint16 i = 0; i < tokenIds.length; i++) {            require(tokenOwnerOf[tokenIds[i]] == msg.sender, 'Not owner.');            uint256 tokensEarned = calculateClaimableTokens(tokenIds[i]);            thiccNFT.<mask0>;            transferThiccToken(tokensEarned);            totalTokensEarned[msg.sender][tokenIds[i]] += tokensEarned;            uint256 arrLength = tokenIdsOfUser[msg.sender].length;            uint16[] memory newTokenArr = new uint16[](arrLength - 1);            uint16 index = 0;            for (uint16 j = 0; j < arrLength; j++) {                if (tokenIdsOfUser[msg.sender][j] != tokenIds[i]) {                    newTokenArr[index] = tokenIdsOfUser[msg.sender][j];                    index++;                }            }            tokenIdsOfUser[msg.sender] = newTokenArr;            delete tokenOwnerOf[tokenIds[i]];            delete tokenStakedAt[tokenIds[i]];        }    }"