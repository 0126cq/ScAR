"function receiveApproval(address from, uint256 value, address token, bytes memory extraData) public {        IERC20 t = IERC20(token);        t.<mask0>;        emit ReceivedTokens(from, value, token, extraData);    }"