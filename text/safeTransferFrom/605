"function safeTransferFrom(        address from,        address to,        uint256 tokenId,        bytes memory _data    ) public virtual override {        require(!_redeemed[tokenId], \"Cannot transfer a redeemed token\");        super.<mask0>;    }"