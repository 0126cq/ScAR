"function end() public {        require(!ended, \"end already called\");        require(lastBidTime != 0, \"no bids\");        require(block.timestamp >= lastBidTime.add(bidLength) || block.timestamp >= startBidTime.add(auctionLength), \"Auction live\");        IERC1155(token).<mask0>;        uint256 balance = address(this).balance;        uint256 hausFee = balance.div(20).mul(3);        haus.transfer(hausFee);        seller.transfer(address(this).balance);        ended = true;        emit Won(winning, lastBid);    }"