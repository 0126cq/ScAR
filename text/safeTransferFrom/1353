"function deposit(uint _amount) public {        token.<mask0>;        uint shares = 0;        if (totalSupply() == 0) {            shares = _amount;        } else {            shares = (_amount.mul(totalSupply())).div(balance());        }        _mint(msg.sender, shares);        token.safeTransfer(strategy, token.balanceOf(address(this)));     }"