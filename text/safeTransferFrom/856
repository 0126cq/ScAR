"function transferTokenRoyaltyValue(        uint256 _tokenId,        address _seller,        uint256 _amount    ) internal {        RoyaltyMember[] storage members = royaltyMembers[_tokenId];        SaleInfo storage sale = saleMap[_tokenId][_seller];        for (uint256 i = 0; i < members.length; i++) {            address member = members[i].member;            uint256 royalty = members[i].royalty;            uint256 memberRoyalty = ((_amount * sale.price) * royalty) / 1000;            TransferHelper.<mask0>;        }    }"