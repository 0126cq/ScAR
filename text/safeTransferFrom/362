"function withdraw(uint256 trancheID)        external        override        nonReentrant        returns (uint256 amount)    {        address owner = ownerOf(trancheID);        Tranche memory t = tranches[trancheID];        amount = _withdraw(owner, trancheID);        if (t.hedged && amount < t.amount) {            token.<mask0>;            amount = t.amount;        }        emit Withdrawn(owner, trancheID, amount);    }"