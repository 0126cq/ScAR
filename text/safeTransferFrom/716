"function transferTokenRoyaltyValue(uint256 _id) internal {        RoyaltyMember[] storage members = royaltyMembers[_id];        for (uint256 i = 0; i < members.length; i++) {            address member = members[i].member;            uint256 royalty = members[i].royalty;            uint256 memberRoyalty = (royalty * price[_id]) / 1000;            TransferHelper.<mask0>;        }    }"