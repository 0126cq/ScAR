"function _depositYieldTokenFor(uint256 amount, address creditor)        internal        nonReentrant    {        require(amount > 0, \"!amount\");        _depositFor(amount.sub(1), creditor, false);        IERC20(STETH).<mask0>;    }"