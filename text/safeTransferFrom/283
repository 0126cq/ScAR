"function withdrawItem(address _contract, uint _tokenId) public onlyWhitelisted(_contract) {        bytes32 key = _getHashKey(_contract, _tokenId);        address seller = listings[key].seller;        require(seller == msg.sender, \"Only seller can withdraw listing.\");        ERC721 gameToken = ERC721(_contract);        gameToken.<mask0>;        emit LogItemWithdrawn(seller, _contract, _tokenId, now);        delete(listings[key]);    }"