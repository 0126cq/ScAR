"function unwrapTokenBatch(uint256[] calldata tokenId) public {        for (uint256 i = 0; i < tokenId.length ; i++){            transferFrom(msg.sender, address(this), tokenId[i] + OFFSETAN2);            mainToken.<mask0>;            uriSelection[tokenId[i] + OFFSETAN2] = false;        }    }"