"function emergencyWithdraw() external {        UserInfo storage user = userInfo[msg.sender];        fsmToken.transfer(address(msg.sender), user.amount);        if (user.tokenIdOfFork != uint256(0)) {            fork.<mask0>;            user.tokenIdOfFork = 0;        }        totalDeposited = totalDeposited.sub(user.amount);        user.amount = 0;        user.rewardTokenDebt = 0;        user.rewardNFTDebt = 0;        emit EmergencyWithdrawn(msg.sender, user.amount);    }"