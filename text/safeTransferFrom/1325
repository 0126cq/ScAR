"function swap1155ETH2BSC(address erc1155Addr, uint256 tokenId, uint256 amount) payable external nonReentrant returns (bool) {        require(registeredERC[erc1155Addr], \"not registered token\");        require(msg.value == swapFee, \"swap fee not equal\");        IERC1155(erc1155Addr).<mask0>;        if (msg.value != 0) {            owner.transfer(msg.value);        }        emit Swap1155Started(erc1155Addr, msg.sender, tokenId, amount, msg.value);        return true;    }"