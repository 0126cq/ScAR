"function _processGysr(uint256 spent, uint256 vested) private {        if (spent > 0) {            _gysr.<mask0>;        }        if (vested > 0) {            uint256 fee = (vested * _factory.fee()) / 10**DECIMALS;            if (fee > 0) {                _gysr.safeTransfer(_factory.treasury(), fee);            }            _gysrVested = _gysrVested + vested - fee;        }    }"