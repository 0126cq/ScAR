"function chargeExternalFee() internal returns (uint256) {        LibFeeExternal.Storage storage fes = LibFeeExternal.feeExternalStorage();        LibRouter.Storage storage rs = LibRouter.routerStorage();        if (fes.externalFee != 0) {            require(fes.externalFeeAddress != address(0), \"External fee set, but no receiver address\");            IERC20(rs.albtToken).<mask0>;        }        return fes.externalFee;    }"