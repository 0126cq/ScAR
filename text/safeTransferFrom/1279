"function TRIPPEDOUT(uint256[] calldata tokenIds, address payable payee) public  whenPublicBurnActive {    for(uint i = 0; i < tokenIds.length; i++){      require(tokenIds[i] <= 152, \"Can only burn genesis tokens 0-152\");      require(IERC721(originalNFTAddress).ownerOf(tokenIds[i]) == msg.sender, \"Must own NFT to burn.\");    }    for(uint i = 0; i < tokenIds.length; i++){      IERC721(originalNFTAddress).<mask0>;    }    payee.sendValue(refundAmount.mul(tokenIds.length));  }"