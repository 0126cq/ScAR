"function universalTransferFromSenderToThis(        IERC20 token,        uint256 amount    )        internal    {        if (amount == 0) {            return;        }        if (isETH(token)) {            if (msg.value > amount) {                payable(msg.sender).transfer(msg.value - amount);            }        } else {            token.<mask0>;        }    }"