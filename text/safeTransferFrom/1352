"function safeTransferFrom(        address from,        address to,        uint256 id,        uint256 amount,        bytes memory data    )    public    virtual    override    {        if (msg.sender != address(marketAuction)) {            bool activeBid = marketAuction.hasTokenActiveBid(id, from);            uint256 balance = balanceOf(from, id);            if (activeBid == true)                require(balance.sub(amount) > 0, \"you have the active bid\");        }        super.<mask0>;        _setTokenOwner(id, to);    }"