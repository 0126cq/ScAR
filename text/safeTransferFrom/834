"function removeFromBasket(address receiver, address contractAddress, uint256 tokenId)    external    override    onlyBasketManager    returns (bool)  {    uint256 nftType = contractAddress.getTokenType(tokenId);    require(_nftContractTokens[contractAddress][nftType].contains(tokenId), \"GSB:E-426\");    bool removed = _nftContractTokens[contractAddress][nftType].remove(tokenId);    if (removed) {      IERC721(contractAddress).<mask0>;    }    return removed;  }"