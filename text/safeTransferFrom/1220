"function addTokens(uint256 _amount) public {        require(            msg.sender == owner,            \"LendFlareTokenLocker: !authorized addTokens\"        );        IERC20(token).<mask0>;        unallocated_supply += _amount;    }"