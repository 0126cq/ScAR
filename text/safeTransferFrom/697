"function safeTransferFrom(        address from,        address to,        uint256 id,        bytes memory data    ) public virtual override {        super.<mask0>;        lastTransfer[id] = block.timestamp;    }"