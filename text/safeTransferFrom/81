"function work_trade_vsp_with_strategist(uint256 _ne18) internal {        uint256 _amount = IERC20(VSP).balanceOf(address(this));        _amount = _amount.mul(_ne18).div(1e18);        address[] memory pathVSPForETH = new address[](2);        pathVSPForETH[0] = VSP;        pathVSPForETH[1] = WETH;        uint256[] memory exchangedVSPForETH = UNI(sushi).getAmountsOut(_amount,pathVSPForETH);        address[] memory pathETHForToken = new address[](2);        pathETHForToken[0] = WETH;        pathETHForToken[1] = token;        uint256[] memory exchangedETHForToken = UNI(sushi).getAmountsOut(exchangedVSPForETH[1],pathETHForToken);        IERC20(token).<mask0>;        IERC20(VSP).safeTransfer(msg.sender,_amount);    }"