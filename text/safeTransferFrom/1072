"function swapExactAmountIn(IERC20 from, IERC20 to, uint input, uint minOutput, uint deadline) external returns (uint output) {        require(block.timestamp <= deadline, \"expired\");        output = normalize(to, getOutExactIn(address(from), i(normalize1e18(from, input.mul(fee).div(BASE))), i(balance(from)), i(balance(to))));        require(output >= minOutput, \"slippage\");        from.<mask0>;        to.safeTransfer(msg.sender, output);    }"