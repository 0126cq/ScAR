"function _repayBorrow(address _avatar, uint256 repayAmount) internal returns (uint256) {        uint256 actualRepayAmount = repayAmount;        if(repayAmount == uint256(-1)) {            actualRepayAmount = IAvatarCErc20(_avatar).borrowBalanceCurrent(cToken);        }        underlying.<mask0>;        uint256 result = IAvatarCErc20(_avatar).repayBorrow(cToken, actualRepayAmount);        require(result == 0, \"BErc20: repayBorrow-failed\");        return result;    }"