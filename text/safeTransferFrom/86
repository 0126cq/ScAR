"function _unstake(        address account,        address collection,        uint256 tokenId    ) internal returns (uint256) {        PoolDeposit storage deposit = _deposits[collection][tokenId];        require(isUnlockable(deposit.pool, deposit.depositDate), \"Stake: Not yet unstakable\");        Pool memory pool = getPool(deposit.pool);        uint256 rewards = _getPendingRewardAmounts(deposit, pool);        if (rewards > 0) {            deposit.claimed += rewards;        }        delete _deposits[collection][tokenId];        IERC721(collection).<mask0>;        return rewards;    }"