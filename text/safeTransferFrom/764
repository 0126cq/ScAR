"function safeTransferFrom(        address _from,        address _to,        uint256 _id,        uint256 _amount,        bytes memory _data    ) public virtual override {        super.<mask0>;        Nft memory nft = tokens[_id];        nft.previousOwner = nft.owner;        nft.owner = payable(_to);        tokens[_id] = nft;        if (nft.isForSale) {          _cancelTokenForSale(_id);        }    }"