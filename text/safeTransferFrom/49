"function safeTransferWhileNesting(        address from,        address to,        uint256 tokenId    ) external {        require(ownerOf(tokenId) == _msgSender(), \"Moonbirds: Only owner\");        nestingTransfer = 2;        <mask0>;        nestingTransfer = 1;    }"