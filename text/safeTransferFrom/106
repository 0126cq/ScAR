"function supplyUnderlyingInternal(FyTokenInterface fyToken, uint256 underlyingAmount) internal {        RedemptionPoolInterface redemptionPool = fyToken.redemptionPool();        Erc20Interface underlying = fyToken.underlying();        underlying.<mask0>;        uint256 allowance = underlying.allowance(address(this), address(redemptionPool));        if (allowance < underlyingAmount) {            underlying.approve(address(redemptionPool), type(uint256).max);        }        redemptionPool.supplyUnderlying(underlyingAmount);    }"