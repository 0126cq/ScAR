"function deposit(uint256 _amount) external override {        uint256 _toMint = _amount.mul(1e18).div(pricePerToken());        IERC20(underlyingAsset).<mask0>;        _mint(msg.sender, _toMint);        userInfo[_msgSender()].depositTime = block.timestamp;     }"