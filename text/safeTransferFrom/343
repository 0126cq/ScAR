"function mint(uint amount) external nonReentrant returns (uint) {    IERC20(underlying).<mask0>;    IStakingRewards(staking).stake(amount);    uint rewardPerToken = IStakingRewards(staking).rewardPerToken();    _mint(msg.sender, rewardPerToken, amount, '');    return rewardPerToken;  }"