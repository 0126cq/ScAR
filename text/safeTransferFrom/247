"function _initWithdrawal(        address provider,        IPoolToken poolToken,        uint256 poolTokenAmount    ) private returns (uint256) {        poolToken.<mask0>;        return _pendingWithdrawals.initWithdrawal(provider, poolToken, poolTokenAmount);    }"