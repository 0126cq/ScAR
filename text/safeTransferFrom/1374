"function stakeItem(uint256[] memory _tokenIds, uint256 _subjectId) external {        require(!isPausable, \"Contract Paused\");        for (uint256 i = 0; i < _tokenIds.length; i++) {            bytes memory data = abi.encodePacked(_subjectId);            <mask0>;        }    }"