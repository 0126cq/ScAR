"function transferIn(address from, IERC20 token, uint amount) internal returns (uint) {        uint balanceBefore = token.balanceOf(address(this));        if (address(token) == addressConfig.wETH) {            IWETH(address(token)).deposit{value : msg.value}();        } else {            token.<mask0>;        }        uint balanceAfter = token.balanceOf(address(this));        return balanceAfter.sub(balanceBefore);    }"