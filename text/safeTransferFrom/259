"function transferNonFungibleToken(        address collection,        address from,        address to,        uint256 tokenId,        uint256 amount    ) external override {        require(            msg.sender == OPEN_PFP_EXCHANGE,            \"Transfer: Only OpenPFP Exchange\"        );        IERC1155(collection).<mask0>;    }"