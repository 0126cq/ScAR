"function deposit(uint256 _amount)      external      nonReentrant  {    require(_amount > 0, \"deposit must be greater than 0\");    pool = calcPoolValueInToken();    IERC20(token).<mask0>;    uint256 userYeldBalance = yeldToken.balanceOf(msg.sender);    uint256 amountTwoPercent = _amount.mul(2).div(100);    require(userYeldBalance >= amountTwoPercent, 'Your YELD balance must be 2% or higher of the amount to deposit');"