"function processPayment(address token, uint256 amount) public {        if (token == address(0)) {            require(msg.value >= amount, \"invalid payment\");        } else {            IERC20(token).<mask0>;        }    }"