"function approve(address _spender, uint256 _amount) public returns (bool success) {        if (isContract(controller)) {            require (TokenController(controller).onApprove(msg.sender, _spender, _amount));        }        return super.<mask0>;    }"