"function _approveBasic() internal {        want.approve(booster, 0);        want.approve(booster, type(uint256).max);        IERC20(wbtc).<mask0>;        IERC20(wbtc).approve(curve, type(uint256).max);    }"