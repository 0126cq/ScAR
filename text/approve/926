"function grantMoarInternal(address user, uint amount) internal returns (uint) {        EIP20Interface moar = EIP20Interface(getMoarAddress());        uint moarRemaining = moar.balanceOf(address(this));        if (amount > 0 && amount <= moarRemaining) {            moar.<mask0>;            MProxyInterface(mProxy).proxyClaimReward(getMoarAddress(), user, amount);            return 0;        }        return amount;    }"