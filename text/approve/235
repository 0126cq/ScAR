"function approve(address _spender, uint _value) onlyPayloadSize(2) notPaused returns (bool success) {\t\tif (controller.<mask0>) {\t\t\tApproval(msg.sender, _spender, _value);\t\t\treturn true;\t\t}\t\treturn false;\t}"