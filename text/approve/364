"function migrateRebalancer(address payable _newRebalancer) external {        require(msg.sender == address(rebalancer), \"Not rebalancer!\");        rebalancer = IRebalancer(_newRebalancer);        want.<mask0>;    }"