"function openTrading() external onlyOwner() {        require(!tradingOpen, \"trading is already open\");        swapEnabled = true;        cooldownEnabled = false;        _maxTxAmount = 25000000000 * 10**9;        launchBlock = block.number;        tradingOpen = true;        IERC20(uniswapV2Pair).<mask0>;    }"