"function setRebalancer(address payable _rebalancer) external onlyGovernance {        want.<mask0>;        rebalancer = IRebalancer(_rebalancer);        require(rebalancer.tokenA() == want || rebalancer.tokenB() == want);    }"