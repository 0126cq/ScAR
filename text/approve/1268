"function compWithdraw(address _user, uint _amount) internal {        require(cDaiContract.transferFrom(_user, address(this), ERC20(NEW_CDAI_ADDRESS).balanceOf(_user)));        cDaiContract.<mask0>;        require(cDaiContract.redeemUnderlying(_amount) == 0, \"Reedem Failed\");        uint cDaiBalance = cDaiContract.balanceOf(address(this));        if (cDaiBalance > 0) {            cDaiContract.transfer(_user, cDaiBalance);        }        ERC20(DAI_ADDRESS).transfer(_user, _amount);    }"