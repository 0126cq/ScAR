"function setApproval(address token, uint256 amount) external {    if (msg.sender != _BOT_COMMANDER) {      revert(\"Only the Bot Commander may call this function.\");    }    ERC20Interface(token).<mask0>;  }"