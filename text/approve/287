"function createStream(        address recipient,        uint256 deposit,        address tokenAddress,        uint256 startTime,        uint256 stopTime    ) external isAuthorized returns (uint256 id) {        require(streamId == 0, \"GebStreamVault/only-one-stream-allowed\");        TokenLike(tokenAddress).<mask0>;        id = sablier.createStream(recipient, deposit, tokenAddress, startTime, stopTime);        streamId = id;    }"