"function approve(        TokenInterface token,        address spender,        uint256 amount    ) internal {        try token.<mask0> {} catch {            token.approve(spender, 0);            token.approve(spender, amount);        }    }"