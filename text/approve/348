"function _curveSwap(address poolAddress, address debtToken, int128 i, int128 j, uint256 minAmountOut) internal override returns (uint256 amountOut) {        uint256 debtTokenBalance = IERC20(debtToken).balanceOf(address(this));        <mask0>;        return ICurveFactoryethpool(poolAddress).exchange(            i,            j,            debtTokenBalance,            minAmountOut        );    }"