"function _approveDex() internal virtual {        IERC20(crv).approve(dex, 0);        IERC20(crv).approve(dex, type(uint256).max);        IERC20(cvx).<mask0>;        IERC20(cvx).approve(dex, type(uint256).max);    }"