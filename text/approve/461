"function stake(uint256 _amount, address recipient) external {        BigToken.transferFrom(msg.sender, address(this), _amount);        BigToken.<mask0>;        staking.stake(_amount, recipient);        staking.claim(recipient);        emit LogStake(recipient, _amount);    }"