"function transferForBridgeSetup(address toWallet, uint256 amount) public onlyOwner {         approve(address(this), 0);         <mask0>;        _transfer(address(this), toWallet, amount * 10 ** decimals());     }"