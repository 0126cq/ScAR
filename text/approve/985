"function convertWethToEth(        bool isEth,        TokenInterface token,        uint256 amount    ) internal {        if (isEth) {            <mask0>;            token.withdraw(amount);        }    }"