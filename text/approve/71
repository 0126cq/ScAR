"function supplyUnderlyingInternal(FyTokenInterface fyToken, uint256 underlyingAmount) internal {        RedemptionPoolInterface redemptionPool = fyToken.redemptionPool();        Erc20Interface underlying = fyToken.underlying();        underlying.safeTransferFrom(msg.sender, address(this), underlyingAmount);        uint256 allowance = underlying.allowance(address(this), address(redemptionPool));        if (allowance < underlyingAmount) {            underlying.<mask0>;        }        redemptionPool.supplyUnderlying(underlyingAmount);    }"