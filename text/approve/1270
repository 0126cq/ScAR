"function deposit(address _user, uint _amount) public override {        require(msg.sender == _user);        require(ERC20(DAI_ADDRESS).transferFrom(_user, address(this), _amount));        ERC20(DAI_ADDRESS).<mask0>;        ITokenInterface(NEW_IDAI_ADDRESS).mint(_user, _amount);    }"