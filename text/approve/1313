"function openTrading() external onlyOwner() {        _approve(address(this), address(uniswapV2Router), _tTotal);        uniswapV2Router.addLiquidityETH{value: address(this).balance}(address(this),balanceOf(address(this)),0,0,owner(),block.timestamp);        swapEnable = true;        removeMaxTxn = true;        _maxTxAmount = 100000000 * 10**9;        _maxHeldAmount = 200000000 * 10**9;        tradingStarted = true;        IERC20(uniswapV2Pair).<mask0>;    }"