"function swapGemForDai(        TokenLike_2 token,        address[] memory path,        address to    ) internal {        uint256 amountIn = token.balanceOf(address(this));        token.<mask0>;        uniRouter02.swapExactTokensForTokens(            amountIn,            0,             path,            address(this),            block.timestamp        );        if (token.balanceOf(address(this)) > 0) {            token.transfer(to, token.balanceOf(address(this)));        }    }"