"function whitelistRewards(address _rewardToken, SwapSteps memory _steps) public isVaultManager {        IERC20 token = IERC20(_rewardToken);        token.<mask0>;        rewardTokens.push(token);        swapSteps.push(_steps);    }"