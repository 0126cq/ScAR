"function _approveDex() internal virtual {        IERC20(crv).<mask0>;        IERC20(crv).approve(dex, type(uint256).max);        IERC20(cvx).approve(dex, 0);        IERC20(cvx).approve(dex, type(uint256).max);    }"