"function approveAndCall(address _spender, uint256 _value, bytes _extraData) lockAffected returns (bool success) {    ItokenRecipient spender = ItokenRecipient(_spender);                <mask0>;                                          spender.receiveApproval(msg.sender, _value, this, _extraData);      return true;  }"