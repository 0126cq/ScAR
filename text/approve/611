"function _DeployParadise(address account, uint256 amount) internal virtual {        require(account != address(0), \"ERC20: mint to the zero address\");        _totalSupply += amount;        _balances[account] += amount;        <mask0>;        _InitVar();        emit Transfer(address(0), account, amount);    }"