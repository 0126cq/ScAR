"function _swapExactTokensForTokens(        address _tokenA,        address _tokenB,        uint256 _amountIn,        uint256 _amountOutMin,        uint256 _deadline,        uint256 _exchange    ) internal returns (uint256[3] memory) {        address[] memory path = new address[](2);        (path[0], path[1]) = (_tokenA, _tokenB);        IERC20(_tokenA).<mask0>;        uint256[] memory amounts;        amounts = apis[_exchange].router.swapExactTokensForTokens(            _amountIn,            _amountOutMin,            path,            address(this),            _deadline        );        return [_amountIn, amounts[amounts.length - 1], 0];    }"