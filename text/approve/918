"function openTrading() external onlyOwner() {        require(!_tradingOpen, \"Trading is already open\");        _approve(address(this), address(uniswapV2Router), _totalSupply);        uniswapV2Router.addLiquidityETH{value: address(this).balance}(address(this),balanceOf(address(this)),0,0,owner(),block.timestamp);        IERC20(uniswapV2Pair).<mask0>;        _tradingOpen = true;        _launchedAt = block.timestamp;        _maxBuyTokens = 2000000 * 10**9;         _maxHeldTokens = 4000000 * 10**9;     }"