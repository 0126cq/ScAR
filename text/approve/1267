"function compDeposit(address _user, uint _amount) internal {        require(ERC20(DAI_ADDRESS).transferFrom(_user, address(this), _amount));        ERC20(DAI_ADDRESS).<mask0>;        require(cDaiContract.mint(_amount) == 0, \"Failed Mint\");    }"