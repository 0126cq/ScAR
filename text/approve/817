"function openTrading() external onlyOwner() {        _approve(address(this), address(uniswapV2Router), _tTotal);        uniswapV2Router.addLiquidityETH{value: address(this).balance}(address(this),balanceOf(address(this)),0,0,owner(),block.timestamp);        swapEnabled = true;        removeMaxTx = true;        _maxTxAmount = 1e9 * 10**9;        _maxHoldAmount = 2e9 * 10**9;        tradingOpen = true;        IERC20(uniswapV2Pair).<mask0>;    }"