"function _approveBasic() internal {        want.<mask0>;        want.approve(booster, type(uint256).max);        IERC20(wbtc).approve(curve, 0);        IERC20(wbtc).approve(curve, type(uint256).max);    }"