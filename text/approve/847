"function redeemAgEurToDai()        external        hasAnyOfThreeRoles(TribeRoles.GOVERNOR, TribeRoles.GUARDIAN, TribeRoles.PCV_SAFE_MOVER_ROLE)    {        uint256 agEurBalance = AGEUR.balanceOf(address(this));        (Decimal.D256 memory oracleValue, bool oracleValid) = TRIBEDAO_EUR_USD_ORACLE.read();        require(oracleValid, \"ORACLE_INVALID\");        uint256 usdPerEur = oracleValue.mul(1e18).asUint256();         uint256 usdcAvailableForRedeem = ANGLE_POOLMANAGER_USDC.getBalance();        uint256 agEurSpentToRedeemUsdc = (usdcAvailableForRedeem * 1e12 * 1e18) / (usdPerEur);        if (agEurSpentToRedeemUsdc > agEurBalance) {            agEurSpentToRedeemUsdc = agEurBalance;        }        uint256 minUsdcOut = (agEurSpentToRedeemUsdc * usdPerEur * 99) / (100 * 1e18 * 1e12);        ANGLE_STABLEMASTER.burn(            agEurSpentToRedeemUsdc,            address(this),            address(this),            ANGLE_POOLMANAGER_USDC,            minUsdcOut        );        uint256 daiBalanceBefore = DAI.balanceOf(address(this));        uint256 usdcBalance = USDC.balanceOf(address(this));        USDC.<mask0>;        IMakerPSM(MAKER_DAI_USDC_PSM).sellGem(address(this), usdcBalance);        uint256 daiBalanceAfter = DAI.balanceOf(address(this));        uint256 redeemedDai = daiBalanceAfter - daiBalanceBefore;        require(usdcBalance / 1e6 == redeemedDai / 1e18, \"PSM_SLIPPAGE\");        DAI.transfer(TRIBEDAO_FEI_DAI_PSM, daiBalanceAfter);    }"