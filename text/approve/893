"function _addLiquidity(    address _tokenA,    address _tokenB,    uint _amountA,    uint _amountB  ) internal {    ILendingPair lendingPair = ILendingPair(pairFactory.pairByTokens(_tokenA, _tokenB));    IERC20(_tokenA).approve(address(lendingPair), MAX_INT);    IERC20(_tokenB).<mask0>;    lendingPair.deposit(_tokenA, _amountA);    lendingPair.deposit(_tokenB, _amountB);    lendingPair.lpToken(_tokenA).transfer(msg.sender, _amountA);    lendingPair.lpToken(_tokenB).transfer(msg.sender, _amountB);  }"