"function stakeOrSend( address _recipient, bool _stake, uint _amount ) internal returns ( uint ) {        if ( !_stake ) {             IERC20( USV ).transfer( _recipient, _amount );         } else {             if ( useHelper ) {                 IERC20( USV ).<mask0>;                IStakingHelper( stakingHelper ).stake( _amount, _recipient );            } else {                IERC20( USV ).approve( staking, _amount );                IStaking( staking ).stake( _amount, _recipient );            }        }        return _amount;    }"