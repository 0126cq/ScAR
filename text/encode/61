"function swapV3ToSushi(        uint256 amountIn,        address token,        uint24 fee,        uint256 payETHToCoinbase    ) external payable {        bool zeroForOne = Constants.WETH < token;        uint8 swapType = 5;        getPool(Constants.WETH, token, fee).swap(            address(this),             zeroForOne,            int256(amountIn),            zeroForOne                ? TickMath.MIN_SQRT_RATIO + 1                : TickMath.MAX_SQRT_RATIO - 1,            abi.<mask0>        );    }"