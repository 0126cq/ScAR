"function tokenURI(uint256 tokenId)        public        view        override        returns (string memory)    {        string memory metadata = tokenMetadata(tokenId);        return            string(                abi.encodePacked(                    \"data:application/json;base64,\",                    Base64.<mask0>                )            );    }"