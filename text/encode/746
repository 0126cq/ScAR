"function calculateSigHash(        bytes32 pHash,        uint256 amount,        bytes32 selectorHash,        address to,        bytes32 nHash    ) internal pure returns (bytes32) {        return keccak256(abi.<mask0>);    }"