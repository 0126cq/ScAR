"function logTransferToSelfCustody(        address selfCustodyAccount,        address token,        uint256 amount)        internal        returns (bool)    {        DragoEventful events = DragoEventful(getDragoEventful());        bytes4 methodHash = bytes4(keccak256(\"transferToSelfCustody(address,address,uint256)\"));        bytes memory encodedParams = abi.<mask0>;        require(            events.customDragoLog(methodHash, encodedParams),            \"ISSUE_IN_EVENTFUL\"            );        return true;    }"