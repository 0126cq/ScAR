"function hashOrderItem(MakerOrder memory makerOrder, uint256 idx)        internal        pure        returns (bytes32)    {        return            keccak256(                abi.<mask0>            );    }"