"function verifyMerkleProof(bytes32[] calldata merkleProof, uint256 editionIndex, uint amount) public view returns (bool) {        if(!editions[editionIndex].merkleProtect)        {            return true;        }        string memory leaf = makeLeaf(msg.sender, amount);        bytes32 node = keccak256(abi.<mask0>);        return MerkleProof.verify(merkleProof, editions[editionIndex].merkleRoot, node);    }"