"function cancel(    uint256 minimumActionGas,    bytes calldata signature  ) external {    uint256 nonceToCancel = _nonce;    _validateActionAndIncrementNonce(      ActionType.Cancel,      abi.<mask0>,      minimumActionGas,      signature,      signature    );    emit Cancel(nonceToCancel);  }"