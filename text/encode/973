"function _exitPool() internal {        uint256 bptBalance = pool.balanceOf(address(this));        if (bptBalance != 0) {            IVault.ExitPoolRequest memory exitRequest;            bytes memory userData = abi.<mask0>;            exitRequest.assets = assets;            exitRequest.minAmountsOut = new uint256[](2);             exitRequest.userData = userData;            exitRequest.toInternalBalance = false;             vault.exitPool(pid, address(this), payable(address(this)), exitRequest);            emit ExitPool();        }    }"