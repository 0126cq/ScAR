"function _hashRequest(LoanRequest memory request, uint32 chainId)        private        pure        returns (bytes32)    {        return            keccak256(                abi.<mask0>            );    }"