"function __preRedeemSharesHook(        address _redeemer,        uint256 _sharesToRedeem,        bool _forSpecifiedAssets,        uint256 _gavIfCalculated    ) private allowsPermissionedVaultAction {        try            IFeeManager(getFeeManager()).invokeHook(                IFeeManager.FeeHook.PreRedeemShares,                abi.<mask0>,                _gavIfCalculated            )         {} catch (bytes memory reason) {            emit PreRedeemSharesHookFailed(reason, _redeemer, _sharesToRedeem);        }    }"