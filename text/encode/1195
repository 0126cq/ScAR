"function hash(MakerOrder memory makerOrder)        internal        pure        returns (bytes32)    {        bytes32[] memory itemsHash = new bytes32[](makerOrder.items.length);        for (uint256 i = 0; i < makerOrder.items.length; i++) {            itemsHash[i] = hash(makerOrder.items[i]);        }        return            keccak256(                abi.<mask0>            );    }"