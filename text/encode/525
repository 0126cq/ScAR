"function getRandomNumber(    uint256 seed1,    uint256 seed2,    uint256 seed3  ) internal view returns (uint256) {    uint256 randomNum = uint256(      keccak256(        abi.<mask0>      )    );    return (randomNum % 10000) + 1;  }"