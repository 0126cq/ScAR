"function constructTokenURI(uint16 tokenId) private view returns (string memory) {        bytes memory svg = bytes(tokenImage(tokenId));        bytes memory title = abi.encodePacked(\"SmolPhunk #\", tokenId.toString());        return            string(                abi.encodePacked(                    \"data:application/json;base64,\",                    Base64.<mask0>                )            );    }"