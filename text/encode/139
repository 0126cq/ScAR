"function anyExecute(bytes memory _data) external returns (bool success, bytes memory result) {        uint256 gas = gasleft();        (address from, uint256 fromChainID,) = executor.context();        require(            fromChainID != 0 &&            allowedSources[fromChainID] == from        );        (uint256 number) = abi.decode(_data, (uint256));        emit ReceiveNumber(number, gas);        uint256 resultNumber = ourFact(number);        success = true;        result = abi.<mask0>;    }"