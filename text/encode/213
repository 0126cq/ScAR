"function safeTransferFrom(address from, address to, uint256 itemId, uint256 amount, bytes calldata data) authorizedOnly override(ItemProjection, IERC1155) external {        require(from != address(0), \"required from\");        require(to != address(0), \"required to\");        IItemMainInterface(mainInterface).mintTransferOrBurn(false, abi.<mask0>);        ERC1155CommonLibrary.doSafeTransferAcceptanceCheck(msg.sender, from, to, itemId, amount, data);        emit TransferSingle(msg.sender, from, to, itemId, amount);    }"