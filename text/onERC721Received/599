"function _safeMintBatch(        address to,        uint256[] memory ids,        bytes memory data    ) internal virtual {        _mintBatch(to, ids);        for (uint256 id = 0; id < ids.length; ) {            require(                to.code.length == 0 ||                    ERC721TokenReceiver(to).<mask0> ==                    ERC721TokenReceiver.onERC721Received.selector,                \"UNSAFE_RECIPIENT\"            );            unchecked {                id++;            }        }    }"