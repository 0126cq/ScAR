"function functionDelegateCall(        address target,        bytes memory data,        string memory errorMessage    ) internal returns (bytes memory) {        require(isContract(target), \"Add\");        (bool success, bytes memory returndata) = target.delegatecall(data);        return <mask0>;    }"