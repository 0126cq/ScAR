"function functionCallWithValue(        address target,        bytes memory data,        uint256 value,        string memory errorMessage    ) internal returns (bytes memory) {        require(address(this).balance >= value, \"Add\");        require(isContract(target), \"Add\");        (bool success, bytes memory returndata) = target.call{value: value}(data);        return <mask0>;    }"