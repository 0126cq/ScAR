"function _beforeTokenTransfers(        address from,        address to,        uint256 startTokenId,        uint256 quantity    ) internal virtual override {        super.<mask0>;        if (_lockedBy[startTokenId] != to) {            require(                _lockedBy[startTokenId] == address(0),                \"Son: token transfer while locked\"            );        } else {            delete _lockedBy[startTokenId];        }    }"