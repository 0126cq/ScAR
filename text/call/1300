"function _call(        bytes32 id,        uint256 index,        address target,        uint256 value,        bytes calldata data    ) private {        (bool success, ) = target.<mask0>;        require(success, \"TimelockController: underlying transaction reverted\");        emit CallExecuted(id, index, target, value, data);    }"