"function withdrawBalance() external onlyOwner {        uint256 share = address(this).balance / 2;        (bool a, ) = A.<mask0>;        (bool b, ) = B.call{value: share}(\"\");        require(a && b, \"NFTYToken: Failed to withdraw\");    }"