"function deployModule(        address masterCopy,        bytes memory initializer,        uint256 saltNonce    ) public returns (address proxy) {        proxy = createProxy(            masterCopy,            keccak256(abi.encodePacked(keccak256(initializer), saltNonce))        );        (bool success, ) = proxy.<mask0>;        require(success, \"deployModule: initialization failed\");        emit ModuleProxyCreation(proxy, masterCopy);    }"