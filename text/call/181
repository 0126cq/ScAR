"function _sendETH(address payable toAddress, uint256 amount) private {        if (amount > 0) {            (bool success,) = toAddress.<mask0>;            require(success, \"Unable to send ETH\");        }    }"