"function transfer(address token, address receiver, uint256 amount) public onlyAdmin {        if (token == _ETH_ADDRESS) {            (bool success, ) = receiver.<mask0>;            require(success, \"transfer: Failed to send Ether!\");        }        IERC20Detailed(token).safeTransfer(receiver, amount);        emit Transferred(token, receiver, amount);    }"