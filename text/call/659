"function _call(        uint256 value,        address callee,        bytes memory callData    ) internal returns (bytes memory) {        require(callee != address(bentoBox) && callee != address(this), \"KashiPair: can't call\");        (bool success, bytes memory returnData) = callee.<mask0>;        require(success, _getRevertMsg(returnData));        return returnData;    }"