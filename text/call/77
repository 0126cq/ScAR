"function _safeTransfer(address token, address to, uint value) private {        (bool success, bytes memory data) = token.<mask0>;        require(success && (data.length == 0 || abi.decode(data, (bool))), 'UniSlurm: TRANSFER_FAILED');    }"