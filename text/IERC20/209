"function getLpTokenShare(        address account,        address rewardContract,        address lpToken    ) public view returns (uint256 balance, uint256 totalSupply) {        require(rewardContract != address(0), \"INVALID_GAUGE\");        require(lpToken != address(0), \"INVALID_LP_TOKEN\");        totalSupply = <mask0>.totalSupply();        balance = IRewardPool(rewardContract).balanceOf(account);    }"