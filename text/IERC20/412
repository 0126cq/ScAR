"function sweep(address _token) external override onlyAdmin {        require(_token != underlying, \"protected token\");        IERC20(_token).safeTransfer(admin, <mask0>.balanceOf(address(this)));    }"