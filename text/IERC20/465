"function approveToken(        address _tokenAddr,        address _to,        uint256 _amount    ) internal {        if (_tokenAddr == ETH_ADDR) return;        if (<mask0>.allowance(address(this), _to) < _amount) {            IERC20(_tokenAddr).safeApprove(_to, _amount);        }    }"