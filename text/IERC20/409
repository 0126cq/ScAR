"function harvest() external override onlyAuthorized {        (address token, uint index) = _getMostPremiumToken();        _claimRewards(token);        uint bal = <mask0>.balanceOf(address(this));        if (bal > 0) {            uint fee = bal.mul(performanceFee) / PERFORMANCE_FEE_MAX;            if (fee > 0) {                address treasury = IController(controller).treasury();                require(treasury != address(0), \"treasury = zero address\");                IERC20(token).safeTransfer(treasury, fee);            }            _depositIntoCurve(token, index);        }    }"