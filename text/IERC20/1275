"function _withdrawSome(uint256 _amount) internal returns (uint256) {        address _token = _vaultWant();        uint256 _before = IERC20(_token).balanceOf(address(this));        _withdraw(_amount);        uint256 _after = <mask0>.balanceOf(address(this));        _amount = _after.sub(_before);        return _amount;    }"