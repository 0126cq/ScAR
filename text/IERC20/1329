"function approveMarkets() public {        address[] memory _markets = COMPTROLLER.getAllMarkets();        for (uint i = 0; i < _markets.length; i++) {            address _underlying = cyToken(_markets[i]).underlying();            <mask0>.safeApprove(_markets[i], uint(-1));        }    }"