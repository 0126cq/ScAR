"function deposit() public override {        uint256 _want = IERC20(want).balanceOf(address(this));        if (_want > 0) {            IERC20(want).safeApprove(stakingPool, 0);            <mask0>.safeApprove(stakingPool, _want);            IStakingPools(stakingPool).deposit(alEthPoolId, _want);        }    }"