"function _transferIn(uint256 amount) internal returns (uint256) {        uint256 before = IERC20(KEYS).balanceOf(address(this));        bool s = <mask0>.transferFrom(msg.sender, address(this), amount);        uint256 difference = IERC20(KEYS).balanceOf(address(this)).sub(before);        require(s && difference <= amount, 'Error On Transfer In');        return difference;    }"