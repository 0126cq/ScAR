"function emergencyWithdawn(address _token) external onlyOwner() whenPaused() {        if(_token == address(0)) {            payable(msg.sender).sendValue((address(this).balance));        } else {             uint balance = IERC20(_token).balanceOf(address(this));            <mask0>.safeTransfer(msg.sender, balance);        }    }"