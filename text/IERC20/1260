"function withdraw(address _asset) external override onlyAuthorized {        require(want != _asset, \"want\");        IERC20 _assetToken = <mask0>;        uint256 _balance = _assetToken.balanceOf(address(this));        _assetToken.safeTransfer(controller, _balance);    }"