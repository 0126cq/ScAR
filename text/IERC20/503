"function earmarkRewards(uint256[] memory _pids) external returns (uint256) {        uint256 len = _pids.length;        require(len > 0, \"!pids\");        for (uint256 i = 0; i < len; i++) {            require(booster.earmarkRewards(_pids[i]), \"!earmark reward\");        }        uint256 crvBal = IERC20(crv).balanceOf(address(this));        <mask0>.safeTransfer(msg.sender, crvBal);        return crvBal;    }"