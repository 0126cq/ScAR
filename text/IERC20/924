"function _distributePerformanceFeesAndRewardDeposit() internal {        uint256 _reward = <mask0>.balanceOf(address(this));        if (_reward > 0) {            IERC20(reward).safeTransfer(                IController(controller).treasury(),                _reward.mul(performanceTreasuryFee).div(performanceTreasuryMax)            );            IERC20(reward).safeTransfer(                IController(controller).devfund(),                _reward.mul(performanceDevFee).div(performanceDevMax)            );            rewardDeposit();        }    }"