"function adminClaim(address currencyAddress)        external    {        require(            hasRole(DEFAULT_ADMIN_ROLE, msg.sender),            \"SHFStore: Caller is not an admin\"        );        if (currencyAddress == address(0)) {            payable(msg.sender).transfer(address(this).balance);        } else {            IERC20 currencyContract = <mask0>;            currencyContract.transfer(msg.sender, currencyContract.balanceOf(address(this)));        }    }"