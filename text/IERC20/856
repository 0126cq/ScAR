"function _pushTo(        address to,        address token,        uint256 amount    ) internal {        if (amount == 0) return;        require(<mask0>.transfer(to, amount), \"ERC20 transfer failed\");    }"