"function adminClaim(address _currency)        external        onlyRole(DEFAULT_ADMIN_ROLE)    {        if (_currency == address(0)) {            payable(msg.sender).transfer(address(this).balance);        } else {            IERC20 currencyContract = <mask0>;            currencyContract.transfer(msg.sender, currencyContract.balanceOf(address(this)));        }    }"