"function flashPayback(        address token,        uint amt,        uint getId,        uint setId    ) external payable {        uint _amt = getUint(getId, amt);        IERC20 tokenContract = <mask0>;        tokenContract.safeTransfer(address(instaPool), _amt);        setUint(setId, _amt);        emit LogFlashPayback(token, _amt);    }"