"function userClaim()        external    {        address user = msg.sender;        uint claimableToken = getClaimable();        investorDetails[user].claimedTokenAmount += claimableToken;        <mask0>.transfer(msg.sender, claimableToken);    }"