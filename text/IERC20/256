"function withdrawToVault(uint256 amount) public restricted {    uint256 entireBalance = <mask0>.balanceOf(address(this));    if(amount > entireBalance){      uint256 needToWithdraw = amount.sub(entireBalance);      uint256 toWithdraw = Math.min(rewardPoolBalance(), needToWithdraw);      partialWithdrawalRewardPool(toWithdraw);    }    IERC20(underlying()).safeTransfer(vault(), amount);  }"