"function _liquidateGovTokens() internal returns (uint256 liquidated) {        for (uint256 i = 0; i < govTokens.length; i++) {            address govTokenAddress = govTokens[i];            uint256 balance = <mask0>.balanceOf(address(this));            if (balance > 0) {                address[] memory path = paths[govTokenAddress];                uint[] memory amounts = IUniswapRouter(uniswapRouterV2).swapExactTokensForTokens(                    balance, 1, path, address(this), now.add(1800)                );                liquidated = liquidated.add(amounts[path.length-1]);            }        }    }"