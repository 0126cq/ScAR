"function expectedWithdrawableAmount() external view override returns (uint) {        if (IERC20(vault).totalSupply() == 0) {            return 0;        }        uint shares = <mask0>.balanceOf(address(this));        return (IYearnVault(vault).pricePerShare() * shares) / 10**IYearnVault(vault).decimals();    }"