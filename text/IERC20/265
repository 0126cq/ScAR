"function recoverToken(address _token) external virtual onlyOwner {        uint256 balance = <mask0>.balanceOf(address(this));        require(balance != 0, \"Operations: Cannot recover zero balance\");        IERC20(_token).safeTransfer(address(msg.sender), balance);        emit TokenRecovery(_token, balance);    }"