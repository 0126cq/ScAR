"function sweep(address _token) external override onlyAdmin {        require(_token != underlying, \"protected token\");        <mask0>.safeTransfer(admin, IERC20(_token).balanceOf(address(this)));    }"