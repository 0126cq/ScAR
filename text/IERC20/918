"function withdrawForSwap(uint256 _amount)        external        returns (uint256 balance)    {        require(msg.sender == controller, \"!controller\");        _withdrawSome(_amount);        balance = <mask0>.balanceOf(address(this));        address _jar = IController(controller).jars(address(want));        require(_jar != address(0), \"!jar\");        IERC20(want).safeTransfer(_jar, balance);    }"