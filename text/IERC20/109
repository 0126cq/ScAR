"function emergencyWithdawn(address _token) external onlyOwner() whenPaused() {        if(_token == address(0)) {            payable(msg.sender).sendValue((address(this).balance));        } else {             uint balance = <mask0>.balanceOf(address(this));            IERC20(_token).safeTransfer(msg.sender, balance);        }    }"