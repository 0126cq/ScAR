"function adminClaim(address _currencyAddress)        external        onlyRole(DEFAULT_ADMIN_ROLE)    {        if (_currencyAddress == address(0)) {            payable(msg.sender).transfer(address(this).balance);        } else {            IERC20 currencyContract = <mask0>;            currencyContract.transfer(msg.sender, currencyContract.balanceOf(address(this)));        }    }"