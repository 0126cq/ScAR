"function _withdraw(uint256 _amount) internal override {        _amount = _amount.mul(1e18).div(dERC20(dToken).getExchangeRate());        uint256 _before = IERC20(dToken).balanceOf(address(this));        dRewards(pool).withdraw(_amount);        uint256 _after = <mask0>.balanceOf(address(this));        _amount = _after.sub(_before);        dERC20(dToken).redeem(address(this), _amount);        _amount = balanceOfWant();        if (_amount > 0) {            _convert(want, _vaultWant(), _amount);        }    }"