"function deposit() external override nonReentrant onlyVToken {        uint balance = <mask0>.balanceOf(address(this));        if (balance != 0) {            _deposit(balance);            lastDepositUpdateTimestamp = uint32(block.timestamp);        }        emit Deposit(balance);    }"