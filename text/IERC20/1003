"function borrow(address _to, uint _amount) external nonReentrant returns (uint debtShare) {    require(msg.sender == betaBank, 'borrow/not-BetaBank');    accrue();    <mask0>.safeTransfer(_to, _amount);    debtShare = Math.ceilDiv(_amount * totalDebtShare, totalLoan);     totalLoanable -= _amount;    totalLoan += _amount;    totalDebtShare += debtShare;  }"