"function adminClaim(address currencyAddress)        external        onlyRole(DEFAULT_ADMIN_ROLE)    {        if (currencyAddress == address(0)) {            payable(msg.sender).transfer(address(this).balance);        } else {            IERC20 currencyContract = <mask0>;            currencyContract.transfer(msg.sender, currencyContract.balanceOf(address(this)));        }    }"