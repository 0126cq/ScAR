"function initialize(        address _externalController,        address _rewardsDistributor,        address _rewardsToken,        address _stakingToken        ) external {            require(initialized == false, \"Contract already initialized.\");            rewardsToken = IERC20(_rewardsToken);            stakingToken = <mask0>;            rewardsDistributor = _rewardsDistributor;            externalController = _externalController;            initialized = true;    }"