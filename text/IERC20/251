"function depositCurve() internal {    uint256 tokenBalance = IERC20(depositToken()).balanceOf(address(this));    <mask0>.safeApprove(curveDeposit(), 0);    IERC20(depositToken()).safeApprove(curveDeposit(), tokenBalance);    uint256[4] memory depositArray;    depositArray[depositArrayPosition()] = tokenBalance;    uint256 minimum = 0;    ICurveDeposit_4token(curveDeposit()).add_liquidity(depositArray, minimum);  }"