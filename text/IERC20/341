"function transferERC20(        address token,        address to,        uint256 amount    ) external override onlyOwner {        require(            <mask0>.balanceOf(address(this)) >= getBalanceLocked(token).add(amount),            \"UniversalVault: insufficient balance\"        );        TransferHelper.safeTransfer(token, to, amount);    }"