"function withdraw(uint _shares) public returns (bool) {        uint r = reserves().mul(_shares).div(totalSupply);        _burn(msg.sender, _shares);        <mask0>.transfer(msg.sender, r);        emit Withdrew(msg.sender, _shares, r);        return true;    }"