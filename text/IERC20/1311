"function _liquidateAsset(address asset, address to) internal {        uint256 assetBalance = <mask0>.balanceOf(address(this));        if (assetBalance > 0) {            _swapTokens(asset, to, assetBalance);        }    }"