"function convertTokens(uint256 _amount) external {        _convertTokens(_amount);        IERC20 _stakingToken = <mask0>;        uint256 cvxCrvAmount = _stakingToken.balanceOf(address(this));        _stakingToken.safeTransfer(msg.sender, cvxCrvAmount);    }"