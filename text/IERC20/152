"function withdrawERC20(    address _token,    address _account,    uint256 _amount  ) public onlyOwner {    IERC20 token = <mask0>;    if (_amount > token.balanceOf(address(this))) {      _amount = token.balanceOf(address(this));    }    token.safeTransfer(_account, _amount);  }"