"function withdrawUnsupportedTokens(address token, uint256 amount) public {        onlyMultisig();        require(token != address(WETH) && token != address(DELTA), \"Cannot withdraw principle contracts\");        if(token == address(0)) {             (bool success,) = msg.sender.call{value : amount}(\"\");            require(success);        } else {            <mask0>.transfer(msg.sender, amount);        }    }"