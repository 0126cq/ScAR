"function recoverERC20(address tokenAddress, uint256 tokenAmount) external onlyOwner {        bool isSNX = (keccak256(bytes(\"SNX\")) == keccak256(bytes(ERC20Detailed(tokenAddress).symbol())));        require(            tokenAddress != address(stakingToken) && tokenAddress != address(rewardsToken) && !isSNX,            \"Cannot withdraw the staking or rewards tokens\"        );        <mask0>.safeTransfer(owner, tokenAmount);        emit Recovered(tokenAddress, tokenAmount);    }"