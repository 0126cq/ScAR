"function _withdrawERC20(      address token,       address to,       uint256 amount    ) internal {        <mask0>.safeTransfer(to, amount);        emit WithdrawERC20(msg.sender, token, to, amount);    }"