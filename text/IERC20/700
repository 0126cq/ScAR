"function rescueTokens(        address token,        address to,        uint256 amount    )        external        onlyGov        returns (bool)    {        SafeERC20.safeTransfer(<mask0>, to, amount);        return true;    }"