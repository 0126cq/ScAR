"function mintTokenInMORE(uint[] memory tokenIds) public virtual {        require(saleIsActive, \"Sale must be active to mint Tokens\");        checkSale(msg.sender, tokenIds);        require(tokenPublicsalePriceInMORE * tokenIds.length <= IERC20(_MORE).balanceOf(msg.sender), \"Have not enough MORE\");        <mask0>.transferFrom(msg.sender, address(this), tokenPublicsalePriceInMORE * tokenIds.length);        mainMint(msg.sender, tokenIds);    }"