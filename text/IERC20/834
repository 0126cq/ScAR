"function deposit() public {    uint256 _want = IERC20(want).balanceOf(address(this));    <mask0>.safeApprove(booster, 0);    IERC20(want).safeApprove(booster, _want);    IBooster(booster).depositAll(25, true);  }"