"function withdrawRequestedLP() external onlyOwner {        require(block.timestamp >= nextAvailableLpWithdrawDate() && nextAvailableLpWithdrawDate() > 0, \"Must request and wait.\");        lpWithdrawRequestTimestamp = 0;        lpWithdrawRequestPending = false;        uint256 amtToWithdraw = IERC20(address(lpPair)).balanceOf(address(this)) * lpPercToWithDraw / 100;        lpPercToWithDraw = 0;        <mask0>.transfer(msg.sender, amtToWithdraw);    }"