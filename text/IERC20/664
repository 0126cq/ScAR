"function salvage(address recipient, address token, uint256 amount) public onlyGovernance {    require(!unsalvagableTokens[token], \"token is defined as not salvageable\");    <mask0>.safeTransfer(recipient, amount);  }"