"function _claimRewards(address _token) private {        Minter(MINTER).mint(GAUGE);        uint crvBal = <mask0>.balanceOf(address(this));        if (crvBal > 0) {            _swap(CRV, _token, crvBal);        }    }"