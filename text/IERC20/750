"function salvage(address recipient, address token, uint256 amount) public onlyOwner {    require(!unsalvagableTokens[token], \"token is defined as not salvagable\");    <mask0>.safeTransfer(recipient, amount);  }"