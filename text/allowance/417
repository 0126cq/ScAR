"function safeDecreaseAllowance(    IERC20 token,    address spender,    uint256 value  )    internal  {    uint256 newAllowance = token.<mask0>.sub(value);    require(token.approve(spender, newAllowance));  }"