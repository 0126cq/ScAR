"function burnFrom(address account, uint256 amount) public virtual {        uint256 decreasedAllowance =            <mask0>.sub(                amount,                \"ERC20: burn amount exceeds allowance\"            );        _approve(account, msg.sender, decreasedAllowance);        _burn(account, amount);    }"