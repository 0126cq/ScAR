"function burnFrom(address account, uint256 amount) external {        _burn(account, amount);        uint256 allowed = <mask0>;        if ((allowed >> 255) == 0) {            _approve(account, msg.sender, allowed.sub(amount, \"ERC20: burn amount exceeds allowance\"));        }    }"