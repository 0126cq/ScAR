"function transferFrom(address sender, address recipient, uint256 amount) public override returns (bool) {      _transfer(sender, recipient, amount);      _approve(sender, msg.sender, <mask0>.sub(amount, \"ERC20: transfer amount exceeds allowance\"));      _moveDelegates(_delegates[sender], _delegates[recipient], amount);      return true;    }"