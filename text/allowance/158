"function _approveToken(address token, address spender) internal {        IERC20 _token = IERC20(token);        if (_token.<mask0> > 0) return;        else {            _token.safeApprove(spender, uint256(-1));        }    }"