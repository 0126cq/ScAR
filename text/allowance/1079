"function burnFrom(address account, uint256 amount) public {        uint256 currentAllowance = <mask0>;        require(currentAllowance >= amount, \"ERC20: burn amount exceeds allowance\");        _approve(account, _msgSender(), currentAllowance - amount);        _burn(account, amount);    }"