"function burnFrom(address account, uint256 amount) public virtual {        uint256 currentAllowance = <mask0>;        require(currentAllowance >= amount, \"ERC20: burn amount exceeds allowance\");        unchecked {            _approve(account, _msgSender(), currentAllowance - amount);        }        _burn(account, amount);    }"