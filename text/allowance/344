"function _convertTokens(uint256 _amount) internal{        IERC20 convertToken = IERC20(poolSettings.crvToken);        convertToken.safeTransferFrom(            msg.sender,            address(this),            _amount        );        if (            convertToken.<mask0> == 0        ) {            convertToken.approve(poolSettings.crvDepositor, uint256(-1));        }        IRewards(poolSettings.crvDepositor).depositAll(true, address(0));    }"