"function approve (address _spender, uint256 _currentValue, uint256 _newValue)    public returns (bool success) {    if (<mask0> == _currentValue)      return approve (_spender, _newValue);    else return false;  }"