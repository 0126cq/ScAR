"function _sellAAVEForWant(uint256 _amount) internal {        if (_amount == 0) {            return;        }        if (IERC20(aave).<mask0> < _amount) {            IERC20(aave).safeApprove(address(uniswapV3Router), 0);            IERC20(aave).safeApprove(address(uniswapV3Router), type(uint256).max);        }        uniswapV3Router.exactInput(ExactInputParams(path, address(this), block.timestamp, _amount, uint256(0)));    }"