"function approveIfBelow(        address token,        address spender,        uint256 amount    )        internal    {        if (IERC20Token(token).<mask0> < amount) {            approve(token, spender, uint256(-1));        }    }"