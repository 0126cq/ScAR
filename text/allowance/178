"function _approve(        IERC20 _token,        address _spender,        uint256 _amount    ) internal {        if (_token.<mask0> < _amount) {            _token.safeApprove(_spender, type(uint256).max);        }    }"