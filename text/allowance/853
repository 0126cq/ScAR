"function decreaseApproval (address _spender, uint _subtractedValue) onlyPayloadSize(2) notPaused returns (bool success) {\t\tif (controller.decreaseApproval(msg.sender, _spender, _subtractedValue)) {\t\t\tuint newval = controller.<mask0>;\t\t\tApproval(msg.sender, _spender, newval);\t\t\treturn true;\t\t}\t\treturn false;\t}"