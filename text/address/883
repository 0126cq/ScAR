"function getNftsList() external view override returns (address[] memory) {    address[] memory _activeNfts = new <mask0>;    for (uint256 i = 0; i < _nftsCount; i++) {      _activeNfts[i] = _nftsList[i];    }    return _activeNfts;  }"