"function reinvest(uint256 amount, uint256 minAssetsOut) public onlyOwner {        stkAAVE.redeem(address(this), amount);        address[] memory path = new <mask0>;        path[0] = AAVE;        path[1] = WETH;        path[2] = currentLendingToken;        uint256 amountIn = ERC20(AAVE).balanceOf(address(this));        uint256 amountOut = _sushiswap(path, amountIn, minAssetsOut);        if (!isShutdown) {            _depositToAave(currentLendingToken, amountOut);        }    }"