"function isApprovedForAll(address owner, address operator)        public        view        override        returns (bool)    {        ProxyRegistry proxyRegistry = ProxyRegistry(proxyRegistryAddress);        return            (OpenSeaGasFreeListing.isApprovedForAll(owner, operator) ||                <mask0> == operator) ||            super.isApprovedForAll(owner, operator);    }"