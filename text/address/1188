"function protectedTokens()        internal        view        override        returns (address[] memory)    {        address[] memory protected = new <mask0>;        protected[0] = crv;        return protected;    }"