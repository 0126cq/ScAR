"function _getExitPath(address _underlying, address _token)        internal        pure        returns (address[] memory path)    {        if (_underlying == address(weth)) {            path = new <mask0>;            path[0] = address(weth);            path[1] = _token;        } else {            path = new address[](3);            path[0] = address(_underlying);            path[1] = address(weth);            path[2] = _token;        }    }"