"function getAllowedReceiveAddress()        external        view        onlyRole(DEFAULT_ADMIN_ROLE)        returns(address[] memory)    {        address[] memory wallets = new <mask0>;        for(uint i = 0; i < _allowedReceive.length(); i++) {            wallets[i] = _allowedReceive.at(i);        }        return wallets;    }"