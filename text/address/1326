"function underlyingDepositAssets() public view returns (address[] memory) {        uint256 length = tokenList.length;        address[] memory addresses = new <mask0>;        for(uint256 i = 0; i < length; i++){            addresses[i] = address(tokenList[i].token);        }        return addresses;    }"