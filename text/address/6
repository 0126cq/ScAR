"function provenance(uint256 id, uint256 start, uint256 end) external view returns (address[] memory) {        address own;        address[] storage owners = id2provenance[id];        if (end == 0) {            end = owners.length;        }        address[] memory result = new <mask0>;        for (uint256 i = start; i < end; i++) {            own = owners[i];            result[i - start] = own;        }        return result;    }"