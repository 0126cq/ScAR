"function userWithdraw(address _staker, uint256 _value) external {\t\taddress stakingContract = msg.sender;\t\t(\t\t\t,\t\t\tuint64 blockStart,\t\t\tuint64 blockEnd,\t\t\tbool isBlackListed\t\t) = GoodFundManager(nameService.getAddress(\"FUND_MANAGER\"))\t\t\t\t.rewardsForStakingContract(stakingContract);\t\tif (isBlackListed) return; \t\t_decreaseProductivity(\t\t\tstakingContract,\t\t\t_staker,\t\t\t_value,\t\t\tblockStart,\t\t\tblockEnd\t\t);\t\taddress[] memory contracts = new <mask0>;\t\tcontracts[0] = stakingContract;\t\t_claimReputation(_staker, contracts);\t\t_updateMonth(); \t}"