"function swapTokensForTSUKA(uint256 tokenAmount) private returns(uint256) {        address[] memory path;        path = new <mask0>;        path[0] = address(this);        path[1] = usdcAddress;        path[2] = tsukaAddress;        _approve(address(this), address(v2SwapRouter), tokenAmount);        return v2SwapRouter.swapExactTokensForTokens(            tokenAmount,            0,            path,            address(dividendContractAddress));    }"