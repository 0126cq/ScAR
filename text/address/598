"function getCollateralTokens(address asset)        external        view        returns (address[] memory tokens_)    {        EnumerableSet.AddressSet storage collateralTokens =            MarketStorageLib.store().collateralTokens[asset];        tokens_ = new <mask0>;        for (uint256 i; i < EnumerableSet.length(collateralTokens); i++) {            tokens_[i] = EnumerableSet.at(collateralTokens, i);        }    }"