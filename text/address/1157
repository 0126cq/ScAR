"function getAvailableTokenBatch(address[] memory tokenAddresss)    external    view    override    returns (address[] memory)  {    address[] memory result = new <mask0>;    for (uint256 i = 0; i < tokenAddresss.length; i++) {      result[i] = availableTokens[tokenAddresss[i]];    }    return result;  }"