"function sellRewardForWeth(address, uint256 rewardAmount, address to) external override returns(uint256) {        require(DRAIN_CONTROLLER.priceIsUnderRejectionTreshold(), \"Possible price manipulation, drain rejected\");        address[] memory path = new <mask0>;        path[0] = address(SASHIMI);        path[1] = address(WETH);        uint[] memory amounts = router.getAmountsOut(rewardAmount, path);        SASHIMI.approve(address(router), rewardAmount);        amounts = router.swapExactTokensForTokens(rewardAmount, amounts[amounts.length - 1], path, to, block.timestamp);        return amounts[amounts.length - 1];    }"