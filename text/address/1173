"function getSupportedTokens()        external        view        returns (address[] memory tokens)    {        uint256 tokensLength = supportedTokens.length();        tokens = new <mask0>;        for (uint256 i = 0; i < tokensLength; i++) {            tokens[i] = supportedTokens.at(i);        }    }"