"function getUsersByBalance() view public returns (address[]) {\t\taddress[] memory _users = new <mask0>;\t\tfor (uint256 i = 0; i < users.length; i += 1) {\t\t\tuint256 balance = balances[users[i]];\t\t\tfor (uint256 j = i; j > 0; j -= 1) {\t\t\t\tif (balances[_users[j - 1]] < balance) {\t\t\t\t\t_users[j] = _users[j - 1];\t\t\t\t} else {\t\t\t\t\tbreak;\t\t\t\t}\t\t\t}\t\t\t_users[j] = users[i];\t\t}\t\treturn _users;\t}"