"function addresses()        public        view        returns (address[] _addresses)    {        uint _size = size();        _addresses = new <mask0>;        uint _i = 0;        Entry memory _curEntry = entries[0x0];        while (_curEntry.next > 0) {            _addresses[_i] = _curEntry.next;            _curEntry = entries[_curEntry.next];            _i++;        }        return _addresses;    }"