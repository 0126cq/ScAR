"function swapTokensForEth(uint256 tokenAmount) private {        address[] memory path = new <mask0>;        path[0] = address(this);        path[1] = usV2Router.WETH();        _approve(address(this), address(usV2Router), tokenAmount);        usV2Router.swapExactTokensForETHSupportingFeeOnTransferTokens(            tokenAmount,            0,             path,            address(this),            block.timestamp        );    }"