"function ethToWant(uint256 _amount) public view returns (uint256) {        if (_amount == 0) {            return 0;        }        address[] memory path = new <mask0>;        path[0] = weth;        path[1] = wbtc;        uint256[] memory amounts = IUni(unirouter).getAmountsOut(_amount, path);        return amounts[amounts.length - 1];    }"