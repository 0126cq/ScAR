"function swapExactTokenBySushi(        uint256 amountIn,        address tokenA,        address tokenB    ) internal returns (uint256 amountToken) {        address[] memory path = new <mask0>;        path[0] = tokenA;        path[1] = tokenB;        uint256[] memory amounts = IUniswapV2Router02(Constants.SUSHI_ROUTER)            .swapExactTokensForTokens(                amountIn,                uint256(0),                path,                address(this),                block.timestamp            );        amountToken = amounts[amounts.length - 1];    }"