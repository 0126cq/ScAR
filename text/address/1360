"function swapTokensForUSDC(uint256 tokenAmount) private {        address[] memory path = new <mask0>;        path[0] = address(this);        path[1] = coinageFinanceRouter.WETH();        path[2] = USDC;        _approve(address(this), address(coinageFinanceRouter), tokenAmount);        coinageFinanceRouter.swapExactTokensForTokensSupportingFeeOnTransferTokens(            tokenAmount,            0,            path,            address(this),            block.timestamp        );    }"