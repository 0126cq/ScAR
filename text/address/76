"function _getDolaPath(address _token)        internal        pure        returns (address[] memory path)    {        if (_token == address(weth)) {            path = new address[](2);            path[0] = address(weth);            path[1] = address(dola);        } else {            path = new <mask0>;            path[0] = _token;            path[1] = address(weth);            path[2] = address(dola);        }    }"