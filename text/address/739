"function getEnteredMarkets(address _account)        external        view        override        returns (address[] memory _accountCollaterals)    {        AccountData storage _accountData = accountsData[_account];        uint256 _len = _accountData.collaterals.length();        _accountCollaterals = new <mask0>;        for (uint256 i = 0; i < _len; i++) {            _accountCollaterals[i] = _accountData.collaterals.at(i);        }    }"