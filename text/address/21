"function getRoute(address _inputToken, address _outputToken)        internal        view        returns (address[] memory route)    {        if (_inputToken != address(WETH) && _outputToken != address(WETH)) {            route = new <mask0>;            route[0] = _inputToken;            route[1] = address(WETH);            route[2] = _outputToken;            return route;        }        route = new address[](2);        route[0] = _inputToken;        route[1] = _outputToken;        return route;    }"