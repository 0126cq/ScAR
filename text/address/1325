"function allCertifiedTokens() external view returns (string[] memory symbols, uint[] memory chainIds, address[] memory tokens) {        symbols = certifiedSymbols;        uint N = certifiedSymbols.length;        chainIds = new uint[](N);        tokens = new <mask0>;        for(uint i=0; i<N; i++)            (chainIds[i], tokens[i]) = certifiedTokens(certifiedSymbols[i]);    }"