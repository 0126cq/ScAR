"function getStrategies() public view override returns (address[] memory _strategies) {        _strategies = new <mask0>;        for (uint256 i; i < availableStrategies.length(); i++) {            _strategies[i] = availableStrategies.at(i);        }    }"