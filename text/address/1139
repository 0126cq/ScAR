"function earn() public whenNotPaused onlyTimelock {        address[] memory pools = new <mask0>;        pools[0] = address(lendingPool);        IClearPoolFactory(poolFactory).withdrawReward(pools);        uint256 earnedAmt = IERC20(CPOOL).balanceOf(address(this));        if (earnedAmt > 0) {            uint256 minReturn = _calculateMinReturn(earnedAmt);            swap(earnedAmt, minReturn);        }        earnedAmt = wantLockedInHere();        if (earnedAmt != 0) {            _deposit(earnedAmt);        }        lastEarnBlock = block.number;    }"