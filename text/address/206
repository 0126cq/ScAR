"function tokenURI(uint256 tokenId) public view virtual override(ERC721A, IERC721Metadata) returns (string memory) {        if (!revealed || <mask0> == address(0x0)) {            return _unrevealedURI;        }        if (tombRaid.isLocked(tokenId)) {            return _lockedURI;        }        string memory baseURI = _baseURI();        uint256 level = tombRaid.tokenTiers(tokenId);        return string(abi.encodePacked(baseURI, tokenId.toString(), \"-\", level.toString()));    }"