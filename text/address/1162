"function withdrawRewards() external override {        require(msg.sender == configurationManager.owner(), \"not owner\");        address[] memory assets = new <mask0>;        assets[0] = this.tokenB();        _claimRewards(assets);        address rewardAsset = _parseAddressFromUint(configurationManager.getParameter(\"REWARD_ASSET\"));        uint256 rewardsToSend = _rewardBalance();        IERC20(rewardAsset).safeTransfer(msg.sender, rewardsToSend);    }"