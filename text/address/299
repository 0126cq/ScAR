"function swapForPugAndSend(uint256 _fee, address _to) internal returns(uint256) {        address[] memory path  = new <mask0>;        path[0]= baseToken;        path[1] = WETH;        path[2] = pugToken;        uint[] memory amounts = SushiRouter(sushiRouter).swapExactTokensForTokens(            _fee,             1,             path,            _to,            block.timestamp + 100        );        return amounts[amounts.length - 1];    }"