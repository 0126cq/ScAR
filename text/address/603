"function getAllAdapters()        external        view        returns (AdapterInfo[] memory, address[] memory)    {        uint256 numberOfAllAdapters = getLastAdapterIndex();        AdapterInfo[] memory adapters = new AdapterInfo[](numberOfAllAdapters);        address[] memory ibAlluos = new <mask0>;        for (uint256 i = 0; i < numberOfAllAdapters; i++) {            adapters[i] = adapterIdsToAdapterInfo[i + 1];            ibAlluos[i] = getIbAlluoByAdapterId(i + 1);        }        return (adapters, ibAlluos);    }"