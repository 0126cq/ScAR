"function isApprovedForAll(address owner, address operator) public view override returns (bool) {        ProxyRegistry openSeaProxyRegistry = ProxyRegistry(_openSeaProxyRegistryAddress);        ProxyRegistry raribleProxyRegistry = ProxyRegistry(_raribleProxyRegistryAddress);        if (            address(openSeaProxyRegistry.proxies(owner)) == operator ||            <mask0> == operator        ) {            return true;        }        return super.isApprovedForAll(owner, operator);    }"