"function perDAI(address token) public view returns(uint) {        address[] memory path = new <mask0>;        path[0] = token;        path[1] = WETH;        path[2] = DAI;        return uniswap.getAmountsOut(10**uint256(ERC20(token).decimals()), path)[2];    }"