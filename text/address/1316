"function getPath(address _tokenToSwap, address _tokenToReceive) public view returns (address[] memory) {        address[] memory path = _tokenToSwap == WETH || _tokenToReceive == WETH ? new <mask0> : new address[](3);        if (path.length == 2) {            path[0] = _tokenToSwap;            path[1] = _tokenToReceive;        } else {            path[0] = _tokenToSwap;            path[1] = WETH;            path[2] = _tokenToReceive;        }        return path;    }"