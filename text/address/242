"function listRoleWallet(bytes32 role)        public        view        returns (address[] memory roleMembers)    {        uint256 count = getRoleMemberCount(role);        roleMembers = new <mask0>;        for (uint256 i = 0; i < count; i++) {            roleMembers[i] = getRoleMember(role, i);        }    }"