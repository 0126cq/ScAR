"function _grantRole(bytes32 role, address account) internal {        if (!<mask0>) {            _roles[role].members[account] = true;            emit RoleGranted(role, account, msg.sender);        }    }"