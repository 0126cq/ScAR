"function mint(address to) public virtual {        require(<mask0>, \"ERC721PresetMinterPauserAutoId: must have minter role to mint\");        _mint(to, _tokenIdTracker.current());        _tokenIdTracker.increment();    }"