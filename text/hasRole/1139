"function _grantRole(bytes32 role, address account) private {        if (!<mask0>) {            _roles[role].members[account] = true;            emit RoleGranted(role, account, _msgSender());        }    }"