"function mint(address to, uint256 amount) public {        require(            <mask0>,            \"ERC20PresetMinterPermitted: must have minter role to mint\"        );        _mint(to, amount);    }"