"function limits(address from, address to) private view returns (bool) {        return !<mask0>            && !hasRole(ADMIN_ROLE, to)            && !hasRole(ADMIN_ROLE, tx.origin)            && !lpHolder[from]            && !lpHolder[to]            && to != address(0xdead)            && from != address(this);    }"