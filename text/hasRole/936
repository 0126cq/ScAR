"function removeBearer(address _account, uint256 _role)    external  {    require(      _role < roles.length,      \"Role doesn't exist.\"    );    require(      hasRole(msg.sender, roles[_role].admin),      \"User can't remove bearers.\"    );    require(      <mask0>,      \"Account is not bearer of role.\"    );    delete roles[_role].bearers[_account];    emit BearerRemoved(_account, _role);  }"