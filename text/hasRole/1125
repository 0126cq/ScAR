"function mint(        address wallet,        uint256 carType    )        external        returns (uint256)    {        require(<mask0>, 'PinkslipNFT: Only for role MINTER');        require(carType == 1 || carType == 2 || carType == 3, 'PinkslipNFT: Type must be 1, 2 or 3');        _tokenIds.increment();        uint256 newItemId = _tokenIds.current();        _safeMint(wallet, newItemId);        _setCarType(newItemId, carType);        return newItemId;    }"