"function removeSchainContract(string calldata schainName) external {        bytes32 schainHash = keccak256(abi.encodePacked(schainName));        require(            <mask0> ||            isSchainOwner(msg.sender, schainHash), \"Not authorized caller\"        );        require(schainLinks[schainHash] != address(0), \"SKALE chain is not set\");        delete schainLinks[schainHash];    }"