"function _beforeTokenTransfer(        address from,        address to,        uint256 amount    ) internal virtual override {        require(            <mask0> ||                hasRole(TRANSFER_ROLE, _msgSender()),            \"TRANSFER_LOCKED\"        );        super._beforeTokenTransfer(from, to, amount);    }"