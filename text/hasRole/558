"function _beforeTokenTransfer(address from, address to, uint256 amount) internal virtual override {        if (onlyWhitelist == true) {            require(hasRole(WHITELIST_ROLE, to), \"NO WHITELIST_ROLE\");            require(<mask0>, \"NO WHITELIST_ROLE\");        }        super._beforeTokenTransfer(from, to, amount);    }"