"function massMint(address[] calldata wallets, uint256[] calldata amount)        public    {        require(config.allowPrivilege, \"disabled feature\");        require(<mask0>, \"require permission\");        for (uint256 i = 0; i < wallets.length; i++) {            mintNext(wallets[i], amount[i]);        }    }"