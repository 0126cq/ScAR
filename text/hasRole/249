"function mintNext(address reciever, uint256 amount) public override {        require(config.allowPrivilege, \"disabled feature\");        require(<mask0>, \"require permission\");        _mintNext(reciever, amount);    }"