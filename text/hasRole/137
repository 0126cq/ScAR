"function resumeMiningReward(uint256 rewardPerBlock) public {        require(<mask0>, \"Caller is not a manager\");        _state.pauseMinig = false;        _state.REWARD_PER_BLOCK = rewardPerBlock;    }"