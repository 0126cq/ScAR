"function harvestYieldFromBalance(uint256 minExpectedAmount, uint256 maxAmount) public {        require(<mask0>, \"finmgmt only\");        uint256 accruedYieldUBT = baseAsset.balanceOf(address(this)).sub(amountStaked);        require(accruedYieldUBT >= minExpectedAmount, \"yield to harvest less than min\");        if (accruedYieldUBT > maxAmount) {            accruedYieldUBT = maxAmount;        }        uint256 amountFee = accruedYieldUBT.mul(profitFee).div(1e18);"