"function claimRewards(address adapter, address platform)        internal        returns (address rewardsToken, uint256 rewardsAmount)    {        bytes memory returnData = adapter.<mask0>;        return abi.decode(returnData, (address, uint256));    }"