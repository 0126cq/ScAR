"function pluck(uint256 tokenId, string memory keyPrefix, string[] memory sourceArray, string[] memory legendsArray) internal pure returns (string memory) {        uint256 rand = random(string(abi.<mask0>));        uint256 greatness = rand % 21;        string memory output = sourceArray[rand % sourceArray.length];        if (greatness >= 19) {            output = legendsArray[rand % legendsArray.length];        }        return output;    }"