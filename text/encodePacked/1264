"function constructTokenURI(uint256 id)        private        view        returns (string memory)    {        uint256 cID = id - 1;        string memory pageSVG = Base64.encode(bytes(generateSVG(cID)));        return            string(                abi.<mask0>            );    }"