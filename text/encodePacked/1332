"function tokenURI(uint256 tokenId) public view override returns (string memory) {        require(_exists(tokenId), \"ERC721Metadata: URI query for nonexistent token\");        if(!reveal) return unrevealURI;        return bytes(_baseURIextended).length > 0 ? string(abi.<mask0>) : \"\";    }"