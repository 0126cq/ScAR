"function getLongitude(uint256 tokenId) public view returns (string memory) {        require(tokenId < tokenIdCounter.current(), \"tokenId is not exist\");        uint256 l = tokenId % 90;        if (l < 45) {            return                string(                    abi.<mask0>                );        } else {            return                string(                    abi.encodePacked(                        toString((l - 45) * 4),                        \"E\",                        \"-\",                        toString((l - 44) * 4),                        \"E\"                    )                );        }    }"