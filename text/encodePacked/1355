"function tokenURI(        uint256 _tokenId    )        public         view         virtual         override         returns (string memory)     {        require(_exists(_tokenId), \"ERC721Metadata: URI query for nonexistent token\");        if (!reveal) {            return string(abi.encodePacked(blindURI));        } else {            return string(abi.<mask0>);        }    }"