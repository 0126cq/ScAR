"function getUri(uint tokenId) public view returns(string memory) {        RevealData[] memory data = revealData;        uint count = data.length;        RevealData memory tokenData;        uint lastRange = 0;        for(uint i = 0; i < count;) {            if(tokenId < data[i].range) {                tokenData = data[i];                break;            }            lastRange = data[i].range;            unchecked {                ++i;             }        }        if(tokenData.randomness == 0) {            return string(abi.<mask0>);        }        uint metadataId = lastRange + ((tokenId + tokenData.randomness) % (tokenData.range - lastRange));        string memory uri;        if(tokenUpgraded[tokenId]) {            uri = upgradeUri;        } else {            uri = tokenData.uri;        }        return string(abi.encodePacked(uri, metadataId.toString()));    }"