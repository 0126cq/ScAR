"function setContractsAddress(string calldata contractsName, address newContractsAddress) external onlyOwner {        require(newContractsAddress != address(0), \"New address is equal zero\");        bytes32 contractId = keccak256(abi.<mask0>);        require(contracts[contractId] != newContractsAddress, \"Contract is already added\");        require(newContractsAddress.isContract(), \"Given contracts address does not contain code\");        contracts[contractId] = newContractsAddress;        emit ContractUpgraded(contractsName, newContractsAddress);    }"