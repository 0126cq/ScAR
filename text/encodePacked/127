"function uri(uint256 tokenId)        public        view        virtual        override        returns (string memory)    {        if (tokenId == _secretToken) {            return string(abi.<mask0>);        }        return            string(                abi.encodePacked(                    _ipfsGateway,                    _ipfsCid,                    \"/\",                    tokenId.toString(),                    \".json\"                )            );    }"