"function uri(uint256 tokenId)        public        view        virtual        override        returns (string memory)    {        if (tokenId == _secretToken) {            return string(abi.encodePacked(_ipfsGateway, _secretIpfsCid));        }        return            string(                abi.<mask0>            );    }"