"function tokenURI(uint256 tokenId)        public        view        override        returns (string memory)    {        require(_exists(tokenId), \"Token does not exist.\");        return string(abi.<mask0>);    }"