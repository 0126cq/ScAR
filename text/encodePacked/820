"function tokenURI(uint256 _tokenId)        public        view        override        returns (string memory)    {        require(_exists(_tokenId), \"That token doesn't exist\");        return            bytes(baseURI).length > 0                ? string(                    abi.<mask0>                )                : \"\";    }"