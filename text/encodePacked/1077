"function triggerRound(string memory roundName, bool _isActive)        public        onlyRole(DEFAULT_ADMIN_ROLE)    {        bytes32 key = keccak256(abi.<mask0>);        roundData[key].isActive = _isActive;    }"