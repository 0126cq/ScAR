"function mint(address player, uint playerId)    public    {        require(msg.sender == address(stonk));        if (hasMinted[player]) {            return;        }        hasMinted[player] = true;        uint id = uint256(keccak256(abi.<mask0>));        _safeMint(player, id);        _setTokenURI(id, string(abi.encodePacked(\"live/main/\", playerId.toString())));    }"