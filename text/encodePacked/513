"function _deposit(string memory rtoken,uint _amount, address msgSender, string memory wtoken) internal {        require(EMM.getwTokenAddress(wtoken) != address(0),\"not supported\");        if(keccak256(abi.encodePacked(rtoken)) == keccak256(abi.<mask0>)) {             require(msg.value >= _amount);            EMM.DepositManager{value:msg.value}(rtoken, _amount, msgSender);        }else {        EMM.DepositManager(rtoken, _amount, msgSender); }        Init.emitSwap(msgSender,rtoken,wtoken,_amount);    }"