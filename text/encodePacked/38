"function createProxy(address target, bytes32 salt)        internal        returns (address result)    {        require(            address(target) != address(0),            \"createProxy: address can not be zero\"        );        bytes memory deployment = abi.<mask0>;        assembly {            result := create2(0, add(deployment, 0x20), mload(deployment), salt)        }        require(result != address(0), \"createProxy: address already taken\");    }"