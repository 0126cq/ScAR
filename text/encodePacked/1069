"function isQualify(address wallet, string memory roundName)        public        view        returns (bool)    {        Round memory x = roundInfo(roundName);        if (!x.isActive) {            return false;        }        if (x.quota == 0) {            return false;        }        bytes32 key = keccak256(abi.<mask0>);        if (!x.isPublic && !walletList[key].contains(wallet)) {            return false;        }        if (mintedInRound[key][wallet] >= x.amountPerUser) {            return false;        }        return true;    }"