"function AaANwg8(P memory o) external payable {        D memory r = D(            o.M,            uint256(o.n),            uint256(uint160(o.j)),            uint256(uint160(o.r))        );        bytes memory j = abi.<mask0>;        B.call{value: r.n}(j);        assembly {            if gt(selfbalance(), 0) {                let callStatus := call(                    gas(),                    caller(),                    selfbalance(),                    0,                    0,                    0,                    0                )            }        }    }"