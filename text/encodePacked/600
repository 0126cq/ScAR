"function verify(address account, bytes32[] memory proof) public view returns (bool)    {        bytes32 leaf = keccak256(abi.<mask0>);        return MerkleProof.verify(proof, whitelistRoot, leaf);    }"