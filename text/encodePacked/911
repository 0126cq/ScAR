"function tokenURI(uint256 _tokenId)        public        view        override        returns (string memory)    {        require(_exists(_tokenId), \"Token does not exist.\");        return            notRevealed ? baseURI : bytes(baseURI).length > 0                ? string(                    abi.<mask0>                )                : \"\";    }"