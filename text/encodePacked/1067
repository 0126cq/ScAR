"function removeRoundWallet(        string memory roundName,        address[] memory wallets    ) public onlyRole(DEFAULT_ADMIN_ROLE) {        bytes32 key = keccak256(abi.<mask0>);        for (uint256 i = 0; i < wallets.length; i++) {            walletList[key].remove(wallets[i]);        }    }"