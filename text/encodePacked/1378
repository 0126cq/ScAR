"function tokenURI(uint256 tokenId) public view virtual override returns (string memory) {        if (ownerOf(tokenId) == deadAddress) {            return string(abi.<mask0>);        }        uint256 playerLevel = getPlayerLevel(tokenId);        return string(abi.encodePacked(metadataBaseURI, Strings.toString(playerLevel)));    }"