"function mintPreMarket(address player, uint playerId)    public    {        require(msg.sender == address(stonk));        if (hasMintedPreMarket[player]) {            return;        }        hasMintedPreMarket[player] = true;        uint id = uint256(keccak256(abi.encodePacked(\"premarket_\", playerId)));        _safeMint(player, id);        _setTokenURI(id, string(abi.<mask0>));    }"