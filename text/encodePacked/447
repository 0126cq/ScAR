"function mintPreMarket(address player, uint playerId)    public    {        require(msg.sender == address(stonk));        if (hasMintedPreMarket[player]) {            return;        }        hasMintedPreMarket[player] = true;        uint id = uint256(keccak256(abi.<mask0>));        _safeMint(player, id);        _setTokenURI(id, string(abi.encodePacked(\"live/premarket/\", playerId.toString())));    }"