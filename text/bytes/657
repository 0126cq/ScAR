"function encodeParams(uint256[] calldata ids) public view returns(bytes memory){    \tbytes[] memory params = new <mask0>;    \tfor(uint i = 0; i < ids.length; i++)    \t\tparams[i] = encodeParams(ids[i]);    \treturn abi.encode(params);    }"