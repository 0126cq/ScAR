"function multicall(bytes[] calldata data) external returns (bytes[] memory results) {        results = new <mask0>;        for (uint256 i = 0; i < data.length; i++) {            results[i] = _fastDelegateCall(data[i]);        }        return results;    }"