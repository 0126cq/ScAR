"function getTrancheLetter(uint256 index, uint256 _trancheCount) internal pure returns (string memory) {        bytes memory trancheLetters = bytes(\"ABCDEFGHIJKLMNOPQRSTUVWXY\");        bytes memory target = new <mask0>;        if (index == _trancheCount - 1) {            target[0] = \"Z\";        } else {            target[0] = trancheLetters[index];        }        return string(target);    }"