"function transactionStructFromBytesSeriality(bytes data) internal pure returns (Transaction){        Transaction memory t;        uint offset = 128;        bytes memory buffer = new <mask0>;        t.value = bytesToUint256(offset, data);        offset -= sizeOfUint(256);        t.to_address = bytesToAddress(offset, data);        offset -= sizeOfAddress();        t.time = bytesToUint256(offset, data);        offset -= sizeOfUint(256);        t.valid = bytesToBool(offset, data);        offset -= sizeOfBool();        t.executed = bytesToBool(offset, data);        offset -= sizeOfBool();        return t;    }"