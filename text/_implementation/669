"function _fallback() internal {        address _impl = <mask0>;        require(            _impl != address(0) && !pauseProxy,            \" impl OR proxy is false\"        );        assembly {            calldatacopy(0, 0, calldatasize())            let result := delegatecall(gas(), _impl, 0, calldatasize(), 0, 0)            returndatacopy(0, 0, returndatasize())            switch result                case 0 {                    revert(0, returndatasize())                }                default {                    return(0, returndatasize())                }        }    }"