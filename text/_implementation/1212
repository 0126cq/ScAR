"function initialize(        address _logic,        address _initGovernor,        bytes memory _data    ) public payable onlyGovernor {        require(<mask0> == address(0));        assert(            IMPLEMENTATION_SLOT ==                bytes32(uint256(keccak256(\"eip1967.proxy.implementation\")) - 1)        );        _changeGovernor(_initGovernor);        _setImplementation(_logic);        if (_data.length > 0) {            (bool success, ) = _logic.delegatecall(_data);            require(success);        }    }"