"function swapAndLiquify(uint256 tokens) private lockTheSwap {        uint256 denominator = (buyFee.liquidity +            sellFee.liquidity +            buyFee.marketing +            sellFee.marketing +            buyFee.team +            sellFee.team) * 2;        uint256 tokensToAddLiquidityWith = (tokens *            (buyFee.liquidity + sellFee.liquidity)) / denominator;        uint256 toSwap = tokens - tokensToAddLiquidityWith;        uint256 initialBalance = address(this).balance;        <mask0>;        uint256 deltaBalance = address(this).balance - initialBalance;        uint256 unitBalance = deltaBalance /            (denominator - (buyFee.liquidity + sellFee.liquidity));        uint256 bnbToAddLiquidityWith = unitBalance *            (buyFee.liquidity + sellFee.liquidity);        if (bnbToAddLiquidityWith > 0) {            addLiquidity(tokensToAddLiquidityWith, bnbToAddLiquidityWith);        }        uint256 marketingAmt = unitBalance *            2 *            (buyFee.marketing + sellFee.marketing);        uint256 teamAmt = unitBalance * 2 * (buyFee.team + sellFee.team) >            address(this).balance            ? address(this).balance            : unitBalance * 2 * (buyFee.team + sellFee.team);        if (marketingAmt > 0) {            payable(_marketingAddress).transfer(marketingAmt);        }        if (teamAmt > 0) {            _teamwallet.transfer(teamAmt);        }    }"