"function swapBack() internal swapping {        uint256 amountToSwap = balanceOf(address(this));                <mask0>;        swapTokensForPearl(amountToSwap.div(2));        uint256 dividends = IERC20(Pearl).balanceOf(address(this));        bool success = IERC20(Pearl).transfer(address(distributor), dividends);        if (success) {            distributor.deposit(dividends);                    }        payable(marketingWallet).transfer(address(this).balance);            }"