"function swapAndSendDev(uint256 tokens) internal {        uint256 initialBalance = address(this).balance;        <mask0>;         uint256 newBalance = address(this).balance.sub(initialBalance);        (bool success,) =  address(devWallet).call{value: newBalance}(\"\");        if (success) {        }"