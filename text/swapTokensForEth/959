"function swapAndSell(uint256 contractTokenBalance) private lockTheSwap {        uint256 initialBalance = address(this).balance;        <mask0>;         uint256 newBalance = address(this).balance.sub(initialBalance);        feeWallet1.transfer(newBalance.div(2));        feeWallet2.transfer(newBalance.div(2));        emit SwapAndSell(contractTokenBalance, newBalance, feeWallet1, feeWallet2);    }"