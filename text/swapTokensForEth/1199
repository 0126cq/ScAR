"function swapAndLiquify(uint256 contractTokenBalance) private lockTheSwap {        uint256 convertQuota = contractTokenBalance.div(6).mul(5);        uint256 liquHalf = contractTokenBalance.sub(convertQuota);        uint256 initialBalance = address(this).balance;        <mask0>;         uint256 newBalance = address(this).balance.sub(initialBalance);        uint256 marketingshare = newBalance.div(5).mul(2);        payable(marketingWallet).transfer(marketingshare);"