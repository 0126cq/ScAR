"function swapAndSendDevelopment(uint256 amount) private lockTheSwap {        uint256 initialBalance = address(this).balance;        <mask0>;        uint256 newBalance = address(this).balance.sub(initialBalance);        payable(_developmentWallet).transfer(newBalance);    }"