"function swapTokens(uint256 contractTokenBalance) private lockTheSwap {    <mask0>;    uint256 contractETHBalance = address(this).balance;    if (contractETHBalance > 0) {      sendETHToMarketing(address(this).balance);    }  }"