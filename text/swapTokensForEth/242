"function manualswapcustom(uint256 percentage) external onlyOwner() {        uint256 contractBalance = balanceOf(address(this));        uint256 swapbalance = contractBalance.div(10**5).mul(percentage);        <mask0>;    }"