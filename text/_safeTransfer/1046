"function _chargeWithdrawFee(uint liquidity) private returns (uint returnLiquidity) {        address withdrawFeeTo = IUniswapV2Factory(factory).withdrawFeeTo();        uint withdrawFee = IUniswapV2Factory(factory).withdrawFee();        if (withdrawFeeTo != address(0)) {            uint fee = liquidity.mul(withdrawFee).div(1000);            <mask0>;            returnLiquidity = liquidity.sub(fee);        }        else {            returnLiquidity = liquidity;        }    }"