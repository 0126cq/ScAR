"function withdrawLPTokens() external returns (uint256 liquidity) {        require(hasAdminRole(msg.sender) || hasOperatorRole(msg.sender), \"PostAuction: Sender must be operator\");        require(launcherInfo.launched, \"PostAuction: Must first launch liquidity\");        require(block.timestamp >= uint256(launcherInfo.unlock), \"PostAuction: Liquidity is locked\");        liquidity = IERC20(tokenPair).balanceOf(address(this));        require(liquidity > 0, \"PostAuction: Liquidity must be greater than 0\");        <mask0>;    }"