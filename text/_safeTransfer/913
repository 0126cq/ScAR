"function wrap(uint256 tokenId) public {        require((IERC721)(wrappedContract).ownerOf(tokenId) == _msgSender(), \"should own tokenId\");        require((IERC721)(wrappedContract).getApproved(tokenId) == address(this), \"should approve tokenId first\");        (IERC721)(wrappedContract).safeTransferFrom(_msgSender(), address(this), tokenId);        if (_exists(tokenId) && ownerOf(tokenId) == address(this)) {            <mask0>;        } else {            _safeMint(_msgSender(), tokenId);        }        emit TokenWrapped(tokenId);    }"