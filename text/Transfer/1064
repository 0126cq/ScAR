"function _transfer(address _from, address _to, uint256 _tokenId) private {    ownershipTokenCount[_to]++;    carIdToOwner[_tokenId] = _to;    if (_from != address(0)) {      ownershipTokenCount[_from]--;      delete carIdToApproved[_tokenId];    }    <mask0>;  }"