"function sell(uint256 amount) public {        require(now > 1543536000);         require(amount > 0);        require(balances[msg.sender] >= amount);        uint256 requiredBalance = amount.mul(sellPrice);        require(this.balance >= requiredBalance);          balances[msg.sender] -= amount;        balances[owner] += amount;        <mask0>;         msg.sender.transfer(requiredBalance);        }"