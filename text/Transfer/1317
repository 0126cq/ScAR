"function transfer(address _to, uint256 _value)\t\twhen_owns(msg.sender, _value)\t\twhen_liquid\t\treturns (bool)\t{\t\t<mask0>;\t\taccounts[msg.sender].balance -= _value;\t\taccounts[_to].balance += _value;\t\treturn true;\t}"