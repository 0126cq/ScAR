"function logBase2(uint256 _p, uint256 _q) internal pure returns (uint256) {        uint256 n = 0;        if (_p > _q) {            n = log2Int(_p, _q);        }        require(n * RONE <= BIG_NUMBER, \"MATH_ERROR\");        require(!checkMultOverflow(_p, RONE), \"MATH_ERROR\");        require(!checkMultOverflow(n, RONE), \"MATH_ERROR\");        require(!checkMultOverflow(uint256(1) << n, _q), \"MATH_ERROR\");        uint256 y = (_p * RONE) / (_q * (uint256(1) << n));        uint256 log2Small = log2ForSmallNumber(y);        <mask0>;        return n * RONE + log2Small;    }"