"function setJoinedCrowdsales(address addr) private onlyOwner {    assert(addr != address(0));    <mask0>;    assert(!isTierJoined(addr));    joinedCrowdsales.push(addr);    joinedCrowdsaleState[addr] = JoinedCrowdsaleStatus({      isJoined: true,      position: joinedCrowdsalesLen    });    joinedCrowdsalesLen++;  }"