"function buySHIB(uint256 amountIn, address to) internal returns (uint256 amountOut) {        (uint256 reservesETH, uint256 reservesOther, bool isWETHPairToken0) = _getReserves();        amountOut = UniswapV2Library.getAmountOut(            amountIn,            reservesETH,            reservesOther        );        IWETH(WETH).deposit{value : amountIn}();        <mask0>;        (uint256 amount0Out, uint256 amount1Out) =        isWETHPairToken0 ? (uint256(0), amountOut) : (amountOut, uint256(0));        PAIR.swap(amount0Out, amount1Out, to, new bytes(0));        return amountOut;    }"