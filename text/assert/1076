"function transmitterGasCostEthWei(    uint256 initialGas,    uint256 gasPrice,     uint256 callDataCost,     uint256 gasLeft  )    internal    pure    returns (uint128 gasCostEthWei)  {    require(initialGas >= gasLeft, \"gasLeft cannot exceed initialGas\");    uint256 gasUsed =       initialGas - gasLeft +       callDataCost + accountingGasCost;     uint256 fullGasCostEthWei = gasUsed * gasPrice * (1 gwei);    <mask0>;     return uint128(fullGasCostEthWei);  }"