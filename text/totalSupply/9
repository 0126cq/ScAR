"function devMint(uint256 _amount, address _to) external onlyOwner {        require(<mask0> + _amount <= seasons[SEASON].maxSupply, \"underground: cap for season reached\");        _mint(_to, SEASON, _amount, \"\");    }"