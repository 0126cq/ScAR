"function treasuryMint(uint quantity)    public    onlyOwner  {    require(      quantity > 0,      \"Invalid mint amount\"    );    require(      <mask0> + quantity <= maxSupply,      \"Maximum supply exceeded\"    );    _safeMint(msg.sender, quantity);  }"