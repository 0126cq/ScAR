"function updateMaxWalletAmount(uint256 newNum) external onlyOwner {        require(newNum >= (<mask0> * 1 / 100)/1e18, \"Cannot set maxWallet lower than 1%\");        maxWallet = newNum * (10**18);    }"