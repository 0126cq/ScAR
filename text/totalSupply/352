"function endSale() external onlyOwner {        require(!hasSaleEnded, \"Sale already ended\");        while (<mask0> < MAX_SUPPLY) {            _safeMint(msg.sender, totalSupply() + 1);        }        isPresale = false;        isPublicSale = false;        hasSaleEnded = true;        emit SaleEnded();    }"