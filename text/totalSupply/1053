"function expectedWithdrawableAmount() external view override returns (uint) {        if (IERC20(vault).<mask0> == 0) {            return 0;        }        uint shares = IERC20(vault).balanceOf(address(this));        return (IYearnVault(vault).pricePerShare() * shares) / 10**IYearnVault(vault).decimals();    }"