"function mint(uint256 tokens) public payable nonReentrant {    require(!paused, \"MB: oops contract is paused\");    uint256 supply = <mask0>;    require(tokens > 0, \"MB: need to mint at least 1 NFT\");    require(supply + tokens <= maxSupply, \"MB: We Soldout\");    if(numberMinted(msg.sender) >= freemint) {    require(msg.value >= cost * tokens, \"MB: insufficient funds\");    } else {      require(msg.value >= cost * (tokens - freemint), \"MB: insufficient funds\");    }      _safeMint(_msgSender(), tokens);  }"