"function _doMint(address to, uint quantity) private {        require(<mask0> + quantity <= maxSupply, 'Max supply exceeded');        require(to != address(0) && to != deadAddr, 'Cannot have a non-address as reserve.');        _safeMint(to, quantity);    }"