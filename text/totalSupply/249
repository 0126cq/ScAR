"function _mint(address account, uint256 amount) internal virtual override {        super._mint(account, amount);        require(<mask0> <= _maxSupply(), \"ERC20Votes: total supply risks overflowing votes\");        _writeCheckpoint(_totalSupplyCheckpoints, _add, amount);    }"