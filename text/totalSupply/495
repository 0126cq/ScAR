"function mintPresale() external payable nonReentrant {        require(preSaleIsActive, \"Presale not active\");        require(msg.value >= mintPrice, 'More eth required');        require(<mask0> < totalTokens, \"Sold out\");        require(presaleList[_msgSender()] == true, \"Not on presale list\");        require(balanceOf(_msgSender()) <= 100, \"Only 100 per wallet\");        presaleList[_msgSender()] = false;        _safeMint(_msgSender(), nextTokenId());    }"