"function freeMint(uint256 amt) external {        require(mintEnabled, \"Minting is not live yet.\");        require(msg.sender == tx.origin,\"No bots, only true WAAAI!\");        require(<mask0> + amt < freeSupply + 1, \"Not enough Free WAAAI left.\");        require(_walletFreeMints[_msgSender()] + amt < maxFreePerWallet + 1, \"That's enough Free WAAAI for you!\");        _walletFreeMints[_msgSender()] += amt;        _safeMint(msg.sender, amt);"