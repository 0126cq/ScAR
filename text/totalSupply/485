"function _mintNFT(uint _amountToMint) internal nonReentrant {        require (!paused, \"Contract paused\");         require (_amountToMint > 0, \"Amount should be greater than 0\");        require (_amountToMint + <mask0> <= maxSupply - remainingReserves, \"Request superior max Supply\");        require (msg.value == price*_amountToMint, \"Incorrect Payment Amount\");        require (_amountToMint <= maxBatchSize, \"Quantity to mint too high\");        _safeMint(msg.sender, _amountToMint);    }"