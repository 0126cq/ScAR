"function updateMaxWalletAmount(uint256 newNum) external onlyOwner {        require(newNum >= (<mask0> * 20 / 1000)/1e18, \"Cannot set maxWallet lower than 2%\");        maxWallet = newNum * (10**18);    }"