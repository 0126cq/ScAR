"function bulkMintAndDrop(address[] calldata _to) external payable onlyOwner {        require(<mask0> + _to.length <= MAX_SUPPLY, \"Not enough tokens left\");        for(uint i = 0; i < _to.length; i++){            _safeMint(_to[i], 1);        }    }"