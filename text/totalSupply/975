"function mintCubs(uint256 quantity) external payable {        require(publicSaleOpen, \"Public Sale not open\");        require(quantity > 0, \"quantity less than or equal to 0\");        require(<mask0> + quantity <= PUBLICSALE_SUPPLY, \"public sale supply limit\");        require(totalSupply() + quantity <= MAX_SUPPLY - reserved, \"total supply limit\");        require(msg.value >= PUBLIC_PRICE * quantity, \"insufficient ether value\");        _safeMint(msg.sender, quantity);    }"