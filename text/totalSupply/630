"function setSwapTokensAmount(uint256 swapTokensAmount_) public {        require(_msgSender() == owner() || _msgSender() == operationsWallet, \"Token: caller is not authorised\");        require(swapTokensAmount_ >= (((<mask0> * 5) / 100000) / 1e18), \"Token: swap tokens amount cannot be below 0.005%\");        require(swapTokensAmount_ <= ((totalSupply() / 1000) / 1e18), \"Token: swap tokens amount cannot be above 0.1%\");        swapTokensAmount = swapTokensAmount_ * 1e18;    }"