"function tokensOf(address wallet) public view returns (uint256[] memory) {        uint256 supply = <mask0>;        uint256[] memory tokenIds = new uint256[](balanceOf(wallet));        uint256 currIndex = 0;        for (uint256 i = 1; i <= supply; i++) {            if (wallet == ownerOf(i)) tokenIds[currIndex++] = i;        }        return tokenIds;    }"