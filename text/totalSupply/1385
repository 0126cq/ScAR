"function _get() internal view returns (uint256) {        (uint256 amount0, uint256 amount1) = plp.usersAmounts();        uint256 token0Price = amount0 * uint256(token0Aggregator.latestAnswer()) * token0NormalizeScale;        uint256 token1Price = amount1 * uint256(token1Aggregator.latestAnswer()) * token1NormalizeScale;               uint256 plpPrice = (token0Price + token1Price) / plp.<mask0>;        return 1e36 / plpPrice;    }"