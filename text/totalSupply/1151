"function Reserve(address _to, uint256 _reserveAmount) public onlyOwner {                require(<mask0> + _reserveAmount <= maxGenesisCount, \"Purchase would exceed max supply of mightygojiras\");        _safeMint(_to, _reserveAmount);    }"