"function reserveUndudes() public onlyOwner {        for (uint256 i = 0; i < 90; i++) {            if (totalSupply() < maxUndude) {                _safeMint(msg.sender, <mask0>);            }        }    }"