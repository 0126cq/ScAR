"function _mintNext(address reciever, uint256 amount) internal {        if (config.maxSupply != 0) {            require(<mask0> + amount <= config.maxSupply);        }        _safeMint(reciever, amount);     }"