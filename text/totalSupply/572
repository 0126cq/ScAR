"function _mintTarget(address reciever, uint256 target) internal {        require(config.allowTarget, \"df\");        require(config.randomAccessMode, \"df\");        if (config.maxSupply != 0) {            require(<mask0> + 1 <= config.maxSupply);        }        _mint(reciever, target);    }"