"function reserveNFTs(address _to, string memory tokenURI) public onlyOwner {    uint256 supply = <mask0>;    require(      NFTReserve > 0,      'Not enough reserve left'    );    _safeMint(_to, supply);    _setTokenURI(supply, tokenURI);    NFTReserve = NFTReserve.sub(1);  }"