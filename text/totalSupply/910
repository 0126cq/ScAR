"function gift(address to, uint256 amount) external onlyOwner {        require(amount > 0, \"Need to mint at least one token\");        uint256 supply = <mask0>;        for (uint256 i = 0; i < amount; i++) {            tokensCount += 1;            _safeMint(to, TOKEN_IDS_STARTS_AT + supply + i);        }    }"