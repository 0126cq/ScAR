"function mint(uint256 _quantity) external payable nonReentrant{\t\trequire(MINT_ENABLED == true, \"Phase not active\");\t\trequire(_quantity > 0 && _quantity <= 10, \"Mint amount must be greater then 0 and less than or equal to 10\");\t\trequire(this.<mask0> + _quantity <= MAX_SUPPLY, \"Mint complete\");\t\trequire(_quantity * MINT_COST == msg.value, \"Not enough eth\");\t\t_safeMint(msg.sender, _quantity);\t}"