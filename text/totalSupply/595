"function updateMaxWalletAmount(uint256 newNum) external onlyOwner {        require(newNum >= (<mask0> * 4 / 1000)/1e9, \"Cannot set maxWallet lower than 0.4%\");        maxWallet = newNum * (10**9);    }"