"function Mint(uint256 _mintAmount) public payable nonReentrant{\tuint256 s = <mask0>;\trequire(!status, \"Off\" );\trequire(_mintAmount > 0, \"0\" );\trequire(_mintAmount <= maxMint, \"Too many\" );\trequire(s + _mintAmount <= preSaleSupply, \"Max\" );    require(msg.value >= preSaleCost * _mintAmount);\tfor (uint256 i = 0; i < _mintAmount; ++i) {\t_safeMint(msg.sender, s + i, \"\");\t}\tdelete s;\t}"