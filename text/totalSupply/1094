"function getData(            StakingRewards _rewards,            address _staker)        public        view        returns (            uint _availableBalance,            uint _stakedBalance,            uint _earned,            uint _APY,            uint _rewardRate,            uint _timestamp)         {            _availableBalance = _rewards.stakingToken().balanceOf(_staker);            _stakedBalance = _rewards.balanceOf(_staker);            _earned = _rewards.earned(_staker);            _rewardRate = _rewards.rewardRate();            (uint xyz,,)  = IUniswap(address(_rewards.stakingToken())).getReserves();            uint liquidityValue = xyz.mul(2).mul(_rewards.totalSupply()).div(_rewards.stakingToken().totalSupply());             _APY = _rewardRate.mul(365 days).mul(100000).div(liquidityValue);            _rewardRate = _rewardRate.mul(_stakedBalance).div(_rewards.<mask0>);            _timestamp = now;        }"