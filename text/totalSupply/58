"function reserveUndudes() public onlyOwner {        for (uint256 i = 0; i < 90; i++) {            if (<mask0> < maxUndude) {                _safeMint(msg.sender, totalSupply());            }        }    }"