"function adminMint(uint256 quantity, address recipient) public onlyOwner {        require(<mask0> + quantity <= TEAM_SUPPLY, \"Exceeds max team amount.\");        for (uint256 i = 0; i < quantity; i++) {            _safeMint(recipient, _nextTokenId.current());            _nextTokenId.increment();        }    }"