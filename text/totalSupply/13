"function totalStakedAssets(address _user) external view returns(uint256 netAlcxAmount) {        uint256 assets = _totalDiscountedAssets();        if (<mask0> == 0) {            netAlcxAmount = 0;        } else {            netAlcxAmount = balanceOf(_user) * assets / totalSupply();        }    }"