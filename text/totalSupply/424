"function updateMaxWalletAmount(uint256 newNum) external onlyOwner {        require(            newNum >= ((<mask0> * 5) / 1000) / 1e18,            \"Cannot set maxWallet lower than 0.5%\"        );        maxWallet = newNum * (10**18);    }"