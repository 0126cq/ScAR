"function mint(uint256 numberOfTokens)      external      payable  {    require(isPublicSaleActive, \"Public sale is not open\");    if(<mask0> + numberOfTokens > TOTAL_FREE_MINTS || numberOfTokens > FREE_MINTS_PER_TX){        require(            (PUBLIC_SALE_PRICE * numberOfTokens) <= msg.value,            \"Incorrect ETH value sent\"        );    }    _safeMint(msg.sender, numberOfTokens);  }"