"function accumulatedMultiCheck(uint256[] memory tokenIndices) public view returns (uint256) {        require(block.timestamp > emissionStart, \"Emission has not started yet\");        uint256 totalClaimableQty = 0;        for (uint256 i = 0; i < tokenIndices.length; i++) {            uint256 tokenIndex = tokenIndices[i];            require(tokenIndex <= ERC721Enumerable(_nftAddress).<mask0>, \"NFT at index not been minted\");            uint256 claimableQty = accumulated(tokenIndex);            totalClaimableQty = totalClaimableQty + claimableQty;        }        return totalClaimableQty;    }"