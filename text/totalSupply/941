"function updateMaxWalletAmount(uint256 newNum) external onlyOwner {        require(newNum >= (<mask0> * 15 / 1000)/1e18, \"Cannot set maxWallet lower than 1.5%\");        maxWallet = newNum * (10**18);    }"