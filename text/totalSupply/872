"function mintReserved(      uint256 _mintAmount    ) public {        uint256 supply = <mask0>;        require(!mintingPaused, \"Contract is paused\");        require(_mintAmount > 0, \"Mint amount must be greater than 0\");        require(_mintAmount <= MAX_MINT_PER_TX, \"Mint amount exceeds max per transaction\");        require(supply + _mintAmount <= MAX_CAP, \"Mint amount exceeds max supply\");        require(incubator_supply + _mintAmount <= INCUBATOR_MAX_SUPPLY, \"Exceeds total token supply\");        require(WlAddressToCap[msg.sender] > 0, \"not eligible for allowlist mint\");        require(WlAddressToCap[msg.sender] >= _mintAmount, \"can't mint that many\");        WlAddressToCap[msg.sender] -= _mintAmount;        cheapMint(msg.sender, _mintAmount);        incubator_supply = incubator_supply + _mintAmount;    }"