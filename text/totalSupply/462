"function giveAwayMany(address[] memory addresses) external onlyOwner() {        require( addresses.length <= _giftReserved, \"Exceeds reserved gift Bots supply\" );        for (uint i = 0; i < addresses.length; i++) {            _safeMint(addresses[i], <mask0> + 1);        }        _giftReserved -= addresses.length;    }"