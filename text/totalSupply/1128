"function Claim(uint256 quantity)    external    payable    callerIsUser  {    require(quantity <= freemintAmount, \"Too many minted at once\");    require(<mask0> + quantity <= collectionSizeFree, \"reached max supply\");    _safeMint(msg.sender, quantity);  }"