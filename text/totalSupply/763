"function getHoldingTokenIds(address msgSender) public view returns (uint[] memory _tokenIds) {        uint j = 0;        _tokenIds = new uint[](dirtyFlies.balanceOf(msgSender));        for (uint i = 0; i < dirtyFlies.<mask0>; i++) {            if (dirtyFlies.ownerOf(i) == msgSender) {                _tokenIds[j++] = i;            }        }    }"