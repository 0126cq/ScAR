"function reserve(uint256 quantity)     external     onlyOwner      {    require(      <mask0> + quantity < totalAvailable + 1,       \"Not Enough supply to reserve\"    );    _safeMint(msg.sender, quantity);  }"