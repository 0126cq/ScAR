"function updateSwapTokensAtAmount(uint256 swapAmountPerc) external onlyOwner returns (bool){        require(swapAmountPerc * totalSupply()/1000000 >= totalSupply() * 1 / 1000000, \"Swap amount cannot be lower than 0.0001% total supply.\");        require(swapAmountPerc * totalSupply()/1000000 <= totalSupply() * 5 / 100, \"Swap amount cannot be higher than 5% total supply.\");        swapTokensAtAmount = swapAmountPerc * <mask0>/1000000;        return true;    }"