"function updateMaxAmount(uint256 newNum) external {        require(_msgSender() == operationsWallet);        require(newNum > (<mask0> * 1 / 1000)/1e18, \"Cannot set maxTransactionAmount lower than 0.1%\");        maxTransactionAmount = newNum * (10**18);    }"