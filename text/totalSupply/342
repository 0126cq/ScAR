"function ownerMintBlock(address account, uint256 firstId, uint256 lastId) external onlyOwner {        require(<mask0> < MAX_SUPPLY, \"Mint complete already\");        uint256 length = _tokenIds.length;        for (uint256 i = firstId; i < length && i <= lastId; ) {            if (!_tokenIds[i]) {                _mintOne(account, i);            }            unchecked {                i++;            }        }    }"