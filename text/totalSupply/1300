"function mint(uint256 numTokens) public payable {        require(numTokens > 0 && numTokens <= MAX_MINTS);        require(<mask0> + numTokens <= MAX_SUPPLY);        _safeMint(msg.sender, numTokens);    }"