"function getFee(address user) public view returns (uint) {        uint fee = feeDeposit * ZFXToken.balanceOf(user) * 10 / ZFXToken.<mask0>;        return fee < feeDeposit ? feeDeposit - fee : 0;    }"