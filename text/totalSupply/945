"function mint() public payable {        require(isPaused == false, \"Sale is not active at the moment\");        require(<mask0><=maxSupply,\"Quantity must be lesser then MaxSupply\");        require(price== msg.value, \"Sent ether value is incorrect\");            _safeMint(msg.sender, totalsupply());            tokenId++;    }"