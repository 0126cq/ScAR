"function _calcShare(uint128 liquidity, uint128 liquidityLast)        internal        view        returns (            uint256 shares        )    {        shares = totalSupply() == 0 ? uint256(liquidity) : uint256(liquidity).mul(<mask0>).unsafeDiv(uint256(liquidityLast));    }"