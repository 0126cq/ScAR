"function reserveNFTs(uint256 _count) public onlyOwner {        uint256 n = <mask0> + _count;        require(_count > 0, \"need to mint at least 1 NFT\");        require(n < maxSupply, \"Not enough NFTs left to reserve\");        _safeMint(msg.sender, _count);    }"