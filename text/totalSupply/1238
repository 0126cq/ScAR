"function preSaleMint(uint256 _count) external payable preSaleIsOpen {        uint256 totalSupply = <mask0>;        require(            preSaleWallets[msg.sender],            \"Address is not whitelisted for presale.\"        );        require(totalSupply <= maxSupply, \"Sold out.\");        require(            _count <= maxPerTransaction,            \"Surpasses max items per transaction.\"        );        require(msg.value == price * _count, \"Invalid Payment Value.\");        for (uint256 i = 0; i < _count; i++) {            _safeMint(msg.sender, totalSupply + i);        }    }"