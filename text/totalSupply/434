"function safeMint(uint256 _amount) public whenNotPaused onlyOwner  {        require(<mask0> + _amount <= maxPasses, \"DickPass: Exceeding MaxSupply\");        address to = msg.sender;        for(uint256 i=0; i<_amount; i++) {            _tokenIdCounter.increment();            uint256 tokenId = _tokenIdCounter.current();            _safeMint(to, tokenId);        }    }"