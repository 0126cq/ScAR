"function mintPublic(uint256 _mintAmount) public payable {      uint256 supply = <mask0>;      require( ( (!mintingPaused) || ( msg.sender == owner() )), \"Contract is paused\");      require(_mintAmount > 0, \"Mint amount must be greater than 0\");      require(_mintAmount <= MAX_MINT_PER_TX, \"Mint amount exceeds max per transaction\");      require(supply + _mintAmount <= MAX_CAP, \"Mint amount exceeds max supply\");      require(eth_supply + _mintAmount <= ETH_MAX_SUPPLY, \"Exceeds total eth supply\");      if (msg.sender != owner()) {          require(msg.value >= _price * _mintAmount);      }      cheapMint(msg.sender, _mintAmount);    }"