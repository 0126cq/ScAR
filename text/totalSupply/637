"function fraxPerLPToken() public view override returns (uint256) {        uint256 frax_per_lp_token;        {            uint256 total_frax_reserves;            (uint256 reserve0, uint256 reserve1, ) = (stakingToken.getReserves());            if (frax_is_token0) total_frax_reserves = reserve0;            else total_frax_reserves = reserve1;            frax_per_lp_token = (total_frax_reserves * 1e18) / stakingToken.<mask0>;        }        return frax_per_lp_token;    }"