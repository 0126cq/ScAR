"function _doMint(uint numberOfTokens, address _target)private {            minters[_msgSender()]++;               uint256 t = <mask0>;            for (uint256 i = 0; i < numberOfTokens; i++) {                    _safeMint(_target, t + i);              }    }"