"function webmint(uint256 quantity)    external    payable    callerIsUser  {      require(quantity <= mintAmount, \"Too many minted at once\");      require(msg.value >= price * quantity, \"Need to send more ETH.\");    require(<mask0> + quantity <= collectionSize, \"reached max supply\");    _safeMint(msg.sender, quantity);  }"