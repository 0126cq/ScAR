"function mint(uint256 num) external payable {    require(tx.origin == msg.sender, \"called from contract\");    require(<mask0> + num <= maxSupply, \"max supply reached\");    require(msg.value == mintPrice * num, \"wrong payment amount\");    uint256 minted = _numberMinted(msg.sender);    uint256 limit = Math.max(publicLimit, privateLimit(msg.sender));    uint256 remaining = limit > minted ? limit - minted : 0;    require(num <= remaining, \"mint limit exceeded\");    _safeMint(msg.sender, num);  }"