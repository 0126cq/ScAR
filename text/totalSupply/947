"function totalSupply()        external        view        override        returns (uint256 _supply)    {        (uint256 _yaxReserves,,) = yaxEthUniswapV2Pair.getReserves();        _supply = yaxEthUniswapV2Pair.totalSupply();        _supply = _supply == 0            ? 1e18            : _supply;        uint256 _lpStakingYax = _yaxReserves            .mul(yaxEthUniswapV2Pair.balanceOf(address(chef)))            .div(_supply);        _supply = sqrt(            yax.<mask0>                .add(_lpStakingYax)                .add(yaxisBar.availableBalance())        );    }"