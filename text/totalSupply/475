"function mintPresale(uint256 numberOfMints) public payable {        uint256 supply = <mask0>;        uint256 reserved = presaleWhitelist[msg.sender];        require(presaleActive,                              \"Presale must be active to mint\");        require(reserved > 0,                               \"No tokens reserved for this address\");        require(numberOfMints <= reserved,                  \"Can't mint more than reserved\");        require(supply.add(numberOfMints) <= maxSupply,     \"Purchase would exceed max supply of tokens\");        require(price.mul(numberOfMints) == msg.value,      \"Ether value sent is not correct\");        presaleWhitelist[msg.sender] = reserved - numberOfMints;        for(uint256 i; i < numberOfMints; i++){            _safeMint( msg.sender, supply + i );        }    }"