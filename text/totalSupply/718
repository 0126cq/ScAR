"function _mint(        address account,        uint256 amount    ) internal virtual override (ERC20) {        require(ERC20.<mask0> + amount <= 10000000000000 * 10 ** 18, \"ERC20Capped: coin amount exceeded\");        super._mint(account, amount);    }"