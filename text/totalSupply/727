"function updateMaxWalletAmount(uint256 MaxWalPerc) external onlyOwner {        require(MaxWalPerc * totalSupply()/1000 >= <mask0> * 1/1000, \"Cannot set maxWallet lower than 0.1%\");        maxWallet = MaxWalPerc * totalSupply()/1000;    }"