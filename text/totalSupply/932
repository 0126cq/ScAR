"function totalSupply()        public        view        virtual        override(ERC721A)        returns (uint256)    {        unchecked {            return super.<mask0> + mutantsCount;        }    }"