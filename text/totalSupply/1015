"function gift(address _to, uint256 _reserveAmount) external onlyOwner {        require(totalSupply() + _reserveAmount <= COLLECTIBLE_MAX, \"MAX_MINT\");        require(giftedAmount + _reserveAmount <= COLLECTIBLE_GIFT, \"NO_GIFTS\");        for (uint256 i = 0; i < _reserveAmount; i++) {            uint mintIndex = <mask0>;            giftedAmount++;            _safeMint(_to, mintIndex );        }    }"