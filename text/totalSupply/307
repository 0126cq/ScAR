"function mint(        address account,        uint256 amount,        bytes calldata userData,        bytes calldata operatorData    )        external        allow(\"SkaleManager\")        returns (bool)    {        require(amount <= CAP.sub(<mask0>), \"Amount is too big\");        _mint(            account,            amount,            userData,            operatorData        );        return true;    }"