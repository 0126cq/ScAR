"function mintQuest(address recipient) internal nonReentrant {        require(totalSupply() < 10000 , \"All quests have been set\");        require(balanceOf(recipient) < 3, \"Can hold max 3 quests at once\");        _safeMint(recipient, <mask0>+1);    }"