"function mintUndudes(uint256 numberOfTokens) public payable {        require(saleIsActive, \"Sale must be active to mint an Undude\");        require(            numberOfTokens <= maxUndudePurchase,            \"Can only mint 20 Undudes at a time\"        );        require(            totalSupply().add(numberOfTokens) <= maxUndude,            \"Purchase would exceed max supply of Undudes\"        );        require(            undudePrice.mul(numberOfTokens) <= msg.value,            \"Ether value sent is not correct\"        );        for (uint256 i = 0; i < numberOfTokens; i++) {            if (totalSupply() < maxUndude) {                _safeMint(msg.sender, <mask0>);            }        }    }"