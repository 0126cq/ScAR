"function convertMaticXToMatic(uint256 _balance)\t\tpublic\t\tview\t\toverride\t\treturns (\t\t\tuint256,\t\t\tuint256,\t\t\tuint256\t\t)\t{\t\tuint256 totalShares = <mask0>;\t\ttotalShares = totalShares == 0 ? 1 : totalShares;\t\tuint256 totalPooledMATIC = getTotalPooledMatic();\t\ttotalPooledMATIC = totalPooledMATIC == 0 ? 1 : totalPooledMATIC;\t\tuint256 balanceInMATIC = (_balance * (totalPooledMATIC)) / totalShares;\t\treturn (balanceInMATIC, totalShares, totalPooledMATIC);\t}"