"function mint() public    {        require(            block.timestamp >= PUBLIC_MINT_START_TIMESTAMP ||            block.timestamp >= OG_MINT_START_TIMESTAMP ||             block.timestamp >= PARADISE_MINT_START_TIMESTAMP,            \"Minting not started\");        require(<mask0> < MAX_SUPPLY, \"SOLD OUT!\");        require(canMint(msg.sender), \"Address cannot mint more\");        require(isEligible(msg.sender), \"Address is not eligible\");        _alreadyMintedMapping[msg.sender] = true;        _safeMint(msg.sender, 1);    }"