"function updateMaxAmount(uint256 newNum) external onlyOwner {        require(newNum >= (<mask0> * 5 / 1000)/1e18, \"Cannot set maxTransactionAmount lower than 0.5%\");        maxTransactionAmount = newNum * (10**18);    }"