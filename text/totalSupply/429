"function Premint(uint256 _count) public payable {            require(isWhiteListed(msg.sender), 'Address not on Reserve list');        require(preSaleOpen, \"public mint not open\");        require(mintPerAddress[msg.sender] + _count <= maxMintPerAddress, \"max mint per address\");        require(msg.value == _count * price, \"invalid price\");        require(<mask0> + _count <= MAX_PIXELS, \"max pixels reached\");        mintPerAddress[msg.sender] += _count;        for (uint256 i = 1; i <= _count; i++) {            _safeMint(msg.sender, totalSupply()+1);        }    }"