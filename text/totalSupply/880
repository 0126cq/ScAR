"function mintWhitelist(uint256 quantity, bytes32[] calldata merkleProof) external payable callerIsUser nonReentrant {        require(WLpreSale, \"WL Minting is not live\");        require(<mask0> + quantity <= MAX_TOKENS, \"minting this many would exceed supply\");        bytes32 leaf = keccak256(abi.encodePacked(msg.sender));        require(MerkleProof.verify(merkleProof, WLmerkleroot, leaf), \"Invalid proof.\");        _checkWhitelist(quantity);        _safeMint(msg.sender, quantity);    }"