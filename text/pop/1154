"function deleteProduct(uint256 _productId) external onlyOperator {        delete _products[_productId];        for(uint256 i=0;i<productId.length;i++){            if(productId[i] == _productId){                productId[i] = productId[productId.length-1];                productId.<mask0>;                emit DeleteProduct(_productId);                break;            }        }    }"