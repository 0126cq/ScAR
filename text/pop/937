"function popTokenArray(address _tokenAddress) private {        for(uint256 i = 0; i <= tokens.length; i++)        {            if(_tokenAddress == tokens[i])            {                tokens[i] = tokens[tokens.length - 1];                tokens.<mask0>;                break;            }        }    }"