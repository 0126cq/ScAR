"function cancelBeneficiary(address beneficiary) external onlyOwner {        for (uint256 i = 0; i < beneficiaries.length; i++) {            if (beneficiaries[i] == beneficiary) {                beneficiaries[i] = beneficiaries[beneficiaries.length - 1];                beneficiaries.<mask0>;                break;            }        }        emit BeneficiaryCanceled(            beneficiary,            tokenAmounts[beneficiary],            releasedAmount[beneficiary]        );        delete tokenAmounts[beneficiary];        delete lastReleaseDate[beneficiary];        delete releasedAmount[beneficiary];    }"