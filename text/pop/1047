"function removePlugin(uint256 seriesId, bytes calldata pluginData) public onlySeriesOwner(seriesId) transferFees() payable override {        (            uint256 toRemove        ) = abi.decode(pluginData, (uint256));        address tokenRemoved = tokensDeployed[seriesId][toRemove];        tokensDeployed[seriesId][toRemove] = tokensDeployed[seriesId][tokensDeployed[seriesId].length - 1];        tokensDeployed[seriesId].<mask0>;        tokensPerEntity[seriesId]--;        emit TokenRemoved(seriesId, tokenRemoved);    }"