"function removeGenToken(address _user, uint256 id) internal {        for (uint256 i; i < accountGenesisStake[_user].tokens.length; i++) {            if (accountGenesisStake[_user].tokens[i] == id) {                accountGenesisStake[_user].tokens[i] = accountGenesisStake[                    _user                ].tokens[accountGenesisStake[_user].tokens.length - 1];                accountGenesisStake[_user].tokens.<mask0>;                break;            }        }    }"