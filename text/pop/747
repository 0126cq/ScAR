"function deleteAll(Set storage set) internal {        uint256 length = set._values.length;        if (length > 0) {            for (uint256 i = 0; i < length; i++) {                uint256 idx = length - i - 1;                address deleteAddress = set._values[idx]._address;                delete set._indexes[deleteAddress];                set._values.<mask0>;            }        }    }"