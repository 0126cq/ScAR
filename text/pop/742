"function unlock(uint256 _id, uint256 pos) external {        require(approvedContract[msg.sender], \"Access denied\");        require(locks[_id][pos] == msg.sender, \"Pos incorrect\");        unchecked {            uint256 lastId = locks[_id].length - 1;            if (pos != lastId) locks[_id][pos] = locks[_id][lastId];            locks[_id].<mask0>;        }    }"