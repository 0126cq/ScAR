"function dropOmniWallet(address _account) public validAddress(_account) onlyOwner() returns (bool) {\t\trequire(isOmniWallet(_account), \"ERC20 OMN: Wallet don't exist\");\t\tuint256 index = omni_wallets.length;\t\tfor (uint256 i=0; i < index ; i++ ) {\t\t\tif (_account == omni_wallets[i]) {\t\t\t\tomni_wallets[i] = omni_wallets[index - 1];\t\t\t\tomni_wallets.<mask0>;\t\t\t\temit OutOmniWallet(_account);\t\t\t\treturn true;\t\t\t}\t\t}\t\treturn false;\t}"