"function _beforeTokenTransfer(        address from,        address to,        uint256 tokenId    ) internal override {        super._beforeTokenTransfer(from, to, tokenId);        if (from != address(0) && to != address(0)) {            uint256[] memory u = user[ownerOf(tokenId)];            for (uint256 i = 0; i < u.length; i++) {                if (u[i] == tokenId) {                    user[ownerOf(tokenId)][i] = u[u.length - 1];                    user[ownerOf(tokenId)].<mask0>;                    user[to].push(tokenId);                }            }        }    }"