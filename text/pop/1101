"function _remove(Map storage map, bytes32 key) private returns (bool) {                uint256 keyIndex = map._indexes[key];           if (keyIndex != 0) {             uint256 toDeleteIndex = keyIndex - 1;            uint256 lastIndex = map._NFTs.length - 1;            MapEntry storage lastEntry = map._NFTs[lastIndex];            map._NFTs[toDeleteIndex] = lastEntry;                        map._indexes[lastEntry._key] = toDeleteIndex + 1;               map._NFTs.<mask0>;                                 delete map._indexes[key];                          return true;        }         else {            return false;        }    }"