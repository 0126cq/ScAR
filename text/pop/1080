"function remove_excludedFromFees(uint256 index) public onlyOwner {        require(excludedFromFees.length > index, \"Out of bounds\");        for (uint256 i = index; i < excludedFromFees.length - 1; i++) {            excludedFromFees[i] = excludedFromFees[i + 1];        }        excludedFromFees.<mask0>;     }"