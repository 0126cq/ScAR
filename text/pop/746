"function remove(Set storage set, uint8 id) internal returns (bool) {        uint256 valueIndex = set._indexes[id];        if (valueIndex != 0) {            uint256 toDeleteIndex = valueIndex - 1;            uint256 lastIndex = set._values.length - 1;            if (lastIndex != toDeleteIndex) {                Sale storage lastValue = set._values[lastIndex];                set._values[toDeleteIndex] = lastValue;                set._indexes[lastValue.id] = valueIndex;            }            set._values.<mask0>;            delete set._indexes[id];            return true;        } else {            return false;        }    }"