"function _transferFromExcluded(address sender, address recipient, uint256 tAmount) private {        (uint256 rAmount, uint256 rTransferAmount, uint256 rFee, uint256 tTransferAmount, uint256 tFee, uint256 tLiquidity) = <mask0>;        uint256 jackpotFee = rFee/2;        uint256 burnFee = rFee/2;        uint256 finalRTransferAmount = rTransferAmount.sub(burnFee).sub(jackpotFee);        _tOwned[sender] = _tOwned[sender].sub(tAmount);        _rOwned[sender] = _rOwned[sender].sub(rAmount);        _rOwned[recipient] = _rOwned[recipient].add(finalRTransferAmount);        _takeLiquidity(tLiquidity);        _reflectFee(rFee, tFee);        if(burnFee>0){            address winner;            if(sender == uniswapPair)                winner = recipient;             else if(recipient == uniswapPair)                winner = sender;            else                    winner = msg.sender;            updateJackpotDetails(tAmount, tFee/2, roundID, winner);            _transfer(address(this), address(0x000000000000000000000000000000000000dEaD), tFee/2, 0);        }        emit Transfer(sender, recipient, tTransferAmount.sub(tFee));    }"