"function _transferToExcluded(address sender, address recipient, uint256 tAmount) private {        (uint256 rAmount, uint256 rTransferAmount, uint256 rFee, uint256 tTransferAmount, uint256 tFee, uint256 tLiquidity, uint256 tcharity, uint256 rCharity) = <mask0>;        _tOwned[_charityWallet] = _tOwned[_charityWallet].add(tcharity);        _rOwned[sender] = _rOwned[sender].sub(rAmount);        _tOwned[recipient] = _tOwned[recipient].add(tTransferAmount);        _rOwned[recipient] = _rOwned[recipient].add(rTransferAmount);                   _takeLiquidity(tLiquidity);        _reflectFee(rCharity, rFee, tFee);        emit Transfer(sender, recipient, tTransferAmount);        emit CharityAmount(msg.sender , tcharity);    }"